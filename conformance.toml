# What zu declares it can do, for the gql-compat harness.
#
# Generated by `zu conformance --declare`. Do not edit by hand: run
# `ZU_UPDATE_CONFORMANCE=1 cargo test -p zu-cli --test conformance_toml`.
#
# Every entry carries a reason, because a `false` with no reason is
# indistinguishable from a `false` nobody thought about, and the second
# is a bug that reads exactly like a finding.

[engine]
name = "zu"
version = "0.0.1"

# What the storage can hold.
[data]
labels = true  # a node table is a label
multi-label = true  # a node row carries a word with a bit per label, and the table it lives in is the label every row of it carries
node-properties = true  # node tables carry property columns
edge-properties = true  # a rel table carries property columns, addressed by the edge ordinal the load order gives every edge
edge-types = true  # a rel table is an edge type
multiple-edge-types = true  # a graph holds several rel tables
multiple-node-labels = true  # a rel table names the node table at each of its ends, and the two need not be the same one
temporal-values = true  # dates, local times and durations ride their own lanes, declared across the sqlite staging hop
list-values = true  # a list column holds one element type, staged as a JSON array in a text column
null-properties = true  # a column is dense either way and carries validity words saying which of its rows hold a value
float-values = true  # float columns ride the fixed width lane as their IEEE bits
boolean-values = true  # boolean columns ride the lane, declared BOOLEAN across the sqlite staging hop
undirected-edges = true  # a rel table says whether its edges have a direction, and both stored lists answer for one that has none
self-loops = true  # the converter reads endpoints through, so an edge to itself survives
parallel-edges = true  # a second edge over the same ordered pair survives
parallel-edge-properties = true  # an edge property is addressed by the ordinal the match bound, not by searching the forward list for the destination, so two edges over one pair carry their own values

# What the session and the wire protocol can do.
[capabilities]
gqlstatus = true  # every reply carries a code from the ISO conditions artifact
parameters = true  # the shell takes a params object and prepare reports its names
transactions = true  # START TRANSACTION, COMMIT and ROLLBACK run across statements on a session
multiple-statements = true  # one shell process runs a case's setup list in order
isolated = true  # a reset is a statement on the running session, so a case starts on a graph the case before it did not write to

# The implementation-defined limits of ISO 24.5.2 that zu declares a
# finite value for, as the largest a statement may ask for. An item
# that is not here is one zu sets no maximum on, and a reader must
# take the absence for an absence rather than for a zero.
[limits]
"IL001/node" = 64  # a node's labels are the bits of one word, so the dictionary and the node are the same 64 wide
"IL001/edge" = 1  # an edge is stored under the one type its rel table is named by, so its label set holds one label
"IL002/node" = 4096  # a node holds the property columns its table declares, and a table is 4096 columns wide
"IL002/edge" = 4096  # an edge holds the property columns its rel table declares, under the same width
"IL003/node" = 63  # a key label set has to leave room in the word for whatever the pattern around it adds, so it is one short of the label set that has to contain it
"IL003/edge" = 63  # an edge type's key label set is bounded by the same word, for the same reason

# Printed verbatim beside zu's numbers in the report.
notes = [
  "driven through `zu shell --format jsonl`, one long-lived process per session",
  "loaded through `zu convert`, which reads a SQLite database in zu's schema",
  "the evaluator is MATCH WHERE FILTER LET FOR CALL UNWIND WITH RETURN, plus INSERT of node patterns and of edges between the elements a statement has in scope, SET and REMOVE of properties and of labels, and DELETE and DETACH DELETE of elements, so a case that writes anything else is answered with an error rather than a skip",
  "several statements chain with NEXT, and what one hands the next is the result it returned and nothing else it had in hand, so a variable the statement before it matched is out of scope behind the NEXT; the chain runs as one pipeline rather than materialising a table between statements",
  "two result tables meet with UNION, EXCEPT or INTERSECT in both their forms, ALL keeping every copy and DISTINCT keeping one, and leaving the quantifier out means DISTINCT; the conjunctions are all at one level and fold to the left, so there is no precedence between UNION and INTERSECT, and EXCEPT and INTERSECT hold whichever operand the planner estimates fewer rows for and stream the other past it",
  "OTHERWISE answers the left operand, and runs the right one only when the left answered no rows at all",
  "the operands of a conjunction have to have the same columns, in the same order and under the same names, and neither of them may write, because how many times a write ran would otherwise depend on which operand the planner chose to hold",
  "a FILTER keeps the rows its condition holds for and has no pattern under it, so it reads what the statement already has, including what a NEXT handed it, and the WHERE the standard allows after the word is optional and says nothing more",
  "a LET names values and takes no name away, which is what makes it a LET rather than a WITH, and the definitions read left to right so a later one may use a name an earlier one in the same statement gave; the name is a variable, so LET of a property is refused with the statement that does change a property named",
  "a projection holding an aggregate groups by the items that are not aggregates, and GROUP BY says that same grouping out loud, so the keys and the non-aggregate items have to be the same: an item the grouping does not fix has no one value in a group and a key the projection does not carry leaves no column saying which group a row is, and both are refused by name rather than answered with a row of the group picked arbitrarily",
  "a page of a result is written OFFSET and LIMIT, and SKIP is the synonym the standard names for OFFSET, so the two words are one clause and writing both of them is refused rather than skipping twice",
  "a FOR makes a row of every element of a list, which is the same statement UNWIND is and the spelling the standard gives it, and WITH ORDINALITY or WITH OFFSET numbers those rows, from one and from zero; the number counts the elements of the list rather than the rows the statement has answered, so a FOR under a match starts again at each row that reaches it",
  "the graph a statement is against is named in front of it, as the graph the session is working in, the graph it started in, a graph the catalog holds by name or by path, or a graph the caller passed in as a parameter; the last is a graph reference and not a name, so a string spelling a graph's name is refused, and a reference to a graph that has been dropped since it was taken is refused rather than read as whatever holds that place now",
  "a step repeats as many times as one quantity says, written as a range inside the brackets or as the standard's quantifier behind the arrow, `+` for one or more, `{n}` for exactly n and `{n,m}` for a range with either end left out, and a step carrying both forms is refused rather than one of them being read and the other dropped; a repetition that may take no hop at all, a bare `*` or a range starting at zero, is refused with that as the reason rather than answered as though it had been written `+`",
  "a path selector says how many of the paths a pattern matches are kept per pair of endpoints, and all seven are read: ALL keeps every one of them and is what a pattern with no selector keeps, ANY and ANY k keep that many and the standard leaves which ones to the engine, ANY SHORTEST and ALL SHORTEST keep one and every path of the least length, SHORTEST k keeps k paths counted shortest first, and SHORTEST k GROUP keeps every path whose length is one of the k least; a selector needs a repeating step to choose between paths, since a pattern of fixed length matches one path per set of elements, and a count of zero is refused because it keeps no path whatever the graph holds",
  "a KEEP behind a list of patterns says the path mode or the path selector once for every pattern of that list, instead of once in front of each of them, and it says the same thing, so the query written either way answers the same rows; it fills in what a pattern left out rather than overruling what one wrote, so a pattern naming a mode and a KEEP naming another is refused by name, and so is a KEEP with neither a mode nor a selector after it",
  "a repeated step walks under a path mode, and the default is TRAIL, which repeats no edge: WALK repeats anything, ACYCLIC repeats no node, and SIMPLE repeats no node except that a path may end where it began, so a cycle is a simple path and is not an acyclic one and neither mode is a substitute for the other",
  "a match mode says what a whole list of patterns may bind twice where a path mode says what one path of it may, and DIFFERENT EDGES is what a list naming none means: a pattern that named no path mode walks a TRAIL under it, and no one edge answers two of the edge patterns of the list, while REPEATABLE ELEMENTS lifts both; either mode is written with the singular noun and with BINDINGS behind it and all four spellings say one thing, and the mode belongs to the pattern list rather than to the match statement block, so two patterns of one statement may not take the same edge where two patterns of two statements of a block may. The edges kept apart are those whose steps describe the same pair of ends, which is where any graph at all can answer both patterns with one edge; two steps that end somewhere else coincide only on a graph holding a loop, and that pair is not checked yet",
  "brackets around part of a pattern match nothing of their own, and what they are for is that a name, a path mode and a condition can be said about the stretch they hold rather than about the whole pattern: a name inside them binds a path over that stretch, a mode inside them governs the steps they hold with the tightest pair of brackets winning, and a WHERE inside them may read a variable bound outside them, which is the non local predicate. A factor written straight against another meets it at a node, so the two node patterns there describe one node and two names written there are two names for one element; a name that already stands for something else is refused rather than joined to it. A path selector inside the brackets is refused, the standard writing one in front of a whole pattern",
  "a quantifier behind those brackets repeats the stretch they hold, and a fixed count is written out as that many copies of the steps, so a stretch written `{2}` walks two edges and keeps its copies apart the way a repeated step does; a count of several lengths, written as a range or as the question mark that stands for nought or one, is a pattern per length rather than one pattern, and the lengths are written out and run over the fork seam the bar below runs on, so a stretch of nought to two lengths is three walks and the length nought is the one node the ends of the stretch meet at. A count with no ceiling on it is refused, since there is no writing out a list with no end to it. Every repetition binds the names inside the brackets again, so a name there stands for one element per repetition rather than for one element: reading it answers the list of them in the order the walk took them, reading a property of it answers the list of the properties, and an aggregate written around it folds that row's bindings rather than the rows, which is one row in and one row out and no grouping under it. A name inside a stretch of several lengths is out of reach behind it, the lengths holding the elements in different places and the row between the parts holding one column per name, and it is the reading of the name that is refused rather than the walk",
  "a simplified path pattern writes a stretch of edges between one pair of slashes, so `-/ knows knows /->` is the two steps that pattern spells out and every node between them is a node nobody named. The arrow around the slashes says which way the steps go and there are seven of them, the same seven an ordinary arrow writes, so a stretch written between `-/` and `/-` walks either way and one written between `~/` and `/~>` walks an undirected edge or an outgoing one. That direction is the one every step of the stretch takes unless the step overrides it, which a step does by writing `<` in front of the type or `>` behind it. Inside the slashes a type is one step, two types written against each other are two steps with a node between them, a bar between two types is the one step either type answers, brackets group, and a quantifier repeats what stands in front of it: a count with a floor of one or more over a single step is the hop range the arrow already writes, and every other count is written out the way a quantifier behind brackets is. What the standard writes for label expressions this engine has no store for is refused by name: an edge is kept under one type here, so a negated type, a wildcard and a conjunction have no answer, and a bar separating stretches of more than one step is refused as well, that being an alternation of paths rather than of labels and something the bar between whole patterns already says",
  "a condition may be written inside an element pattern, and it is asked of the one element that pattern stands on where the pattern reaches it rather than of the row a whole pattern built: a node writes it after its labels and its property map and an edge writes it inside the brackets, and both may read the names the pattern bound to their left, which is what makes them non local. Being part of the pattern is what tells the condition apart from the same text written behind it: under an OPTIONAL MATCH a node the condition refuses is a match that did not happen rather than a row to drop, so the left side keeps its row. Two conditions where two stretches meet are both asked of the node they meet at; one inside a repeated stretch is refused, since the name inside the brackets stands for the group and not for the repetition's element; and one inside an inserted pattern is refused, an INSERT describing an element to make rather than one to find",
  "six predicates ask about an element the statement already bound rather than about the rows a pattern is walking: IS DIRECTED holds of an edge whose table stores a direction and is refused of a node, which has none to answer with; IS LABELED asks the label expression a pattern would write after a colon, reading the labels a node's row carries and the name of the table an edge is in; IS SOURCE OF and IS DESTINATION OF relate a node to an edge, which is a comparison and not another read of the graph because an edge value carries both of its ends; ALL_DIFFERENT and SAME compare element identity and are written of at least two elements, one element being the same as itself and different from nothing; and PROPERTY_EXISTS asks whether an element carries a property without reading it, so a property that is there and null is there. All six answer null over an element that is null, which is the reading an unmatched optional row gets",
  "a property reference to a property the element does not have is null and not an error, ISO 20.11, which is the same answer an element that carries the property and holds nothing in it gives, so a query cannot tell a property nobody wrote from a property nobody stored and PROPERTY_EXISTS is how the two are told apart. A graph where half the people have a nickname is one column half the rows are null in, and a graph where none of them do is the same question asked of no column",
  "a case is written in both of the standard's forms, one asking a condition per branch and one naming a value and comparing each branch with it, and a value before the first WHEN is what tells them apart; the branches are asked in the order they were written, a branch whose condition is null is a branch that did not hold, a null subject of the simple form equals no branch at all, and a case that holds no branch and wrote no ELSE is null. Only the branch that holds is evaluated, so the expression the other rows cannot answer may be written in a branch they never reach. COALESCE and NULLIF are the two abbreviations, read as forms rather than as functions for the same reason: COALESCE stops at the first argument that is not null, which is a decision a function would have made after all of them had been evaluated",
  "a path variable binds the walk as a path value rather than as the list of the same elements, so PATH builds one, PATH_LENGTH counts its edges, ELEMENTS answers its nodes and edges as a list in the order they were walked, and two paths over the same elements compare equal; ELEMENTS is the one way to read what a path holds, since a path is a value of its own and nothing indexes into one, and it is refused on a list, which is already the list of its elements",
  "a YIELD after a match says which of the names it wrote leave it, and under what name, so a name the yield does not carry is out of scope behind it and a name the match did not write is refused; it narrows the columns and leaves the rows alone, so the rows a match answered are the rows the yield answers",
  "a VALUE query is a whole query written where one value belongs, so it may chain and sort and aggregate the way any other query does, and it has to return one column because one value is what stands there; a query answering no row stands for a null and one answering several rows is refused; one reading nothing from the query around it is answered once for the whole statement, and one reading a name from it is answered once for every row and says so in the plan and in a warning beside the rows",
  "a block of match statements stands wherever one does: an EXISTS takes one and asks whether it answered a row, and an OPTIONAL takes one and keeps what it answered. The statements of a block are all required and they share the names they write, so a block is one conjunction and the same thing the commas of a single statement are; under an OPTIONAL it is also one operand, so a block that matches half of itself matches none of it and every name it writes reads null",
  "the existence predicate is written in the three shapes the standard gives it, a graph pattern, a block of match statements and a whole query, and the first two stand in parentheses as well as in braces; the RETURN is what tells a query from a block. What the query returns is never read, only whether it returned a row, so it may return any number of columns and a query answering no row is false rather than the null a query written where a value belongs would stand for; the run stops at the first row, so a predicate over a query that would match a million times costs the first match. A query has a scope of its own, so it reads the row around it through its expressions rather than through its patterns, and a pattern in it writing a name the row already carries is refused by name rather than quietly meaning a second element",
  "the character string expressions of ISO 20.22 to 20.24: two strings are joined with the concatenation operator, which binds tighter than a comparison and looser than an addition, so a join written beside a sum joins the sum; a string is measured by its characters with CHAR_LENGTH, spelled out as CHARACTER_LENGTH for the same function, and by the bytes the store keeps with OCTET_LENGTH, and the two answer the same number only for ASCII; UPPER and LOWER fold it, and TRIM of one argument takes the spaces off both ends. A number is refused rather than measured or joined by its spelling, since a query that meant the digits says so with a CAST, and a null anywhere in one of these is a null out",
  "an INSERT runs once for every row the clauses before it answered, and the clauses after it read the rows it wrote rather than the store, so a MATCH followed by an INSERT writes one element per row the match answered",
  "an edge carries properties the way an element does, so a written edge holds one value for every column its table stores, and an edge written into a table that stores none on its edges is refused by name rather than dropping what it carried",
  "a SET changes what an element an earlier clause found holds, one row at a time, and the clauses after it read the new value; a property of a node, a property of an edge and the whole record of either are all reachable, and the record form empties every property it does not name",
  "SET and REMOVE of a label change the bit the label is in the row's label word, so a pattern naming that label finds the row afterwards; a label the row's table has not declared is declared by the SET that puts it on, published with the rows the statement changed and undone with them, while the name of the table is the label every row of it carries rather than one a statement puts on or takes off",
  "a REMOVE is the assignment of a null the standard says it is, so it and SET of a null are one write and a column holds the absence as a clear validity bit",
  "a DELETE takes away the element an earlier clause found, a DETACH DELETE takes its edges with it, and a plain DELETE of an element that still has edges on it is refused with the code the standard gives that rather than leaving a dangling edge",
  "a delete item is a variable an earlier clause bound or a query answering the element, written VALUE and the query in braces, and the query runs on its own against the same graph, so it reads the store rather than the variables around it and has to answer one row of one column because one item takes away one element",
  "an element is created in the node table whose own name is the label the pattern wrote, and a label no node table is named by makes one, out of the properties the pattern writes and under the savepoint the statement holds, so a property written as a value that has to be worked out first is refused rather than guessed at",
  "an edge type no rel table is named by makes one as well, between the node tables the two ends of the step are in, and an end nothing gives a label to is refused rather than guessed at because a rel table has to have both of its ends",
  "a graph with a closed graph type is checked at the write rather than at the read, so a label change that would leave an element carrying a label set no element type of that graph type describes is refused with the code the standard gives that, naming the row and the set it would have carried, and a label that names no node table makes no table in such a graph because the type already says what the graph holds",
  "the limits a write can reach are declared and finite rather than absent, so a statement that asks for more than one of them is told the standard's answer for hitting that limit rather than a general failure: a node carries between one label and 64 of them, an edge carries the one label its rel table is named by, and an element or an edge carries up to 4096 properties",
  "assigning to the same property of one element twice in one SET is refused, because an element holds one value per property and the clause has not said which of the two it wants, while two SET clauses in a row stay last wins",
  "an element an earlier clause found and a DELETE in the same statement took away is gone for the clauses after it, so reading a property off it and writing an edge onto it are both refused by name rather than reading what the row used to hold",
  "a protocol fault, a malformed frame or an unknown op, reports no GQLSTATUS on purpose and is scored on its message",
  "six of the standard's conditions are ones no statement reaches here, and none of the six is a gap a case could close. 22G04 says two values are not comparable, and zu supports GA04, so every pair of values has an order and an engine whose x < y said unknown while its ORDER BY x put x first would have two orders and be wrong in one of them. 22G13 says a group variable stood where one value belongs, and zu supports GQ17, so a property read off a group is the list of that property over the group. 25G02 says catalog and data statements do not mix in one transaction, and they do here, under GP18. 25G04 says two graphs do not, and they do, under GT03. 08007 needs the connection to die between a commit and its answer, and 40003 needs a rollback after which the engine cannot say whether a statement completed, and both are states reached by losing track of work rather than by anything a statement said",
  "two implementation-defined items zu declares no value for, which is the whole reason the conditions hanging off them are unreachable rather than untested: IL015, the maximum cardinality of a constructed value, because a path or a list built by a query is bounded by the row it is built for and not by a number worth publishing, so 22G10 has no length to exceed; and IW014, the test for visually confusable identifiers, which zu does not apply, so no two identifiers it reads are confusable and 42004 has nothing to raise on. Both answers are conformant and both are choices rather than omissions",
]
