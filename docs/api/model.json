{
  "schema": 1,
  "crate": "zu",
  "version": "0.0.1",
  "rustdoc_format_version": 61,
  "entities": [
    {
      "id": "zu::Appender",
      "kind": "struct",
      "name": "Appender",
      "source": "zu::append::Appender",
      "doc": "A bulk load in progress: rows buffered in memory, written to the\ndatabase when you flush.\n\nTake one with [`crate::Connection::appender`], append rows to it,\nand call [`Appender::close`]. Dropping it without closing flushes\ntoo, because a loader that forgot is better served by its data\narriving than by it vanishing, but a drop has nowhere to put an\nerror and so cannot tell you whether the flush worked. That is the\none footgun dx/04 §6 accepts, and `#[must_use]` is what points a\ncaller at the version that reports. The drop does not panic on a\nbuffer it is handed, in debug builds either: a loader that hits an\nerror between rows and returns is exactly the case where an\nappender is dropped with rows in it, and turning that into an abort\non the way out of an unwind would be the worse failure.\n\nTwo things a table can be that an appender will not append to, both\nrefused when the appender opens rather than at the flush that would\nhave failed. A column that holds a null has no way in this API to\nsay which rows the appended values are for, so the write statements\nof G3 are what answers it. A table whose row domain a rel table's\nkey index is built over cannot grow without that index being\nreallocated, so the fold refuses it, and this refuses it earlier,\nbefore a load is buffered against it."
    },
    {
      "id": "zu::Appender::append_row",
      "kind": "method",
      "name": "append_row",
      "of": "zu::Appender",
      "signature": "fn append_row<R>(&mut self, row: R) -> Result<()>",
      "doc": "Appends one row: every column of a node table in declared\norder, or a source and a destination for a rel table.\n\nThe row goes into memory and nothing else happens, so this is a\npush per column and, on a string column, one copy. A row of the\nwrong width or with a field of the wrong type is refused with\nnothing of it kept, so an appender is still usable after a\ncaller fixes the row and tries again."
    },
    {
      "id": "zu::Appender::buffered",
      "kind": "method",
      "name": "buffered",
      "of": "zu::Appender",
      "signature": "fn buffered(&self) -> u64",
      "doc": "Rows buffered and not yet written."
    },
    {
      "id": "zu::Appender::close",
      "kind": "method",
      "name": "close",
      "of": "zu::Appender",
      "signature": "fn close(self) -> Result<u64>",
      "doc": "Flushes and gives back the rows committed, which is how to\nfinish an appender when you want to know that it worked."
    },
    {
      "id": "zu::Appender::committed",
      "kind": "method",
      "name": "committed",
      "of": "zu::Appender",
      "signature": "fn committed(&self) -> u64",
      "doc": "Rows this appender has committed."
    },
    {
      "id": "zu::Appender::flush",
      "kind": "method",
      "name": "flush",
      "of": "zu::Appender",
      "signature": "fn flush(&mut self) -> Result<()>",
      "doc": "Writes every buffered row and makes it readable.\n\nOne commit, whatever the buffer holds: the values are sealed\ninto the data file as segments, one frame naming them is\nappended and synced to the log, which is the commit point, and\nthe fold that follows seals them into the base every query\nreads. On return the buffer is empty and the rows are there.\n\nA flush with nothing buffered touches no file and returns, so a\nloader can flush on a timer without writing empty commits."
    },
    {
      "id": "zu::Batch",
      "kind": "struct",
      "name": "Batch",
      "source": "zu_query::row::Batch",
      "doc": "A batch of rows handed to a streaming caller\n([`crate::exec::execute_streaming`]).\n\nIt is the same view a whole result gives, over a piece of one: the\ncolumn names and some rows, borrowed for the length of the call. A\ncaller that wants to keep rows past the call copies what it wants\nout of them, which is the trade streaming makes and the reason the\nbatch cannot outlive the sink."
    },
    {
      "id": "zu::Batch::columns",
      "kind": "method",
      "name": "columns",
      "of": "zu::Batch",
      "signature": "fn columns(&self) -> &[String]",
      "doc": "The column names, which are the statement's and so are the same\nfor every batch of one run."
    },
    {
      "id": "zu::Batch::is_empty",
      "kind": "method",
      "name": "is_empty",
      "of": "zu::Batch",
      "signature": "fn is_empty(&self) -> bool",
      "doc": "Whether the batch is empty, which the sink is never handed."
    },
    {
      "id": "zu::Batch::iter",
      "kind": "method",
      "name": "iter",
      "of": "zu::Batch",
      "signature": "fn iter(&self) -> RowIter",
      "doc": "The rows, typed, exactly as a whole result reads them."
    },
    {
      "id": "zu::Batch::len",
      "kind": "method",
      "name": "len",
      "of": "zu::Batch",
      "signature": "fn len(&self) -> usize",
      "doc": "How many rows this batch holds. It is never zero: a filter that\nrejected everything it read hands over nothing rather than an\nempty batch."
    },
    {
      "id": "zu::Batch::new",
      "kind": "method",
      "name": "new",
      "of": "zu::Batch",
      "signature": "fn new(columns: &[String], rows: &[Vec<Value>]) -> Batch",
      "doc": "A batch over names and rows that belong together."
    },
    {
      "id": "zu::Batch::rows",
      "kind": "method",
      "name": "rows",
      "of": "zu::Batch",
      "signature": "fn rows(&self) -> &[Vec<Value>]",
      "doc": "The rows, untyped."
    },
    {
      "id": "zu::C_ABI_VERSION",
      "kind": "constant",
      "name": "C_ABI_VERSION",
      "signature": "&str",
      "source": "zu_common::C_ABI_VERSION",
      "doc": "The revision of the C ABI this workspace publishes (dx/02 §8).\n\nHere rather than in `zu-capi` because the two things that report it\nare the header, which is the ABI's specification, and `zu version`,\nwhich is how a binding asks a build on the PATH what it is. Neither\nshould have to depend on the other to say one number, and a CLI that\nlinked the C ABI to read a string would export the whole of it.\n`cargo xtask package` holds `ZU_ABI_VERSION` in the header to this."
    },
    {
      "id": "zu::CheckpointMode",
      "kind": "enum",
      "name": "CheckpointMode",
      "source": "zu_storage::CheckpointMode",
      "doc": "How much work a checkpoint is allowed to do."
    },
    {
      "id": "zu::CheckpointMode::Full",
      "kind": "variant",
      "name": "Full",
      "of": "zu::CheckpointMode",
      "signature": "Full",
      "doc": "Also compact tombstoned groups and rewrite fragmented CSR."
    },
    {
      "id": "zu::CheckpointMode::Normal",
      "kind": "variant",
      "name": "Normal",
      "of": "zu::CheckpointMode",
      "signature": "Normal",
      "doc": "Fold deltas and flip the root header."
    },
    {
      "id": "zu::Column",
      "kind": "struct",
      "name": "Column",
      "source": "zu_query::frame::Column",
      "doc": "One column of a frame as the caller describes it."
    },
    {
      "id": "zu::Column::layout",
      "kind": "field",
      "name": "layout",
      "of": "zu::Column",
      "signature": "Layout"
    },
    {
      "id": "zu::Column::name",
      "kind": "field",
      "name": "name",
      "of": "zu::Column",
      "signature": "String"
    },
    {
      "id": "zu::Column::ty",
      "kind": "field",
      "name": "ty",
      "of": "zu::Column",
      "signature": "zu_common::LogicalType",
      "doc": "What a value read out of it is. The layout says how wide the\nbytes are and this says what they mean, which is the whole\ndifference between a count of days and a number."
    },
    {
      "id": "zu::Config",
      "kind": "struct",
      "name": "Config",
      "source": "zu::db::Config",
      "doc": "How a database is opened and what its statements are allowed to do.\n\nEvery field has a default that works, so a caller sets the one thing\nit cares about and nothing else. The builder takes `self` and gives\nit back, so the whole configuration is one expression and a\nhalf-built `Config` is never a thing anybody holds."
    },
    {
      "id": "zu::Config::memory_limit",
      "kind": "method",
      "name": "memory_limit",
      "of": "zu::Config",
      "signature": "fn memory_limit(self, bytes: usize) -> Config",
      "doc": "Bytes the caches may hold, split between block frames and the\ndecoded-object pools by the ratios in perf/04 §2. This is a\nbudget for what the engine keeps warm, not a ceiling on the\nprocess: a query still allocates its own intermediate state."
    },
    {
      "id": "zu::Config::new",
      "kind": "method",
      "name": "new",
      "of": "zu::Config",
      "signature": "fn new() -> Config",
      "doc": "The defaults: the 128 MiB cache budget the perf gates run under,\na thread count the executor picks from the machine, and writes\nallowed."
    },
    {
      "id": "zu::Config::read_only",
      "kind": "method",
      "name": "read_only",
      "of": "zu::Config",
      "signature": "fn read_only(self, read_only: bool) -> Config",
      "doc": "Opens the file on a descriptor this process cannot write\nthrough, so nothing here or under here can modify the database,\nand a database on a read-only mount opens rather than failing at\nthe open. Statements that would write are refused with an error\nnaming the database, before the operating system gets a chance\nto refuse them less helpfully."
    },
    {
      "id": "zu::Config::stale_bound",
      "kind": "method",
      "name": "stale_bound",
      "of": "zu::Config",
      "signature": "fn stale_bound(self, epochs: u64) -> Config",
      "doc": "How many epochs a session parameter holding a binding table may\nfall behind before the statement after it carries a warning\nsaying so (plan/06 §2).\n\nA pin holds no blocks in zu, since a binding table parameter\nholds rows copied out of the snapshot rather than a reader into\nit, so this is not a knob against the store growing. It is how\nlong a caller wants to go without being told that what it is\nholding was read a while ago."
    },
    {
      "id": "zu::Config::threads",
      "kind": "method",
      "name": "threads",
      "of": "zu::Config",
      "signature": "fn threads(self, threads: usize) -> Config",
      "doc": "Worker threads for the parallel stages of a query. Zero, the\ndefault, lets the executor pick `min(cores, 8)`; one forces\nsequential execution, which is what a caller running many\nconnections at once wants, since the parallelism is then across\nconnections and a thread pool per connection would oversubscribe\nthe machine."
    },
    {
      "id": "zu::Connection",
      "kind": "struct",
      "name": "Connection",
      "source": "zu::db::Connection",
      "doc": "One connection: statements run on it, in order, one at a time.\n\nIt is `Send` and not `Sync`, and every method takes `&mut self`, so\nthe compiler is what stops two threads from using one connection.\nThat is the same rule the C ABI states and has to check at runtime\n(`dx/02` §5), enforced here at no cost.\n\nA connection reads the database as of the statement it is running.\nEvery connection to one file shares the write side, and a statement\npicks up what that side has published before it compiles anything,\nso a commit on another connection is visible to the next statement\nhere without reconnecting. Nothing moves under a statement that has\nstarted: what it took at the top is what it reads to the end, which\nis the snapshot isolation of docs/08 §1.\n\nWrites queue. One connection at a time holds the write side of a\nfile, for the length of a write statement or of an explicit\ntransaction, and the rest wait in the order they asked."
    },
    {
      "id": "zu::Connection::appender",
      "kind": "method",
      "name": "appender",
      "of": "zu::Connection",
      "signature": "fn appender(&mut self, table: &str) -> Result<Appender>",
      "doc": "Opens an appender on `table`, the bulk-load path of dx/04 §6.\n\nA load through statements pays a commit per row; an appender\nbuffers rows and pays one per flush. Only one appender exists at\na time on a connection, which the borrow says rather than a\ncheck: it holds the file this connection reads through, so the\nconnection is unusable until the appender is closed or dropped,\nand a row appended is a row the next statement on it sees."
    },
    {
      "id": "zu::Connection::close_prepared",
      "kind": "method",
      "name": "close_prepared",
      "of": "zu::Connection",
      "signature": "fn close_prepared(&mut self, stmt: u64) -> bool",
      "doc": "Releases a prepared statement, reporting whether the id was one."
    },
    {
      "id": "zu::Connection::duplicate",
      "kind": "method",
      "name": "duplicate",
      "of": "zu::Connection",
      "signature": "fn duplicate(&self) -> Result<Connection>",
      "doc": "Another connection to the same database, made from this one\nrather than from the path.\n\nThis is how a pool is written. [`Database::connect`] opens the\nfile and looks up the write side under its path; this forks a\ndescriptor off the side this connection already holds, so it\ncosts a schema load and no lookup, and it works on a database\nin memory, which has no path to look up.\n\nThe two are connections in every sense, not two names for one:\neach has its own plan cache, its own readers, its own interrupt\nand its own transaction. What they share is the write side, so\nthey queue behind each other to write and each sees what the\nother has committed, exactly as two connections from the same\n[`Database`] do. The switches and the read-only setting are\ncarried across, because a pool that handed out connections\nconfigured differently from the one it was seeded with would be\na trap.\n\n```no_run\nuse zu::Database;\n\nlet db = Database::memory()?;\nlet mut conn = db.connect()?;\nconn.query(\"INSERT (p:person {id: 1, name: 'ada'})\")?;\nlet mut other = conn.duplicate()?;\nlet rows = other.query(\"MATCH (p:person) RETURN p.name AS name\")?;\nassert_eq!(rows.rows.len(), 1);\n# Ok::<(), zu_common::ZuError>(())\n```"
    },
    {
      "id": "zu::Connection::execute",
      "kind": "method",
      "name": "execute",
      "of": "zu::Connection",
      "signature": "fn execute(&mut self, source: &str) -> Result<u64>",
      "doc": "Runs a statement for its effect and returns the number of rows\nit produced, which is zero for the catalog statements that are\nthe writes this milestone has."
    },
    {
      "id": "zu::Connection::execute_prepared",
      "kind": "method",
      "name": "execute_prepared",
      "of": "zu::Connection",
      "signature": "fn execute_prepared(&mut self, stmt: u64, params: &[(&str, Value)]) -> Result<QueryResult>",
      "doc": "Runs a prepared statement with a set of bindings."
    },
    {
      "id": "zu::Connection::explain",
      "kind": "method",
      "name": "explain",
      "of": "zu::Connection",
      "signature": "fn explain(&mut self, source: &str) -> Result<String>",
      "doc": "The plan this connection would run for `source`, rendered,\nwithout running it."
    },
    {
      "id": "zu::Connection::explain_plan",
      "kind": "method",
      "name": "explain_plan",
      "of": "zu::Connection",
      "signature": "fn explain_plan(&mut self, source: &str) -> Result<QueryPlan>",
      "doc": "The same plan as operators rather than as text: the tree, the\ncolumns the statement answers with, the parameters it wants, and\nthe notes compiling it raised.\n\nA caller that reads a plan is asking a question about it, and\nevery one of those questions is easier to ask of a tree than of\na listing: whether the scan reaches an index, how deep the\nexpands go, which tables are touched. [`QueryPlan::render`] is\nwhat [`Self::explain`] returns, so the two are one thing printed\ntwo ways rather than two renderings that can drift.\n\n```no_run\nuse zu::Database;\n\nlet db = Database::open(\"social.zu1\")?;\nlet mut conn = db.connect()?;\nlet plan = conn.explain_plan(\"MATCH (p:person) RETURN p.id AS id\")?;\nlet root = plan.root.as_ref().expect(\"a statement with operators\");\nassert_eq!(root.op, \"Project\");\nassert_eq!(plan.columns, [\"id\"]);\n# Ok::<(), zu::ZuError>(())\n```"
    },
    {
      "id": "zu::Connection::interrupt",
      "kind": "method",
      "name": "interrupt",
      "of": "zu::Connection",
      "signature": "fn interrupt(&self) -> Interrupt",
      "doc": "The handle a statement on this connection can be stopped\nthrough, and the count of rows it has read.\n\nA statement runs on the thread that asked for it, so the handle\nis taken before the call and raised from another thread: it is\nhow a shell answers `Ctrl-C` and how a server answers a client\nthat hung up. The statement returns\n[`zu_common::ZuError::Interrupted`] and the connection is\nunchanged, which is what makes this different from closing it."
    },
    {
      "id": "zu::Connection::is_read_only",
      "kind": "method",
      "name": "is_read_only",
      "of": "zu::Connection",
      "signature": "fn is_read_only(&self) -> bool",
      "doc": "Whether this connection refuses writes."
    },
    {
      "id": "zu::Connection::prepare",
      "kind": "method",
      "name": "prepare",
      "of": "zu::Connection",
      "signature": "fn prepare(&mut self, source: &str) -> Result<(u64, Vec<String>)>",
      "doc": "Compiles a statement and pins it under an id, so a loop binds\nand executes without recompiling. The names it wants come back\nwith it, in the order the binder assigned them."
    },
    {
      "id": "zu::Connection::profile",
      "kind": "method",
      "name": "profile",
      "of": "zu::Connection",
      "signature": "fn profile(&mut self, source: &str, params: &[(&str, Value)]) -> Result<Profile>",
      "doc": "Runs `source` with the counters on and hands back what it\nobserved, one entry per operator per stage.\n\nThis costs the execution, so it is the tool a caller reaches for\nwhen a statement is slower than the plan says it should be: the\nrows an operator really produced sit next to what the optimizer\nexpected, and the self time says which one of them the wall\nclock went into. [`Profile::render`] prints it the way\n`EXPLAIN ANALYZE` does in the shell.\n\nA statement that writes is refused, because it runs as the parts\nit was split at its write into rather than as the one plan a\nprofile describes, and profiling it would apply the write."
    },
    {
      "id": "zu::Connection::query",
      "kind": "method",
      "name": "query",
      "of": "zu::Connection",
      "signature": "fn query(&mut self, source: &str) -> Result<QueryResult>",
      "doc": "Runs one statement and returns its rows."
    },
    {
      "id": "zu::Connection::query_stream",
      "kind": "method",
      "name": "query_stream",
      "of": "zu::Connection",
      "signature": "fn query_stream(&mut self, source: &str, params: &[(&str, Value)], sink: impl FnMut) -> Result<Streamed>",
      "doc": "Runs one statement and hands its rows to `sink` in batches as\nthey are made, instead of returning them all.\n\nThis is the shape for a result that is too big to want in\nmemory, and for a caller that will not read all of it: the sink\nanswers [`Flow::Stop`] and the scan under it stops at the next\nboundary, the same boundary an interrupt is answered at. A batch\nborrows the rows for the length of the call, so a caller keeping\nanything past it copies what it wants out.\n\n```no_run\nuse zu::{Database, Flow};\n\nlet db = Database::open(\"social.zu1\")?;\nlet mut conn = db.connect()?;\nlet mut total = 0i64;\nconn.query_stream(\"MATCH (p:person) RETURN p.id AS id\", &[], |batch| {\n    for row in batch.iter() {\n        total += row.get_at::<i64>(0)?;\n    }\n    Ok(Flow::More)\n})?;\n# Ok::<(), zu::ZuError>(())\n```"
    },
    {
      "id": "zu::Connection::query_stream_batched",
      "kind": "method",
      "name": "query_stream_batched",
      "of": "zu::Connection",
      "signature": "fn query_stream_batched(&mut self, source: &str, params: &[(&str, Value)], batch_rows: usize, sink: impl FnMut) -> Result<Streamed>",
      "doc": "The same, with the batch size named. One vector of rows is the\ndefault because it is the unit the executor already works in;\na caller writing batches somewhere with a size of its own, an\nArrow record batch or an HTTP chunk, says so here."
    },
    {
      "id": "zu::Connection::query_with",
      "kind": "method",
      "name": "query_with",
      "of": "zu::Connection",
      "signature": "fn query_with(&mut self, source: &str, params: &[(&str, Value)]) -> Result<QueryResult>",
      "doc": "Runs one statement with parameters bound by name, without the\nleading `$`."
    },
    {
      "id": "zu::Connection::register",
      "kind": "method",
      "name": "register",
      "of": "zu::Connection",
      "signature": "fn register(&mut self, frame: Frame) -> Result<()>",
      "doc": "Registers a frame as a table of this connection, under the name\nit carries.\n\nThis is the replacement scan: a caller holding columns in memory\ngets to name them in a statement without loading them into the\ndatabase first. Nothing is copied, at registration or at read.\nThe frame is this connection's alone and goes when it does, and\na name a stored table already holds is refused."
    },
    {
      "id": "zu::Connection::registered",
      "kind": "method",
      "name": "registered",
      "of": "zu::Connection",
      "signature": "fn registered(&self) -> Vec<String>",
      "doc": "The names registered on this connection, sorted."
    },
    {
      "id": "zu::Connection::session_mut",
      "kind": "method",
      "name": "session_mut",
      "of": "zu::Connection",
      "signature": "fn session_mut(&mut self) -> &mut Session",
      "doc": "The session under this connection, for the paths that have not\ngrown an API here yet: profiling, EXPLAIN ANALYZE, and the\nengine's own tooling."
    },
    {
      "id": "zu::Connection::table_name",
      "kind": "method",
      "name": "table_name",
      "of": "zu::Connection",
      "signature": "fn table_name(&self, table: u32) -> Option<&str>",
      "doc": "What a table id is called, or nothing when no table has that id.\n\nA node value is a table and a row offset and an edge value is a\ntable and two of them, because neither part identifies anything\non its own: two tables both number their rows from zero. The\nnumber is what the engine passes around and the name is what a\nuser asked about, so a binding that hands a node value out has\nto be able to turn one into the other, and until now the only\nway was to reach past this type into the session and read the\ncatalog. Node and rel tables share one id space, so this is one\ncall and not two.\n\nThe name is borrowed from the catalog, which the next statement\nthat creates or drops a table replaces, so a caller keeping one\npast the next statement copies it."
    },
    {
      "id": "zu::Connection::unregister",
      "kind": "method",
      "name": "unregister",
      "of": "zu::Connection",
      "signature": "fn unregister(&mut self, name: &str) -> Result<bool>",
      "doc": "Drops a registered frame, answering whether there was one under\nthat name."
    },
    {
      "id": "zu::Database",
      "kind": "struct",
      "name": "Database",
      "source": "zu::db::Database",
      "doc": "An open database: a path, a configuration, and the fact that both\nhave been checked against a real file.\n\nIt is `Send + Sync` and holds no file descriptor and no cache, which\nis what makes it shareable without a lock. The state that cannot be\nshared, the seek position of a handle and the plans compiled against\na catalog, lives on the connections instead, one set per connection,\nwhich is the same division the C ABI and every binding above it use."
    },
    {
      "id": "zu::Database::config",
      "kind": "method",
      "name": "config",
      "of": "zu::Database",
      "signature": "fn config(&self) -> &Config",
      "doc": "The configuration it was opened with."
    },
    {
      "id": "zu::Database::connect",
      "kind": "method",
      "name": "connect",
      "of": "zu::Database",
      "signature": "fn connect(&self) -> Result<Connection>",
      "doc": "A new connection: its own file handle, its own caches, its own\nplan cache. Costs an open and a catalog load, which on the\n11 GB file of the B7 gate is tens of microseconds.\n\nThe open goes through the registry rather than ahead of it, so\nthat a connection made while the last one is closing waits for\nthe fold that close is running instead of reading the file\nhalf way through it."
    },
    {
      "id": "zu::Database::create",
      "kind": "method",
      "name": "create",
      "of": "zu::Database",
      "signature": "fn create(path: impl AsRef<Path>) -> Result<Database>",
      "doc": "Creates a database at `path` and opens it, with the default\nconfiguration.\n\nThe path must not exist. A create that found a database there\nand opened it instead would be the call that quietly writes into\ndata somebody else put there, and the caller who wanted either\none has [`Database::open`] to fall back to and a decision to make\nabout which.\n\nWhat it makes is a valid database with nothing in it: a file\nheader, a database header, and no tables. Statements make the\ntables, and until v0 grows one that does, a bulk load is what\nputs rows in."
    },
    {
      "id": "zu::Database::create_with",
      "kind": "method",
      "name": "create_with",
      "of": "zu::Database",
      "signature": "fn create_with(path: impl AsRef<Path>, config: Config) -> Result<Database>",
      "doc": "[`Database::create`] with a configuration."
    },
    {
      "id": "zu::Database::is_memory",
      "kind": "method",
      "name": "is_memory",
      "of": "zu::Database",
      "signature": "fn is_memory(&self) -> bool",
      "doc": "Whether this database is the kind that goes away with the\nprocess. A caller that offers to back one up, or warns before\nclosing one, needs to be able to ask."
    },
    {
      "id": "zu::Database::memory",
      "kind": "method",
      "name": "memory",
      "of": "zu::Database",
      "signature": "fn memory() -> Result<Database>",
      "doc": "Creates a database that never touches the filesystem, with the\ndefault configuration.\n\nThe blocks a file would hold are held in memory instead, and the\nlog beside it too, so everything above this point runs unchanged:\nthe same headers, the same log, the same fold and the same\nrecovery, on bytes that go away when the last handle does. That\nis what makes it worth having for a test or a scratch load and\nwhat makes it useless for anything that has to survive the\nprocess.\n\nEvery call makes a database of its own. Two of them share\nnothing, and cloning one, or connecting to it twice, is what\ngets two views of the same graph."
    },
    {
      "id": "zu::Database::memory_with",
      "kind": "method",
      "name": "memory_with",
      "of": "zu::Database",
      "signature": "fn memory_with(config: Config) -> Result<Database>",
      "doc": "[`Database::memory`] with a configuration.\n\n`read_only` is refused for the same reason [`Database::create`]\nrefuses it: a fresh database nothing may write to is one that\nstays empty forever, and here there is not even a file somebody\nelse could have filled."
    },
    {
      "id": "zu::Database::open",
      "kind": "method",
      "name": "open",
      "of": "zu::Database",
      "signature": "fn open(path: impl AsRef<Path>) -> Result<Database>",
      "doc": "Opens the database at `path` with the default configuration.\n\nThe file is opened once here and closed again, so a path that is\nnot a zu1 file, or is one this build cannot read, fails now\nrather than on the first connection. That open is the O(1) one\nof docs/04 §1: 12 KiB and two header slots."
    },
    {
      "id": "zu::Database::open_with",
      "kind": "method",
      "name": "open_with",
      "of": "zu::Database",
      "signature": "fn open_with(path: impl AsRef<Path>, config: Config) -> Result<Database>",
      "doc": "[`Database::open`] with a configuration."
    },
    {
      "id": "zu::Database::path",
      "kind": "method",
      "name": "path",
      "of": "zu::Database",
      "signature": "fn path(&self) -> &Path",
      "doc": "Where this database lives."
    },
    {
      "id": "zu::DiagnosticRecord",
      "kind": "struct",
      "name": "DiagnosticRecord",
      "source": "zu_common::gqlstatus::DiagnosticRecord",
      "doc": "What zu hands back when a condition is raised: the standard's code and\nzu's own account of what happened.\n\nThe two are kept apart on purpose. `status` is comparable across\nengines and is what a conformance run grades. `detail` is ours and can\nsay whatever is most useful, including the line and column, without\nputting that in the field a harness matches on.\n\n`position` is that line and column again, as a pair. The detail keeps\nsaying it because a message a user reads should be complete on its\nown, and the pair exists because a caller that wants to underline the\noffending token should not have to parse English back into numbers.\n\n`excerpt` is the line that position falls on, kept because the\ncaller furthest from the statement is the one most likely to be\nshowing this to a person: a driver, a notebook cell, a log line. The\nengine has the text in hand when it raises the condition and the\ncaller may not have it at all by the time it prints one."
    },
    {
      "id": "zu::DiagnosticRecord::about",
      "kind": "method",
      "name": "about",
      "of": "zu::DiagnosticRecord",
      "signature": "fn about(self, subject: Subject) -> Self",
      "doc": "The same, about a thing the statement named.\n\nWritten as a builder so a raise site reads as one expression and\nso that adding the subject to a site never reshapes the call it\nwas already making."
    },
    {
      "id": "zu::DiagnosticRecord::at",
      "kind": "method",
      "name": "at",
      "of": "zu::DiagnosticRecord",
      "signature": "fn at(status: GqlStatus, position: Position, detail: impl fmt::Display) -> Self",
      "doc": "The same, raised at a place. The position is written into the\ndetail as well, ahead of it and separated by a colon, which is\nthe form every parser message already had."
    },
    {
      "id": "zu::DiagnosticRecord::detail",
      "kind": "field",
      "name": "detail",
      "of": "zu::DiagnosticRecord",
      "signature": "String"
    },
    {
      "id": "zu::DiagnosticRecord::doc_url",
      "kind": "method",
      "name": "doc_url",
      "of": "zu::DiagnosticRecord",
      "signature": "fn doc_url(&self) -> String",
      "doc": "The page that documents the condition this record carries."
    },
    {
      "id": "zu::DiagnosticRecord::excerpt",
      "kind": "field",
      "name": "excerpt",
      "of": "zu::DiagnosticRecord",
      "signature": "Option<String>",
      "doc": "The whole line `position` is on, without its newline, so that\n`column` indexes into it. `None` when there is no position, when\nthe line is empty, and when it is longer than a person is going\nto read, which are the three cases where quoting it back helps\nnobody."
    },
    {
      "id": "zu::DiagnosticRecord::graph",
      "kind": "field",
      "name": "graph",
      "of": "zu::DiagnosticRecord",
      "signature": "Option<String>",
      "doc": "The graph the statement was running against, and the schema that\ngraph was reached through. Both are filled at the session\nboundary rather than at the raise site, because that is the one\nplace that knows them and there are two hundred raise sites."
    },
    {
      "id": "zu::DiagnosticRecord::in_source",
      "kind": "method",
      "name": "in_source",
      "of": "zu::DiagnosticRecord",
      "signature": "fn in_source(status: GqlStatus, source: &str, offset: usize, detail: impl fmt::Display) -> Self",
      "doc": "The same again, raised at a byte offset into the statement text.\n\nThis is the form to reach for wherever the text is in hand,\nwhich is every place inside the front end, because it is the\nonly form that can fill the excerpt: the line comes from the\nsource and nothing downstream of here still has it."
    },
    {
      "id": "zu::DiagnosticRecord::new",
      "kind": "method",
      "name": "new",
      "of": "zu::DiagnosticRecord",
      "signature": "fn new(status: GqlStatus, detail: impl Into<String>) -> Self"
    },
    {
      "id": "zu::DiagnosticRecord::position",
      "kind": "field",
      "name": "position",
      "of": "zu::DiagnosticRecord",
      "signature": "Option<Position>",
      "doc": "Where in the statement, for the conditions that happen somewhere.\nA division by zero happens at runtime and has no token to point\nat, so this is `None` rather than a guess at one."
    },
    {
      "id": "zu::DiagnosticRecord::retryable",
      "kind": "method",
      "name": "retryable",
      "of": "zu::DiagnosticRecord",
      "signature": "fn retryable(&self) -> bool",
      "doc": "Whether running the statement again could succeed."
    },
    {
      "id": "zu::DiagnosticRecord::schema",
      "kind": "field",
      "name": "schema",
      "of": "zu::DiagnosticRecord",
      "signature": "Option<String>"
    },
    {
      "id": "zu::DiagnosticRecord::severity",
      "kind": "method",
      "name": "severity",
      "of": "zu::DiagnosticRecord",
      "signature": "fn severity(&self) -> Severity"
    },
    {
      "id": "zu::DiagnosticRecord::status",
      "kind": "field",
      "name": "status",
      "of": "zu::DiagnosticRecord",
      "signature": "GqlStatus"
    },
    {
      "id": "zu::DiagnosticRecord::subject",
      "kind": "field",
      "name": "subject",
      "of": "zu::DiagnosticRecord",
      "signature": "Option<Subject>",
      "doc": "What the condition is about, where it is about something the\nstatement named. `None` for the conditions that are about no\nparticular thing: a division by zero is about an expression and\nan expression has no name."
    },
    {
      "id": "zu::DiagnosticRecord::within",
      "kind": "method",
      "name": "within",
      "of": "zu::DiagnosticRecord",
      "signature": "fn within(&mut self, graph: &str, schema: &str)",
      "doc": "Where the statement was running, filled in on the way out.\n\nIt does not overwrite: a condition raised about a graph other\nthan the working one has already said which, and the session\nsaying otherwise on the way past would be the session being\nwrong."
    },
    {
      "id": "zu::Direction",
      "kind": "enum",
      "name": "Direction",
      "source": "zu_storage::Direction",
      "doc": "Traversal direction for adjacency access."
    },
    {
      "id": "zu::Direction::Bwd",
      "kind": "variant",
      "name": "Bwd",
      "of": "zu::Direction",
      "signature": "Bwd"
    },
    {
      "id": "zu::Direction::Fwd",
      "kind": "variant",
      "name": "Fwd",
      "of": "zu::Direction",
      "signature": "Fwd"
    },
    {
      "id": "zu::DurationKind",
      "kind": "enum",
      "name": "DurationKind",
      "source": "zu_common::types::DurationKind",
      "doc": "The two duration kinds, which do not mix.\n\nA year-month duration plus a day-time duration is a type error and\nnot a normalisation, because a month is not a number of days. An\nengine that stores months, days and nanoseconds in one value has to\ninvent an answer for \"one month after 31 January\" and the standard\ndoes not ask it to. zu stores two types and refuses the arithmetic\nthat would need the invention."
    },
    {
      "id": "zu::DurationKind::DayTime",
      "kind": "variant",
      "name": "DayTime",
      "of": "zu::DurationKind",
      "signature": "DayTime"
    },
    {
      "id": "zu::DurationKind::YearMonth",
      "kind": "variant",
      "name": "YearMonth",
      "of": "zu::DurationKind",
      "signature": "YearMonth"
    },
    {
      "id": "zu::Engine",
      "kind": "enum",
      "name": "Engine",
      "source": "zu_query::exec::Engine",
      "doc": "Which executor runs a plan the pipeline one covers. `Pipeline` is\nevery plan it takes, falling back to the row-at-a-time executor for\nthe rest, and is the default. `Rows` pins the row executor for the\nwhole statement, which is how the differential tests get an oracle\nto compare an answer against.\n\nIt is a switch on the options rather than a reading of the\nenvironment because the environment is one variable for a whole\nprocess and a test binary runs its tests in parallel threads: a\ntest that set it for its own statement was setting it for whichever\nother test happened to be between plans, and that answered on the\nwrong engine (#513)."
    },
    {
      "id": "zu::Engine::Pipeline",
      "kind": "variant",
      "name": "Pipeline",
      "of": "zu::Engine",
      "signature": "Pipeline"
    },
    {
      "id": "zu::Engine::Rows",
      "kind": "variant",
      "name": "Rows",
      "of": "zu::Engine",
      "signature": "Rows"
    },
    {
      "id": "zu::Epoch",
      "kind": "type-alias",
      "name": "Epoch",
      "signature": "u64",
      "source": "zu_common::id::Epoch",
      "doc": "Checkpoint or commit epoch, monotonically increasing."
    },
    {
      "id": "zu::Field",
      "kind": "enum",
      "name": "Field",
      "source": "zu::append::Field",
      "doc": "One value on its way into a column.\n\nThis is deliberately not [`crate::query::Value`]. A value coming out\nof a query owns its string, because it outlives the row it was read\nfrom; a value going into a column borrows one, because the caller\nalready has it and the appender is about to copy it into a buffer\neither way. On a string column that is the difference between one\ncopy per row and two.\n\nThere is no null. A column that holds a null cannot be appended to\nat all, for the reason [`Appender`] gives, so a null here could only\never be refused, and refusing it in the type is better than\nrefusing it at flush."
    },
    {
      "id": "zu::Field::Bool",
      "kind": "variant",
      "name": "Bool",
      "of": "zu::Field",
      "signature": "Bool(bool)"
    },
    {
      "id": "zu::Field::Bytes",
      "kind": "variant",
      "name": "Bytes",
      "of": "zu::Field",
      "signature": "Bytes(&[u8])"
    },
    {
      "id": "zu::Field::Float",
      "kind": "variant",
      "name": "Float",
      "of": "zu::Field",
      "signature": "Float(f64)"
    },
    {
      "id": "zu::Field::Int",
      "kind": "variant",
      "name": "Int",
      "of": "zu::Field",
      "signature": "Int(i64)"
    },
    {
      "id": "zu::Field::Str",
      "kind": "variant",
      "name": "Str",
      "of": "zu::Field",
      "signature": "Str(&str)"
    },
    {
      "id": "zu::Field::Temporal",
      "kind": "variant",
      "name": "Temporal",
      "of": "zu::Field",
      "signature": "Temporal(zu_common::Temporal)"
    },
    {
      "id": "zu::FloatBits",
      "kind": "enum",
      "name": "FloatBits",
      "source": "zu_common::types::FloatBits",
      "doc": "Width of a floating point type, in bits.\n\n16, 128 and 256 have no hardware anywhere zu runs. They are here\nbecause the standard has them; they compute in software and the\nbenchmark matrix never uses them, which gql/plan/09 section 6 says\nout loud so nobody reads a soft float number as an engine number."
    },
    {
      "id": "zu::FloatBits::B128",
      "kind": "variant",
      "name": "B128",
      "of": "zu::FloatBits",
      "signature": "B128"
    },
    {
      "id": "zu::FloatBits::B16",
      "kind": "variant",
      "name": "B16",
      "of": "zu::FloatBits",
      "signature": "B16"
    },
    {
      "id": "zu::FloatBits::B256",
      "kind": "variant",
      "name": "B256",
      "of": "zu::FloatBits",
      "signature": "B256"
    },
    {
      "id": "zu::FloatBits::B32",
      "kind": "variant",
      "name": "B32",
      "of": "zu::FloatBits",
      "signature": "B32"
    },
    {
      "id": "zu::FloatBits::B64",
      "kind": "variant",
      "name": "B64",
      "of": "zu::FloatBits",
      "signature": "B64"
    },
    {
      "id": "zu::FloatBits::bits",
      "kind": "method",
      "name": "bits",
      "of": "zu::FloatBits",
      "signature": "fn bits(self) -> u16",
      "doc": "The width in bits."
    },
    {
      "id": "zu::FloatBits::for_digits",
      "kind": "method",
      "name": "for_digits",
      "of": "zu::FloatBits",
      "signature": "fn for_digits(digits: u16) -> Option<FloatBits>",
      "doc": "The narrowest binary width whose significand holds `digits`\ndecimal digits, which is what `FLOAT(p)` (GV22) asks for."
    },
    {
      "id": "zu::Flow",
      "kind": "enum",
      "name": "Flow",
      "source": "zu_query::row::Flow",
      "doc": "What a streaming caller wants after a batch: the next one, or no\nmore. Stopping is not a failure and leaves nothing to clean up, so\nit is a value rather than an error."
    },
    {
      "id": "zu::Flow::More",
      "kind": "variant",
      "name": "More",
      "of": "zu::Flow",
      "signature": "More",
      "doc": "Keep going."
    },
    {
      "id": "zu::Flow::Stop",
      "kind": "variant",
      "name": "Stop",
      "of": "zu::Flow",
      "signature": "Stop",
      "doc": "Stop the statement here. The rows already handed over stand."
    },
    {
      "id": "zu::Frame",
      "kind": "struct",
      "name": "Frame",
      "source": "zu_query::frame::Frame",
      "doc": "A registered frame: rows of columns the engine reads and never owns.\n\nCloning one costs the column names and two handle bumps. It does not\ncopy a value, because there is no value here to copy: both frames\npoint at the caller's bytes and both hold the owner that keeps them\nalive. That is what lets a label be re-derived when the epoch moves\nwithout a statement mid-flight losing the frame it was reading."
    },
    {
      "id": "zu::Frame::column",
      "kind": "method",
      "name": "column",
      "of": "zu::Frame",
      "signature": "fn column(&self, name: &str) -> Option<ColId>",
      "doc": "The position of a column by name."
    },
    {
      "id": "zu::Frame::columns",
      "kind": "method",
      "name": "columns",
      "of": "zu::Frame",
      "signature": "fn columns(&self) -> impl Iterator",
      "doc": "The column positions and names, in the order they were\nregistered, which is the order the caller's frame held them in."
    },
    {
      "id": "zu::Frame::gather",
      "kind": "method",
      "name": "gather",
      "of": "zu::Frame",
      "signature": "fn gather(&self, col: ColId, rows: &[u64], arena: &mut MorselArena) -> ValueVector",
      "doc": "One column of arbitrary rows as a vector, in the caller's row\norder. Rows that are not a run cannot be pointed at, so this one\nalways builds."
    },
    {
      "id": "zu::Frame::id",
      "kind": "method",
      "name": "id",
      "of": "zu::Frame",
      "signature": "fn id(&self) -> TableId"
    },
    {
      "id": "zu::Frame::labels",
      "kind": "method",
      "name": "labels",
      "of": "zu::Frame",
      "signature": "fn labels(&self) -> u64",
      "doc": "The label bitset every row of a frame carries, the answer a\nstored table gives out of its catalog entry."
    },
    {
      "id": "zu::Frame::lane",
      "kind": "method",
      "name": "lane",
      "of": "zu::Frame",
      "signature": "fn lane(&self, col: ColId) -> Option<ColType>",
      "doc": "What the vector layer would carry this column as, `None` for a\ncolumn it has no lane for. The lanes a frame hands over are\nintegers, doubles and strings; a boolean or a temporal column\nresolves to nothing here and the statement reads it a row at a\ntime. A stored temporal column does have a lane, because it is\nwritten as the count it is and read back as one; a frame holds\nwhatever a caller lent it, and there is no temporal layout to\nlend yet."
    },
    {
      "id": "zu::Frame::name",
      "kind": "method",
      "name": "name",
      "of": "zu::Frame",
      "signature": "fn name(&self) -> &str"
    },
    {
      "id": "zu::Frame::new",
      "kind": "method",
      "name": "new",
      "of": "zu::Frame",
      "signature": "unsafe fn new(name: impl Into<String>, rows: u64, columns: Vec<Column>, owner: Arc<dyn Any + Send + Sync>) -> Result<Frame>",
      "doc": "Registers `columns` under `name` as `rows` rows.\n\nEvery check a read could have made is made here instead: that a\nword column is aligned for the words it will be read as, that\nan unsigned value fits the signed lane, that a scaled one does\nnot overflow, and that a string offset stays inside its buffer.\nA frame that comes back from this reads without failing.\n\n# Safety\n\nEvery pointer in `columns` must address at least the bytes the\nrow count implies, initialized, and neither written nor freed by\nanyone for as long as `owner` lives. `owner` must be what keeps\nthem alive, so that holding it here holds them."
    },
    {
      "id": "zu::Frame::resolve",
      "kind": "method",
      "name": "resolve",
      "of": "zu::Frame",
      "signature": "fn resolve(&self, name: &str) -> Option<(ColId, ColType)>",
      "doc": "Resolves a column by name as the vector layer would carry it."
    },
    {
      "id": "zu::Frame::rows",
      "kind": "method",
      "name": "rows",
      "of": "zu::Frame",
      "signature": "fn rows(&self) -> u64"
    },
    {
      "id": "zu::Frame::scan",
      "kind": "method",
      "name": "scan",
      "of": "zu::Frame",
      "signature": "fn scan(&self, chunk: u64, cols: &[ColId], pred: Option<&ZonePred>, arena: &mut MorselArena) -> Option<ScanChunk>",
      "doc": "Reads chunk `chunk`, `None` past the last one.\n\nThe vectors of a chunk point at the caller's buffers wherever\nthe layouts agree, which is what makes this a scan of somebody\nelse's memory rather than a copy of it. Where they do not, the\nwidening lands in `arena` and dies with the morsel.\n\nA frame carries no zone maps, so `pred` cannot skip a chunk\nwithout looking at it. What it can still do is thin one: the\nbound is checked a row at a time against the same eight-byte\nword a stored column would have decoded to, and a chunk it\nempties never reaches an operator at all."
    },
    {
      "id": "zu::Frame::value",
      "kind": "method",
      "name": "value",
      "of": "zu::Frame",
      "signature": "fn value(&self, col: ColId, row: u64) -> Value",
      "doc": "One value of one row, the read the row-at-a-time executor makes.\nThe row is the caller's and has already been bounds checked by\nthe scan that produced it."
    },
    {
      "id": "zu::FromRow",
      "kind": "trait",
      "name": "FromRow",
      "source": "zu_query::row::FromRow",
      "doc": "A Rust type one whole row can be read as, which is a tuple in every\ncase this crate provides. It is a trait rather than an inherent\nmethod so that a caller can implement it for its own struct without\nwaiting for a derive."
    },
    {
      "id": "zu::FromRow::from_row",
      "kind": "method",
      "name": "from_row",
      "of": "zu::FromRow",
      "signature": "fn from_row(row: Row) -> Result<Self>",
      "doc": "Reads the row, or raises what the first column that disagreed\nwith the type raised."
    },
    {
      "id": "zu::FromValue",
      "kind": "trait",
      "name": "FromValue",
      "source": "zu_query::row::FromValue",
      "doc": "A Rust type one column can be read as.\n\nThe lifetime is the result's, which is what lets `&'r str` be an\nimplementation here and is the whole reason this is not a plain\n`TryFrom<&Value>`: a borrowed string has to name the result it\nborrows from."
    },
    {
      "id": "zu::FromValue::from_value",
      "kind": "method",
      "name": "from_value",
      "of": "zu::FromValue",
      "signature": "fn from_value(value: &Value) -> Result<Self>",
      "doc": "Reads `value`, or raises `22G03` naming both types."
    },
    {
      "id": "zu::GqlStatus",
      "kind": "struct",
      "name": "GqlStatus",
      "source": "zu_common::gqlstatus::GqlStatus",
      "doc": "A GQLSTATUS value. Constructible only from the generated table, so\nevery value of this type is a code the standard defines."
    },
    {
      "id": "zu::GqlStatus::class",
      "kind": "method",
      "name": "class",
      "of": "zu::GqlStatus",
      "signature": "fn class(self) -> &str",
      "doc": "The two characters that open the code, which is the condition\nclass: `22` for the data exceptions, `42` for the syntax\nerrors, `40` for the rollbacks. A binding raises one exception\ntype per class, so this is the field it switches on, and taking\nthe first two characters of a five-character code is the sort of\nthing every binding would otherwise write for itself."
    },
    {
      "id": "zu::GqlStatus::code",
      "kind": "method",
      "name": "code",
      "of": "zu::GqlStatus",
      "signature": "fn code(self) -> &str"
    },
    {
      "id": "zu::GqlStatus::condition",
      "kind": "method",
      "name": "condition",
      "of": "zu::GqlStatus",
      "signature": "fn condition(self) -> &Condition",
      "doc": "The row this status names."
    },
    {
      "id": "zu::GqlStatus::doc_url",
      "kind": "method",
      "name": "doc_url",
      "of": "zu::GqlStatus",
      "signature": "fn doc_url(self) -> String",
      "doc": "The page that documents this condition. Derived from the code\nrather than stored, because a table of eighty urls is eighty\nchances for one of them to be the url of a different condition."
    },
    {
      "id": "zu::GqlStatus::from_code",
      "kind": "method",
      "name": "from_code",
      "of": "zu::GqlStatus",
      "signature": "fn from_code(code: &str) -> Option<Self>",
      "doc": "Looks a code up by its five characters. Returns `None` for\nanything the standard does not define, which is how a code\narriving from outside zu gets checked rather than trusted."
    },
    {
      "id": "zu::GqlStatus::retryable",
      "kind": "method",
      "name": "retryable",
      "of": "zu::GqlStatus",
      "signature": "fn retryable(self) -> bool",
      "doc": "Whether running the same statement again could succeed.\n\nTrue for `40000 transaction rollback`, which is the engine\nsaying it undid the work and nothing of it is in the file. False\nfor `40003 statement completion unknown`, which is the same\nclass and the opposite advice: a statement that may or may not\nhave committed is one a retry could apply twice, and a caller\nthat wants to retry it has to establish which happened first.\nFalse for everything else, since a statement that failed to\nparse parses no better the second time."
    },
    {
      "id": "zu::GqlStatus::severity",
      "kind": "method",
      "name": "severity",
      "of": "zu::GqlStatus",
      "signature": "fn severity(self) -> Severity"
    },
    {
      "id": "zu::GqlStatus::standard_text",
      "kind": "method",
      "name": "standard_text",
      "of": "zu::GqlStatus",
      "signature": "fn standard_text(self) -> String",
      "doc": "The standard's own words for this condition: the class name, and\nthe subclass name after it when there is one. This is the text a\nconformance harness compares against, so zu never paraphrases it.\nAnything zu wants to add goes in [`DiagnosticRecord::detail`]."
    },
    {
      "id": "zu::GraphStore",
      "kind": "trait",
      "name": "GraphStore",
      "source": "zu_storage::GraphStore",
      "doc": "A storage engine: zu1, sqlite, or s3."
    },
    {
      "id": "zu::GraphStore::catalog",
      "kind": "method",
      "name": "catalog",
      "of": "zu::GraphStore",
      "signature": "fn catalog(&self) -> Arc<Catalog>"
    },
    {
      "id": "zu::GraphStore::checkpoint",
      "kind": "method",
      "name": "checkpoint",
      "of": "zu::GraphStore",
      "signature": "fn checkpoint(&self, mode: CheckpointMode) -> Result<()>"
    },
    {
      "id": "zu::GraphStore::commit",
      "kind": "method",
      "name": "commit",
      "of": "zu::GraphStore",
      "signature": "fn commit(&self, batch: CommitBatch) -> Result<Epoch>"
    },
    {
      "id": "zu::GraphStore::ingest",
      "kind": "method",
      "name": "ingest",
      "of": "zu::GraphStore",
      "signature": "fn ingest(&self, groups: Vec<SealedNodeGroup>) -> Result<Epoch>"
    },
    {
      "id": "zu::GraphStore::snapshot",
      "kind": "method",
      "name": "snapshot",
      "of": "zu::GraphStore",
      "signature": "fn snapshot(&self) -> Arc<dyn Snapshot>"
    },
    {
      "id": "zu::IntBits",
      "kind": "enum",
      "name": "IntBits",
      "source": "zu_common::types::IntBits",
      "doc": "Width of an integer type, in bits.\n\nThe tower stops at 256 because the standard does, and every width is\na fixed stride, so the FOR and delta cascades in `zu-encoding` need\nthe width registered and nothing else."
    },
    {
      "id": "zu::IntBits::B128",
      "kind": "variant",
      "name": "B128",
      "of": "zu::IntBits",
      "signature": "B128"
    },
    {
      "id": "zu::IntBits::B16",
      "kind": "variant",
      "name": "B16",
      "of": "zu::IntBits",
      "signature": "B16"
    },
    {
      "id": "zu::IntBits::B256",
      "kind": "variant",
      "name": "B256",
      "of": "zu::IntBits",
      "signature": "B256"
    },
    {
      "id": "zu::IntBits::B32",
      "kind": "variant",
      "name": "B32",
      "of": "zu::IntBits",
      "signature": "B32"
    },
    {
      "id": "zu::IntBits::B64",
      "kind": "variant",
      "name": "B64",
      "of": "zu::IntBits",
      "signature": "B64"
    },
    {
      "id": "zu::IntBits::B8",
      "kind": "variant",
      "name": "B8",
      "of": "zu::IntBits",
      "signature": "B8"
    },
    {
      "id": "zu::IntBits::bits",
      "kind": "method",
      "name": "bits",
      "of": "zu::IntBits",
      "signature": "fn bits(self) -> u16",
      "doc": "The width in bits."
    },
    {
      "id": "zu::IntBits::for_digits",
      "kind": "method",
      "name": "for_digits",
      "of": "zu::IntBits",
      "signature": "fn for_digits(digits: u16) -> Option<IntBits>",
      "doc": "The narrowest width that holds `digits` decimal digits, which is\nwhat `INT(p)` (GV09) asks for. `None` when no width in the tower\nis wide enough.\n\nThe bound is the count of decimal digits a width holds in full:\nan i64 reaches 9_223_372_036_854_775_807, so it holds every 18\ndigit number and only some 19 digit ones, and a type that\npromised 19 would be promising a range it does not have."
    },
    {
      "id": "zu::IntBits::holds",
      "kind": "method",
      "name": "holds",
      "of": "zu::IntBits",
      "signature": "fn holds(self, word: u64, signed: bool) -> bool",
      "doc": "Whether a 64 bit lane word is a value of an integer type this\nwide.\n\nThe word is two's complement for a signed type and plain for an\nunsigned one, which is the reading the lane gives it, so this\nasks about the value rather than about the bits. A type 64 bits\nor wider holds every word there is, since a word is all a lane\ncarries and the wide towers have no lane of their own."
    },
    {
      "id": "zu::Interrupt",
      "kind": "struct",
      "name": "Interrupt",
      "source": "zu_common::interrupt::Interrupt",
      "doc": "A handle on a running statement.\n\nCloning is cloning the handle, not the state: every clone reads and\nwrites the same word, which is what lets one thread hand a copy to\nthe executor and keep one to raise. [`Interrupt::default`] is the\nhandle nobody is holding, and it is the one that costs nothing."
    },
    {
      "id": "zu::Interrupt::armed",
      "kind": "method",
      "name": "armed",
      "of": "zu::Interrupt",
      "signature": "fn armed() -> Interrupt",
      "doc": "A handle a caller can raise, with the word allocated for it.\n\nSpelled apart from [`Default`] because the two are different\nthings: this one can stop a statement and the default one\ncannot, and a constructor that quietly returned the second when\nasked for the first would be a cancellation that never arrives."
    },
    {
      "id": "zu::Interrupt::check",
      "kind": "method",
      "name": "check",
      "of": "zu::Interrupt",
      "signature": "fn check(&self) -> Result<()>",
      "doc": "The check an executor makes at a boundary: `Ok` while nobody has\nasked, and the condition that ends the statement once somebody\nhas."
    },
    {
      "id": "zu::Interrupt::clear",
      "kind": "method",
      "name": "clear",
      "of": "zu::Interrupt",
      "signature": "fn clear(&self)",
      "doc": "Puts the flag down and the count back to zero, which is what\nmakes one handle serve a session rather than one statement.\n\nCalled before a statement rather than after it, so that a stop\nwhich arrived while nothing was running cannot end the next\nthing the user types."
    },
    {
      "id": "zu::Interrupt::read",
      "kind": "method",
      "name": "read",
      "of": "zu::Interrupt",
      "signature": "fn read(&self, rows: u64)",
      "doc": "The executor reporting rows it has read out of storage.\n\nRows read rather than rows answered, because the number is there\nto show a person that something is happening, and a statement\nthat reads a hundred million rows to answer one row is exactly\nthe statement they are waiting on."
    },
    {
      "id": "zu::Interrupt::rows",
      "kind": "method",
      "name": "rows",
      "of": "zu::Interrupt",
      "signature": "fn rows(&self) -> u64",
      "doc": "How many rows it has read so far."
    },
    {
      "id": "zu::Interrupt::stop",
      "kind": "method",
      "name": "stop",
      "of": "zu::Interrupt",
      "signature": "fn stop(&self)",
      "doc": "Asks the statement to stop. Does nothing on a handle nobody\narmed, which is the honest answer: there is no statement on the\nother end of it."
    },
    {
      "id": "zu::Interrupt::stopped",
      "kind": "method",
      "name": "stopped",
      "of": "zu::Interrupt",
      "signature": "fn stopped(&self) -> bool",
      "doc": "Whether somebody has asked."
    },
    {
      "id": "zu::Layout",
      "kind": "enum",
      "name": "Layout",
      "source": "zu_query::frame::Layout",
      "doc": "Where one column's bytes are and how they are laid out.\n\nThis is the caller's description of memory it owns, in the terms\nArrow puts it in, and it is deliberately not an Arrow type: the\nengine never sees a schema, only widths and pointers, and mapping\none to the other is the client's business because the client is\nwhat knows which Arrow it is holding.\n\nIt clones, because a description is pointers and widths and copying\none copies no data. A client that registers the same columns on two\nconnections describes them once."
    },
    {
      "id": "zu::Layout::Bool",
      "kind": "variant",
      "name": "Bool",
      "of": "zu::Layout",
      "signature": "Bool { ptr: std::ptr::NonNull<u8> }",
      "doc": "One bit a row, packed low bit of the first byte first, which is\nArrow's bitmap and this engine's alike. The first row is the\npointer's low bit: a caller holding a sliced array with a bit\noffset of its own owes the shift before it gets here."
    },
    {
      "id": "zu::Layout::Float",
      "kind": "variant",
      "name": "Float",
      "of": "zu::Layout",
      "signature": "Float { ptr: std::ptr::NonNull<u8>, bits: zu_common::FloatBits }",
      "doc": "IEEE floats of one width."
    },
    {
      "id": "zu::Layout::Int",
      "kind": "variant",
      "name": "Int",
      "of": "zu::Layout",
      "signature": "Int { ptr: std::ptr::NonNull<u8>, bits: zu_common::IntBits, signed: bool, scale: i64 }",
      "doc": "Integers of one width, signed or not, and what one of them has\nto be multiplied by to reach the unit its logical type counts\nin. That is 1 for an integer and a date, and 1000 for the\nmicroseconds Arrow keeps a time or a timestamp in against the\nnanoseconds this engine keeps them in."
    },
    {
      "id": "zu::Layout::Str",
      "kind": "variant",
      "name": "Str",
      "of": "zu::Layout",
      "signature": "Str { offsets: std::ptr::NonNull<u8>, wide: bool, data: std::ptr::NonNull<u8>, data_len: usize }",
      "doc": "Characters end to end with offsets cutting them up, Arrow's\n`Utf8` when the offsets are 32 bits and `LargeUtf8` when they\nare 64. There are `rows + 1` of them."
    },
    {
      "id": "zu::Layout::View",
      "kind": "variant",
      "name": "View",
      "of": "zu::Layout",
      "signature": "View { views: std::ptr::NonNull<u8>, data: Vec<(std::ptr::NonNull<u8>, usize)> }",
      "doc": "Arrow's `Utf8View`: sixteen bytes a row over any number of data\nbuffers. A short string is already the engine's own view, byte\nfor byte; a long one names its buffer and offset in a different\norder and is rebuilt."
    },
    {
      "id": "zu::LogicalType",
      "kind": "enum",
      "name": "LogicalType",
      "source": "zu_common::types::LogicalType",
      "doc": "What GQL talks about: the language facing type lattice.\n\nNever present in a vector. A vector holds a [`PhysicalType`] and a\nvalidity bitmap, and the logical type is what the binder knows about\nthe column, which is a different and larger thing: `INT(4) NOT NULL`\nand `BIGINT` are two logical types over one physical layout."
    },
    {
      "id": "zu::LogicalType::Any",
      "kind": "variant",
      "name": "Any",
      "of": "zu::LogicalType",
      "signature": "Any",
      "doc": "GV65 and GV66. The open dynamic union: any type at all."
    },
    {
      "id": "zu::LogicalType::AnyProperty",
      "kind": "variant",
      "name": "AnyProperty",
      "of": "zu::LogicalType",
      "signature": "AnyProperty",
      "doc": "GV68. Any type a property may hold, which is `Any` minus the\nreference and constructed types."
    },
    {
      "id": "zu::LogicalType::BindingTable",
      "kind": "variant",
      "name": "BindingTable",
      "of": "zu::LogicalType",
      "signature": "BindingTable(Option<RecordType>)",
      "doc": "GV61. A binding table handle, with the record type of its rows\nwhen that is known."
    },
    {
      "id": "zu::LogicalType::Bool",
      "kind": "variant",
      "name": "Bool",
      "of": "zu::LogicalType",
      "signature": "Bool"
    },
    {
      "id": "zu::LogicalType::Bytes",
      "kind": "variant",
      "name": "Bytes",
      "of": "zu::LogicalType",
      "signature": "Bytes { min: Option<u32>, max: Option<u32>, fixed: bool }",
      "doc": "GV35 to GV38. Lengths are in octets."
    },
    {
      "id": "zu::LogicalType::Date",
      "kind": "variant",
      "name": "Date",
      "of": "zu::LogicalType",
      "signature": "Date",
      "doc": "GV39. Proleptic Gregorian, no zone."
    },
    {
      "id": "zu::LogicalType::Decimal",
      "kind": "variant",
      "name": "Decimal",
      "of": "zu::LogicalType",
      "signature": "Decimal { precision: u16, scale: u16 }",
      "doc": "GV17. Exact decimal, stored in the narrowest integer width that\nholds `precision` digits and scaled by `scale`."
    },
    {
      "id": "zu::LogicalType::Duration",
      "kind": "variant",
      "name": "Duration",
      "of": "zu::LogicalType",
      "signature": "Duration(DurationKind)",
      "doc": "GV41."
    },
    {
      "id": "zu::LogicalType::Edge",
      "kind": "variant",
      "name": "Edge",
      "of": "zu::LogicalType",
      "signature": "Edge(Option<String>)",
      "doc": "An edge reference, optionally of a named edge type."
    },
    {
      "id": "zu::LogicalType::Float",
      "kind": "variant",
      "name": "Float",
      "of": "zu::LogicalType",
      "signature": "Float { bits: FloatBits, precision: Option<u16> }",
      "doc": "GV20 to GV26."
    },
    {
      "id": "zu::LogicalType::Graph",
      "kind": "variant",
      "name": "Graph",
      "of": "zu::LogicalType",
      "signature": "Graph(Option<String>)",
      "doc": "GV60. A graph handle: a catalog id and a snapshot epoch, not\ndata. Making it a value is what turns `USE` from a statement\nprefix into something composable."
    },
    {
      "id": "zu::LogicalType::Int",
      "kind": "variant",
      "name": "Int",
      "of": "zu::LogicalType",
      "signature": "Int { signed: bool, bits: IntBits, precision: Option<u16> }",
      "doc": "GV01 to GV19. `signed` and the width are the tower; `precision`\nis the declared decimal digit count of `INT(p)`, kept because it\nis a check the engine owes the user and not a layout."
    },
    {
      "id": "zu::LogicalType::List",
      "kind": "variant",
      "name": "List",
      "of": "zu::LogicalType",
      "signature": "List { elem: Box<LogicalType>, max: Option<u32> }",
      "doc": "GV50."
    },
    {
      "id": "zu::LogicalType::LocalDatetime",
      "kind": "variant",
      "name": "LocalDatetime",
      "of": "zu::LogicalType",
      "signature": "LocalDatetime",
      "doc": "GV39. An instant with no zone."
    },
    {
      "id": "zu::LogicalType::LocalTime",
      "kind": "variant",
      "name": "LocalTime",
      "of": "zu::LogicalType",
      "signature": "LocalTime",
      "doc": "GV39. Time of day with no zone and therefore no DST."
    },
    {
      "id": "zu::LogicalType::Node",
      "kind": "variant",
      "name": "Node",
      "of": "zu::LogicalType",
      "signature": "Node(Option<String>)",
      "doc": "A node reference, optionally of a named node type."
    },
    {
      "id": "zu::LogicalType::Nothing",
      "kind": "variant",
      "name": "Nothing",
      "of": "zu::LogicalType",
      "signature": "Nothing",
      "doc": "GV72. The empty type: no value has it. A column can never be\ndeclared with it, an expression can have it, and the binder\nrefuses anything that would have to materialise one."
    },
    {
      "id": "zu::LogicalType::Null",
      "kind": "variant",
      "name": "Null",
      "of": "zu::LogicalType",
      "signature": "Null",
      "doc": "GV71. The type of the null literal, which every nullable type\naccepts and which carries no other value."
    },
    {
      "id": "zu::LogicalType::Nullable",
      "kind": "variant",
      "name": "Nullable",
      "of": "zu::LogicalType",
      "signature": "Nullable(Box<LogicalType>)",
      "doc": "GV90, orthogonal to everything above. Nullability is a wrapper\nand not a flag on each variant, so no variant can forget it."
    },
    {
      "id": "zu::LogicalType::Path",
      "kind": "variant",
      "name": "Path",
      "of": "zu::LogicalType",
      "signature": "Path(Option<Box<PathType>>)",
      "doc": "GV55. A path, whose type describes the alternating node and edge\ntypes it walks."
    },
    {
      "id": "zu::LogicalType::Record",
      "kind": "variant",
      "name": "Record",
      "of": "zu::LogicalType",
      "signature": "Record(RecordType)",
      "doc": "GV45 to GV48."
    },
    {
      "id": "zu::LogicalType::Str",
      "kind": "variant",
      "name": "Str",
      "of": "zu::LogicalType",
      "signature": "Str { min: Option<u32>, max: Option<u32>, fixed: bool }",
      "doc": "GV30 to GV32. Lengths are in characters, and a character is a\nUnicode scalar value. `fixed` is GV32, the one length constraint\nthat changes the layout rather than adding a check."
    },
    {
      "id": "zu::LogicalType::Union",
      "kind": "variant",
      "name": "Union",
      "of": "zu::LogicalType",
      "signature": "Union(Vec<LogicalType>)",
      "doc": "GV67. A closed dynamic union, `ANY<INT|STRING>`."
    },
    {
      "id": "zu::LogicalType::ZonedDatetime",
      "kind": "variant",
      "name": "ZonedDatetime",
      "of": "zu::LogicalType",
      "signature": "ZonedDatetime",
      "doc": "GV40. An instant with an offset from UTC in minutes."
    },
    {
      "id": "zu::LogicalType::ZonedTime",
      "kind": "variant",
      "name": "ZonedTime",
      "of": "zu::LogicalType",
      "signature": "ZonedTime",
      "doc": "GV40. Time of day with an offset from UTC in minutes."
    },
    {
      "id": "zu::LogicalType::base",
      "kind": "method",
      "name": "base",
      "of": "zu::LogicalType",
      "signature": "fn base(&self) -> &LogicalType",
      "doc": "This type with any nullability wrapper removed, which is the\nform every layout question is asked about."
    },
    {
      "id": "zu::LogicalType::bytes",
      "kind": "method",
      "name": "bytes",
      "of": "zu::LogicalType",
      "signature": "fn bytes() -> LogicalType",
      "doc": "A byte string with no length constraint."
    },
    {
      "id": "zu::LogicalType::float",
      "kind": "method",
      "name": "float",
      "of": "zu::LogicalType",
      "signature": "fn float(bits: FloatBits) -> LogicalType"
    },
    {
      "id": "zu::LogicalType::int",
      "kind": "method",
      "name": "int",
      "of": "zu::LogicalType",
      "signature": "fn int(bits: IntBits) -> LogicalType",
      "doc": "The common integer spellings, so a caller does not write the\nstruct out to say `BIGINT`."
    },
    {
      "id": "zu::LogicalType::is_material",
      "kind": "method",
      "name": "is_material",
      "of": "zu::LogicalType",
      "signature": "fn is_material(&self) -> bool",
      "doc": "Whether this type may hold a value at all. The immaterial types\n(GV71, GV72) cannot: `NOTHING` has no values and `NULL` has one\nthat is never stored in a vector."
    },
    {
      "id": "zu::LogicalType::is_nullable",
      "kind": "method",
      "name": "is_nullable",
      "of": "zu::LogicalType",
      "signature": "fn is_nullable(&self) -> bool",
      "doc": "Whether a value of this type may be null. `NULL` itself is\nnullable and nothing else is unless it says so, which is the\nGV90 reading: a declared type is not null until wrapped."
    },
    {
      "id": "zu::LogicalType::nullable",
      "kind": "method",
      "name": "nullable",
      "of": "zu::LogicalType",
      "signature": "fn nullable(self) -> LogicalType",
      "doc": "A nullable version of this type, without stacking wrappers."
    },
    {
      "id": "zu::LogicalType::physical",
      "kind": "method",
      "name": "physical",
      "of": "zu::LogicalType",
      "signature": "fn physical(&self) -> Option<PhysicalType>",
      "doc": "The layout a vector of this type uses, or `None` when there is\nnothing to lay out.\n\nThe dynamic unions map to `Struct`, which holds a type code\nchild and one child per member. That is the one construct in the\nsystem that puts a tag back next to each value, and it is\nconfined to columns declared as unions, which in practice is the\nschemaless ingest path and nothing else."
    },
    {
      "id": "zu::LogicalType::string",
      "kind": "method",
      "name": "string",
      "of": "zu::LogicalType",
      "signature": "fn string() -> LogicalType",
      "doc": "A string with no length constraint."
    },
    {
      "id": "zu::LogicalType::uint",
      "kind": "method",
      "name": "uint",
      "of": "zu::LogicalType",
      "signature": "fn uint(bits: IntBits) -> LogicalType"
    },
    {
      "id": "zu::NodeId",
      "kind": "struct",
      "name": "NodeId",
      "source": "zu_common::id::NodeId",
      "doc": "Packed node identifier. See the module docs for the bit layout."
    },
    {
      "id": "zu::NodeId::0",
      "kind": "field",
      "name": "0",
      "of": "zu::NodeId",
      "signature": "u64"
    },
    {
      "id": "zu::NodeId::group",
      "kind": "method",
      "name": "group",
      "of": "zu::NodeId",
      "signature": "fn group(self) -> NodeGroupId"
    },
    {
      "id": "zu::NodeId::new",
      "kind": "method",
      "name": "new",
      "of": "zu::NodeId",
      "signature": "fn new(table: TableId, group: NodeGroupId, row: NodeOffset) -> Self",
      "doc": "Packs the three components. Debug-asserts that each fits its field."
    },
    {
      "id": "zu::NodeId::row",
      "kind": "method",
      "name": "row",
      "of": "zu::NodeId",
      "signature": "fn row(self) -> NodeOffset"
    },
    {
      "id": "zu::NodeId::table",
      "kind": "method",
      "name": "table",
      "of": "zu::NodeId",
      "signature": "fn table(self) -> TableId"
    },
    {
      "id": "zu::NodeId::table_row",
      "kind": "method",
      "name": "table_row",
      "of": "zu::NodeId",
      "signature": "fn table_row(self) -> u64",
      "doc": "Row index within the table, `group * GROUP_ROWS + row`."
    },
    {
      "id": "zu::OpProfile",
      "kind": "struct",
      "name": "OpProfile",
      "source": "zu_query::exec::OpProfile",
      "doc": "One operator line of an EXPLAIN ANALYZE profile."
    },
    {
      "id": "zu::OpProfile::bnd",
      "kind": "field",
      "name": "bnd",
      "of": "zu::OpProfile",
      "signature": "Option<f64>",
      "doc": "The most rows the optimizer's ceiling allowed this operator,\nwhen the statistics were there to set one. `flat` above this is\na bound violation and perf/12 §6 makes that a hard fail."
    },
    {
      "id": "zu::OpProfile::bound_violation",
      "kind": "method",
      "name": "bound_violation",
      "of": "zu::OpProfile",
      "signature": "fn bound_violation(&self) -> bool",
      "doc": "The q-error of this operator's estimate, `max(est/act, act/est)`\n(perf/12 §4). Both sides are floored at one row: a zero actual\nagainst an estimate of 3 is an error of 3, not of infinity, and\nan operator nobody pulled has no error at all.\nWhether this operator produced more rows than the optimizer's\nceiling promised it could. Compared on the flattened count,\nwhich is what the estimate stands for.\n\nThe slack is there because the ceiling comes out of roots and\nfractional powers: a table whose every node holds one edge has\nan exact bound of `sqrt(n) * l2`, and in f64 that lands a\ncouple of ULPs under the edge count it is supposed to equal. A\nreal violation is a factor out, never a rounding."
    },
    {
      "id": "zu::OpProfile::detail",
      "kind": "field",
      "name": "detail",
      "of": "zu::OpProfile",
      "signature": "String",
      "doc": "What the operator is working on, rendered the way the statement\nwrote it: the tables a scan reads, the pattern an expand walks,\nthe predicate a filter asks. Empty where the operator has no\narguments to name."
    },
    {
      "id": "zu::OpProfile::est",
      "kind": "field",
      "name": "est",
      "of": "zu::OpProfile",
      "signature": "Option<f64>",
      "doc": "What the optimizer expected this operator to produce, when the\noperator is one that produces rows at all. `None` for the\nsource, the flattens, and the brackets themselves, which pass\ntheir input through and have no cardinality of their own."
    },
    {
      "id": "zu::OpProfile::flat",
      "kind": "field",
      "name": "flat",
      "of": "zu::OpProfile",
      "signature": "u64",
      "doc": "The rows those values stand for with the factorization\nmultiplied out: on a chain it equals `rows`, on a star it is\nthe product over every vector still unflat beside this one."
    },
    {
      "id": "zu::OpProfile::kind",
      "kind": "field",
      "name": "kind",
      "of": "zu::OpProfile",
      "signature": "&str",
      "doc": "The operator itself, the word a reader groups by."
    },
    {
      "id": "zu::OpProfile::name",
      "kind": "method",
      "name": "name",
      "of": "zu::OpProfile",
      "signature": "fn name(&self) -> String",
      "doc": "What the listing calls this operator, the kind and its detail\njoined the way EXPLAIN ANALYZE prints them."
    },
    {
      "id": "zu::OpProfile::nanos",
      "kind": "field",
      "name": "nanos",
      "of": "zu::OpProfile",
      "signature": "u64",
      "doc": "Self time in nanoseconds, child time excluded."
    },
    {
      "id": "zu::OpProfile::pulls",
      "kind": "field",
      "name": "pulls",
      "of": "zu::OpProfile",
      "signature": "u64",
      "doc": "Successful pulls: how many chunks this operator produced."
    },
    {
      "id": "zu::OpProfile::qerror",
      "kind": "method",
      "name": "qerror",
      "of": "zu::OpProfile",
      "signature": "fn qerror(&self) -> Option<f64>"
    },
    {
      "id": "zu::OpProfile::rows",
      "kind": "field",
      "name": "rows",
      "of": "zu::OpProfile",
      "signature": "u64",
      "doc": "Values produced across all pulls. Rows over pulls is the\naverage vector length, the factorization stat."
    },
    {
      "id": "zu::Options",
      "kind": "struct",
      "name": "Options",
      "source": "zu_query::exec::Options",
      "doc": "Execution switches. `flat` forces tuple-at-a-time execution by\nflattening after every producer, the differential oracle for the\nfactorized paths; flat runs also stay single-threaded so the\noracle is the fully sequential baseline."
    },
    {
      "id": "zu::Options::clock",
      "kind": "field",
      "name": "clock",
      "of": "zu::Options",
      "signature": "Option<zu_common::Clock>",
      "doc": "The instant the datetime value functions of ISO 20.27 answer,\n`None` for a run that has not read a clock yet.\n\nIt is here for the reason the interrupt is: it is state one\nstatement has and every path through the executor already\ncarries the switches. [`run_stages`] fills it in on the way in\nand hands the filled copy down, so a query written inside an\nexpression answers the same instant as the query around it\nrather than reading a clock of its own halfway through the scan.\nA caller may set it before running, which is how a test pins the\ntime."
    },
    {
      "id": "zu::Options::engine",
      "kind": "field",
      "name": "engine",
      "of": "zu::Options",
      "signature": "Engine",
      "doc": "Which executor runs a plan the pipeline one covers. Read here\nrather than from the environment so that one statement can ask\nfor the row executor without answering for every statement\nrunning beside it (#513)."
    },
    {
      "id": "zu::Options::flat",
      "kind": "field",
      "name": "flat",
      "of": "zu::Options",
      "signature": "bool"
    },
    {
      "id": "zu::Options::interrupt",
      "kind": "field",
      "name": "interrupt",
      "of": "zu::Options",
      "signature": "zu_common::Interrupt",
      "doc": "The handle whoever asked for the statement can stop it through,\nand the count of rows it has read so far. Here rather than as a\nparameter of its own because every execution path already\ncarries the switches, and a cancellation that only some of them\nwere handed would be a cancellation that works on some\nstatements. The default is the handle nobody armed, so a caller\nwho wants none pays a branch per chunk."
    },
    {
      "id": "zu::Options::morsel_rows",
      "kind": "field",
      "name": "morsel_rows",
      "of": "zu::Options",
      "signature": "usize",
      "doc": "Rows per morsel, 0 picks the 2048-tuple target from docs/02.\nSmaller values force many morsels on small graphs, which is how\nthe tests drive the parallel path over the mock fixtures."
    },
    {
      "id": "zu::Options::sink",
      "kind": "field",
      "name": "sink",
      "of": "zu::Options",
      "signature": "Sink",
      "doc": "What the sink of a plain projection keeps."
    },
    {
      "id": "zu::Options::sip",
      "kind": "field",
      "name": "sip",
      "of": "zu::Options",
      "signature": "Sip",
      "doc": "Whether a join hands what it knows about its keys to the\noperators producing its probe side, the sideways pass of\nperf/13 §1."
    },
    {
      "id": "zu::Options::status",
      "kind": "field",
      "name": "status",
      "of": "zu::Options",
      "signature": "Option<std::sync::Arc<dyn LastOutcome>>",
      "doc": "GA08. The GQL-status object the statement before this one in\nthis session ended with, as the record value\n[`crate::binder::STATUS_FN`] answers, and `None` for a session\nthat has run nothing yet.\n\nHere for the reason the clock is here: it is session state, one\nvalue for the length of a statement, and every path through the\nexecutor already carries the switches.\n\nKept as what the statement ended with rather than as the record\nitself, because every statement ends and almost none of them\nasks what the one before ended with: building the record here\nwould put five strings and a list on the way out of every query\nin the session for the sake of the one query in a thousand that\nreads them. [`LastOutcome::value`] builds it where it is read."
    },
    {
      "id": "zu::Options::threads",
      "kind": "field",
      "name": "threads",
      "of": "zu::Options",
      "signature": "usize",
      "doc": "Worker threads for morsel-parallel stages: 0 picks\n`min(cores, 8)` per docs/02, 1 forces sequential execution."
    },
    {
      "id": "zu::Options::wcoj",
      "kind": "field",
      "name": "wcoj",
      "of": "zu::Options",
      "signature": "Wcoj",
      "doc": "How closing-edge probes reach the `MultiwayIntersect`, the\nWCOJ closing step of docs/07 §4."
    },
    {
      "id": "zu::Options::zone",
      "kind": "field",
      "name": "zone",
      "of": "zu::Options",
      "signature": "i16",
      "doc": "GS07 and GS15. The session time zone as minutes east of UTC,\nwhich is the displacement the clock read below is stamped with.\nNought is UTC, which is what a session opens in and what every\nrun that never set one keeps.\n\nA displacement and never a zone name: a name is a rule the zone\ndatabase can change, so a session set to one would answer one\nthing today and another after an upgrade, while a session set to\nan offset answers the offset for as long as it holds it."
    },
    {
      "id": "zu::PlanNode",
      "kind": "struct",
      "name": "PlanNode",
      "source": "zu_query::plan::PlanNode",
      "doc": "One operator of a plan, with the operators feeding it underneath.\n\nThis is the tree EXPLAIN prints, before it is printed. `op` is what\nthe operator is and `detail` is what the listing writes after it,\nwhich is the operator's arguments rendered the way the statement\nwrote them. They are separate fields because a caller grouping\noperators by kind, or drawing them, should not have to cut a printed\nline back apart to find out what it is holding, and a listing is a\nrendering of this rather than the other way around."
    },
    {
      "id": "zu::PlanNode::binds",
      "kind": "field",
      "name": "binds",
      "of": "zu::PlanNode",
      "signature": "Vec<String>",
      "doc": "The variables this operator introduces, in the order it binds\nthem, and empty for the operators that only pass rows through."
    },
    {
      "id": "zu::PlanNode::bracket",
      "kind": "field",
      "name": "bracket",
      "of": "zu::PlanNode",
      "signature": "Option<Bracket>",
      "doc": "The bracket group this operator belongs to, `None` for a plain\nmatch. It prints in front of the operator, so an OPTIONAL MATCH\nexpand is [`PlanNode::name`] `OptionalExpand` and `op` `Expand`."
    },
    {
      "id": "zu::PlanNode::children",
      "kind": "field",
      "name": "children",
      "of": "zu::PlanNode",
      "signature": "Vec<PlanNode>"
    },
    {
      "id": "zu::PlanNode::count",
      "kind": "method",
      "name": "count",
      "of": "zu::PlanNode",
      "signature": "fn count(&self) -> usize",
      "doc": "How many operators the subtree holds, this one counted."
    },
    {
      "id": "zu::PlanNode::detail",
      "kind": "field",
      "name": "detail",
      "of": "zu::PlanNode",
      "signature": "String"
    },
    {
      "id": "zu::PlanNode::line",
      "kind": "method",
      "name": "line",
      "of": "zu::PlanNode",
      "signature": "fn line(&self) -> String",
      "doc": "The one line this operator prints, without its children and\nwithout the indent its depth would give it."
    },
    {
      "id": "zu::PlanNode::name",
      "kind": "method",
      "name": "name",
      "of": "zu::PlanNode",
      "signature": "fn name(&self) -> String",
      "doc": "What the listing calls this operator, the bracket prefix\nincluded."
    },
    {
      "id": "zu::PlanNode::op",
      "kind": "field",
      "name": "op",
      "of": "zu::PlanNode",
      "signature": "&str"
    },
    {
      "id": "zu::PlanNode::render",
      "kind": "method",
      "name": "render",
      "of": "zu::PlanNode",
      "signature": "fn render(&self) -> String",
      "doc": "The subtree as EXPLAIN prints it, this operator first and one\nchild per line under it, indented by depth."
    },
    {
      "id": "zu::PlanNode::tables",
      "kind": "field",
      "name": "tables",
      "of": "zu::PlanNode",
      "signature": "Vec<String>",
      "doc": "The tables this operator touches: node tables for a scan, rel\ntables for an expand, both for an insert. Empty elsewhere."
    },
    {
      "id": "zu::PlanNode::walk",
      "kind": "method",
      "name": "walk",
      "of": "zu::PlanNode",
      "signature": "fn walk(&self, visit: &mut impl FnMut)",
      "doc": "Every operator of the subtree with its depth, this one first,\nwhich is the order the listing prints them in."
    },
    {
      "id": "zu::Position",
      "kind": "struct",
      "name": "Position",
      "source": "zu_common::gqlstatus::Position",
      "doc": "Where in the statement text a condition was raised, said three ways.\n\n`line` and `column` are 1-based and the column counts characters\nrather than bytes, so a line of multi-byte text does not read as\nwider than it looks. That pair is what an editor or a shell wants:\nit has printed the line already and needs somewhere to put the\ncaret.\n\n`offset` is the same place as a byte index into the statement, and\nit is here for the caller the pair does not serve: a tool that holds\nthe text and wants to slice it, an editor mapping into a buffer it\nindexes by byte, a highlighter marking a range. Recovering it from\nthe pair means counting lines and characters again, over text the\nengine had in hand when it raised the condition. It is always on a\ncharacter boundary of that text, so slicing at it cannot panic."
    },
    {
      "id": "zu::Position::column",
      "kind": "field",
      "name": "column",
      "of": "zu::Position",
      "signature": "u32"
    },
    {
      "id": "zu::Position::line",
      "kind": "field",
      "name": "line",
      "of": "zu::Position",
      "signature": "u32"
    },
    {
      "id": "zu::Position::of",
      "kind": "method",
      "name": "of",
      "of": "zu::Position",
      "signature": "fn of(source: &str, offset: usize) -> Self",
      "doc": "The place `offset` bytes into `source`. An offset past the end\nlands at the end, which is where an error about a query that\nstopped too early belongs, and an offset inside a character\nlands on the start of that character."
    },
    {
      "id": "zu::Position::offset",
      "kind": "field",
      "name": "offset",
      "of": "zu::Position",
      "signature": "u32",
      "doc": "Bytes into the statement, 0-based."
    },
    {
      "id": "zu::Profile",
      "kind": "struct",
      "name": "Profile",
      "source": "zu_query::exec::Profile"
    },
    {
      "id": "zu::Profile::qerrors",
      "kind": "method",
      "name": "qerrors",
      "of": "zu::Profile",
      "signature": "fn qerrors(&self) -> Vec<f64>",
      "doc": "Every operator's q-error across every stage, unordered. This is\nwhat `zu bench cardinality` accumulates into percentiles."
    },
    {
      "id": "zu::Profile::render",
      "kind": "method",
      "name": "render",
      "of": "zu::Profile",
      "signature": "fn render(&self) -> String",
      "doc": "Renders one block per stage: the sink with its row count and\nwall time, then each operator top-down with pulls, rows, the\nestimate and its q-error where there is one, the average vector\nlength per pull, and self time."
    },
    {
      "id": "zu::Profile::stages",
      "kind": "field",
      "name": "stages",
      "of": "zu::Profile",
      "signature": "Vec<StageProfile>"
    },
    {
      "id": "zu::QueryPlan",
      "kind": "struct",
      "name": "QueryPlan",
      "source": "zu_query::plan::QueryPlan",
      "doc": "A compiled statement described without being run: its operators,\nthe columns it answers with, and the parameters it wants bound.\n\n`notes` is what the optimizer wants said about the plan that the\ntree does not carry, and the caller filling it in is the session,\nwhich is where compiling raised them."
    },
    {
      "id": "zu::QueryPlan::columns",
      "kind": "field",
      "name": "columns",
      "of": "zu::QueryPlan",
      "signature": "Vec<String>"
    },
    {
      "id": "zu::QueryPlan::notes",
      "kind": "field",
      "name": "notes",
      "of": "zu::QueryPlan",
      "signature": "Vec<String>"
    },
    {
      "id": "zu::QueryPlan::params",
      "kind": "field",
      "name": "params",
      "of": "zu::QueryPlan",
      "signature": "Vec<String>"
    },
    {
      "id": "zu::QueryPlan::render",
      "kind": "method",
      "name": "render",
      "of": "zu::QueryPlan",
      "signature": "fn render(&self) -> String",
      "doc": "The listing EXPLAIN prints, notes first, the tree under them,\nand the plan of each value query expression under that."
    },
    {
      "id": "zu::QueryPlan::root",
      "kind": "field",
      "name": "root",
      "of": "zu::QueryPlan",
      "signature": "Option<PlanNode>",
      "doc": "The top operator, `None` for the plan with no operators at all,\nwhich is the one row a statement with no clauses runs over."
    },
    {
      "id": "zu::QueryPlan::scalars",
      "kind": "field",
      "name": "scalars",
      "of": "zu::QueryPlan",
      "signature": "Vec<ScalarPlan>",
      "doc": "The value query expressions this statement holds, in the order\n`VALUE {n}` in the tree names them (GQ18).\n\nEach is a plan of its own rather than an operator of this one,\nbecause that is what it is: a query written where a value\nbelongs is run on its own and what stands in the tree is the\nvalue it answered."
    },
    {
      "id": "zu::Result",
      "kind": "type-alias",
      "name": "Result",
      "signature": "std::result::Result<T, ZuError>",
      "source": "zu_common::Result",
      "doc": "Result alias used across the workspace."
    },
    {
      "id": "zu::Row",
      "kind": "struct",
      "name": "Row",
      "source": "zu_query::row::Row",
      "doc": "One row of a result, borrowed from it.\n\nThe column names ride along with the values so that a row read out\nof an iterator can still be asked for a column by name, which is\nwhat a caller with a wide result wants and what a bare `&[Value]`\ncannot answer."
    },
    {
      "id": "zu::Row::columns",
      "kind": "method",
      "name": "columns",
      "of": "zu::Row",
      "signature": "fn columns(&self) -> &[String]",
      "doc": "The column names, in the order the statement projected them."
    },
    {
      "id": "zu::Row::get",
      "kind": "method",
      "name": "get",
      "of": "zu::Row",
      "signature": "fn get<T>(&self) -> Result<T>",
      "doc": "The whole row as one type: a tuple for the usual case, or\nanything implementing [`FromRow`].\n\nThe arity is checked before the columns are read, so a tuple\nthat does not match the projection is one error naming both\ncounts rather than a type error about whichever column happened\nto be first."
    },
    {
      "id": "zu::Row::get_at",
      "kind": "method",
      "name": "get_at",
      "of": "zu::Row",
      "signature": "fn get_at<T>(&self, ix: usize) -> Result<T>",
      "doc": "One column by position, typed."
    },
    {
      "id": "zu::Row::get_by_name",
      "kind": "method",
      "name": "get_by_name",
      "of": "zu::Row",
      "signature": "fn get_by_name<T>(&self, name: &str) -> Result<T>",
      "doc": "One column by name, typed. The name is the one the statement\nprojected, so `RETURN p.name AS name` is `name` and a projection\nwith no alias is spelled the way the plan spells it."
    },
    {
      "id": "zu::Row::is_empty",
      "kind": "method",
      "name": "is_empty",
      "of": "zu::Row",
      "signature": "fn is_empty(&self) -> bool",
      "doc": "Whether the row has no columns, which is what a statement with\nno projection returns."
    },
    {
      "id": "zu::Row::len",
      "kind": "method",
      "name": "len",
      "of": "zu::Row",
      "signature": "fn len(&self) -> usize",
      "doc": "How many columns this row has."
    },
    {
      "id": "zu::Row::new",
      "kind": "method",
      "name": "new",
      "of": "zu::Row",
      "signature": "fn new(columns: &[String], values: &[Value]) -> Row",
      "doc": "A row over names and values that belong together. The two are\nthe same length in every result the executor builds; a caller\nthat pairs mismatched slices gets a row whose extra values have\nno name, which reads as a missing column rather than a panic."
    },
    {
      "id": "zu::Row::value",
      "kind": "method",
      "name": "value",
      "of": "zu::Row",
      "signature": "fn value(&self, ix: usize) -> Result<&Value>",
      "doc": "One column by position, untyped."
    },
    {
      "id": "zu::Row::value_by_name",
      "kind": "method",
      "name": "value_by_name",
      "of": "zu::Row",
      "signature": "fn value_by_name(&self, name: &str) -> Result<&Value>",
      "doc": "One column by name, untyped."
    },
    {
      "id": "zu::Row::values",
      "kind": "method",
      "name": "values",
      "of": "zu::Row",
      "signature": "fn values(&self) -> &[Value]",
      "doc": "The values, untyped, for a caller doing its own matching."
    },
    {
      "id": "zu::RowIter",
      "kind": "struct",
      "name": "RowIter",
      "source": "zu_query::row::RowIter",
      "doc": "The rows of a result, borrowed one at a time."
    },
    {
      "id": "zu::ScalarPlan",
      "kind": "struct",
      "name": "ScalarPlan",
      "source": "zu_query::plan::ScalarPlan",
      "doc": "One value query expression as a plan, and how often it runs."
    },
    {
      "id": "zu::ScalarPlan::exists",
      "kind": "field",
      "name": "exists",
      "of": "zu::ScalarPlan",
      "signature": "bool",
      "doc": "True when what was written around it asks only whether it\nanswered a row, which is the `EXISTS` form rather than the\n`VALUE` one. It is what the listing titles the plan with."
    },
    {
      "id": "zu::ScalarPlan::plan",
      "kind": "field",
      "name": "plan",
      "of": "zu::ScalarPlan",
      "signature": "QueryPlan"
    },
    {
      "id": "zu::ScalarPlan::reads",
      "kind": "field",
      "name": "reads",
      "of": "zu::ScalarPlan",
      "signature": "Vec<String>",
      "doc": "The names it reads from the query around it, empty for one that\nreads none. Empty is the whole test for whether it decorrelates:\na query reading nothing answers the same value for every row and\nis run once, and a query reading a name is run per row."
    },
    {
      "id": "zu::Severity",
      "kind": "enum",
      "name": "Severity",
      "source": "zu_common::gqlstatus::Severity",
      "doc": "What a condition means for the statement that raised it, taken from\nthe class category letter in the artifact."
    },
    {
      "id": "zu::Severity::Exception",
      "kind": "variant",
      "name": "Exception",
      "of": "zu::Severity",
      "signature": "Exception",
      "doc": "`X`: the statement did not succeed."
    },
    {
      "id": "zu::Severity::Informational",
      "kind": "variant",
      "name": "Informational",
      "of": "zu::Severity",
      "signature": "Informational",
      "doc": "`I`: informational."
    },
    {
      "id": "zu::Severity::NoData",
      "kind": "variant",
      "name": "NoData",
      "of": "zu::Severity",
      "signature": "NoData",
      "doc": "`N`: the statement succeeded and returned nothing."
    },
    {
      "id": "zu::Severity::Success",
      "kind": "variant",
      "name": "Success",
      "of": "zu::Severity",
      "signature": "Success",
      "doc": "`S`: the statement succeeded."
    },
    {
      "id": "zu::Severity::Warning",
      "kind": "variant",
      "name": "Warning",
      "of": "zu::Severity",
      "signature": "Warning",
      "doc": "`W`: the statement succeeded and has something to say about it."
    },
    {
      "id": "zu::Severity::is_success",
      "kind": "method",
      "name": "is_success",
      "of": "zu::Severity",
      "signature": "const fn is_success(self) -> bool",
      "doc": "True when the statement kept its result. Warnings ride along with\nan answer; exceptions replace it."
    },
    {
      "id": "zu::Severity::letter",
      "kind": "method",
      "name": "letter",
      "of": "zu::Severity",
      "signature": "const fn letter(self) -> &str",
      "doc": "The one character ISO writes it as, which is what a status read\nas a value carries: a client that has to print or compare one\nshould not have to pick its own spelling of these five words."
    },
    {
      "id": "zu::Sink",
      "kind": "enum",
      "name": "Sink",
      "source": "zu_query::exec::Sink",
      "doc": "What the sink of a projection with nothing above it keeps.\n`Columns` fills the buffers the executor computed in, which is the\ndefault and is what a columnar client reads without a transpose.\n`Rows` flattens each row as it is found, the way every other sink\ndoes, and is the baseline the columns are measured against and the\nanswer they have to match."
    },
    {
      "id": "zu::Sink::Columns",
      "kind": "variant",
      "name": "Columns",
      "of": "zu::Sink",
      "signature": "Columns"
    },
    {
      "id": "zu::Sink::Rows",
      "kind": "variant",
      "name": "Rows",
      "of": "zu::Sink",
      "signature": "Rows"
    },
    {
      "id": "zu::Sip",
      "kind": "enum",
      "name": "Sip",
      "source": "zu_query::exec::Sip",
      "doc": "The sideways information passing switch. `On` lets a join publish a\nfilter over its build keys to the level its probe key is read from,\nwhich is the default. `Off` pins the plain probe, so a run with it\nis the baseline the filter is measured against and the rows either\nway have to match."
    },
    {
      "id": "zu::Sip::Off",
      "kind": "variant",
      "name": "Off",
      "of": "zu::Sip",
      "signature": "Off"
    },
    {
      "id": "zu::Sip::On",
      "kind": "variant",
      "name": "On",
      "of": "zu::Sip",
      "signature": "On"
    },
    {
      "id": "zu::Snapshot",
      "kind": "trait",
      "name": "Snapshot",
      "source": "zu_storage::Snapshot",
      "doc": "A consistent read view at a fixed epoch."
    },
    {
      "id": "zu::Snapshot::csr",
      "kind": "method",
      "name": "csr",
      "of": "zu::Snapshot",
      "signature": "fn csr(&self, t: TableId, g: NodeGroupId, dir: Direction) -> Result<CsrRef>"
    },
    {
      "id": "zu::Snapshot::epoch",
      "kind": "method",
      "name": "epoch",
      "of": "zu::Snapshot",
      "signature": "fn epoch(&self) -> Epoch"
    },
    {
      "id": "zu::Snapshot::lookup_pk",
      "kind": "method",
      "name": "lookup_pk",
      "of": "zu::Snapshot",
      "signature": "fn lookup_pk(&self, t: TableId, key: &[u8]) -> Result<Option<NodeOffset>>"
    },
    {
      "id": "zu::Snapshot::scan_column",
      "kind": "method",
      "name": "scan_column",
      "of": "zu::Snapshot",
      "signature": "fn scan_column(&self, t: TableId, g: NodeGroupId, c: u32) -> Result<SegmentRef>"
    },
    {
      "id": "zu::StageProfile",
      "kind": "struct",
      "name": "StageProfile",
      "source": "zu_query::exec::StageProfile",
      "doc": "One stage of a profiled run: the operator pipeline bottom-up plus\nthe sink that materialized the rows."
    },
    {
      "id": "zu::StageProfile::nanos",
      "kind": "field",
      "name": "nanos",
      "of": "zu::StageProfile",
      "signature": "u64",
      "doc": "Wall time of the whole stage in nanoseconds, sink included."
    },
    {
      "id": "zu::StageProfile::ops",
      "kind": "field",
      "name": "ops",
      "of": "zu::StageProfile",
      "signature": "Vec<OpProfile>"
    },
    {
      "id": "zu::StageProfile::out_rows",
      "kind": "field",
      "name": "out_rows",
      "of": "zu::StageProfile",
      "signature": "u64"
    },
    {
      "id": "zu::StageProfile::sink",
      "kind": "field",
      "name": "sink",
      "of": "zu::StageProfile",
      "signature": "String"
    },
    {
      "id": "zu::Streamed",
      "kind": "struct",
      "name": "Streamed",
      "source": "zu_query::exec::Streamed",
      "doc": "What a streamed statement did, once its rows have all been handed\nover. The rows themselves are gone by now, which is the point."
    },
    {
      "id": "zu::Streamed::columns",
      "kind": "field",
      "name": "columns",
      "of": "zu::Streamed",
      "signature": "Vec<String>",
      "doc": "The column names, the one part of a result a streaming caller\nstill wants after the fact."
    },
    {
      "id": "zu::Streamed::notices",
      "kind": "field",
      "name": "notices",
      "of": "zu::Streamed",
      "signature": "Vec<zu_common::gqlstatus::DiagnosticRecord>",
      "doc": "Conditions raised without stopping the statement, as on any\nother result."
    },
    {
      "id": "zu::Streamed::rows",
      "kind": "field",
      "name": "rows",
      "of": "zu::Streamed",
      "signature": "u64",
      "doc": "How many rows were handed over, which is fewer than the\nstatement would have returned when the caller stopped early."
    },
    {
      "id": "zu::Streamed::stopped",
      "kind": "field",
      "name": "stopped",
      "of": "zu::Streamed",
      "signature": "bool",
      "doc": "Whether the caller stopped it."
    },
    {
      "id": "zu::Streamed::streamed",
      "kind": "field",
      "name": "streamed",
      "of": "zu::Streamed",
      "signature": "bool",
      "doc": "Whether the rows were made and handed over in batches, rather\nthan buffered whole and cut into batches afterwards."
    },
    {
      "id": "zu::Temporal",
      "kind": "enum",
      "name": "Temporal",
      "source": "zu_common::temporal::Temporal",
      "doc": "One temporal value.\n\nThe variants carry counts and not fields. A zoned value carries the\ninstant and the offset separately, so two zoned values compare by\ninstant while each still prints in the zone it was written in."
    },
    {
      "id": "zu::Temporal::Date",
      "kind": "variant",
      "name": "Date",
      "of": "zu::Temporal",
      "signature": "Date(i32)",
      "doc": "Days since 1970-01-01."
    },
    {
      "id": "zu::Temporal::Duration",
      "kind": "variant",
      "name": "Duration",
      "of": "zu::Temporal",
      "signature": "Duration(zu_common::types::DurationKind, i64)",
      "doc": "Months for a year-month duration, nanoseconds for a day-time\none. The kind is carried because the number means nothing on\nits own."
    },
    {
      "id": "zu::Temporal::LocalDatetime",
      "kind": "variant",
      "name": "LocalDatetime",
      "of": "zu::Temporal",
      "signature": "LocalDatetime(i64)",
      "doc": "Nanoseconds since 1970-01-01T00:00:00."
    },
    {
      "id": "zu::Temporal::LocalTime",
      "kind": "variant",
      "name": "LocalTime",
      "of": "zu::Temporal",
      "signature": "LocalTime(i64)",
      "doc": "Nanoseconds since midnight."
    },
    {
      "id": "zu::Temporal::ZonedDatetime",
      "kind": "variant",
      "name": "ZonedDatetime",
      "of": "zu::Temporal",
      "signature": "ZonedDatetime { nanos: i64, offset: i16 }",
      "doc": "Nanoseconds since the epoch in UTC, and the offset from UTC in\nminutes that the value was written with."
    },
    {
      "id": "zu::Temporal::ZonedTime",
      "kind": "variant",
      "name": "ZonedTime",
      "of": "zu::Temporal",
      "signature": "ZonedTime { nanos: i64, offset: i16 }",
      "doc": "Nanoseconds since midnight in the offset's own day, and the\noffset from UTC in minutes."
    },
    {
      "id": "zu::Temporal::between",
      "kind": "method",
      "name": "between",
      "of": "zu::Temporal",
      "signature": "fn between(from: Temporal, to: Temporal, kind: DurationKind) -> Option<Temporal>",
      "doc": "The duration from `from` to `to`, of the kind asked for, or\n`None` when the two are not two instants of one shape or the\nanswer leaves the calendar.\n\nThis is what `DURATION_BETWEEN` answers and what a minus\nbetween two instants answers, so the operator and the function\ncannot drift apart.\n\nThe two operands have to be the same shape. A date and a time\nhave no length between them, and a local datetime and a zoned\none have none either, because a local value names a wall clock\nand not an instant, so the length would depend on a zone nobody\nwrote.\n\nA day-time answer is a count of nanoseconds and needs no\ncalendar. A year-month answer is the largest count of whole\nmonths that does not carry `from` past `to`, which is stated\nagainst zu's own month addition rather than against a\ncomparison of day numbers. The two readings differ: 31 March to\n30 April is one month here, because 31 March plus one month is\n30 April and lands exactly on it, and is nought under a rule\nthat compares the days of the month first. Stating it against\nthe addition is what makes `from + DURATION_BETWEEN(from, to)`\nland at or before `to` and one month more overshoot, and a law\nthat holds is worth more than agreement with a rule that does\nnot.\n\nA backwards answer truncates toward nought the same way, so the\ntwo directions are not mirror images when a month is clipped."
    },
    {
      "id": "zu::Temporal::from_parts",
      "kind": "method",
      "name": "from_parts",
      "of": "zu::Temporal",
      "signature": "fn from_parts(ty: &LogicalType, parts: &Parts) -> Option<Temporal>",
      "doc": "The value a record constructor spells, or `None` where it\nspells none.\n\nThis is the second way ISO 20.27 and 20.29 write a temporal\nvalue: `DATE({year: 2024, month: 1, day: 1})` beside\n`DATE('2024-01-01')`. A field the record left out takes the\nleast value it has, so `DATE({year: 2024})` is the first of\nJanuary, which is what the standard's default for an omitted\n`<field>` comes to.\n\n`None` is the calendar refusing, not the record being malformed:\nthe names were checked before anything got here and what is\nleft is a thirteenth month or a thirty-first of February, which\nthe caller reports as 22G06."
    },
    {
      "id": "zu::Temporal::logical_type",
      "kind": "method",
      "name": "logical_type",
      "of": "zu::Temporal",
      "signature": "fn logical_type(&self) -> LogicalType",
      "doc": "The lattice type of this value."
    },
    {
      "id": "zu::Temporal::parse",
      "kind": "method",
      "name": "parse",
      "of": "zu::Temporal",
      "signature": "fn parse(ty: &LogicalType, text: &str) -> Option<Temporal>",
      "doc": "The value `text` spells as a value of type `ty`, or `None` when\nit spells none.\n\nA datetime type accepts a date on its own, at midnight, because\nthe standard's datetime literal does. Nothing else is widened:\na date is not a datetime and reading one as the other silently\nwould answer a question nobody asked."
    },
    {
      "id": "zu::Temporal::parse_any",
      "kind": "method",
      "name": "parse_any",
      "of": "zu::Temporal",
      "signature": "fn parse_any(text: &str) -> Option<Temporal>",
      "doc": "The value `text` spells when the type is not written out, which\nis what a property in a fixture and a value read back from a\ncolumn both need.\n\nThe shape decides: a leading `P` is a duration, a `T` or a space\nin the middle is a datetime, a colon is a time, and a dash is a\ndate."
    },
    {
      "id": "zu::Temporal::parse_sql_interval",
      "kind": "method",
      "name": "parse_sql_interval",
      "of": "zu::Temporal",
      "signature": "fn parse_sql_interval(text: &str, qualifier: &IntervalQualifier) -> Option<Temporal>",
      "doc": "The duration `text` spells when it is written the way SQL\nwrites one, `INTERVAL '3 04:05:06' DAY TO SECOND`, with the\nqualifier already read.\n\nThe qualifier is not decoration. It is the whole of what says\nhow to read the string, because `'1-2'` is a year and two\nmonths under `YEAR TO MONTH` and is nothing at all under `DAY`,\nand `'3'` is three of whichever single field was named. So this\nreads the fields the qualifier lists, in order, and refuses\nanything the qualifier did not ask for.\n\nThe leading field is unbounded and the ones behind it are not:\n`'25' HOUR` is twenty five hours, and `'1 25:00:00' DAY TO\nSECOND` is not a day and an hour, because a written hour that\nfollows a written day has 24 of its own and the standard says\nso. A number that runs over its field is refused rather than\ncarried, since carrying it would answer a question the\nstatement did not ask."
    },
    {
      "id": "zu::Wcoj",
      "kind": "enum",
      "name": "Wcoj",
      "source": "zu_query::exec::Wcoj",
      "doc": "The WCOJ fusion switch. The optimizer marks cyclic closes on the\nplan; `Auto` honors those marks, which is the default path. `Force`\nfuses every close the fusion can structurally take, marked or not,\nand `Off` pins the binary ExpandInto/AspJoin pair, the baseline for\ndifferential runs."
    },
    {
      "id": "zu::Wcoj::Auto",
      "kind": "variant",
      "name": "Auto",
      "of": "zu::Wcoj",
      "signature": "Auto"
    },
    {
      "id": "zu::Wcoj::Force",
      "kind": "variant",
      "name": "Force",
      "of": "zu::Wcoj",
      "signature": "Force"
    },
    {
      "id": "zu::Wcoj::Off",
      "kind": "variant",
      "name": "Off",
      "of": "zu::Wcoj",
      "signature": "Off"
    },
    {
      "id": "zu::ZuError",
      "kind": "enum",
      "name": "ZuError",
      "source": "zu_common::error::ZuError",
      "doc": "Top-level error for all zu operations."
    },
    {
      "id": "zu::ZuError::Conflict",
      "kind": "variant",
      "name": "Conflict",
      "of": "zu::ZuError",
      "signature": "Conflict(String)",
      "doc": "A compare-and-swap or ownership check lost to a concurrent writer."
    },
    {
      "id": "zu::ZuError::Corrupt",
      "kind": "variant",
      "name": "Corrupt",
      "of": "zu::ZuError",
      "signature": "Corrupt { what: &str, detail: String }"
    },
    {
      "id": "zu::ZuError::Gql",
      "kind": "variant",
      "name": "Gql",
      "of": "zu::ZuError",
      "signature": "Gql(Box<zu_common::gqlstatus::DiagnosticRecord>)",
      "doc": "A GQL condition, with the standard's code and our own detail."
    },
    {
      "id": "zu::ZuError::Interrupted",
      "kind": "variant",
      "name": "Interrupted",
      "of": "zu::ZuError",
      "signature": "Interrupted",
      "doc": "The caller asked the statement to stop and it did, at the next\nboundary the executor checks ([`crate::Interrupt`]). No condition\ncode, because the standard has none for this and the statement\ndid not fail: it was stopped, which is a fact about the caller\nrather than about the query."
    },
    {
      "id": "zu::ZuError::InvalidArgument",
      "kind": "variant",
      "name": "InvalidArgument",
      "of": "zu::ZuError",
      "signature": "InvalidArgument(String)"
    },
    {
      "id": "zu::ZuError::Io",
      "kind": "variant",
      "name": "Io",
      "of": "zu::ZuError",
      "signature": "Io(std::io::Error)"
    },
    {
      "id": "zu::ZuError::Unsupported",
      "kind": "variant",
      "name": "Unsupported",
      "of": "zu::ZuError",
      "signature": "Unsupported { what: &str, id: u32 }"
    },
    {
      "id": "zu::ZuError::about",
      "kind": "method",
      "name": "about",
      "of": "zu::ZuError",
      "signature": "fn about(self, subject: Subject) -> Self",
      "doc": "The same error, about a thing the statement named.\n\nA builder rather than a fourth constructor, so that a raise site\nadds a subject without changing which of the three it went\nthrough. It does nothing to an engine-internal failure, which\nhas no record to put a subject on."
    },
    {
      "id": "zu::ZuError::diagnostic",
      "kind": "method",
      "name": "diagnostic",
      "of": "zu::ZuError",
      "signature": "fn diagnostic(&self) -> Option<&DiagnosticRecord>",
      "doc": "The diagnostic record for this error, if it has one."
    },
    {
      "id": "zu::ZuError::excerpt",
      "kind": "method",
      "name": "excerpt",
      "of": "zu::ZuError",
      "signature": "fn excerpt(&self) -> Option<&str>",
      "doc": "The line of the statement this was raised on, when the statement\ntext was in hand where it was raised."
    },
    {
      "id": "zu::ZuError::gql",
      "kind": "method",
      "name": "gql",
      "of": "zu::ZuError",
      "signature": "fn gql(status: GqlStatus, detail: impl Into<String>) -> Self",
      "doc": "Raises `status` with our own account of what happened."
    },
    {
      "id": "zu::ZuError::gql_at",
      "kind": "method",
      "name": "gql_at",
      "of": "zu::ZuError",
      "signature": "fn gql_at(status: GqlStatus, position: Position, detail: impl std::fmt::Display) -> Self",
      "doc": "The same, raised at a place in the statement text. The message\nreads as it did before, with the line and column ahead of the\ndetail, and the pair is also kept as a pair so that a caller can\npoint at the token instead of parsing the sentence."
    },
    {
      "id": "zu::ZuError::gql_in",
      "kind": "method",
      "name": "gql_in",
      "of": "zu::ZuError",
      "signature": "fn gql_in(status: GqlStatus, source: &str, offset: usize, detail: impl std::fmt::Display) -> Self",
      "doc": "The same, raised at a byte offset into the statement text.\n\nThe front end raises through this one rather than through\n[`Self::gql_at`], because the source is what turns an offset\ninto a line and a column and it is also the only way to quote\nthe line back. A caller three layers up has the message and the\nnumbers and no longer has the text."
    },
    {
      "id": "zu::ZuError::gqlstatus",
      "kind": "method",
      "name": "gqlstatus",
      "of": "zu::ZuError",
      "signature": "fn gqlstatus(&self) -> Option<GqlStatus>",
      "doc": "The GQLSTATUS value for this error, if it has one. Engine-internal\nerrors return `None` rather than a code that would be a guess."
    },
    {
      "id": "zu::ZuError::position",
      "kind": "method",
      "name": "position",
      "of": "zu::ZuError",
      "signature": "fn position(&self) -> Option<Position>",
      "doc": "Where in the statement this was raised, when it was raised\nsomewhere. An engine-internal failure and a runtime condition\nboth answer `None`."
    },
    {
      "id": "zu::ZuError::retryable",
      "kind": "method",
      "name": "retryable",
      "of": "zu::ZuError",
      "signature": "fn retryable(&self) -> bool",
      "doc": "Whether running the same thing again could succeed, which is the\none question a caller writing a retry loop is asking.\n\nA lost compare-and-swap is the case: a concurrent writer got\nthere first, so the work was not applied and the same call on a\nfresh read may well win. A condition answers for itself, which\nis `40000 transaction rollback` and nothing else. Everything\nremaining is false, including an interruption, which is not the\nengine failing but the caller having asked it to stop."
    },
    {
      "id": "zu::ZuError::subject",
      "kind": "method",
      "name": "subject",
      "of": "zu::ZuError",
      "signature": "fn subject(&self) -> Option<&Subject>",
      "doc": "What the condition is about, when it is about something named."
    },
    {
      "id": "zu::append",
      "kind": "module",
      "name": "append",
      "doc": "The bulk-load appender of dx/04 §6.\n\nA row at a time through a write statement is the wrong shape for\nloading data: every row would be parsed, bound, planned, and\ncommitted, and the commit is the expensive part, so a million rows\nwould be a million commits and the load would be dominated by\ndurability work nobody asked for. The appender is the other shape.\nRows go into per-column buffers in memory, and a flush turns the\nwhole buffer into sealed segments in the data file with one\n`IngestRef` frame in the log, which is the WAL bypass docs/08 §6\ndescribes: the log stays a handful of bytes whether the flush\ncarries ten rows or ten million.\n\n```no_run\nuse zu::Database;\n\nlet db = Database::open(\"social.zu1\")?;\nlet mut conn = db.connect()?;\nlet mut app = conn.appender(\"person\")?;\napp.append_row((1i64, \"ada\"))?;\napp.append_row((2i64, \"grace\"))?;\napp.close()?;\n# Ok::<(), zu::ZuError>(())\n```\n\nA row is every column of the table, in the order the table declares\nthem, and a column is a position rather than a name: naming the\ncolumns per row would cost a lookup per value on the one path where\nper-value cost is the whole story, and a loader knows its own\ncolumn order.\n\nA flush is one commit. When it returns the rows are durable and\nevery later query sees them, and before it returns nothing sees\nanything, because a flush ends by folding the sealed segments into\nthe base the query path reads. That fold costs time proportional to\nthe table rather than to the batch, which is the whole reason the\nappender buffers: flush once per load, not once per row."
    },
    {
      "id": "zu::append::AppendRow",
      "kind": "trait",
      "name": "AppendRow",
      "doc": "What [`Appender::append_row`] accepts: a tuple of things that are\nfields, or a slice of fields already.\n\nA tuple is what a loader writing rows by hand has, and a slice is\nwhat a loader forwarding rows from somewhere else has."
    },
    {
      "id": "zu::append::AppendRow::write_into",
      "kind": "method",
      "name": "write_into",
      "of": "zu::append::AppendRow",
      "signature": "fn write_into(self, sink: &mut RowSink)",
      "doc": "Writes this row's fields, in column order."
    },
    {
      "id": "zu::append::Appender",
      "kind": "struct",
      "name": "Appender",
      "doc": "A bulk load in progress: rows buffered in memory, written to the\ndatabase when you flush.\n\nTake one with [`crate::Connection::appender`], append rows to it,\nand call [`Appender::close`]. Dropping it without closing flushes\ntoo, because a loader that forgot is better served by its data\narriving than by it vanishing, but a drop has nowhere to put an\nerror and so cannot tell you whether the flush worked. That is the\none footgun dx/04 §6 accepts, and `#[must_use]` is what points a\ncaller at the version that reports. The drop does not panic on a\nbuffer it is handed, in debug builds either: a loader that hits an\nerror between rows and returns is exactly the case where an\nappender is dropped with rows in it, and turning that into an abort\non the way out of an unwind would be the worse failure.\n\nTwo things a table can be that an appender will not append to, both\nrefused when the appender opens rather than at the flush that would\nhave failed. A column that holds a null has no way in this API to\nsay which rows the appended values are for, so the write statements\nof G3 are what answers it. A table whose row domain a rel table's\nkey index is built over cannot grow without that index being\nreallocated, so the fold refuses it, and this refuses it earlier,\nbefore a load is buffered against it."
    },
    {
      "id": "zu::append::Appender::append_row",
      "kind": "method",
      "name": "append_row",
      "of": "zu::append::Appender",
      "signature": "fn append_row<R>(&mut self, row: R) -> Result<()>",
      "doc": "Appends one row: every column of a node table in declared\norder, or a source and a destination for a rel table.\n\nThe row goes into memory and nothing else happens, so this is a\npush per column and, on a string column, one copy. A row of the\nwrong width or with a field of the wrong type is refused with\nnothing of it kept, so an appender is still usable after a\ncaller fixes the row and tries again."
    },
    {
      "id": "zu::append::Appender::buffered",
      "kind": "method",
      "name": "buffered",
      "of": "zu::append::Appender",
      "signature": "fn buffered(&self) -> u64",
      "doc": "Rows buffered and not yet written."
    },
    {
      "id": "zu::append::Appender::close",
      "kind": "method",
      "name": "close",
      "of": "zu::append::Appender",
      "signature": "fn close(self) -> Result<u64>",
      "doc": "Flushes and gives back the rows committed, which is how to\nfinish an appender when you want to know that it worked."
    },
    {
      "id": "zu::append::Appender::committed",
      "kind": "method",
      "name": "committed",
      "of": "zu::append::Appender",
      "signature": "fn committed(&self) -> u64",
      "doc": "Rows this appender has committed."
    },
    {
      "id": "zu::append::Appender::flush",
      "kind": "method",
      "name": "flush",
      "of": "zu::append::Appender",
      "signature": "fn flush(&mut self) -> Result<()>",
      "doc": "Writes every buffered row and makes it readable.\n\nOne commit, whatever the buffer holds: the values are sealed\ninto the data file as segments, one frame naming them is\nappended and synced to the log, which is the commit point, and\nthe fold that follows seals them into the base every query\nreads. On return the buffer is empty and the rows are there.\n\nA flush with nothing buffered touches no file and returns, so a\nloader can flush on a timer without writing empty commits."
    },
    {
      "id": "zu::append::Field",
      "kind": "enum",
      "name": "Field",
      "doc": "One value on its way into a column.\n\nThis is deliberately not [`crate::query::Value`]. A value coming out\nof a query owns its string, because it outlives the row it was read\nfrom; a value going into a column borrows one, because the caller\nalready has it and the appender is about to copy it into a buffer\neither way. On a string column that is the difference between one\ncopy per row and two.\n\nThere is no null. A column that holds a null cannot be appended to\nat all, for the reason [`Appender`] gives, so a null here could only\never be refused, and refusing it in the type is better than\nrefusing it at flush."
    },
    {
      "id": "zu::append::Field::Bool",
      "kind": "variant",
      "name": "Bool",
      "of": "zu::append::Field",
      "signature": "Bool(bool)"
    },
    {
      "id": "zu::append::Field::Bytes",
      "kind": "variant",
      "name": "Bytes",
      "of": "zu::append::Field",
      "signature": "Bytes(&[u8])"
    },
    {
      "id": "zu::append::Field::Float",
      "kind": "variant",
      "name": "Float",
      "of": "zu::append::Field",
      "signature": "Float(f64)"
    },
    {
      "id": "zu::append::Field::Int",
      "kind": "variant",
      "name": "Int",
      "of": "zu::append::Field",
      "signature": "Int(i64)"
    },
    {
      "id": "zu::append::Field::Str",
      "kind": "variant",
      "name": "Str",
      "of": "zu::append::Field",
      "signature": "Str(&str)"
    },
    {
      "id": "zu::append::Field::Temporal",
      "kind": "variant",
      "name": "Temporal",
      "of": "zu::append::Field",
      "signature": "Temporal(zu_common::Temporal)"
    },
    {
      "id": "zu::append::RowSink",
      "kind": "struct",
      "name": "RowSink",
      "doc": "Where a row's fields go as they are read out of it.\n\nA row writes itself into the appender's column buffers directly,\nwith no vector of values in between, because the row is the hot\npath of a bulk load and an allocation per row is the one cost the\nappender exists to avoid. A field that a column will not take\nleaves the whole row unwritten: the sink counts what it kept, and\nthe appender pops it back off on the way out, so a refused row is a\nrow that never happened rather than half of one nobody can find."
    },
    {
      "id": "zu::append::RowSink::push",
      "kind": "method",
      "name": "push",
      "of": "zu::append::RowSink",
      "signature": "fn push(&mut self, field: Field)",
      "doc": "Writes the next field of this row.\n\nA field of the wrong type, or one past the end of the row, is\nrecorded and the rest of the row is written nowhere. Nothing\nhere returns a result, because a row implementation pushing its\nfields in order has nothing useful to do with one and the\nappender is where the error belongs."
    },
    {
      "id": "zu::bytes",
      "kind": "module",
      "name": "bytes",
      "source": "zu_common::bytes",
      "doc": "Byte strings, GV35: how one is written down and how one is read\nback.\n\nA byte string is a sequence of octets, and there is no encoding to\nguess at: the value is the bytes. What there is, is a spelling, and\nISO 21.3 gives it as `X'00AB'`, two hexits to a byte. Writing and\nreading that spelling is the whole of this module, and it is here\nrather than beside the lexer because the same spelling comes back\nout of a result column, so a value that went in as `X'00ab'` and\ncame back as `X'00AB'` is the same value and reads as one."
    },
    {
      "id": "zu::bytes::from_hexits",
      "kind": "function",
      "name": "from_hexits",
      "signature": "fn from_hexits(text: &str) -> Option<Vec<u8>>",
      "doc": "The bytes a run of hexits names, `None` for anything that is not a\nrun of hexits or names half a byte.\n\nSpace is allowed anywhere and dropped, which is what the standard's\nproduction allows and what lets a long literal be written in groups."
    },
    {
      "id": "zu::bytes::hexits",
      "kind": "function",
      "name": "hexits",
      "signature": "fn hexits(bytes: &[u8]) -> String",
      "doc": "The hexits of a byte string, upper case, no quotes and no `X`.\n\nUpper case because ISO writes the literal that way, and a reader\ncomparing two of these is comparing text: one case is one answer."
    },
    {
      "id": "zu::bytes::literal",
      "kind": "function",
      "name": "literal",
      "signature": "fn literal(bytes: &[u8]) -> String",
      "doc": "The literal a byte string is written as, quotes and `X` and all."
    },
    {
      "id": "zu::catalog_stmt",
      "kind": "module",
      "name": "catalog_stmt",
      "doc": "Running the statements that change what a file declares: schemas\n(GC01, GC02), graphs (GC04, GC05) and graph types (GC03), each\ncreated or dropped (docs/07 §9).\n\nThe parser hands over names, because the label dictionary belongs to\nthe catalog and the parser has never seen one. Resolving them is\nthis module's job, and it is the reason a catalog statement is not\na plan: there is no binding table, nothing to optimize, and the only\noutput is a new catalog.\n\nApplying one is read, change, publish. The change happens on the\ncatalog this call loaded, so a statement that turns out to be\nimpossible halfway through leaves the file exactly as it was, even\nthough building the type interned labels along the way.\n\nISO writes an element type as the pattern an element of it matches,\nand a pattern does not have to be named. The catalog needs a name\nper element type, since that is what an edge type's endpoints refer\nto, so an anonymous type gets one made out of its labels here."
    },
    {
      "id": "zu::catalog_stmt::Effect",
      "kind": "enum",
      "name": "Effect",
      "doc": "What running a catalog statement did. `IF EXISTS` and `IF NOT\nEXISTS` both turn a refusal into a statement that did nothing, and\na caller that wants to report which one happened needs to be told."
    },
    {
      "id": "zu::catalog_stmt::Effect::Created",
      "kind": "variant",
      "name": "Created",
      "of": "zu::catalog_stmt::Effect",
      "signature": "Created"
    },
    {
      "id": "zu::catalog_stmt::Effect::Dropped",
      "kind": "variant",
      "name": "Dropped",
      "of": "zu::catalog_stmt::Effect",
      "signature": "Dropped"
    },
    {
      "id": "zu::catalog_stmt::Effect::Nothing",
      "kind": "variant",
      "name": "Nothing",
      "of": "zu::catalog_stmt::Effect",
      "signature": "Nothing",
      "doc": "The name was already taken, or already free, and the statement\nsaid that was acceptable."
    },
    {
      "id": "zu::catalog_stmt::apply",
      "kind": "function",
      "name": "apply",
      "signature": "fn apply(db: &mut zu::zu1::file::Zu1File, stmt: &zu_query::ast::CatalogStmt, params: &[(&str, zu_query::exec::Value)]) -> zu_common::Result<Effect>",
      "doc": "Runs one catalog statement against `db`, publishing a new catalog\nwhen it changes anything.\n\nThe parameters are here for `AS COPY OF $g`, which names the graph\nto copy the way a `USE` names the graph to read."
    },
    {
      "id": "zu::convert",
      "kind": "module",
      "name": "convert",
      "doc": "Engine conversion: the same graph, the other file format.\n\nBoth engines share the dense row contract, node offsets from zero,\nso conversion is a catalog walk with no id translation: node tables\ncopy row by row with their typed property columns, rel tables copy\ntheir forward adjacency, and the result answers every query the\nsource answers with the same rows. Losslessness is the round trip\ntest's claim to check, not this module's to assume; `zuql` results\nand raw reads are compared in `tests/convert.rs`.\n\nScope follows the writers that exist today. The zu1 bulk loader\nbinds a rel table to one node table and gives node tables their\nrow domain through a load, so a sqlite rel with two distinct\nendpoint tables or a node table no rel touches has no zu1 shape\nyet and converts to a clean error instead of a guess. sqlite text\nmust be UTF-8 and property columns must be dense and uniformly\ntyped, because zu1 columns are."
    },
    {
      "id": "zu::convert::sqlite_to_zu1",
      "kind": "function",
      "name": "sqlite_to_zu1",
      "signature": "fn sqlite_to_zu1(db_path: &std::path::Path, zu1_path: &std::path::Path) -> zu_common::Result<()>",
      "doc": "Converts a sqlite store into a fresh zu1 file at `zu1_path`."
    },
    {
      "id": "zu::convert::zu1_to_sqlite",
      "kind": "function",
      "name": "zu1_to_sqlite",
      "signature": "fn zu1_to_sqlite(zu1_path: &std::path::Path, db_path: &std::path::Path) -> zu_common::Result<()>",
      "doc": "Converts a zu1 file into a fresh sqlite store at `db_path`."
    },
    {
      "id": "zu::dataset",
      "kind": "module",
      "name": "dataset",
      "doc": "A directory of node files and rel files, loaded as the graph it\ndescribes rather than as the edge list underneath it.\n\nThe edge list loader builds one node table and one rel table, which\nis the whole of a SNAP or GAP graph and none of a labelled one. A\nfinbench dataset is three node labels and six rel types, and a query\nover it asks for a transfer's amount and a person's name, so a load\nthat flattens it answers nothing it was written to answer. What this\nadds is the tables: one node table per node file, one rel table per\nrel file, each rel table bound to the two node tables its ends name.\n\nIds are the dataset's own and stay that way. A node file gives every\nrow an id, a rel file names its endpoints by those ids, and the two\nneed not be dense or start at zero or even be disjoint from another\nlabel's, because each label is mapped separately. A row of a table is\nthe line it was written on; the id it came in with is kept twice, as\nthe `id` property so `RETURN n.id` answers with it, and as the\nprimary-key index so `{id: $k}` finds the row without a scan.\n\nWhat this does not do is guess. Every node file names its table,\nevery rel file names its table and both of its ends, and an edge\nwhose endpoint no node file declared is an error naming the id\nrather than a row invented for it."
    },
    {
      "id": "zu::dataset::DatasetStats",
      "kind": "struct",
      "name": "DatasetStats",
      "doc": "What a load put in the file, for the caller that reports it."
    },
    {
      "id": "zu::dataset::DatasetStats::keyed",
      "kind": "field",
      "name": "keyed",
      "of": "zu::dataset::DatasetStats",
      "signature": "Vec<String>",
      "doc": "The node tables whose rows carry a primary-key index."
    },
    {
      "id": "zu::dataset::DatasetStats::node_columns",
      "kind": "field",
      "name": "node_columns",
      "of": "zu::dataset::DatasetStats",
      "signature": "u64",
      "doc": "The property columns stored, node ones and edge ones."
    },
    {
      "id": "zu::dataset::DatasetStats::nodes",
      "kind": "field",
      "name": "nodes",
      "of": "zu::dataset::DatasetStats",
      "signature": "Vec<(String, u64)>",
      "doc": "Each node table and how many rows it holds, in load order."
    },
    {
      "id": "zu::dataset::DatasetStats::rel_columns",
      "kind": "field",
      "name": "rel_columns",
      "of": "zu::dataset::DatasetStats",
      "signature": "u64"
    },
    {
      "id": "zu::dataset::DatasetStats::rels",
      "kind": "field",
      "name": "rels",
      "of": "zu::dataset::DatasetStats",
      "signature": "Vec<(String, u64)>",
      "doc": "Each rel table and how many edges it holds, in load order."
    },
    {
      "id": "zu::dataset::NodeFile",
      "kind": "struct",
      "name": "NodeFile",
      "doc": "One node file: the table its rows load as, and where to read them."
    },
    {
      "id": "zu::dataset::NodeFile::path",
      "kind": "field",
      "name": "path",
      "of": "zu::dataset::NodeFile",
      "signature": "std::path::PathBuf"
    },
    {
      "id": "zu::dataset::NodeFile::table",
      "kind": "field",
      "name": "table",
      "of": "zu::dataset::NodeFile",
      "signature": "String"
    },
    {
      "id": "zu::dataset::RelFile",
      "kind": "struct",
      "name": "RelFile",
      "doc": "One rel file: the table its edges load as, the node tables its two\nends name their rows in, and where to read them."
    },
    {
      "id": "zu::dataset::RelFile::from",
      "kind": "field",
      "name": "from",
      "of": "zu::dataset::RelFile",
      "signature": "String"
    },
    {
      "id": "zu::dataset::RelFile::path",
      "kind": "field",
      "name": "path",
      "of": "zu::dataset::RelFile",
      "signature": "std::path::PathBuf"
    },
    {
      "id": "zu::dataset::RelFile::table",
      "kind": "field",
      "name": "table",
      "of": "zu::dataset::RelFile",
      "signature": "String"
    },
    {
      "id": "zu::dataset::RelFile::to",
      "kind": "field",
      "name": "to",
      "of": "zu::dataset::RelFile",
      "signature": "String"
    },
    {
      "id": "zu::dataset::RelFile::undirected",
      "kind": "field",
      "name": "undirected",
      "of": "zu::dataset::RelFile",
      "signature": "bool"
    },
    {
      "id": "zu::dataset::load_dataset",
      "kind": "function",
      "name": "load_dataset",
      "signature": "fn load_dataset(nodes: &[NodeFile], rels: &[RelFile], out: &std::path::Path) -> zu_common::Result<DatasetStats>",
      "doc": "Loads a dataset of node files and rel files into a fresh zu1 file at\n`out`.\n\nEvery rel file's two ends must name node files given here, and every\nnode file must be an end of some rel file, because a node table's row\ndomain is declared by the loads that touch it and a table no load\ntouches would come out of this holding nothing."
    },
    {
      "id": "zu::db",
      "kind": "module",
      "name": "db",
      "doc": "The public Rust API: a [`Database`] you open once and share, and a\n[`Connection`] you take one of per thread.\n\n[`crate::session::Session`] is the engine's own entry point and it\nis one object doing both jobs, which works exactly as long as a\nprocess wants one. The moment two threads want to read the same\ngraph, or a pool wants to hand a connection out and take it back,\nthe two jobs come apart: the database is the thing that is shared\nand immutable, and the connection is the thing that is cheap,\nserial, and owned by whoever is using it. Every binding on the C ABI\ninherits this split (`dx/02` §3), so it is the Rust API that has to\nhave it first.\n\n```no_run\nuse zu::Database;\n\nlet db = Database::open(\"social.zu1\")?;\nlet mut conn = db.connect()?;\nlet rows = conn.query(\"MATCH (p:Person) RETURN p.name\")?;\n# Ok::<(), zu::ZuError>(())\n```\n\n[`Database::open`] takes a path and nothing else, because a\nconfiguration argument every caller has to write is a tax on every\ncaller to serve the few who set anything; [`Database::open_with`] is\nfor those few. Opening reads 12 KiB and pages the rest lazily, so\nconnecting is an open and a catalog load rather than a copy of the\nfile, and a connection is genuinely cheap to take."
    },
    {
      "id": "zu::db::Config",
      "kind": "struct",
      "name": "Config",
      "doc": "How a database is opened and what its statements are allowed to do.\n\nEvery field has a default that works, so a caller sets the one thing\nit cares about and nothing else. The builder takes `self` and gives\nit back, so the whole configuration is one expression and a\nhalf-built `Config` is never a thing anybody holds."
    },
    {
      "id": "zu::db::Config::memory_limit",
      "kind": "method",
      "name": "memory_limit",
      "of": "zu::db::Config",
      "signature": "fn memory_limit(self, bytes: usize) -> Config",
      "doc": "Bytes the caches may hold, split between block frames and the\ndecoded-object pools by the ratios in perf/04 §2. This is a\nbudget for what the engine keeps warm, not a ceiling on the\nprocess: a query still allocates its own intermediate state."
    },
    {
      "id": "zu::db::Config::new",
      "kind": "method",
      "name": "new",
      "of": "zu::db::Config",
      "signature": "fn new() -> Config",
      "doc": "The defaults: the 128 MiB cache budget the perf gates run under,\na thread count the executor picks from the machine, and writes\nallowed."
    },
    {
      "id": "zu::db::Config::read_only",
      "kind": "method",
      "name": "read_only",
      "of": "zu::db::Config",
      "signature": "fn read_only(self, read_only: bool) -> Config",
      "doc": "Opens the file on a descriptor this process cannot write\nthrough, so nothing here or under here can modify the database,\nand a database on a read-only mount opens rather than failing at\nthe open. Statements that would write are refused with an error\nnaming the database, before the operating system gets a chance\nto refuse them less helpfully."
    },
    {
      "id": "zu::db::Config::stale_bound",
      "kind": "method",
      "name": "stale_bound",
      "of": "zu::db::Config",
      "signature": "fn stale_bound(self, epochs: u64) -> Config",
      "doc": "How many epochs a session parameter holding a binding table may\nfall behind before the statement after it carries a warning\nsaying so (plan/06 §2).\n\nA pin holds no blocks in zu, since a binding table parameter\nholds rows copied out of the snapshot rather than a reader into\nit, so this is not a knob against the store growing. It is how\nlong a caller wants to go without being told that what it is\nholding was read a while ago."
    },
    {
      "id": "zu::db::Config::threads",
      "kind": "method",
      "name": "threads",
      "of": "zu::db::Config",
      "signature": "fn threads(self, threads: usize) -> Config",
      "doc": "Worker threads for the parallel stages of a query. Zero, the\ndefault, lets the executor pick `min(cores, 8)`; one forces\nsequential execution, which is what a caller running many\nconnections at once wants, since the parallelism is then across\nconnections and a thread pool per connection would oversubscribe\nthe machine."
    },
    {
      "id": "zu::db::Connection",
      "kind": "struct",
      "name": "Connection",
      "doc": "One connection: statements run on it, in order, one at a time.\n\nIt is `Send` and not `Sync`, and every method takes `&mut self`, so\nthe compiler is what stops two threads from using one connection.\nThat is the same rule the C ABI states and has to check at runtime\n(`dx/02` §5), enforced here at no cost.\n\nA connection reads the database as of the statement it is running.\nEvery connection to one file shares the write side, and a statement\npicks up what that side has published before it compiles anything,\nso a commit on another connection is visible to the next statement\nhere without reconnecting. Nothing moves under a statement that has\nstarted: what it took at the top is what it reads to the end, which\nis the snapshot isolation of docs/08 §1.\n\nWrites queue. One connection at a time holds the write side of a\nfile, for the length of a write statement or of an explicit\ntransaction, and the rest wait in the order they asked."
    },
    {
      "id": "zu::db::Connection::appender",
      "kind": "method",
      "name": "appender",
      "of": "zu::db::Connection",
      "signature": "fn appender(&mut self, table: &str) -> Result<Appender>",
      "doc": "Opens an appender on `table`, the bulk-load path of dx/04 §6.\n\nA load through statements pays a commit per row; an appender\nbuffers rows and pays one per flush. Only one appender exists at\na time on a connection, which the borrow says rather than a\ncheck: it holds the file this connection reads through, so the\nconnection is unusable until the appender is closed or dropped,\nand a row appended is a row the next statement on it sees."
    },
    {
      "id": "zu::db::Connection::close_prepared",
      "kind": "method",
      "name": "close_prepared",
      "of": "zu::db::Connection",
      "signature": "fn close_prepared(&mut self, stmt: u64) -> bool",
      "doc": "Releases a prepared statement, reporting whether the id was one."
    },
    {
      "id": "zu::db::Connection::duplicate",
      "kind": "method",
      "name": "duplicate",
      "of": "zu::db::Connection",
      "signature": "fn duplicate(&self) -> Result<Connection>",
      "doc": "Another connection to the same database, made from this one\nrather than from the path.\n\nThis is how a pool is written. [`Database::connect`] opens the\nfile and looks up the write side under its path; this forks a\ndescriptor off the side this connection already holds, so it\ncosts a schema load and no lookup, and it works on a database\nin memory, which has no path to look up.\n\nThe two are connections in every sense, not two names for one:\neach has its own plan cache, its own readers, its own interrupt\nand its own transaction. What they share is the write side, so\nthey queue behind each other to write and each sees what the\nother has committed, exactly as two connections from the same\n[`Database`] do. The switches and the read-only setting are\ncarried across, because a pool that handed out connections\nconfigured differently from the one it was seeded with would be\na trap.\n\n```no_run\nuse zu::Database;\n\nlet db = Database::memory()?;\nlet mut conn = db.connect()?;\nconn.query(\"INSERT (p:person {id: 1, name: 'ada'})\")?;\nlet mut other = conn.duplicate()?;\nlet rows = other.query(\"MATCH (p:person) RETURN p.name AS name\")?;\nassert_eq!(rows.rows.len(), 1);\n# Ok::<(), zu_common::ZuError>(())\n```"
    },
    {
      "id": "zu::db::Connection::execute",
      "kind": "method",
      "name": "execute",
      "of": "zu::db::Connection",
      "signature": "fn execute(&mut self, source: &str) -> Result<u64>",
      "doc": "Runs a statement for its effect and returns the number of rows\nit produced, which is zero for the catalog statements that are\nthe writes this milestone has."
    },
    {
      "id": "zu::db::Connection::execute_prepared",
      "kind": "method",
      "name": "execute_prepared",
      "of": "zu::db::Connection",
      "signature": "fn execute_prepared(&mut self, stmt: u64, params: &[(&str, Value)]) -> Result<QueryResult>",
      "doc": "Runs a prepared statement with a set of bindings."
    },
    {
      "id": "zu::db::Connection::explain",
      "kind": "method",
      "name": "explain",
      "of": "zu::db::Connection",
      "signature": "fn explain(&mut self, source: &str) -> Result<String>",
      "doc": "The plan this connection would run for `source`, rendered,\nwithout running it."
    },
    {
      "id": "zu::db::Connection::explain_plan",
      "kind": "method",
      "name": "explain_plan",
      "of": "zu::db::Connection",
      "signature": "fn explain_plan(&mut self, source: &str) -> Result<QueryPlan>",
      "doc": "The same plan as operators rather than as text: the tree, the\ncolumns the statement answers with, the parameters it wants, and\nthe notes compiling it raised.\n\nA caller that reads a plan is asking a question about it, and\nevery one of those questions is easier to ask of a tree than of\na listing: whether the scan reaches an index, how deep the\nexpands go, which tables are touched. [`QueryPlan::render`] is\nwhat [`Self::explain`] returns, so the two are one thing printed\ntwo ways rather than two renderings that can drift.\n\n```no_run\nuse zu::Database;\n\nlet db = Database::open(\"social.zu1\")?;\nlet mut conn = db.connect()?;\nlet plan = conn.explain_plan(\"MATCH (p:person) RETURN p.id AS id\")?;\nlet root = plan.root.as_ref().expect(\"a statement with operators\");\nassert_eq!(root.op, \"Project\");\nassert_eq!(plan.columns, [\"id\"]);\n# Ok::<(), zu::ZuError>(())\n```"
    },
    {
      "id": "zu::db::Connection::interrupt",
      "kind": "method",
      "name": "interrupt",
      "of": "zu::db::Connection",
      "signature": "fn interrupt(&self) -> Interrupt",
      "doc": "The handle a statement on this connection can be stopped\nthrough, and the count of rows it has read.\n\nA statement runs on the thread that asked for it, so the handle\nis taken before the call and raised from another thread: it is\nhow a shell answers `Ctrl-C` and how a server answers a client\nthat hung up. The statement returns\n[`zu_common::ZuError::Interrupted`] and the connection is\nunchanged, which is what makes this different from closing it."
    },
    {
      "id": "zu::db::Connection::is_read_only",
      "kind": "method",
      "name": "is_read_only",
      "of": "zu::db::Connection",
      "signature": "fn is_read_only(&self) -> bool",
      "doc": "Whether this connection refuses writes."
    },
    {
      "id": "zu::db::Connection::prepare",
      "kind": "method",
      "name": "prepare",
      "of": "zu::db::Connection",
      "signature": "fn prepare(&mut self, source: &str) -> Result<(u64, Vec<String>)>",
      "doc": "Compiles a statement and pins it under an id, so a loop binds\nand executes without recompiling. The names it wants come back\nwith it, in the order the binder assigned them."
    },
    {
      "id": "zu::db::Connection::profile",
      "kind": "method",
      "name": "profile",
      "of": "zu::db::Connection",
      "signature": "fn profile(&mut self, source: &str, params: &[(&str, Value)]) -> Result<Profile>",
      "doc": "Runs `source` with the counters on and hands back what it\nobserved, one entry per operator per stage.\n\nThis costs the execution, so it is the tool a caller reaches for\nwhen a statement is slower than the plan says it should be: the\nrows an operator really produced sit next to what the optimizer\nexpected, and the self time says which one of them the wall\nclock went into. [`Profile::render`] prints it the way\n`EXPLAIN ANALYZE` does in the shell.\n\nA statement that writes is refused, because it runs as the parts\nit was split at its write into rather than as the one plan a\nprofile describes, and profiling it would apply the write."
    },
    {
      "id": "zu::db::Connection::query",
      "kind": "method",
      "name": "query",
      "of": "zu::db::Connection",
      "signature": "fn query(&mut self, source: &str) -> Result<QueryResult>",
      "doc": "Runs one statement and returns its rows."
    },
    {
      "id": "zu::db::Connection::query_stream",
      "kind": "method",
      "name": "query_stream",
      "of": "zu::db::Connection",
      "signature": "fn query_stream(&mut self, source: &str, params: &[(&str, Value)], sink: impl FnMut) -> Result<Streamed>",
      "doc": "Runs one statement and hands its rows to `sink` in batches as\nthey are made, instead of returning them all.\n\nThis is the shape for a result that is too big to want in\nmemory, and for a caller that will not read all of it: the sink\nanswers [`Flow::Stop`] and the scan under it stops at the next\nboundary, the same boundary an interrupt is answered at. A batch\nborrows the rows for the length of the call, so a caller keeping\nanything past it copies what it wants out.\n\n```no_run\nuse zu::{Database, Flow};\n\nlet db = Database::open(\"social.zu1\")?;\nlet mut conn = db.connect()?;\nlet mut total = 0i64;\nconn.query_stream(\"MATCH (p:person) RETURN p.id AS id\", &[], |batch| {\n    for row in batch.iter() {\n        total += row.get_at::<i64>(0)?;\n    }\n    Ok(Flow::More)\n})?;\n# Ok::<(), zu::ZuError>(())\n```"
    },
    {
      "id": "zu::db::Connection::query_stream_batched",
      "kind": "method",
      "name": "query_stream_batched",
      "of": "zu::db::Connection",
      "signature": "fn query_stream_batched(&mut self, source: &str, params: &[(&str, Value)], batch_rows: usize, sink: impl FnMut) -> Result<Streamed>",
      "doc": "The same, with the batch size named. One vector of rows is the\ndefault because it is the unit the executor already works in;\na caller writing batches somewhere with a size of its own, an\nArrow record batch or an HTTP chunk, says so here."
    },
    {
      "id": "zu::db::Connection::query_with",
      "kind": "method",
      "name": "query_with",
      "of": "zu::db::Connection",
      "signature": "fn query_with(&mut self, source: &str, params: &[(&str, Value)]) -> Result<QueryResult>",
      "doc": "Runs one statement with parameters bound by name, without the\nleading `$`."
    },
    {
      "id": "zu::db::Connection::register",
      "kind": "method",
      "name": "register",
      "of": "zu::db::Connection",
      "signature": "fn register(&mut self, frame: Frame) -> Result<()>",
      "doc": "Registers a frame as a table of this connection, under the name\nit carries.\n\nThis is the replacement scan: a caller holding columns in memory\ngets to name them in a statement without loading them into the\ndatabase first. Nothing is copied, at registration or at read.\nThe frame is this connection's alone and goes when it does, and\na name a stored table already holds is refused."
    },
    {
      "id": "zu::db::Connection::registered",
      "kind": "method",
      "name": "registered",
      "of": "zu::db::Connection",
      "signature": "fn registered(&self) -> Vec<String>",
      "doc": "The names registered on this connection, sorted."
    },
    {
      "id": "zu::db::Connection::session_mut",
      "kind": "method",
      "name": "session_mut",
      "of": "zu::db::Connection",
      "signature": "fn session_mut(&mut self) -> &mut Session",
      "doc": "The session under this connection, for the paths that have not\ngrown an API here yet: profiling, EXPLAIN ANALYZE, and the\nengine's own tooling."
    },
    {
      "id": "zu::db::Connection::table_name",
      "kind": "method",
      "name": "table_name",
      "of": "zu::db::Connection",
      "signature": "fn table_name(&self, table: u32) -> Option<&str>",
      "doc": "What a table id is called, or nothing when no table has that id.\n\nA node value is a table and a row offset and an edge value is a\ntable and two of them, because neither part identifies anything\non its own: two tables both number their rows from zero. The\nnumber is what the engine passes around and the name is what a\nuser asked about, so a binding that hands a node value out has\nto be able to turn one into the other, and until now the only\nway was to reach past this type into the session and read the\ncatalog. Node and rel tables share one id space, so this is one\ncall and not two.\n\nThe name is borrowed from the catalog, which the next statement\nthat creates or drops a table replaces, so a caller keeping one\npast the next statement copies it."
    },
    {
      "id": "zu::db::Connection::unregister",
      "kind": "method",
      "name": "unregister",
      "of": "zu::db::Connection",
      "signature": "fn unregister(&mut self, name: &str) -> Result<bool>",
      "doc": "Drops a registered frame, answering whether there was one under\nthat name."
    },
    {
      "id": "zu::db::Database",
      "kind": "struct",
      "name": "Database",
      "doc": "An open database: a path, a configuration, and the fact that both\nhave been checked against a real file.\n\nIt is `Send + Sync` and holds no file descriptor and no cache, which\nis what makes it shareable without a lock. The state that cannot be\nshared, the seek position of a handle and the plans compiled against\na catalog, lives on the connections instead, one set per connection,\nwhich is the same division the C ABI and every binding above it use."
    },
    {
      "id": "zu::db::Database::config",
      "kind": "method",
      "name": "config",
      "of": "zu::db::Database",
      "signature": "fn config(&self) -> &Config",
      "doc": "The configuration it was opened with."
    },
    {
      "id": "zu::db::Database::connect",
      "kind": "method",
      "name": "connect",
      "of": "zu::db::Database",
      "signature": "fn connect(&self) -> Result<Connection>",
      "doc": "A new connection: its own file handle, its own caches, its own\nplan cache. Costs an open and a catalog load, which on the\n11 GB file of the B7 gate is tens of microseconds.\n\nThe open goes through the registry rather than ahead of it, so\nthat a connection made while the last one is closing waits for\nthe fold that close is running instead of reading the file\nhalf way through it."
    },
    {
      "id": "zu::db::Database::create",
      "kind": "method",
      "name": "create",
      "of": "zu::db::Database",
      "signature": "fn create(path: impl AsRef<Path>) -> Result<Database>",
      "doc": "Creates a database at `path` and opens it, with the default\nconfiguration.\n\nThe path must not exist. A create that found a database there\nand opened it instead would be the call that quietly writes into\ndata somebody else put there, and the caller who wanted either\none has [`Database::open`] to fall back to and a decision to make\nabout which.\n\nWhat it makes is a valid database with nothing in it: a file\nheader, a database header, and no tables. Statements make the\ntables, and until v0 grows one that does, a bulk load is what\nputs rows in."
    },
    {
      "id": "zu::db::Database::create_with",
      "kind": "method",
      "name": "create_with",
      "of": "zu::db::Database",
      "signature": "fn create_with(path: impl AsRef<Path>, config: Config) -> Result<Database>",
      "doc": "[`Database::create`] with a configuration."
    },
    {
      "id": "zu::db::Database::is_memory",
      "kind": "method",
      "name": "is_memory",
      "of": "zu::db::Database",
      "signature": "fn is_memory(&self) -> bool",
      "doc": "Whether this database is the kind that goes away with the\nprocess. A caller that offers to back one up, or warns before\nclosing one, needs to be able to ask."
    },
    {
      "id": "zu::db::Database::memory",
      "kind": "method",
      "name": "memory",
      "of": "zu::db::Database",
      "signature": "fn memory() -> Result<Database>",
      "doc": "Creates a database that never touches the filesystem, with the\ndefault configuration.\n\nThe blocks a file would hold are held in memory instead, and the\nlog beside it too, so everything above this point runs unchanged:\nthe same headers, the same log, the same fold and the same\nrecovery, on bytes that go away when the last handle does. That\nis what makes it worth having for a test or a scratch load and\nwhat makes it useless for anything that has to survive the\nprocess.\n\nEvery call makes a database of its own. Two of them share\nnothing, and cloning one, or connecting to it twice, is what\ngets two views of the same graph."
    },
    {
      "id": "zu::db::Database::memory_with",
      "kind": "method",
      "name": "memory_with",
      "of": "zu::db::Database",
      "signature": "fn memory_with(config: Config) -> Result<Database>",
      "doc": "[`Database::memory`] with a configuration.\n\n`read_only` is refused for the same reason [`Database::create`]\nrefuses it: a fresh database nothing may write to is one that\nstays empty forever, and here there is not even a file somebody\nelse could have filled."
    },
    {
      "id": "zu::db::Database::open",
      "kind": "method",
      "name": "open",
      "of": "zu::db::Database",
      "signature": "fn open(path: impl AsRef<Path>) -> Result<Database>",
      "doc": "Opens the database at `path` with the default configuration.\n\nThe file is opened once here and closed again, so a path that is\nnot a zu1 file, or is one this build cannot read, fails now\nrather than on the first connection. That open is the O(1) one\nof docs/04 §1: 12 KiB and two header slots."
    },
    {
      "id": "zu::db::Database::open_with",
      "kind": "method",
      "name": "open_with",
      "of": "zu::db::Database",
      "signature": "fn open_with(path: impl AsRef<Path>, config: Config) -> Result<Database>",
      "doc": "[`Database::open`] with a configuration."
    },
    {
      "id": "zu::db::Database::path",
      "kind": "method",
      "name": "path",
      "of": "zu::db::Database",
      "signature": "fn path(&self) -> &Path",
      "doc": "Where this database lives."
    },
    {
      "id": "zu::gqlstatus",
      "kind": "module",
      "name": "gqlstatus",
      "source": "zu_common::gqlstatus",
      "doc": "GQLSTATUS: the ISO/IEC 39075:2024 condition codes and the diagnostic\nrecord that carries one back to a caller (Spec/2064g/gql/plan/07).\n\nEvery condition GQL defines is a five-character code, a two-character\nclass and a three-character subclass, and the standard fixes both the\ncode and the natural-language name that goes with it. Those names are\nnot ours to invent, so the table in `generated.rs` is machine-written\nfrom `artifacts/gql-conditions.xml`, the digital artifact published\nalongside the standard. `tests/gqlstatus_table.rs` regenerates it and\nfails on drift.\n\nA [`GqlStatus`] is an index into that table, so it is two bytes, it is\n`Copy`, and a code that is not in the standard cannot be constructed.\nThat last property is the point: an engine that answers with a code it\nmade up has not raised a condition, it has printed a string.\n\nThe conformance denominator is the 68 subclass rows. The table also\ncarries one row per class, the class code followed by `000`, because\nthat is a GQLSTATUS value too and it is the one a statement reports\nwhen nothing in particular happened: `00000 successful completion`.\nThe artifact only writes those rows out for the four classes that\nhave no subclasses, so the generator synthesises the other eight. That\nis why the totals here are 80 and 68 rather than one number."
    },
    {
      "id": "zu::gqlstatus::Condition",
      "kind": "struct",
      "name": "Condition",
      "doc": "One row of the standard's condition table."
    },
    {
      "id": "zu::gqlstatus::Condition::class",
      "kind": "field",
      "name": "class",
      "of": "zu::gqlstatus::Condition",
      "signature": "&str",
      "doc": "The class name, for example `data exception`."
    },
    {
      "id": "zu::gqlstatus::Condition::code",
      "kind": "field",
      "name": "code",
      "of": "zu::gqlstatus::Condition",
      "signature": "&str",
      "doc": "The five-character GQLSTATUS value, for example `22012`."
    },
    {
      "id": "zu::gqlstatus::Condition::severity",
      "kind": "field",
      "name": "severity",
      "of": "zu::gqlstatus::Condition",
      "signature": "Severity"
    },
    {
      "id": "zu::gqlstatus::Condition::subclass",
      "kind": "field",
      "name": "subclass",
      "of": "zu::gqlstatus::Condition",
      "signature": "Option<&str>",
      "doc": "The subclass name, for example `division by zero`. `None` on the\ntwelve class rows, whose code ends in `000`."
    },
    {
      "id": "zu::gqlstatus::DOC_BASE",
      "kind": "constant",
      "name": "DOC_BASE",
      "signature": "&str",
      "doc": "Where a condition is written up. The code goes on the end, so an\nerror hands the reader a url rather than a code to go and search\nfor, which is the difference between a message a user can act on\nand a message a user has to research."
    },
    {
      "id": "zu::gqlstatus::DiagnosticRecord",
      "kind": "struct",
      "name": "DiagnosticRecord",
      "doc": "What zu hands back when a condition is raised: the standard's code and\nzu's own account of what happened.\n\nThe two are kept apart on purpose. `status` is comparable across\nengines and is what a conformance run grades. `detail` is ours and can\nsay whatever is most useful, including the line and column, without\nputting that in the field a harness matches on.\n\n`position` is that line and column again, as a pair. The detail keeps\nsaying it because a message a user reads should be complete on its\nown, and the pair exists because a caller that wants to underline the\noffending token should not have to parse English back into numbers.\n\n`excerpt` is the line that position falls on, kept because the\ncaller furthest from the statement is the one most likely to be\nshowing this to a person: a driver, a notebook cell, a log line. The\nengine has the text in hand when it raises the condition and the\ncaller may not have it at all by the time it prints one."
    },
    {
      "id": "zu::gqlstatus::DiagnosticRecord::about",
      "kind": "method",
      "name": "about",
      "of": "zu::gqlstatus::DiagnosticRecord",
      "signature": "fn about(self, subject: Subject) -> Self",
      "doc": "The same, about a thing the statement named.\n\nWritten as a builder so a raise site reads as one expression and\nso that adding the subject to a site never reshapes the call it\nwas already making."
    },
    {
      "id": "zu::gqlstatus::DiagnosticRecord::at",
      "kind": "method",
      "name": "at",
      "of": "zu::gqlstatus::DiagnosticRecord",
      "signature": "fn at(status: GqlStatus, position: Position, detail: impl fmt::Display) -> Self",
      "doc": "The same, raised at a place. The position is written into the\ndetail as well, ahead of it and separated by a colon, which is\nthe form every parser message already had."
    },
    {
      "id": "zu::gqlstatus::DiagnosticRecord::detail",
      "kind": "field",
      "name": "detail",
      "of": "zu::gqlstatus::DiagnosticRecord",
      "signature": "String"
    },
    {
      "id": "zu::gqlstatus::DiagnosticRecord::doc_url",
      "kind": "method",
      "name": "doc_url",
      "of": "zu::gqlstatus::DiagnosticRecord",
      "signature": "fn doc_url(&self) -> String",
      "doc": "The page that documents the condition this record carries."
    },
    {
      "id": "zu::gqlstatus::DiagnosticRecord::excerpt",
      "kind": "field",
      "name": "excerpt",
      "of": "zu::gqlstatus::DiagnosticRecord",
      "signature": "Option<String>",
      "doc": "The whole line `position` is on, without its newline, so that\n`column` indexes into it. `None` when there is no position, when\nthe line is empty, and when it is longer than a person is going\nto read, which are the three cases where quoting it back helps\nnobody."
    },
    {
      "id": "zu::gqlstatus::DiagnosticRecord::graph",
      "kind": "field",
      "name": "graph",
      "of": "zu::gqlstatus::DiagnosticRecord",
      "signature": "Option<String>",
      "doc": "The graph the statement was running against, and the schema that\ngraph was reached through. Both are filled at the session\nboundary rather than at the raise site, because that is the one\nplace that knows them and there are two hundred raise sites."
    },
    {
      "id": "zu::gqlstatus::DiagnosticRecord::in_source",
      "kind": "method",
      "name": "in_source",
      "of": "zu::gqlstatus::DiagnosticRecord",
      "signature": "fn in_source(status: GqlStatus, source: &str, offset: usize, detail: impl fmt::Display) -> Self",
      "doc": "The same again, raised at a byte offset into the statement text.\n\nThis is the form to reach for wherever the text is in hand,\nwhich is every place inside the front end, because it is the\nonly form that can fill the excerpt: the line comes from the\nsource and nothing downstream of here still has it."
    },
    {
      "id": "zu::gqlstatus::DiagnosticRecord::new",
      "kind": "method",
      "name": "new",
      "of": "zu::gqlstatus::DiagnosticRecord",
      "signature": "fn new(status: GqlStatus, detail: impl Into<String>) -> Self"
    },
    {
      "id": "zu::gqlstatus::DiagnosticRecord::position",
      "kind": "field",
      "name": "position",
      "of": "zu::gqlstatus::DiagnosticRecord",
      "signature": "Option<Position>",
      "doc": "Where in the statement, for the conditions that happen somewhere.\nA division by zero happens at runtime and has no token to point\nat, so this is `None` rather than a guess at one."
    },
    {
      "id": "zu::gqlstatus::DiagnosticRecord::retryable",
      "kind": "method",
      "name": "retryable",
      "of": "zu::gqlstatus::DiagnosticRecord",
      "signature": "fn retryable(&self) -> bool",
      "doc": "Whether running the statement again could succeed."
    },
    {
      "id": "zu::gqlstatus::DiagnosticRecord::schema",
      "kind": "field",
      "name": "schema",
      "of": "zu::gqlstatus::DiagnosticRecord",
      "signature": "Option<String>"
    },
    {
      "id": "zu::gqlstatus::DiagnosticRecord::severity",
      "kind": "method",
      "name": "severity",
      "of": "zu::gqlstatus::DiagnosticRecord",
      "signature": "fn severity(&self) -> Severity"
    },
    {
      "id": "zu::gqlstatus::DiagnosticRecord::status",
      "kind": "field",
      "name": "status",
      "of": "zu::gqlstatus::DiagnosticRecord",
      "signature": "GqlStatus"
    },
    {
      "id": "zu::gqlstatus::DiagnosticRecord::subject",
      "kind": "field",
      "name": "subject",
      "of": "zu::gqlstatus::DiagnosticRecord",
      "signature": "Option<Subject>",
      "doc": "What the condition is about, where it is about something the\nstatement named. `None` for the conditions that are about no\nparticular thing: a division by zero is about an expression and\nan expression has no name."
    },
    {
      "id": "zu::gqlstatus::DiagnosticRecord::within",
      "kind": "method",
      "name": "within",
      "of": "zu::gqlstatus::DiagnosticRecord",
      "signature": "fn within(&mut self, graph: &str, schema: &str)",
      "doc": "Where the statement was running, filled in on the way out.\n\nIt does not overwrite: a condition raised about a graph other\nthan the working one has already said which, and the session\nsaying otherwise on the way past would be the session being\nwrong."
    },
    {
      "id": "zu::gqlstatus::GqlStatus",
      "kind": "struct",
      "name": "GqlStatus",
      "doc": "A GQLSTATUS value. Constructible only from the generated table, so\nevery value of this type is a code the standard defines."
    },
    {
      "id": "zu::gqlstatus::GqlStatus::class",
      "kind": "method",
      "name": "class",
      "of": "zu::gqlstatus::GqlStatus",
      "signature": "fn class(self) -> &str",
      "doc": "The two characters that open the code, which is the condition\nclass: `22` for the data exceptions, `42` for the syntax\nerrors, `40` for the rollbacks. A binding raises one exception\ntype per class, so this is the field it switches on, and taking\nthe first two characters of a five-character code is the sort of\nthing every binding would otherwise write for itself."
    },
    {
      "id": "zu::gqlstatus::GqlStatus::code",
      "kind": "method",
      "name": "code",
      "of": "zu::gqlstatus::GqlStatus",
      "signature": "fn code(self) -> &str"
    },
    {
      "id": "zu::gqlstatus::GqlStatus::condition",
      "kind": "method",
      "name": "condition",
      "of": "zu::gqlstatus::GqlStatus",
      "signature": "fn condition(self) -> &Condition",
      "doc": "The row this status names."
    },
    {
      "id": "zu::gqlstatus::GqlStatus::doc_url",
      "kind": "method",
      "name": "doc_url",
      "of": "zu::gqlstatus::GqlStatus",
      "signature": "fn doc_url(self) -> String",
      "doc": "The page that documents this condition. Derived from the code\nrather than stored, because a table of eighty urls is eighty\nchances for one of them to be the url of a different condition."
    },
    {
      "id": "zu::gqlstatus::GqlStatus::from_code",
      "kind": "method",
      "name": "from_code",
      "of": "zu::gqlstatus::GqlStatus",
      "signature": "fn from_code(code: &str) -> Option<Self>",
      "doc": "Looks a code up by its five characters. Returns `None` for\nanything the standard does not define, which is how a code\narriving from outside zu gets checked rather than trusted."
    },
    {
      "id": "zu::gqlstatus::GqlStatus::retryable",
      "kind": "method",
      "name": "retryable",
      "of": "zu::gqlstatus::GqlStatus",
      "signature": "fn retryable(self) -> bool",
      "doc": "Whether running the same statement again could succeed.\n\nTrue for `40000 transaction rollback`, which is the engine\nsaying it undid the work and nothing of it is in the file. False\nfor `40003 statement completion unknown`, which is the same\nclass and the opposite advice: a statement that may or may not\nhave committed is one a retry could apply twice, and a caller\nthat wants to retry it has to establish which happened first.\nFalse for everything else, since a statement that failed to\nparse parses no better the second time."
    },
    {
      "id": "zu::gqlstatus::GqlStatus::severity",
      "kind": "method",
      "name": "severity",
      "of": "zu::gqlstatus::GqlStatus",
      "signature": "fn severity(self) -> Severity"
    },
    {
      "id": "zu::gqlstatus::GqlStatus::standard_text",
      "kind": "method",
      "name": "standard_text",
      "of": "zu::gqlstatus::GqlStatus",
      "signature": "fn standard_text(self) -> String",
      "doc": "The standard's own words for this condition: the class name, and\nthe subclass name after it when there is one. This is the text a\nconformance harness compares against, so zu never paraphrases it.\nAnything zu wants to add goes in [`DiagnosticRecord::detail`]."
    },
    {
      "id": "zu::gqlstatus::Position",
      "kind": "struct",
      "name": "Position",
      "doc": "Where in the statement text a condition was raised, said three ways.\n\n`line` and `column` are 1-based and the column counts characters\nrather than bytes, so a line of multi-byte text does not read as\nwider than it looks. That pair is what an editor or a shell wants:\nit has printed the line already and needs somewhere to put the\ncaret.\n\n`offset` is the same place as a byte index into the statement, and\nit is here for the caller the pair does not serve: a tool that holds\nthe text and wants to slice it, an editor mapping into a buffer it\nindexes by byte, a highlighter marking a range. Recovering it from\nthe pair means counting lines and characters again, over text the\nengine had in hand when it raised the condition. It is always on a\ncharacter boundary of that text, so slicing at it cannot panic."
    },
    {
      "id": "zu::gqlstatus::Position::column",
      "kind": "field",
      "name": "column",
      "of": "zu::gqlstatus::Position",
      "signature": "u32"
    },
    {
      "id": "zu::gqlstatus::Position::line",
      "kind": "field",
      "name": "line",
      "of": "zu::gqlstatus::Position",
      "signature": "u32"
    },
    {
      "id": "zu::gqlstatus::Position::of",
      "kind": "method",
      "name": "of",
      "of": "zu::gqlstatus::Position",
      "signature": "fn of(source: &str, offset: usize) -> Self",
      "doc": "The place `offset` bytes into `source`. An offset past the end\nlands at the end, which is where an error about a query that\nstopped too early belongs, and an offset inside a character\nlands on the start of that character."
    },
    {
      "id": "zu::gqlstatus::Position::offset",
      "kind": "field",
      "name": "offset",
      "of": "zu::gqlstatus::Position",
      "signature": "u32",
      "doc": "Bytes into the statement, 0-based."
    },
    {
      "id": "zu::gqlstatus::Severity",
      "kind": "enum",
      "name": "Severity",
      "doc": "What a condition means for the statement that raised it, taken from\nthe class category letter in the artifact."
    },
    {
      "id": "zu::gqlstatus::Severity::Exception",
      "kind": "variant",
      "name": "Exception",
      "of": "zu::gqlstatus::Severity",
      "signature": "Exception",
      "doc": "`X`: the statement did not succeed."
    },
    {
      "id": "zu::gqlstatus::Severity::Informational",
      "kind": "variant",
      "name": "Informational",
      "of": "zu::gqlstatus::Severity",
      "signature": "Informational",
      "doc": "`I`: informational."
    },
    {
      "id": "zu::gqlstatus::Severity::NoData",
      "kind": "variant",
      "name": "NoData",
      "of": "zu::gqlstatus::Severity",
      "signature": "NoData",
      "doc": "`N`: the statement succeeded and returned nothing."
    },
    {
      "id": "zu::gqlstatus::Severity::Success",
      "kind": "variant",
      "name": "Success",
      "of": "zu::gqlstatus::Severity",
      "signature": "Success",
      "doc": "`S`: the statement succeeded."
    },
    {
      "id": "zu::gqlstatus::Severity::Warning",
      "kind": "variant",
      "name": "Warning",
      "of": "zu::gqlstatus::Severity",
      "signature": "Warning",
      "doc": "`W`: the statement succeeded and has something to say about it."
    },
    {
      "id": "zu::gqlstatus::Severity::is_success",
      "kind": "method",
      "name": "is_success",
      "of": "zu::gqlstatus::Severity",
      "signature": "const fn is_success(self) -> bool",
      "doc": "True when the statement kept its result. Warnings ride along with\nan answer; exceptions replace it."
    },
    {
      "id": "zu::gqlstatus::Severity::letter",
      "kind": "method",
      "name": "letter",
      "of": "zu::gqlstatus::Severity",
      "signature": "const fn letter(self) -> &str",
      "doc": "The one character ISO writes it as, which is what a status read\nas a value carries: a client that has to print or compare one\nshould not have to pick its own spelling of these five words."
    },
    {
      "id": "zu::gqlstatus::Subject",
      "kind": "enum",
      "name": "Subject",
      "doc": "What a condition is about, when it is about something a statement\nnamed.\n\nISO 23.2 calls this the subject of the diagnostic record, and what it\nbuys is the difference between a message and a fact. `42002` says a\nname is not defined and the detail says which one in English; the\nsubject says which one in a field, so a client underlining the name,\na linter counting the labels a graph is missing, or a driver mapping\nthe condition onto its own exception type never has to read the\nsentence back.\n\nThe variants are the kinds of thing a GQL statement can name, and\nthere is deliberately no `Other(String)`: a condition about something\nwith no kind here has no subject, which is honest, where a catch-all\nwould be a second message field wearing the subject's name."
    },
    {
      "id": "zu::gqlstatus::Subject::Function",
      "kind": "variant",
      "name": "Function",
      "of": "zu::gqlstatus::Subject",
      "signature": "Function(String)",
      "doc": "A function or a procedure, by name."
    },
    {
      "id": "zu::gqlstatus::Subject::Graph",
      "kind": "variant",
      "name": "Graph",
      "of": "zu::gqlstatus::Subject",
      "signature": "Graph(String)",
      "doc": "A graph, by the name the statement reached it under."
    },
    {
      "id": "zu::gqlstatus::Subject::Label",
      "kind": "variant",
      "name": "Label",
      "of": "zu::gqlstatus::Subject",
      "signature": "Label(String)",
      "doc": "A label, which is what names a node type or an edge type."
    },
    {
      "id": "zu::gqlstatus::Subject::Property",
      "kind": "variant",
      "name": "Property",
      "of": "zu::gqlstatus::Subject",
      "signature": "Property(String)",
      "doc": "A property, by the name written after the dot or before the\ncolon."
    },
    {
      "id": "zu::gqlstatus::Subject::Schema",
      "kind": "variant",
      "name": "Schema",
      "of": "zu::gqlstatus::Subject",
      "signature": "Schema(String)",
      "doc": "A schema, by its path."
    },
    {
      "id": "zu::gqlstatus::Subject::Type",
      "kind": "variant",
      "name": "Type",
      "of": "zu::gqlstatus::Subject",
      "signature": "Type(String)",
      "doc": "A value type, spelled the way a statement would spell it."
    },
    {
      "id": "zu::gqlstatus::Subject::Variable",
      "kind": "variant",
      "name": "Variable",
      "of": "zu::gqlstatus::Subject",
      "signature": "Variable(String)",
      "doc": "A binding variable."
    },
    {
      "id": "zu::gqlstatus::Subject::kind",
      "kind": "method",
      "name": "kind",
      "of": "zu::gqlstatus::Subject",
      "signature": "const fn kind(&self) -> &str",
      "doc": "What kind of thing this is, in one word, for a caller rendering\nthe record rather than matching on it."
    },
    {
      "id": "zu::gqlstatus::Subject::name",
      "kind": "method",
      "name": "name",
      "of": "zu::gqlstatus::Subject",
      "signature": "fn name(&self) -> &str",
      "doc": "The name itself, without the kind."
    },
    {
      "id": "zu::gqlstatus::all",
      "kind": "function",
      "name": "all",
      "signature": "fn all() -> &[Condition]",
      "doc": "Every condition the standard defines, in code order."
    },
    {
      "id": "zu::gqlstatus::codes",
      "kind": "module",
      "name": "codes",
      "source": "zu_common::gqlstatus::generated::codes",
      "doc": "One constant per condition, named `C` followed by the code.\nThe doc comment on each is the standard's own wording."
    },
    {
      "id": "zu::gqlstatus::codes::C00000",
      "kind": "constant",
      "name": "C00000",
      "signature": "super::GqlStatus",
      "doc": "`00000` successful completion"
    },
    {
      "id": "zu::gqlstatus::codes::C00001",
      "kind": "constant",
      "name": "C00001",
      "signature": "super::GqlStatus",
      "doc": "`00001` successful completion, omitted result"
    },
    {
      "id": "zu::gqlstatus::codes::C01000",
      "kind": "constant",
      "name": "C01000",
      "signature": "super::GqlStatus",
      "doc": "`01000` warning"
    },
    {
      "id": "zu::gqlstatus::codes::C01004",
      "kind": "constant",
      "name": "C01004",
      "signature": "super::GqlStatus",
      "doc": "`01004` warning, string data, right truncation"
    },
    {
      "id": "zu::gqlstatus::codes::C01G03",
      "kind": "constant",
      "name": "C01G03",
      "signature": "super::GqlStatus",
      "doc": "`01G03` warning, graph does not exist"
    },
    {
      "id": "zu::gqlstatus::codes::C01G04",
      "kind": "constant",
      "name": "C01G04",
      "signature": "super::GqlStatus",
      "doc": "`01G04` warning, graph type does not exist"
    },
    {
      "id": "zu::gqlstatus::codes::C01G11",
      "kind": "constant",
      "name": "C01G11",
      "signature": "super::GqlStatus",
      "doc": "`01G11` warning, null value eliminated in set function"
    },
    {
      "id": "zu::gqlstatus::codes::C02000",
      "kind": "constant",
      "name": "C02000",
      "signature": "super::GqlStatus",
      "doc": "`02000` no data"
    },
    {
      "id": "zu::gqlstatus::codes::C03000",
      "kind": "constant",
      "name": "C03000",
      "signature": "super::GqlStatus",
      "doc": "`03000` informational"
    },
    {
      "id": "zu::gqlstatus::codes::C08000",
      "kind": "constant",
      "name": "C08000",
      "signature": "super::GqlStatus",
      "doc": "`08000` connection exception"
    },
    {
      "id": "zu::gqlstatus::codes::C08007",
      "kind": "constant",
      "name": "C08007",
      "signature": "super::GqlStatus",
      "doc": "`08007` connection exception, transaction resolution unknown"
    },
    {
      "id": "zu::gqlstatus::codes::C22000",
      "kind": "constant",
      "name": "C22000",
      "signature": "super::GqlStatus",
      "doc": "`22000` data exception"
    },
    {
      "id": "zu::gqlstatus::codes::C22001",
      "kind": "constant",
      "name": "C22001",
      "signature": "super::GqlStatus",
      "doc": "`22001` data exception, string data, right truncation"
    },
    {
      "id": "zu::gqlstatus::codes::C22003",
      "kind": "constant",
      "name": "C22003",
      "signature": "super::GqlStatus",
      "doc": "`22003` data exception, numeric value out of range"
    },
    {
      "id": "zu::gqlstatus::codes::C22004",
      "kind": "constant",
      "name": "C22004",
      "signature": "super::GqlStatus",
      "doc": "`22004` data exception, null value not allowed"
    },
    {
      "id": "zu::gqlstatus::codes::C22007",
      "kind": "constant",
      "name": "C22007",
      "signature": "super::GqlStatus",
      "doc": "`22007` data exception, invalid date, time, or, datetime format"
    },
    {
      "id": "zu::gqlstatus::codes::C22008",
      "kind": "constant",
      "name": "C22008",
      "signature": "super::GqlStatus",
      "doc": "`22008` data exception, datetime field overflow"
    },
    {
      "id": "zu::gqlstatus::codes::C22011",
      "kind": "constant",
      "name": "C22011",
      "signature": "super::GqlStatus",
      "doc": "`22011` data exception, substring error"
    },
    {
      "id": "zu::gqlstatus::codes::C22012",
      "kind": "constant",
      "name": "C22012",
      "signature": "super::GqlStatus",
      "doc": "`22012` data exception, division by zero"
    },
    {
      "id": "zu::gqlstatus::codes::C22015",
      "kind": "constant",
      "name": "C22015",
      "signature": "super::GqlStatus",
      "doc": "`22015` data exception, interval field overflow"
    },
    {
      "id": "zu::gqlstatus::codes::C22018",
      "kind": "constant",
      "name": "C22018",
      "signature": "super::GqlStatus",
      "doc": "`22018` data exception, invalid character value for cast"
    },
    {
      "id": "zu::gqlstatus::codes::C2201E",
      "kind": "constant",
      "name": "C2201E",
      "signature": "super::GqlStatus",
      "doc": "`2201E` data exception, invalid argument for natural logarithm"
    },
    {
      "id": "zu::gqlstatus::codes::C2201F",
      "kind": "constant",
      "name": "C2201F",
      "signature": "super::GqlStatus",
      "doc": "`2201F` data exception, invalid argument for power function"
    },
    {
      "id": "zu::gqlstatus::codes::C22027",
      "kind": "constant",
      "name": "C22027",
      "signature": "super::GqlStatus",
      "doc": "`22027` data exception, trim error"
    },
    {
      "id": "zu::gqlstatus::codes::C2202F",
      "kind": "constant",
      "name": "C2202F",
      "signature": "super::GqlStatus",
      "doc": "`2202F` data exception, array data, right truncation"
    },
    {
      "id": "zu::gqlstatus::codes::C22G02",
      "kind": "constant",
      "name": "C22G02",
      "signature": "super::GqlStatus",
      "doc": "`22G02` data exception, negative limit value"
    },
    {
      "id": "zu::gqlstatus::codes::C22G03",
      "kind": "constant",
      "name": "C22G03",
      "signature": "super::GqlStatus",
      "doc": "`22G03` data exception, invalid value type"
    },
    {
      "id": "zu::gqlstatus::codes::C22G04",
      "kind": "constant",
      "name": "C22G04",
      "signature": "super::GqlStatus",
      "doc": "`22G04` data exception, values not comparable"
    },
    {
      "id": "zu::gqlstatus::codes::C22G05",
      "kind": "constant",
      "name": "C22G05",
      "signature": "super::GqlStatus",
      "doc": "`22G05` data exception, invalid date, time, or datetime function field name"
    },
    {
      "id": "zu::gqlstatus::codes::C22G06",
      "kind": "constant",
      "name": "C22G06",
      "signature": "super::GqlStatus",
      "doc": "`22G06` data exception, invalid datetime function value"
    },
    {
      "id": "zu::gqlstatus::codes::C22G07",
      "kind": "constant",
      "name": "C22G07",
      "signature": "super::GqlStatus",
      "doc": "`22G07` data exception, invalid duration function field name"
    },
    {
      "id": "zu::gqlstatus::codes::C22G0B",
      "kind": "constant",
      "name": "C22G0B",
      "signature": "super::GqlStatus",
      "doc": "`22G0B` data exception, list data, right truncation"
    },
    {
      "id": "zu::gqlstatus::codes::C22G0C",
      "kind": "constant",
      "name": "C22G0C",
      "signature": "super::GqlStatus",
      "doc": "`22G0C` data exception, list element error"
    },
    {
      "id": "zu::gqlstatus::codes::C22G0F",
      "kind": "constant",
      "name": "C22G0F",
      "signature": "super::GqlStatus",
      "doc": "`22G0F` data exception, invalid number of paths or groups"
    },
    {
      "id": "zu::gqlstatus::codes::C22G0H",
      "kind": "constant",
      "name": "C22G0H",
      "signature": "super::GqlStatus",
      "doc": "`22G0H` data exception, invalid duration format"
    },
    {
      "id": "zu::gqlstatus::codes::C22G0M",
      "kind": "constant",
      "name": "C22G0M",
      "signature": "super::GqlStatus",
      "doc": "`22G0M` data exception, multiple assignments to a graph element property"
    },
    {
      "id": "zu::gqlstatus::codes::C22G0N",
      "kind": "constant",
      "name": "C22G0N",
      "signature": "super::GqlStatus",
      "doc": "`22G0N` data exception, number of node labels below supported minimum"
    },
    {
      "id": "zu::gqlstatus::codes::C22G0P",
      "kind": "constant",
      "name": "C22G0P",
      "signature": "super::GqlStatus",
      "doc": "`22G0P` data exception, number of node labels exceeds supported maximum"
    },
    {
      "id": "zu::gqlstatus::codes::C22G0Q",
      "kind": "constant",
      "name": "C22G0Q",
      "signature": "super::GqlStatus",
      "doc": "`22G0Q` data exception, number of edge labels below supported minimum"
    },
    {
      "id": "zu::gqlstatus::codes::C22G0R",
      "kind": "constant",
      "name": "C22G0R",
      "signature": "super::GqlStatus",
      "doc": "`22G0R` data exception, number of edge labels exceeds supported maximum"
    },
    {
      "id": "zu::gqlstatus::codes::C22G0S",
      "kind": "constant",
      "name": "C22G0S",
      "signature": "super::GqlStatus",
      "doc": "`22G0S` data exception, number of node properties exceeds supported maximum"
    },
    {
      "id": "zu::gqlstatus::codes::C22G0T",
      "kind": "constant",
      "name": "C22G0T",
      "signature": "super::GqlStatus",
      "doc": "`22G0T` data exception, number of edge properties exceeds supported maximum"
    },
    {
      "id": "zu::gqlstatus::codes::C22G0U",
      "kind": "constant",
      "name": "C22G0U",
      "signature": "super::GqlStatus",
      "doc": "`22G0U` data exception, record fields do not match"
    },
    {
      "id": "zu::gqlstatus::codes::C22G0V",
      "kind": "constant",
      "name": "C22G0V",
      "signature": "super::GqlStatus",
      "doc": "`22G0V` data exception, reference value, invalid base type"
    },
    {
      "id": "zu::gqlstatus::codes::C22G0W",
      "kind": "constant",
      "name": "C22G0W",
      "signature": "super::GqlStatus",
      "doc": "`22G0W` data exception, reference value, invalid constrained type"
    },
    {
      "id": "zu::gqlstatus::codes::C22G0X",
      "kind": "constant",
      "name": "C22G0X",
      "signature": "super::GqlStatus",
      "doc": "`22G0X` data exception, record data, field unassignable"
    },
    {
      "id": "zu::gqlstatus::codes::C22G0Y",
      "kind": "constant",
      "name": "C22G0Y",
      "signature": "super::GqlStatus",
      "doc": "`22G0Y` data exception, record data, field missing"
    },
    {
      "id": "zu::gqlstatus::codes::C22G0Z",
      "kind": "constant",
      "name": "C22G0Z",
      "signature": "super::GqlStatus",
      "doc": "`22G0Z` data exception, malformed path"
    },
    {
      "id": "zu::gqlstatus::codes::C22G10",
      "kind": "constant",
      "name": "C22G10",
      "signature": "super::GqlStatus",
      "doc": "`22G10` data exception, path data, right truncation"
    },
    {
      "id": "zu::gqlstatus::codes::C22G11",
      "kind": "constant",
      "name": "C22G11",
      "signature": "super::GqlStatus",
      "doc": "`22G11` data exception, reference value, referent deleted"
    },
    {
      "id": "zu::gqlstatus::codes::C22G12",
      "kind": "constant",
      "name": "C22G12",
      "signature": "super::GqlStatus",
      "doc": "`22G12` data exception, invalid value type"
    },
    {
      "id": "zu::gqlstatus::codes::C22G13",
      "kind": "constant",
      "name": "C22G13",
      "signature": "super::GqlStatus",
      "doc": "`22G13` data exception, invalid group variable value"
    },
    {
      "id": "zu::gqlstatus::codes::C22G14",
      "kind": "constant",
      "name": "C22G14",
      "signature": "super::GqlStatus",
      "doc": "`22G14` data exception, incompatible temporal instant unit groups"
    },
    {
      "id": "zu::gqlstatus::codes::C25000",
      "kind": "constant",
      "name": "C25000",
      "signature": "super::GqlStatus",
      "doc": "`25000` invalid transaction state"
    },
    {
      "id": "zu::gqlstatus::codes::C25G01",
      "kind": "constant",
      "name": "C25G01",
      "signature": "super::GqlStatus",
      "doc": "`25G01` invalid transaction state, active GQL-transaction"
    },
    {
      "id": "zu::gqlstatus::codes::C25G02",
      "kind": "constant",
      "name": "C25G02",
      "signature": "super::GqlStatus",
      "doc": "`25G02` invalid transaction state, catalog and data statement mixing not supported"
    },
    {
      "id": "zu::gqlstatus::codes::C25G03",
      "kind": "constant",
      "name": "C25G03",
      "signature": "super::GqlStatus",
      "doc": "`25G03` invalid transaction state, read-only GQL-transaction"
    },
    {
      "id": "zu::gqlstatus::codes::C25G04",
      "kind": "constant",
      "name": "C25G04",
      "signature": "super::GqlStatus",
      "doc": "`25G04` invalid transaction state, accessing multiple graphs not supported"
    },
    {
      "id": "zu::gqlstatus::codes::C2D000",
      "kind": "constant",
      "name": "C2D000",
      "signature": "super::GqlStatus",
      "doc": "`2D000` invalid transaction termination"
    },
    {
      "id": "zu::gqlstatus::codes::C40000",
      "kind": "constant",
      "name": "C40000",
      "signature": "super::GqlStatus",
      "doc": "`40000` transaction rollback"
    },
    {
      "id": "zu::gqlstatus::codes::C40003",
      "kind": "constant",
      "name": "C40003",
      "signature": "super::GqlStatus",
      "doc": "`40003` transaction rollback, statement completion unknown"
    },
    {
      "id": "zu::gqlstatus::codes::C42000",
      "kind": "constant",
      "name": "C42000",
      "signature": "super::GqlStatus",
      "doc": "`42000` syntax error or access rule violation"
    },
    {
      "id": "zu::gqlstatus::codes::C42001",
      "kind": "constant",
      "name": "C42001",
      "signature": "super::GqlStatus",
      "doc": "`42001` syntax error or access rule violation, invalid syntax"
    },
    {
      "id": "zu::gqlstatus::codes::C42002",
      "kind": "constant",
      "name": "C42002",
      "signature": "super::GqlStatus",
      "doc": "`42002` syntax error or access rule violation, invalid reference"
    },
    {
      "id": "zu::gqlstatus::codes::C42004",
      "kind": "constant",
      "name": "C42004",
      "signature": "super::GqlStatus",
      "doc": "`42004` syntax error or access rule violation, use of visually confusable identifiers"
    },
    {
      "id": "zu::gqlstatus::codes::C42006",
      "kind": "constant",
      "name": "C42006",
      "signature": "super::GqlStatus",
      "doc": "`42006` syntax error or access rule violation, number of edge labels below supported minimum"
    },
    {
      "id": "zu::gqlstatus::codes::C42007",
      "kind": "constant",
      "name": "C42007",
      "signature": "super::GqlStatus",
      "doc": "`42007` syntax error or access rule violation, number of edge labels exceeds supported maximum"
    },
    {
      "id": "zu::gqlstatus::codes::C42008",
      "kind": "constant",
      "name": "C42008",
      "signature": "super::GqlStatus",
      "doc": "`42008` syntax error or access rule violation, number of edge properties exceeds supported maximum"
    },
    {
      "id": "zu::gqlstatus::codes::C42009",
      "kind": "constant",
      "name": "C42009",
      "signature": "super::GqlStatus",
      "doc": "`42009` syntax error or access rule violation, number of node labels below supported minimum"
    },
    {
      "id": "zu::gqlstatus::codes::C42010",
      "kind": "constant",
      "name": "C42010",
      "signature": "super::GqlStatus",
      "doc": "`42010` syntax error or access rule violation, number of node labels exceeds supported maximum"
    },
    {
      "id": "zu::gqlstatus::codes::C42011",
      "kind": "constant",
      "name": "C42011",
      "signature": "super::GqlStatus",
      "doc": "`42011` syntax error or access rule violation, number of node properties exceeds supported maximum"
    },
    {
      "id": "zu::gqlstatus::codes::C42012",
      "kind": "constant",
      "name": "C42012",
      "signature": "super::GqlStatus",
      "doc": "`42012` syntax error or access rule violation, number of node type key labels below supported minimum"
    },
    {
      "id": "zu::gqlstatus::codes::C42013",
      "kind": "constant",
      "name": "C42013",
      "signature": "super::GqlStatus",
      "doc": "`42013` syntax error or access rule violation, number of node type key labels exceeds supported maximum"
    },
    {
      "id": "zu::gqlstatus::codes::C42014",
      "kind": "constant",
      "name": "C42014",
      "signature": "super::GqlStatus",
      "doc": "`42014` syntax error or access rule violation, number of edge type key labels below supported minimum"
    },
    {
      "id": "zu::gqlstatus::codes::C42015",
      "kind": "constant",
      "name": "C42015",
      "signature": "super::GqlStatus",
      "doc": "`42015` syntax error or access rule violation, number of edge type key labels exceeds supported maximum"
    },
    {
      "id": "zu::gqlstatus::codes::CG1000",
      "kind": "constant",
      "name": "CG1000",
      "signature": "super::GqlStatus",
      "doc": "`G1000` dependent object error"
    },
    {
      "id": "zu::gqlstatus::codes::CG1001",
      "kind": "constant",
      "name": "CG1001",
      "signature": "super::GqlStatus",
      "doc": "`G1001` dependent object error, edges still exist"
    },
    {
      "id": "zu::gqlstatus::codes::CG1002",
      "kind": "constant",
      "name": "CG1002",
      "signature": "super::GqlStatus",
      "doc": "`G1002` dependent object error, endpoint node is deleted"
    },
    {
      "id": "zu::gqlstatus::codes::CG1003",
      "kind": "constant",
      "name": "CG1003",
      "signature": "super::GqlStatus",
      "doc": "`G1003` dependent object error, endpoint node not in current working graph"
    },
    {
      "id": "zu::gqlstatus::codes::CG2000",
      "kind": "constant",
      "name": "CG2000",
      "signature": "super::GqlStatus",
      "doc": "`G2000` graph type violation"
    },
    {
      "id": "zu::gqlstatus::subclass_rows",
      "kind": "function",
      "name": "subclass_rows",
      "signature": "fn subclass_rows() -> impl Iterator",
      "doc": "The 68 subclass rows, which are the conformance denominator."
    },
    {
      "id": "zu::list_text",
      "kind": "module",
      "name": "list_text",
      "doc": "The text a staged list column holds: a JSON array of scalars.\n\nA list has to cross sqlite to reach a zu1 file, and sqlite has no\nlist. A JSON array in a text column is the form that crossing takes,\nbecause it is the one every writer of a staging file already has a\nlibrary for, and the element type is not read out of it: the column's\ndeclaration says what the elements are and this module is told, so a\n`[1, 2]` under a REALLIST declaration loads as two doubles rather\nthan guessing from the spelling.\n\nThe reader is deliberately small. It accepts an array of the one\nscalar kind it was asked for and nothing else, so no nesting, no\nobjects and no mixed arrays, which is the whole of what a list\nproperty column can hold anyway."
    },
    {
      "id": "zu::list_text::Item",
      "kind": "enum",
      "name": "Item",
      "doc": "One element of a staged list, in the kind its column was declared\nwith."
    },
    {
      "id": "zu::list_text::Item::Bool",
      "kind": "variant",
      "name": "Bool",
      "of": "zu::list_text::Item",
      "signature": "Bool(bool)"
    },
    {
      "id": "zu::list_text::Item::Int",
      "kind": "variant",
      "name": "Int",
      "of": "zu::list_text::Item",
      "signature": "Int(i64)"
    },
    {
      "id": "zu::list_text::Item::Real",
      "kind": "variant",
      "name": "Real",
      "of": "zu::list_text::Item",
      "signature": "Real(f64)"
    },
    {
      "id": "zu::list_text::Item::Text",
      "kind": "variant",
      "name": "Text",
      "of": "zu::list_text::Item",
      "signature": "Text(String)"
    },
    {
      "id": "zu::list_text::Kind",
      "kind": "enum",
      "name": "Kind",
      "doc": "The element kind a list column holds, which its declared type names."
    },
    {
      "id": "zu::list_text::Kind::Bool",
      "kind": "variant",
      "name": "Bool",
      "of": "zu::list_text::Kind",
      "signature": "Bool"
    },
    {
      "id": "zu::list_text::Kind::Int",
      "kind": "variant",
      "name": "Int",
      "of": "zu::list_text::Kind",
      "signature": "Int"
    },
    {
      "id": "zu::list_text::Kind::Real",
      "kind": "variant",
      "name": "Real",
      "of": "zu::list_text::Kind",
      "signature": "Real"
    },
    {
      "id": "zu::list_text::Kind::Text",
      "kind": "variant",
      "name": "Text",
      "of": "zu::list_text::Kind",
      "signature": "Text"
    },
    {
      "id": "zu::list_text::parse",
      "kind": "function",
      "name": "parse",
      "signature": "fn parse(kind: Kind, text: &str) -> zu_common::Result<Vec<Item>>",
      "doc": "Reads a JSON array of `kind` elements."
    },
    {
      "id": "zu::list_text::write",
      "kind": "function",
      "name": "write",
      "signature": "fn write(items: &[Item]) -> String",
      "doc": "Writes elements back as a JSON array, the inverse of [`parse`]."
    },
    {
      "id": "zu::params",
      "kind": "macro",
      "name": "params",
      "source": "zu_query::params",
      "doc": "The bindings for a statement, written the way the statement names\nthem.\n\n```ignore\nlet rows = conn.query_with(\n    \"MATCH (p:person {id: $id}) WHERE p.name STARTS WITH $prefix RETURN p.name AS name\",\n    &params! { \"id\" => 42, \"prefix\" => \"A\" },\n)?;\n```\n\nIt expands to an array literal and not a `Vec`, so the bindings live\non the caller's stack and `&params!{...}` is the slice the query\nmethods take with no allocation at all. The names carry no `$`,\nwhich is the same spelling the C ABI and the JSONL protocol use, so\none statement's parameter list reads the same in all three."
    },
    {
      "id": "zu::query",
      "kind": "module",
      "name": "query",
      "doc": "zuQL against an engine catalog: the facade that turns the storage\ncatalog into the binder's `Schema` and runs text through the\nfrontend. The binder itself is engine-agnostic; this is where zu1\ntable definitions become labels and relationship types."
    },
    {
      "id": "zu::query::Batch",
      "kind": "struct",
      "name": "Batch",
      "source": "zu_query::row::Batch",
      "doc": "A batch of rows handed to a streaming caller\n([`crate::exec::execute_streaming`]).\n\nIt is the same view a whole result gives, over a piece of one: the\ncolumn names and some rows, borrowed for the length of the call. A\ncaller that wants to keep rows past the call copies what it wants\nout of them, which is the trade streaming makes and the reason the\nbatch cannot outlive the sink."
    },
    {
      "id": "zu::query::Batch::columns",
      "kind": "method",
      "name": "columns",
      "of": "zu::query::Batch",
      "signature": "fn columns(&self) -> &[String]",
      "doc": "The column names, which are the statement's and so are the same\nfor every batch of one run."
    },
    {
      "id": "zu::query::Batch::is_empty",
      "kind": "method",
      "name": "is_empty",
      "of": "zu::query::Batch",
      "signature": "fn is_empty(&self) -> bool",
      "doc": "Whether the batch is empty, which the sink is never handed."
    },
    {
      "id": "zu::query::Batch::iter",
      "kind": "method",
      "name": "iter",
      "of": "zu::query::Batch",
      "signature": "fn iter(&self) -> RowIter",
      "doc": "The rows, typed, exactly as a whole result reads them."
    },
    {
      "id": "zu::query::Batch::len",
      "kind": "method",
      "name": "len",
      "of": "zu::query::Batch",
      "signature": "fn len(&self) -> usize",
      "doc": "How many rows this batch holds. It is never zero: a filter that\nrejected everything it read hands over nothing rather than an\nempty batch."
    },
    {
      "id": "zu::query::Batch::new",
      "kind": "method",
      "name": "new",
      "of": "zu::query::Batch",
      "signature": "fn new(columns: &[String], rows: &[Vec<Value>]) -> Batch",
      "doc": "A batch over names and rows that belong together."
    },
    {
      "id": "zu::query::Batch::rows",
      "kind": "method",
      "name": "rows",
      "of": "zu::query::Batch",
      "signature": "fn rows(&self) -> &[Vec<Value>]",
      "doc": "The rows, untyped."
    },
    {
      "id": "zu::query::Flow",
      "kind": "enum",
      "name": "Flow",
      "source": "zu_query::row::Flow",
      "doc": "What a streaming caller wants after a batch: the next one, or no\nmore. Stopping is not a failure and leaves nothing to clean up, so\nit is a value rather than an error."
    },
    {
      "id": "zu::query::Flow::More",
      "kind": "variant",
      "name": "More",
      "of": "zu::query::Flow",
      "signature": "More",
      "doc": "Keep going."
    },
    {
      "id": "zu::query::Flow::Stop",
      "kind": "variant",
      "name": "Stop",
      "of": "zu::query::Flow",
      "signature": "Stop",
      "doc": "Stop the statement here. The rows already handed over stand."
    },
    {
      "id": "zu::query::FromRow",
      "kind": "trait",
      "name": "FromRow",
      "source": "zu_query::row::FromRow",
      "doc": "A Rust type one whole row can be read as, which is a tuple in every\ncase this crate provides. It is a trait rather than an inherent\nmethod so that a caller can implement it for its own struct without\nwaiting for a derive."
    },
    {
      "id": "zu::query::FromRow::from_row",
      "kind": "method",
      "name": "from_row",
      "of": "zu::query::FromRow",
      "signature": "fn from_row(row: Row) -> Result<Self>",
      "doc": "Reads the row, or raises what the first column that disagreed\nwith the type raised."
    },
    {
      "id": "zu::query::FromValue",
      "kind": "trait",
      "name": "FromValue",
      "source": "zu_query::row::FromValue",
      "doc": "A Rust type one column can be read as.\n\nThe lifetime is the result's, which is what lets `&'r str` be an\nimplementation here and is the whole reason this is not a plain\n`TryFrom<&Value>`: a borrowed string has to name the result it\nborrows from."
    },
    {
      "id": "zu::query::FromValue::from_value",
      "kind": "method",
      "name": "from_value",
      "of": "zu::query::FromValue",
      "signature": "fn from_value(value: &Value) -> Result<Self>",
      "doc": "Reads `value`, or raises `22G03` naming both types."
    },
    {
      "id": "zu::query::QueryResult",
      "kind": "struct",
      "name": "QueryResult",
      "source": "zu_query::exec::QueryResult",
      "doc": "The rows a query returns, one column name per RETURN item, plus any\nconditions raised along the way that did not stop it.\n\nThis and [`QueryResult::status`] are the other half of the GQLSTATUS\nenvelope (Spec/2064g/gql/plan/07). An exception replaces the result\nand comes back as `Err`; a warning rides with the answer and lands in\n`notices`, because a statement that dropped a null out of an\naggregate still has rows to give you and the standard still wants you\ntold. Almost every query leaves `notices` empty, so it costs one empty\n`Vec` and no allocation on the path that raises nothing."
    },
    {
      "id": "zu::query::QueryResult::column_index",
      "kind": "method",
      "name": "column_index",
      "of": "zu::query::QueryResult",
      "signature": "fn column_index(&self, name: &str) -> Option<usize>",
      "doc": "Where a column is, by name, or `None` when the result has no\nsuch column. This is the cheap question a caller asks once\nbefore a loop, so that the loop reads by index and never\ncompares a string per row."
    },
    {
      "id": "zu::query::QueryResult::columnar",
      "kind": "method",
      "name": "columnar",
      "of": "zu::query::QueryResult",
      "signature": "fn columnar(&self) -> Result<Columns, MixedColumn>",
      "doc": "This result read down its columns.\n\nTwo passes over the rows in row order, both of them sequential:\nthe first settles each column's type, the second fills each\ncolumn's buffer. Not two passes per column. A client that wanted\nArrow used to walk the result once per column to infer and once\nper column per batch to gather, which on a wide result is a\nmultiple of this and on any result is strided.\n\nFails only where a column's rows are types no single column\nholds, and says which column, which row, and what the two types\nwere. Every other refusal belongs to the client, because the\nformat decides it: a time with an offset has no Arrow type and\nthat is Arrow's fact rather than the engine's.\n\nThe buffers are owned and the complex columns borrow, so the\nresult stays readable afterwards and a client may ask twice.\n\nA sink that filled the columns itself is answered from those and\nnothing here runs, which is the case that matters: it is the\nplain projection, it is what an export asks for, and it is the\none where the rows are never built at all."
    },
    {
      "id": "zu::query::QueryResult::columns",
      "kind": "field",
      "name": "columns",
      "of": "zu::query::QueryResult",
      "signature": "Vec<String>"
    },
    {
      "id": "zu::query::QueryResult::held",
      "kind": "method",
      "name": "held",
      "of": "zu::query::QueryResult",
      "signature": "fn held(columns: Vec<String>, held: Held) -> Self",
      "doc": "The result of a statement whose sink filled columns."
    },
    {
      "id": "zu::query::QueryResult::into_columns",
      "kind": "method",
      "name": "into_columns",
      "of": "zu::query::QueryResult",
      "signature": "fn into_columns(self) -> Result<Held, QueryResult>",
      "doc": "The buffers the sink filled, taken rather than lent.\n\n[`QueryResult::columnar`] copies them, and has to: it hands back a\nview of a result that stays readable, so the buffer behind the view\nis still the result's and cannot be given away. That copy is a\nmemcpy of bytes that are already contiguous and already the right\nshape, which is cheap next to what it replaced and not cheap next\nto nothing. A caller turning the whole answer into Arrow and\nthrowing the result away is paying it for no reason.\n\nSo this takes the result by value and gives up its buffers. What\ncomes back is what the executor wrote, and an Arrow array built on\none of them holds the executor's own allocation rather than a copy\nof it, which is what makes an export of a hundred million integers\ncost the pointer and not the eight hundred megabytes.\n\nA result whose rows were built rather than filled has no buffers to\ngive, and comes back untouched rather than converted, because the\nconversion for that shape is [`QueryResult::columnar`] and it\nallocates the buffers fresh anyway, so there is nothing to save and\nno reason for this to have two answers.\n\n# Errors\n\nThe result itself, when the sink built rows instead of columns."
    },
    {
      "id": "zu::query::QueryResult::iter",
      "kind": "method",
      "name": "iter",
      "of": "zu::query::QueryResult",
      "signature": "fn iter(&self) -> RowIter",
      "doc": "The rows, as typed views over this result."
    },
    {
      "id": "zu::query::QueryResult::new",
      "kind": "method",
      "name": "new",
      "of": "zu::query::QueryResult",
      "signature": "fn new(columns: Vec<String>, rows: Vec<Vec<Value>>) -> Self",
      "doc": "The result of a statement that ran to completion."
    },
    {
      "id": "zu::query::QueryResult::notice",
      "kind": "method",
      "name": "notice",
      "of": "zu::query::QueryResult",
      "signature": "fn notice(&mut self, record: DiagnosticRecord)",
      "doc": "Attaches a condition to a result that is still an answer. Nothing\nhere may be an exception: an exception is not something a\nstatement returns alongside rows, it is what it returns instead."
    },
    {
      "id": "zu::query::QueryResult::notices",
      "kind": "field",
      "name": "notices",
      "of": "zu::query::QueryResult",
      "signature": "Vec<zu_common::gqlstatus::DiagnosticRecord>"
    },
    {
      "id": "zu::query::QueryResult::row",
      "kind": "method",
      "name": "row",
      "of": "zu::query::QueryResult",
      "signature": "fn row(&self, ix: usize) -> Result<Row>",
      "doc": "One row by position, for the query that returns exactly one and\nwhose caller should not have to write a loop to say so."
    },
    {
      "id": "zu::query::QueryResult::rows",
      "kind": "field",
      "name": "rows",
      "of": "zu::query::QueryResult",
      "signature": "Rows"
    },
    {
      "id": "zu::query::QueryResult::status",
      "kind": "method",
      "name": "status",
      "of": "zu::query::QueryResult",
      "signature": "fn status(&self) -> GqlStatus",
      "doc": "The completion condition for the statement, which is the single\nGQLSTATUS value a caller gets back when nothing went wrong.\n\n`00000 successful completion` unless the statement had no\nprojection to give back, in which case the standard has a\ncondition for exactly that: `00001 successful completion, omitted\nresult`. It is derived here rather than stored at each call site\nso no executor can forget it, and because \"did this statement\nproduce columns\" is the whole test.\n\nThis is deliberately not a notice. A statement reports one\noutcome and any number of warnings alongside it, and folding the\noutcome into the warning list makes the two indistinguishable to\nanything reading the envelope."
    },
    {
      "id": "zu::query::Row",
      "kind": "struct",
      "name": "Row",
      "source": "zu_query::row::Row",
      "doc": "One row of a result, borrowed from it.\n\nThe column names ride along with the values so that a row read out\nof an iterator can still be asked for a column by name, which is\nwhat a caller with a wide result wants and what a bare `&[Value]`\ncannot answer."
    },
    {
      "id": "zu::query::Row::columns",
      "kind": "method",
      "name": "columns",
      "of": "zu::query::Row",
      "signature": "fn columns(&self) -> &[String]",
      "doc": "The column names, in the order the statement projected them."
    },
    {
      "id": "zu::query::Row::get",
      "kind": "method",
      "name": "get",
      "of": "zu::query::Row",
      "signature": "fn get<T>(&self) -> Result<T>",
      "doc": "The whole row as one type: a tuple for the usual case, or\nanything implementing [`FromRow`].\n\nThe arity is checked before the columns are read, so a tuple\nthat does not match the projection is one error naming both\ncounts rather than a type error about whichever column happened\nto be first."
    },
    {
      "id": "zu::query::Row::get_at",
      "kind": "method",
      "name": "get_at",
      "of": "zu::query::Row",
      "signature": "fn get_at<T>(&self, ix: usize) -> Result<T>",
      "doc": "One column by position, typed."
    },
    {
      "id": "zu::query::Row::get_by_name",
      "kind": "method",
      "name": "get_by_name",
      "of": "zu::query::Row",
      "signature": "fn get_by_name<T>(&self, name: &str) -> Result<T>",
      "doc": "One column by name, typed. The name is the one the statement\nprojected, so `RETURN p.name AS name` is `name` and a projection\nwith no alias is spelled the way the plan spells it."
    },
    {
      "id": "zu::query::Row::is_empty",
      "kind": "method",
      "name": "is_empty",
      "of": "zu::query::Row",
      "signature": "fn is_empty(&self) -> bool",
      "doc": "Whether the row has no columns, which is what a statement with\nno projection returns."
    },
    {
      "id": "zu::query::Row::len",
      "kind": "method",
      "name": "len",
      "of": "zu::query::Row",
      "signature": "fn len(&self) -> usize",
      "doc": "How many columns this row has."
    },
    {
      "id": "zu::query::Row::new",
      "kind": "method",
      "name": "new",
      "of": "zu::query::Row",
      "signature": "fn new(columns: &[String], values: &[Value]) -> Row",
      "doc": "A row over names and values that belong together. The two are\nthe same length in every result the executor builds; a caller\nthat pairs mismatched slices gets a row whose extra values have\nno name, which reads as a missing column rather than a panic."
    },
    {
      "id": "zu::query::Row::value",
      "kind": "method",
      "name": "value",
      "of": "zu::query::Row",
      "signature": "fn value(&self, ix: usize) -> Result<&Value>",
      "doc": "One column by position, untyped."
    },
    {
      "id": "zu::query::Row::value_by_name",
      "kind": "method",
      "name": "value_by_name",
      "of": "zu::query::Row",
      "signature": "fn value_by_name(&self, name: &str) -> Result<&Value>",
      "doc": "One column by name, untyped."
    },
    {
      "id": "zu::query::Row::values",
      "kind": "method",
      "name": "values",
      "of": "zu::query::Row",
      "signature": "fn values(&self) -> &[Value]",
      "doc": "The values, untyped, for a caller doing its own matching."
    },
    {
      "id": "zu::query::RowIter",
      "kind": "struct",
      "name": "RowIter",
      "source": "zu_query::row::RowIter",
      "doc": "The rows of a result, borrowed one at a time."
    },
    {
      "id": "zu::query::Value",
      "kind": "enum",
      "name": "Value",
      "source": "zu_query::exec::Value",
      "doc": "One runtime value. Nodes and rels carry their table so multi-table\nslots stay unambiguous."
    },
    {
      "id": "zu::query::Value::BindingTable",
      "kind": "variant",
      "name": "BindingTable",
      "of": "zu::query::Value",
      "signature": "BindingTable(std::sync::Arc<zu_query::refs::BindingTable>)",
      "doc": "GV61. A binding table reference: the rows of some earlier\nresult, behind a handle so that passing the table costs a\npointer rather than a copy."
    },
    {
      "id": "zu::query::Value::Bool",
      "kind": "variant",
      "name": "Bool",
      "of": "zu::query::Value",
      "signature": "Bool(bool)"
    },
    {
      "id": "zu::query::Value::Bytes",
      "kind": "variant",
      "name": "Bytes",
      "of": "zu::query::Value",
      "signature": "Bytes(Vec<u8>)",
      "doc": "GV35, a byte string: a sequence of octets and not of characters.\n`X'0041'` is one byte string of two bytes and never the letter\nA, which is the whole reason it is an arm here rather than a\n`Str` holding the same bytes."
    },
    {
      "id": "zu::query::Value::Chain",
      "kind": "variant",
      "name": "Chain",
      "of": "zu::query::Value",
      "signature": "Chain(std::sync::Arc<PathLink>)",
      "doc": "A PMR chain (docs/07 §5): the executor-internal form of a\nvariable-length path. [`settle`] turns it into the edge list\nbefore any value leaves the pipeline, so results never hold one."
    },
    {
      "id": "zu::query::Value::Decimal",
      "kind": "variant",
      "name": "Decimal",
      "of": "zu::query::Value",
      "signature": "Decimal(zu_common::Decimal)",
      "doc": "GV17, an exact decimal: an integer of units and the scale that\nsays how large a unit is.\n\nIt is an arm of its own rather than a float because a tenth is\nnot a binary fraction, so a price held as binary64 is not the\nprice. Two decimals written at different scales are one value\nand print differently, which is why the scale rides on the\nnumber: a decimal in a `RETURN` has no column to ask."
    },
    {
      "id": "zu::query::Value::Float",
      "kind": "variant",
      "name": "Float",
      "of": "zu::query::Value",
      "signature": "Float(f64)"
    },
    {
      "id": "zu::query::Value::Graph",
      "kind": "variant",
      "name": "Graph",
      "of": "zu::query::Value",
      "signature": "Graph(zu_query::refs::GraphHandle)",
      "doc": "GV60. A graph reference: which graph, not the graph. The engine\nhands one out and a query carries it; there is no literal that\nwrites one, because a graph is in the catalog and not in the\ntext."
    },
    {
      "id": "zu::query::Value::Int",
      "kind": "variant",
      "name": "Int",
      "of": "zu::query::Value",
      "signature": "Int(i64)"
    },
    {
      "id": "zu::query::Value::List",
      "kind": "variant",
      "name": "List",
      "of": "zu::query::Value",
      "signature": "List(Vec<Value>)"
    },
    {
      "id": "zu::query::Value::NO_REL_ROW",
      "kind": "assoc-const",
      "name": "NO_REL_ROW",
      "of": "zu::query::Value",
      "signature": "u64",
      "doc": "The `ord` of an edge that has no row in the stored property\ncolumns: one a write staged and no fold has landed yet, or one\na pattern matched over a different rel table. A property read\noff such an edge answers null rather than reading somebody\nelse's row."
    },
    {
      "id": "zu::query::Value::Node",
      "kind": "variant",
      "name": "Node",
      "of": "zu::query::Value",
      "signature": "Node { table: u32, offset: u64 }"
    },
    {
      "id": "zu::query::Value::Null",
      "kind": "variant",
      "name": "Null",
      "of": "zu::query::Value",
      "signature": "Null"
    },
    {
      "id": "zu::query::Value::Path",
      "kind": "variant",
      "name": "Path",
      "of": "zu::query::Value",
      "signature": "Path(Vec<Value>)",
      "doc": "GV55. A path: the nodes and edges of a walk, alternating, a node\nat each end. A one node path has no edges and is the shortest\nthere is; there is no empty path, because a path is a walk and a\nwalk starts somewhere. Build one with [`Value::path`], which\nchecks that shape."
    },
    {
      "id": "zu::query::Value::Record",
      "kind": "variant",
      "name": "Record",
      "of": "zu::query::Value",
      "signature": "Record(Vec<(String, Value)>)",
      "doc": "GV45. A record: named fields, each holding a value. The fields\nare kept sorted by name and a name appears once, which is what\nmakes two records with the same fields written in different\norders one value rather than two. Build one with\n[`Value::record`] rather than by hand, so that holds."
    },
    {
      "id": "zu::query::Value::Rel",
      "kind": "variant",
      "name": "Rel",
      "of": "zu::query::Value",
      "signature": "Rel { table: u32, src: u64, dst: u64, ord: u64 }"
    },
    {
      "id": "zu::query::Value::Str",
      "kind": "variant",
      "name": "Str",
      "of": "zu::query::Value",
      "signature": "Str(String)"
    },
    {
      "id": "zu::query::Value::Temporal",
      "kind": "variant",
      "name": "Temporal",
      "of": "zu::query::Value",
      "signature": "Temporal(zu_common::Temporal)",
      "doc": "A date, a time, a datetime or a duration. One arm rather than\nsix, because a temporal value is a count and a meaning and the\nmeaning is what [`Temporal`] carries; the executor treats them\nalike everywhere except where the calendar is involved."
    },
    {
      "id": "zu::query::Value::field",
      "kind": "method",
      "name": "field",
      "of": "zu::query::Value",
      "signature": "fn field(&self, name: &str) -> Option<&Value>",
      "doc": "The value of the field named `name`, or `None` when the record\nhas no such field."
    },
    {
      "id": "zu::query::Value::path",
      "kind": "method",
      "name": "path",
      "of": "zu::query::Value",
      "signature": "fn path(elements: Vec<Value>) -> Result<Value>",
      "doc": "A path out of an alternating element list, or the condition the\nlist breaks.\n\nGE06 builds a path from elements the query names, which means\nthe two things a matched path gets for free have to be checked\nhere instead. The shape is the first: nodes and edges alternate\nand a node is at each end, so an even length or an edge in a\nnode's place is not a path at all. The joining is the second and\nis the one worth having: an edge between two nodes it does not\ntouch is a sequence of the right shape that describes a walk\nnobody can take, and 22G0Z is the condition for exactly that. An\nedge is allowed to be traversed against its direction, because a\nwalk may go either way along one and ISO's path values are not\ndirected."
    },
    {
      "id": "zu::query::Value::record",
      "kind": "method",
      "name": "record",
      "of": "zu::query::Value",
      "signature": "fn record(fields: Vec<(String, Value)>) -> Value",
      "doc": "A record out of the fields as they were written.\n\nThe fields are sorted by name here rather than compared by name\nlater, because every reader of a record then gets the cheap\nversion of the question: two records have the same fields when\ntheir name lists are equal, and equality is a walk down the two\nin step. A field written twice is the caller's error and the\nbinder refuses it before this is reached, so the first one\nstands rather than this having an error path nothing can reach."
    },
    {
      "id": "zu::query::Zu1Graph",
      "kind": "struct",
      "name": "Zu1Graph",
      "doc": "The executor's view of one open zu1 file: readers load lazily per\nrel table and cache their directories across calls, and props\nreaders load lazily per node table the same way."
    },
    {
      "id": "zu::query::Zu1Graph::catalog",
      "kind": "method",
      "name": "catalog",
      "of": "zu::query::Zu1Graph",
      "signature": "fn catalog(&self) -> &Catalog"
    },
    {
      "id": "zu::query::Zu1Graph::catalog_handle",
      "kind": "method",
      "name": "catalog_handle",
      "of": "zu::query::Zu1Graph",
      "signature": "fn catalog_handle(&self) -> Arc<Catalog>",
      "doc": "The catalog as the handle a snapshot wants, so a statement that\nopens one hands over a pointer rather than a copy."
    },
    {
      "id": "zu::query::Zu1Graph::file",
      "kind": "method",
      "name": "file",
      "of": "zu::query::Zu1Graph",
      "signature": "fn file(&self) -> &Zu1File"
    },
    {
      "id": "zu::query::Zu1Graph::file_mut",
      "kind": "method",
      "name": "file_mut",
      "of": "zu::query::Zu1Graph",
      "signature": "fn file_mut(&mut self) -> &mut Zu1File"
    },
    {
      "id": "zu::query::Zu1Graph::frames",
      "kind": "method",
      "name": "frames",
      "of": "zu::query::Zu1Graph",
      "signature": "fn frames(&self) -> &Arc<FrameSet>",
      "doc": "The frames a statement run through this graph reads as tables."
    },
    {
      "id": "zu::query::Zu1Graph::new",
      "kind": "method",
      "name": "new",
      "of": "zu::query::Zu1Graph",
      "signature": "fn new(db: &mut Zu1File, catalog: impl Into<Arc<Catalog>>) -> Self"
    },
    {
      "id": "zu::query::Zu1Graph::owned",
      "kind": "method",
      "name": "owned",
      "of": "zu::query::Zu1Graph",
      "signature": "fn owned(db: Zu1File, catalog: impl Into<Arc<Catalog>>) -> Zu1Graph",
      "doc": "A graph that owns its handle outright, the shape a [`Session`]\nkeeps alive across queries so decoded groups and directories\nstay warm instead of dying with each call.\n\n[`Session`]: crate::session::Session"
    },
    {
      "id": "zu::query::Zu1Graph::set_catalog",
      "kind": "method",
      "name": "set_catalog",
      "of": "zu::query::Zu1Graph",
      "signature": "fn set_catalog(&mut self, catalog: impl Into<Arc<Catalog>>)",
      "doc": "Swaps in a fresh catalog and drops every cached reader; the\nsession calls this when the header epoch moves, because the\ncached directories and decoded groups describe the old epoch."
    },
    {
      "id": "zu::query::Zu1Graph::set_frames",
      "kind": "method",
      "name": "set_frames",
      "of": "zu::query::Zu1Graph",
      "signature": "fn set_frames(&mut self, frames: Arc<FrameSet>)",
      "doc": "Hands over the session's frames. Registering builds a new set\nrather than editing this one, so a statement already running\nkeeps reading the set it started with."
    },
    {
      "id": "zu::query::Zu1Graph::set_patches",
      "kind": "method",
      "name": "set_patches",
      "of": "zu::query::Zu1Graph",
      "signature": "fn set_patches(&mut self, patches: Arc<Patches>)",
      "doc": "Hands the readers the cells a commit has left in the overlay,\nwhich the session does after every write that did not fold.\n\nThe readers stay: the columns they describe have not moved, and\nthat is the point of not folding. What changes is what they read\non top of them."
    },
    {
      "id": "zu::query::bind",
      "kind": "function",
      "name": "bind",
      "signature": "fn bind(source: &str, catalog: &zu::zu1::catalog::Catalog) -> zu_common::Result<zu_query::binder::BoundQuery>",
      "doc": "Parses and binds one query against a zu1 catalog.\n\nThe epoch the schema is stamped with is zero, and it can be: a\ncatalog on its own is not a snapshot, and nothing here runs, so no\ngraph reference this binds ever reaches a caller."
    },
    {
      "id": "zu::query::check",
      "kind": "function",
      "name": "check",
      "signature": "fn check(source: &str) -> zu_common::Result<()>",
      "doc": "Whether the text is a statement at all, without a catalog and\nwithout a file.\n\nThis is the front end and nothing behind it, which is what an editor\nwith no database open can still say something true about. A name it\ncannot check is a name it does not look at: `MATCH (p:Person)` parses\nwhether or not a `Person` exists anywhere, so the answer is either a\nsyntax error at a place in the text or nothing. Everything past the\nparser needs a catalog, and [`bind`] is where that starts."
    },
    {
      "id": "zu::query::column",
      "kind": "module",
      "name": "column",
      "source": "zu_query::column",
      "doc": "A result read down its columns instead of across its rows.\n\nEvery client that hands a result to something else hands it over as\ncolumns. Arrow is columns, pandas is columns, polars is columns, and\nthe C ABI's chunked read is columns. A result is [`Vec<Vec<Value>>`],\nso each of those clients has been writing the same transpose, and\nwriting it badly: `zu-python` walked the whole result once per column\nto decide the column's type and again per batch to gather the values,\nwhich on a million rows of three columns is six strided passes over\nthirty two byte cells and a `Vec` of pointers per batch per column.\n`docs/clients/duckdb.md` measured what that costs. Twenty times\nDuckDB, for the same bytes in the same layout at the other end.\n\nThis is the transpose written once, in the engine, where the next\nchange makes it free. Two passes over the rows in row order: one to\nsettle each column's type, one to fill each column's buffer. Not two\nper column, two in total, and both of them sequential over memory the\nresult already had hot. The buffers that come out are Arrow's own\nlayouts, so a client's remaining job is to name them rather than to\ncopy them: a `Vec<i64>` becomes an `Int64Array` by moving, offsets\nand bytes become a `StringArray` by moving, and a validity bitmap is\nArrow's null buffer with the same bit order and the same meaning of\nset.\n\nThat was a transpose because the sink used to flatten.\n`crates/zu-vector` is the vector layer the executor computes in, and\nthe place the vectors died was the sink, which filled a row at a time\nout of them. It does not any more: on a plan with nothing above the\nprojection the sink fills [`Held`] columns straight out of the\nvectors, [`QueryResult::columnar`] hands those buffers back without\nwalking anything, and the rows are built only if somebody asks for\nthem. No client changed a line. That is the whole reason the answer\nis shaped like this and lives here rather than in a binding.\n\nThe walk below is still what answers for every other plan, because a\nsort or a dedup or a group is a step over rows and the sink hands\nthose on as rows."
    },
    {
      "id": "zu::query::column::Column",
      "kind": "struct",
      "name": "Column",
      "doc": "One column of a result: its name, the type its rows turned out to\nbe, its buffer, and which of its rows have a value."
    },
    {
      "id": "zu::query::column::Column::data",
      "kind": "field",
      "name": "data",
      "of": "zu::query::column::Column",
      "signature": "ColumnData"
    },
    {
      "id": "zu::query::column::Column::len",
      "kind": "field",
      "name": "len",
      "of": "zu::query::column::Column",
      "signature": "usize"
    },
    {
      "id": "zu::query::column::Column::name",
      "kind": "field",
      "name": "name",
      "of": "zu::query::column::Column",
      "signature": "&str"
    },
    {
      "id": "zu::query::column::Column::ty",
      "kind": "field",
      "name": "ty",
      "of": "zu::query::column::Column",
      "signature": "ColumnType"
    },
    {
      "id": "zu::query::column::Column::validity",
      "kind": "field",
      "name": "validity",
      "of": "zu::query::column::Column",
      "signature": "Option<Validity>",
      "doc": "Absent when every row has a value."
    },
    {
      "id": "zu::query::column::ColumnData",
      "kind": "enum",
      "name": "ColumnData",
      "doc": "The buffer holding one column's values.\n\nEvery arm but [`ColumnData::Complex`] is a contiguous buffer of one\nphysical type, in the layout a columnar reader wants, and a client\nturns it into an array by taking ownership rather than by walking\nit. `Complex` is the fallback for what no primitive covers: nodes,\nrels, paths, lists, records and the two handles. Those still arrive\nas values, borrowed from the result rather than cloned out of it,\nand a client builds them the way it always did. They are also the\ncolumns nobody exports a million of.\n\nA null row still occupies its cell in a flat buffer, holding\nwhatever the type's zero is. That is what the validity bitmap is\nfor, and it is why the buffers are strided and can be moved rather\nthan rebuilt."
    },
    {
      "id": "zu::query::column::ColumnData::Bool",
      "kind": "variant",
      "name": "Bool",
      "of": "zu::query::column::ColumnData",
      "signature": "Bool { bits: Vec<u8> }",
      "doc": "One bit per row, least significant bit first, the same packing\nas [`Validity`]."
    },
    {
      "id": "zu::query::column::ColumnData::Bytes",
      "kind": "variant",
      "name": "Bytes",
      "of": "zu::query::column::ColumnData",
      "signature": "Bytes(StrColumn)",
      "doc": "GV35, a byte string column. The same two buffers a string\ncolumn keeps, over bytes that are not text, which is why the\narm is its own and not a flag on the one above."
    },
    {
      "id": "zu::query::column::ColumnData::Complex",
      "kind": "variant",
      "name": "Complex",
      "of": "zu::query::column::ColumnData",
      "signature": "Complex(Vec<&zu_query::exec::Value>)",
      "doc": "The values themselves, for the types no buffer covers."
    },
    {
      "id": "zu::query::column::ColumnData::Days",
      "kind": "variant",
      "name": "Days",
      "of": "zu::query::column::ColumnData",
      "signature": "Days(Vec<i32>)",
      "doc": "Days, for [`ColumnType::Date`]."
    },
    {
      "id": "zu::query::column::ColumnData::Float",
      "kind": "variant",
      "name": "Float",
      "of": "zu::query::column::ColumnData",
      "signature": "Float(Vec<f64>)"
    },
    {
      "id": "zu::query::column::ColumnData::Int",
      "kind": "variant",
      "name": "Int",
      "of": "zu::query::column::ColumnData",
      "signature": "Int(Vec<i64>)"
    },
    {
      "id": "zu::query::column::ColumnData::Months",
      "kind": "variant",
      "name": "Months",
      "of": "zu::query::column::ColumnData",
      "signature": "Months(Vec<i64>)",
      "doc": "Months, for [`ColumnType::YearMonth`]."
    },
    {
      "id": "zu::query::column::ColumnData::Nanos",
      "kind": "variant",
      "name": "Nanos",
      "of": "zu::query::column::ColumnData",
      "signature": "Nanos(Vec<i64>)",
      "doc": "Nanoseconds, for every time, datetime and day-time duration."
    },
    {
      "id": "zu::query::column::ColumnData::Null",
      "kind": "variant",
      "name": "Null",
      "of": "zu::query::column::ColumnData",
      "signature": "Null",
      "doc": "A column of nothing but nulls. It has a length and no buffer,\nbecause there is nothing to put in one."
    },
    {
      "id": "zu::query::column::ColumnData::Str",
      "kind": "variant",
      "name": "Str",
      "of": "zu::query::column::ColumnData",
      "signature": "Str(StrColumn)"
    },
    {
      "id": "zu::query::column::ColumnType",
      "kind": "enum",
      "name": "ColumnType",
      "doc": "The type a whole column turned out to be.\n\nA column has one type and the values decide it: the first one that\nis not null settles it, and every value after has to fit. There is\nexactly one widening, integer to float, because a projection that\nreturns `count(x)` on one row and `avg(x)` on another means a number\nand every reader of the column reads it as one. Everything else that\ndoes not fit is refused and named, because a column that quietly\nbecame strings is worse than one that would not build.\n\nThis is not [`LogicalType`]. A logical type is what the language\ndeclared, with its nullability and its width and its precision. This\nis what a finished column holds, which is a smaller question with a\nsmaller answer: nullability is the validity bitmap and never the\ntype, and a width is a check that already happened.\n\n[`LogicalType`]: zu_common::LogicalType"
    },
    {
      "id": "zu::query::column::ColumnType::BindingTable",
      "kind": "variant",
      "name": "BindingTable",
      "of": "zu::query::column::ColumnType",
      "signature": "BindingTable",
      "doc": "GV61, a reference to a binding table."
    },
    {
      "id": "zu::query::column::ColumnType::Bool",
      "kind": "variant",
      "name": "Bool",
      "of": "zu::query::column::ColumnType",
      "signature": "Bool"
    },
    {
      "id": "zu::query::column::ColumnType::Bytes",
      "kind": "variant",
      "name": "Bytes",
      "of": "zu::query::column::ColumnType",
      "signature": "Bytes",
      "doc": "GV35, a byte string. It rides the same offsets and bytes a\nstring column does and is not one: the bytes are octets and\nneed not be text at all, so a reader that took this for a\nstring would be handed something it cannot decode."
    },
    {
      "id": "zu::query::column::ColumnType::Date",
      "kind": "variant",
      "name": "Date",
      "of": "zu::query::column::ColumnType",
      "signature": "Date",
      "doc": "Days since 1970-01-01."
    },
    {
      "id": "zu::query::column::ColumnType::DayTime",
      "kind": "variant",
      "name": "DayTime",
      "of": "zu::query::column::ColumnType",
      "signature": "DayTime",
      "doc": "Nanoseconds."
    },
    {
      "id": "zu::query::column::ColumnType::Decimal",
      "kind": "variant",
      "name": "Decimal",
      "of": "zu::query::column::ColumnType",
      "signature": "Decimal { scale: u16 }",
      "doc": "GV17, exact decimals at one scale. The scale is the column's\nand not each value's: a column mixing `1.5` and `1.25` settles\non hundredths, because the wider scale holds both exactly and\nthe narrower one would round a value away."
    },
    {
      "id": "zu::query::column::ColumnType::Float",
      "kind": "variant",
      "name": "Float",
      "of": "zu::query::column::ColumnType",
      "signature": "Float"
    },
    {
      "id": "zu::query::column::ColumnType::Graph",
      "kind": "variant",
      "name": "Graph",
      "of": "zu::query::column::ColumnType",
      "signature": "Graph",
      "doc": "GV60, a reference to a graph."
    },
    {
      "id": "zu::query::column::ColumnType::Int",
      "kind": "variant",
      "name": "Int",
      "of": "zu::query::column::ColumnType",
      "signature": "Int"
    },
    {
      "id": "zu::query::column::ColumnType::List",
      "kind": "variant",
      "name": "List",
      "of": "zu::query::column::ColumnType",
      "signature": "List(Box<ColumnType>)"
    },
    {
      "id": "zu::query::column::ColumnType::LocalDatetime",
      "kind": "variant",
      "name": "LocalDatetime",
      "of": "zu::query::column::ColumnType",
      "signature": "LocalDatetime",
      "doc": "Nanoseconds since 1970-01-01T00:00:00."
    },
    {
      "id": "zu::query::column::ColumnType::LocalTime",
      "kind": "variant",
      "name": "LocalTime",
      "of": "zu::query::column::ColumnType",
      "signature": "LocalTime",
      "doc": "Nanoseconds since midnight."
    },
    {
      "id": "zu::query::column::ColumnType::Node",
      "kind": "variant",
      "name": "Node",
      "of": "zu::query::column::ColumnType",
      "signature": "Node"
    },
    {
      "id": "zu::query::column::ColumnType::Null",
      "kind": "variant",
      "name": "Null",
      "of": "zu::query::column::ColumnType",
      "signature": "Null",
      "doc": "Nothing but nulls. Every columnar format has a type for it, and\na column of nulls with no type would be the one case a reader\ncould not name."
    },
    {
      "id": "zu::query::column::ColumnType::Path",
      "kind": "variant",
      "name": "Path",
      "of": "zu::query::column::ColumnType",
      "signature": "Path"
    },
    {
      "id": "zu::query::column::ColumnType::Record",
      "kind": "variant",
      "name": "Record",
      "of": "zu::query::column::ColumnType",
      "signature": "Record(Vec<(String, ColumnType)>)"
    },
    {
      "id": "zu::query::column::ColumnType::Rel",
      "kind": "variant",
      "name": "Rel",
      "of": "zu::query::column::ColumnType",
      "signature": "Rel"
    },
    {
      "id": "zu::query::column::ColumnType::Str",
      "kind": "variant",
      "name": "Str",
      "of": "zu::query::column::ColumnType",
      "signature": "Str"
    },
    {
      "id": "zu::query::column::ColumnType::YearMonth",
      "kind": "variant",
      "name": "YearMonth",
      "of": "zu::query::column::ColumnType",
      "signature": "YearMonth",
      "doc": "Months."
    },
    {
      "id": "zu::query::column::ColumnType::ZonedDatetime",
      "kind": "variant",
      "name": "ZonedDatetime",
      "of": "zu::query::column::ColumnType",
      "signature": "ZonedDatetime { offset: i16 }",
      "doc": "Nanoseconds since the epoch in UTC, and the offset in minutes\nthe value was written with. Later rows may have been written\nsomewhere else and are the same instant either way, so the\noffset decides how the column prints and never what it holds."
    },
    {
      "id": "zu::query::column::ColumnType::ZonedTime",
      "kind": "variant",
      "name": "ZonedTime",
      "of": "zu::query::column::ColumnType",
      "signature": "ZonedTime { offset: i16 }",
      "doc": "Nanoseconds since midnight in the offset's own day, and the\noffset in minutes. The first such value in the column names the\noffset, the way the first zoned datetime does."
    },
    {
      "id": "zu::query::column::ColumnType::is_flat",
      "kind": "method",
      "name": "is_flat",
      "of": "zu::query::column::ColumnType",
      "signature": "fn is_flat(&self) -> bool",
      "doc": "Whether a column of this type is held as one buffer of fixed\nwidth cells, which is the case a client turns into an array by\nmoving a `Vec` rather than by walking values."
    },
    {
      "id": "zu::query::column::ColumnType::name",
      "kind": "method",
      "name": "name",
      "of": "zu::query::column::ColumnType",
      "signature": "fn name(&self) -> String",
      "doc": "What to call this in a message a person reads."
    },
    {
      "id": "zu::query::column::ColumnType::of",
      "kind": "method",
      "name": "of",
      "of": "zu::query::column::ColumnType",
      "signature": "fn of(value: &Value) -> Result<ColumnType, Mixture>",
      "doc": "The type of one value on its own, which is where inference\nstarts.\n\nFails on the one disagreement that hides below a row: a list, or\na record holding one, whose own items are types no single list\nholds. The row loop cannot see inside a value, so the mixture is\nfound here and named where the row number is known."
    },
    {
      "id": "zu::query::column::ColumnType::unify",
      "kind": "method",
      "name": "unify",
      "of": "zu::query::column::ColumnType",
      "signature": "fn unify(left: ColumnType, right: ColumnType) -> Option<ColumnType>",
      "doc": "The one type two types are both, or nothing when they are not."
    },
    {
      "id": "zu::query::column::Columns",
      "kind": "struct",
      "name": "Columns",
      "doc": "A whole result read down its columns."
    },
    {
      "id": "zu::query::column::Columns::columns",
      "kind": "field",
      "name": "columns",
      "of": "zu::query::column::Columns",
      "signature": "Vec<Column>"
    },
    {
      "id": "zu::query::column::Columns::is_empty",
      "kind": "method",
      "name": "is_empty",
      "of": "zu::query::column::Columns",
      "signature": "fn is_empty(&self) -> bool",
      "doc": "Whether the result had no columns, which is what a statement\nwith no projection gives back."
    },
    {
      "id": "zu::query::column::Columns::len",
      "kind": "method",
      "name": "len",
      "of": "zu::query::column::Columns",
      "signature": "fn len(&self) -> usize",
      "doc": "How many columns."
    },
    {
      "id": "zu::query::column::Columns::rows",
      "kind": "field",
      "name": "rows",
      "of": "zu::query::column::Columns",
      "signature": "usize",
      "doc": "Rows in the result, which is every column's length and is also\nthe answer for a result with no columns at all."
    },
    {
      "id": "zu::query::column::Held",
      "kind": "struct",
      "name": "Held",
      "doc": "A whole result the executor filled down its columns, owning every\nbuffer in it.\n\n[`Columns`] is the same answer borrowed: its complex arm points at\nvalues that live in a [`QueryResult`], which is what lets the walk\nbelow hand back node and list columns without cloning them. A sink\nthat filled the buffers itself has nothing to point at, so this is\nthe shape it keeps them in, and [`Held::borrow`] is the view a client\nreads.\n\nThe arms are the ones a projection produces: a stored column is an\ninteger, a real or a string, a row id is an integer, and everything\nelse a RETURN item can name is one value at a time. Dates and\ndurations have buffers in [`ColumnData`] and no arm here on purpose,\nbecause the only way one reaches a projection in this executor is as\na constant, and a constant column that went through a buffer and\nback would have to be told which of its rows carried which offset."
    },
    {
      "id": "zu::query::column::Held::borrow",
      "kind": "method",
      "name": "borrow",
      "of": "zu::query::column::Held",
      "signature": "fn borrow(&self) -> Columns",
      "doc": "This answer as the borrowed view a client reads.\n\nThe flat buffers are copied and the complex ones are borrowed,\nwhich is the one copy left on the export path: a memcpy of the\nbytes that are already contiguous and already the right shape,\nagainst the two strided walks of a row vector this replaced. It\nis here because a caller is allowed to ask twice, and it goes\nwhen a caller says it will not: [`QueryResult::into_columns`]\ntakes the result by value and hands these buffers over instead\nof copying them."
    },
    {
      "id": "zu::query::column::Held::columns",
      "kind": "field",
      "name": "columns",
      "of": "zu::query::column::Held",
      "signature": "Vec<HeldColumn>"
    },
    {
      "id": "zu::query::column::Held::names",
      "kind": "method",
      "name": "names",
      "of": "zu::query::column::Held",
      "signature": "fn names(&self) -> Vec<String>",
      "doc": "The column names, which are the result's own."
    },
    {
      "id": "zu::query::column::Held::rows",
      "kind": "field",
      "name": "rows",
      "of": "zu::query::column::Held",
      "signature": "usize",
      "doc": "Rows in the result, which is every column's length."
    },
    {
      "id": "zu::query::column::HeldColumn",
      "kind": "struct",
      "name": "HeldColumn",
      "doc": "One column of a [`Held`] result."
    },
    {
      "id": "zu::query::column::HeldColumn::data",
      "kind": "field",
      "name": "data",
      "of": "zu::query::column::HeldColumn",
      "signature": "HeldData"
    },
    {
      "id": "zu::query::column::HeldColumn::name",
      "kind": "field",
      "name": "name",
      "of": "zu::query::column::HeldColumn",
      "signature": "String"
    },
    {
      "id": "zu::query::column::HeldColumn::ty",
      "kind": "field",
      "name": "ty",
      "of": "zu::query::column::HeldColumn",
      "signature": "ColumnType"
    },
    {
      "id": "zu::query::column::HeldColumn::validity",
      "kind": "field",
      "name": "validity",
      "of": "zu::query::column::HeldColumn",
      "signature": "Option<Validity>",
      "doc": "Absent when every row has a value."
    },
    {
      "id": "zu::query::column::HeldData",
      "kind": "enum",
      "name": "HeldData",
      "doc": "The buffer one [`HeldColumn`] holds, in the layouts [`ColumnData`]\ndescribes."
    },
    {
      "id": "zu::query::column::HeldData::Bool",
      "kind": "variant",
      "name": "Bool",
      "of": "zu::query::column::HeldData",
      "signature": "Bool { bits: Vec<u8> }"
    },
    {
      "id": "zu::query::column::HeldData::Complex",
      "kind": "variant",
      "name": "Complex",
      "of": "zu::query::column::HeldData",
      "signature": "Complex(Vec<zu_query::exec::Value>)",
      "doc": "The values themselves, for nodes and for anything else no buffer\ncovers."
    },
    {
      "id": "zu::query::column::HeldData::Float",
      "kind": "variant",
      "name": "Float",
      "of": "zu::query::column::HeldData",
      "signature": "Float(Vec<f64>)"
    },
    {
      "id": "zu::query::column::HeldData::Int",
      "kind": "variant",
      "name": "Int",
      "of": "zu::query::column::HeldData",
      "signature": "Int(Vec<i64>)"
    },
    {
      "id": "zu::query::column::HeldData::Null",
      "kind": "variant",
      "name": "Null",
      "of": "zu::query::column::HeldData",
      "signature": "Null"
    },
    {
      "id": "zu::query::column::HeldData::Str",
      "kind": "variant",
      "name": "Str",
      "of": "zu::query::column::HeldData",
      "signature": "Str(StrColumn)"
    },
    {
      "id": "zu::query::column::MixedColumn",
      "kind": "struct",
      "name": "MixedColumn",
      "doc": "Two rows of one column that no single column holds.\n\nThe row is in it because that is the only part a caller can act on:\nthe column name says where to look and the row number says where to\nlook in it, and a message with neither is a message that sends\nsomebody to read a million rows by hand."
    },
    {
      "id": "zu::query::column::MixedColumn::arrived",
      "kind": "field",
      "name": "arrived",
      "of": "zu::query::column::MixedColumn",
      "signature": "String",
      "doc": "What this row brought."
    },
    {
      "id": "zu::query::column::MixedColumn::column",
      "kind": "field",
      "name": "column",
      "of": "zu::query::column::MixedColumn",
      "signature": "String"
    },
    {
      "id": "zu::query::column::MixedColumn::held",
      "kind": "field",
      "name": "held",
      "of": "zu::query::column::MixedColumn",
      "signature": "String",
      "doc": "What the column held before this row."
    },
    {
      "id": "zu::query::column::MixedColumn::in_list",
      "kind": "field",
      "name": "in_list",
      "of": "zu::query::column::MixedColumn",
      "signature": "bool",
      "doc": "Set when the disagreement is between the items of one list\nrather than between rows of the column."
    },
    {
      "id": "zu::query::column::MixedColumn::row",
      "kind": "field",
      "name": "row",
      "of": "zu::query::column::MixedColumn",
      "signature": "usize"
    },
    {
      "id": "zu::query::column::Mixture",
      "kind": "struct",
      "name": "Mixture",
      "doc": "Two types found inside one value, which is a disagreement with no\nrow number on it yet. [`QueryResult::columnar`] adds the row and the\ncolumn and turns it into a [`MixedColumn`]."
    },
    {
      "id": "zu::query::column::Mixture::arrived",
      "kind": "field",
      "name": "arrived",
      "of": "zu::query::column::Mixture",
      "signature": "String"
    },
    {
      "id": "zu::query::column::Mixture::held",
      "kind": "field",
      "name": "held",
      "of": "zu::query::column::Mixture",
      "signature": "String"
    },
    {
      "id": "zu::query::column::Offsets",
      "kind": "enum",
      "name": "Offsets",
      "doc": "String offsets, narrow until they cannot be."
    },
    {
      "id": "zu::query::column::Offsets::I32",
      "kind": "variant",
      "name": "I32",
      "of": "zu::query::column::Offsets",
      "signature": "I32(Vec<i32>)"
    },
    {
      "id": "zu::query::column::Offsets::I64",
      "kind": "variant",
      "name": "I64",
      "of": "zu::query::column::Offsets",
      "signature": "I64(Vec<i64>)"
    },
    {
      "id": "zu::query::column::Offsets::is_empty",
      "kind": "method",
      "name": "is_empty",
      "of": "zu::query::column::Offsets",
      "signature": "fn is_empty(&self) -> bool",
      "doc": "Whether the column has no rows."
    },
    {
      "id": "zu::query::column::Offsets::len",
      "kind": "method",
      "name": "len",
      "of": "zu::query::column::Offsets",
      "signature": "fn len(&self) -> usize",
      "doc": "How many strings these offsets describe."
    },
    {
      "id": "zu::query::column::StrColumn",
      "kind": "struct",
      "name": "StrColumn",
      "doc": "A string column in the layout Arrow calls Utf8 and LargeUtf8: the\nbytes of every string end to end, and `len + 1` offsets into them,\nwhere row `i` spans `offsets[i]..offsets[i + 1]`.\n\nThe offsets start narrow and widen once, when the bytes pass what a\n32 bit offset addresses. Widening is a walk of the offsets and never\nof the bytes, so the column that needed it pays a pass over four\nbytes a row and the columns that did not pay nothing."
    },
    {
      "id": "zu::query::column::StrColumn::bytes",
      "kind": "field",
      "name": "bytes",
      "of": "zu::query::column::StrColumn",
      "signature": "Vec<u8>"
    },
    {
      "id": "zu::query::column::StrColumn::offsets",
      "kind": "field",
      "name": "offsets",
      "of": "zu::query::column::StrColumn",
      "signature": "Offsets"
    },
    {
      "id": "zu::query::column::StrColumn::span",
      "kind": "method",
      "name": "span",
      "of": "zu::query::column::StrColumn",
      "signature": "fn span(&self, at: usize) -> (usize, usize)",
      "doc": "Where row `at` starts and ends in the bytes."
    },
    {
      "id": "zu::query::column::Validity",
      "kind": "struct",
      "name": "Validity",
      "doc": "Validity, in the layout every columnar format keeps it in: one bit\nper row, least significant bit first, set meaning the row has a\nvalue.\n\nAbsent from a [`Column`] when nothing in it is null, which is the\ncommon case and the one where a reader gets to skip the AND\nentirely. Present means at least one null, so a client never has to\ncount to find out whether the buffer is worth attaching."
    },
    {
      "id": "zu::query::column::Validity::bits",
      "kind": "field",
      "name": "bits",
      "of": "zu::query::column::Validity",
      "signature": "Vec<u8>",
      "doc": "Packed bits, `len.div_ceil(8)` bytes of them."
    },
    {
      "id": "zu::query::column::Validity::is_valid",
      "kind": "method",
      "name": "is_valid",
      "of": "zu::query::column::Validity",
      "signature": "fn is_valid(&self, at: usize) -> bool",
      "doc": "Whether row `at` has a value."
    },
    {
      "id": "zu::query::column::Validity::len",
      "kind": "field",
      "name": "len",
      "of": "zu::query::column::Validity",
      "signature": "usize",
      "doc": "How many rows the bits cover."
    },
    {
      "id": "zu::query::column::Validity::nulls",
      "kind": "field",
      "name": "nulls",
      "of": "zu::query::column::Validity",
      "signature": "usize",
      "doc": "How many of them are null, which is the count a format's null\nbuffer carries beside the bits."
    },
    {
      "id": "zu::query::explain",
      "kind": "function",
      "name": "explain",
      "signature": "fn explain(source: &str, catalog: &zu::zu1::catalog::Catalog) -> zu_common::Result<String>",
      "doc": "Parses, binds, plans, and optimizes one query, returning the\nEXPLAIN listing of the plan that would execute."
    },
    {
      "id": "zu::query::explain_analyze",
      "kind": "function",
      "name": "explain_analyze",
      "signature": "fn explain_analyze(source: &str, db: &mut zu::zu1::file::Zu1File, params: &[(&str, Value)]) -> zu_common::Result<String>",
      "doc": "Executes one query with per-operator counters and returns the\nrendered EXPLAIN ANALYZE listing: pulls, rows, the estimate and its\nq-error, the average vector length, and self time per operator, per\nstage. The grammar has no EXPLAIN keyword yet, so this is the API\nentry point."
    },
    {
      "id": "zu::query::merge_frames",
      "kind": "function",
      "name": "merge_frames",
      "signature": "fn merge_frames(schema: &mut zu_query::binder::Schema, frames: &zu_query::frame::FrameSet) -> zu_common::Result<Vec<u16>>",
      "doc": "Merges the registered frames into a schema as node tables of it,\nanswering the label each one's name took.\n\nA frame is a table to everything above the binder and nothing at all\nto the catalog, so this runs after the schema is built rather than\ninside the build: a schema loaded with statistics keeps them, and a\nregistration costs the merge and not a reload."
    },
    {
      "id": "zu::query::profile",
      "kind": "function",
      "name": "profile",
      "signature": "fn profile(source: &str, db: &mut zu::zu1::file::Zu1File, params: &[(&str, Value)]) -> zu_common::Result<exec::Profile>",
      "doc": "The same profiled run handing back the counters instead of the\nrendering. The cardinality phase of the LDBC bench reads q-error\noff this (perf/12 §4)."
    },
    {
      "id": "zu::query::run",
      "kind": "function",
      "name": "run",
      "signature": "fn run(source: &str, db: &mut zu::zu1::file::Zu1File, params: &[(&str, Value)]) -> zu_common::Result<QueryResult>",
      "doc": "Parses, plans, optimizes, and executes one query against an open\nzu1 file, returning the result rows. Scan-driven stages run on the\nmorsel scheduler with `min(cores, 8)` workers; `ZU_THREADS` in the\nenvironment overrides the count, `ZU_THREADS=1` forces sequential\nexecution."
    },
    {
      "id": "zu::query::run_with",
      "kind": "function",
      "name": "run_with",
      "signature": "fn run_with(source: &str, db: &mut zu::zu1::file::Zu1File, params: &[(&str, Value)], options: &exec::Options) -> zu_common::Result<QueryResult>",
      "doc": "The same read under switches the caller chose rather than the ones\nthe environment names.\n\n[`run`] is this with [`env_options`], and everything true of one is\ntrue of the other. What this adds is a statement that can ask for\nsomething the environment cannot say per statement: the row\nexecutor as a differential oracle, a thread count for one query, a\npinned clock. A test binary runs its tests in parallel threads and\nan environment variable belongs to the process, so a test that set\none was setting it for every test running beside it (#513).\n\nA session takes its switches once, at open, and\n[`crate::session::Session::set_options`] replaces them. This is the\nsame thing for the entry point that has no session behind it."
    },
    {
      "id": "zu::query::schema_of",
      "kind": "function",
      "name": "schema_of",
      "signature": "fn schema_of(catalog: &zu::zu1::catalog::Catalog, epoch: u64) -> zu_common::Result<zu_query::binder::Schema>",
      "doc": "Builds the binder schema for the home graph, which is the graph a\nstatement is against when nothing said otherwise."
    },
    {
      "id": "zu::query::schema_of_graph",
      "kind": "function",
      "name": "schema_of_graph",
      "signature": "fn schema_of_graph(catalog: &zu::zu1::catalog::Catalog, graph: u32, epoch: u64) -> zu_common::Result<zu_query::binder::Schema>",
      "doc": "Builds the binder schema from one graph of a zu1 catalog. A query\nsees the tables of the graph it runs against and no others, which\nis what lets two graphs in one file both hold a `person`.\n\nThe epoch is the one the catalog was read at, and it is here for\nthe graph references a statement may write (GE01): a reference\nrecords when it was taken, and the answer to `CURRENT_PROPERTY_GRAPH`\nwas taken when the schema was built. A schema outlives no epoch,\nsince the session throws every one of them away when the epoch\nmoves, so the stamp stays true for as long as the schema is around."
    },
    {
      "id": "zu::session",
      "kind": "module",
      "name": "session",
      "doc": "A persistent query session over one zu1 file: the catalog, stats,\nand schema stay resident, compiled plans are cached by source text,\nand the graph readers keep their decoded groups warm across\nqueries. The one-shot entry points in [`crate::query`] pay the full\nopen-load-parse-plan cost on every call, which is fine for a CLI\ninvocation and hopeless for a server loop; a session pays it once\nand then a warm query is a hash lookup, parameter binding, and\nexecution.\n\nStaleness is a single u64 compare: every entry point checks the\nhandle's header epoch and reloads the catalog, stats, and plan\ncache when it moved. Reads through one session are serial (the\nunderlying handle seeks), so a session is Send but queries on it\ndo not overlap; open one session per thread."
    },
    {
      "id": "zu::session::DEFAULT_STALE_BOUND",
      "kind": "constant",
      "name": "DEFAULT_STALE_BOUND",
      "signature": "u64",
      "doc": "How many epochs a held binding table may fall behind before the\nsession mentions it.\n\nAny number here is a judgement rather than a fact, and this one is\nthe judgement that a session which has fallen sixteen epochs behind\nis holding rows from before a fair amount of churn and would like to\nhear about it, where one or two epochs behind is what an ordinary\nbusy store does under a session that is doing nothing wrong."
    },
    {
      "id": "zu::session::Pin",
      "kind": "struct",
      "name": "Pin",
      "doc": "What one held reference parameter is pinned to (plan/06 §2).\n\nWorth being clear about what a pin is here and what it is not. It is\nan epoch number written on a handle, and it holds no blocks: a lease\nlives for a statement, and a binding table parameter holds rows that\nwere copied out of the snapshot rather than a reader into it. So a\nsession sitting on an old parameter does not stop a checkpoint from\nreusing anything and does not grow the store. What goes stale is the\nmeaning of the reference, not the space behind it, and that is what\nthis reports."
    },
    {
      "id": "zu::session::Pin::epoch",
      "kind": "field",
      "name": "epoch",
      "of": "zu::session::Pin",
      "signature": "u64",
      "doc": "The epoch the reference was taken at."
    },
    {
      "id": "zu::session::Pin::kind",
      "kind": "field",
      "name": "kind",
      "of": "zu::session::Pin",
      "signature": "&str",
      "doc": "`GRAPH` or `BINDING TABLE`, the word it was set with."
    },
    {
      "id": "zu::session::Pin::name",
      "kind": "field",
      "name": "name",
      "of": "zu::session::Pin",
      "signature": "String",
      "doc": "The name the session holds it under, without the dollar."
    },
    {
      "id": "zu::session::Pin::stale",
      "kind": "field",
      "name": "stale",
      "of": "zu::session::Pin",
      "signature": "Stale",
      "doc": "Whether it still means what it meant."
    },
    {
      "id": "zu::session::Pin::what",
      "kind": "field",
      "name": "what",
      "of": "zu::session::Pin",
      "signature": "String",
      "doc": "What it names, as a line a person reads."
    },
    {
      "id": "zu::session::Session",
      "kind": "struct",
      "name": "Session"
    },
    {
      "id": "zu::session::Session::attached",
      "kind": "method",
      "name": "attached",
      "of": "zu::session::Session",
      "signature": "fn attached(handle: Arc<FileHandle>) -> Result<Session>",
      "doc": "A session on a file this process already holds the write side\nof: its own descriptor for reading, forked off that side so the\nblock cache and the decoded pools are shared, and the roots the\nside has published."
    },
    {
      "id": "zu::session::Session::binding_table",
      "kind": "method",
      "name": "binding_table",
      "of": "zu::session::Session",
      "signature": "fn binding_table(&self, result: QueryResult) -> Value",
      "doc": "A binding table reference over the rows of a result (GV61).\n\nThe rows are taken as they are: a binding table value is a\nresult that has already been read, held behind a handle so that\nhanding it to the next statement costs a pointer. The epoch it\nwas read at rides along, because that is what makes a table\nholding element references answerable later."
    },
    {
      "id": "zu::session::Session::catalog",
      "kind": "method",
      "name": "catalog",
      "of": "zu::session::Session",
      "signature": "fn catalog(&self) -> &Catalog",
      "doc": "The catalog this session last loaded, which is how a caller\nstaging a write turns a label into the table id the transaction\nwants."
    },
    {
      "id": "zu::session::Session::close_stmt",
      "kind": "method",
      "name": "close_stmt",
      "of": "zu::session::Session",
      "signature": "fn close_stmt(&mut self, stmt: u64) -> bool"
    },
    {
      "id": "zu::session::Session::epoch",
      "kind": "method",
      "name": "epoch",
      "of": "zu::session::Session",
      "signature": "fn epoch(&self) -> u64",
      "doc": "The epoch this session last reloaded at."
    },
    {
      "id": "zu::session::Session::execute",
      "kind": "method",
      "name": "execute",
      "of": "zu::session::Session",
      "signature": "fn execute(&mut self, stmt: u64, params: &[(&str, Value)]) -> Result<QueryResult>"
    },
    {
      "id": "zu::session::Session::explain",
      "kind": "method",
      "name": "explain",
      "of": "zu::session::Session",
      "signature": "fn explain(&mut self, source: &str) -> Result<String>",
      "doc": "The plan this session would run for `source`, rendered, without\nrunning it.\n\n[`Self::explain_analyze`] is the same listing with the observed\ncounters in it, and it costs a full execution to produce. A\ncaller that wants to record how a statement ran beside a latency\nit measured somewhere else cannot afford that execution: for a\nread it would double the work being timed, and for a write it\nwould apply the write a second time. This one compiles and\nrenders, which on a warm session is a hash lookup and a string.\n\nIt takes no parameters because zu plans on the statement text\nand the schema alone. Accepting them would say the plan depends\non their values, and then a caller would have some reason to\npass the ones it is about to bind."
    },
    {
      "id": "zu::session::Session::explain_analyze",
      "kind": "method",
      "name": "explain_analyze",
      "of": "zu::session::Session",
      "signature": "fn explain_analyze(&mut self, source: &str, params: &[(&str, Value)]) -> Result<String>",
      "doc": "EXPLAIN ANALYZE through the session: same cache, same options,\nprofiled execution."
    },
    {
      "id": "zu::session::Session::explain_plan",
      "kind": "method",
      "name": "explain_plan",
      "of": "zu::session::Session",
      "signature": "fn explain_plan(&mut self, source: &str) -> Result<QueryPlan>",
      "doc": "The same plan as the operators it is made of, for a caller that\nwants to read it rather than print it.\n\nA plan viewer, a test asserting that a statement reaches an\nindex, a tool colouring the expands red: each of those wants the\ntree, and each of them parsing the listing back into one would\nbe a parser of a format nothing promised to keep. The listing is\n[`zu_query::plan::QueryPlan::render`] of this, so what a caller\nreads and what a caller prints cannot disagree."
    },
    {
      "id": "zu::session::Session::file_mut",
      "kind": "method",
      "name": "file_mut",
      "of": "zu::session::Session",
      "signature": "fn file_mut(&mut self) -> Result<&mut Zu1File>",
      "doc": "The file this session reads through.\n\nThe write paths that are not statements need it: the appender\nof [`crate::append`] seals its buffer straight into this handle,\nand it has to be this one rather than a second open of the same\npath, so that the epoch the write publishes is the epoch\n[`Self::refresh`] reads on the next statement.\n\nHanding out the file hands out the log with it. An appender\nopens the same sidecar this session's writer commits to and can\ntruncate it, so the writer goes first and the next write opens\na fresh one. That costs a recovery pass and loses nothing: a\nwriter that has folded holds an empty overlay store, so what it\nwas holding is what recovery reconstructs.\n\nThe publish before that is what makes the truncation safe. A\ncommit folds without checkpointing and leaves the frames in the\nlog, so a log cut with the header still behind them would lose\ncommitted statements. Publishing first puts the folds on the\nfile and empties the log itself, and the appender then opens a\nlog that says nothing anyone still needs.\n\nWhat goes out is the shared write side, so this session holds\nthe writer lock from here until its next statement or its drop.\nNothing else could be true: the file itself is on loan and the\ncaller says when it is done with it by stopping using it, which\nis not something another connection can wait on."
    },
    {
      "id": "zu::session::Session::fold_count",
      "kind": "method",
      "name": "fold_count",
      "of": "zu::session::Session",
      "signature": "fn fold_count(&mut self) -> Result<u64>",
      "doc": "How many folds the writer behind this session has run since it\nwas opened.\n\nUnlike [`Self::file_mut`] this disturbs nothing. It takes the\nwrite side, reads a counter off it and gives it straight back,\nso a caller can ask between statements without the asking\nchanging what the next statement does. It is here for the write\nbench, which has to know whether the window it just measured\nheld the folds it was sized for."
    },
    {
      "id": "zu::session::Session::graph_ref",
      "kind": "method",
      "name": "graph_ref",
      "of": "zu::session::Session",
      "signature": "fn graph_ref(&mut self, schema: &str, name: &str) -> Result<Value>",
      "doc": "A graph reference value naming one graph in the catalog (GV60),\nor `42002` when the catalog has no such graph.\n\nThis is where a graph reference comes from. GQL has no literal\nthat writes one and there is no expression yet that returns one\neither, so the engine hands them out: a caller resolves a name\nonce, holds the handle, and passes it to as many statements as\nit likes."
    },
    {
      "id": "zu::session::Session::handle",
      "kind": "method",
      "name": "handle",
      "of": "zu::session::Session",
      "signature": "fn handle(&self) -> &Arc<FileHandle>",
      "doc": "The file this session reads through, shared with every other\nsession this process has on the same file.\n\nIt is handed out so that a caller holding one session can open\nanother beside it without going back to the path:\n[`Session::attached`] takes exactly this and forks a descriptor\noff it. What the two share is the write side and the caches\nunder it; plans, readers and the interrupt are per session."
    },
    {
      "id": "zu::session::Session::home_graph_ref",
      "kind": "method",
      "name": "home_graph_ref",
      "of": "zu::session::Session",
      "signature": "fn home_graph_ref(&mut self) -> Result<Value>",
      "doc": "A graph reference on the graph this session started in, which\nis what `HOME_PROPERTY_GRAPH` hands back. It is the working\ngraph until something moves the working graph, and the two are\nseparate calls for the same reason the two words are separate\nwords: a caller that wants to go back has to be able to say so."
    },
    {
      "id": "zu::session::Session::idle",
      "kind": "method",
      "name": "idle",
      "of": "zu::session::Session",
      "signature": "fn idle(&mut self)",
      "doc": "Lets go of the epoch the statement that just ended was reading.\n\nA connection between statements holds no lease, so a writer is\nfree to reuse everything it has freed. Not calling it is a\ncorrectness-free mistake: the next statement's lease replaces\nthis one, and until then the file grows instead of reusing."
    },
    {
      "id": "zu::session::Session::in_transaction",
      "kind": "method",
      "name": "in_transaction",
      "of": "zu::session::Session",
      "signature": "fn in_transaction(&self) -> bool",
      "doc": "Whether an explicit transaction is running on this session."
    },
    {
      "id": "zu::session::Session::interrupt",
      "kind": "method",
      "name": "interrupt",
      "of": "zu::session::Session",
      "signature": "fn interrupt(&self) -> Interrupt",
      "doc": "The handle a statement on this session can be stopped through.\n\nOne handle for the life of the session, so a caller can take it\nonce and keep it. It is raised from another thread while a\nstatement runs, which is the only time it means anything: the\nstatement stops at the next boundary the executor checks and\nanswers [`ZuError::Interrupted`], and this session is exactly as\nit was, plans and readers included. Clear it before a statement\nrather than after, so a stop that arrived while nothing was\nrunning cannot end the next one."
    },
    {
      "id": "zu::session::Session::is_closed",
      "kind": "method",
      "name": "is_closed",
      "of": "zu::session::Session",
      "signature": "fn is_closed(&self) -> bool",
      "doc": "Whether a `SESSION CLOSE` has ended this session (ISO 7.3), so\nthat a caller holding one can tell without sending a statement\nto find out."
    },
    {
      "id": "zu::session::Session::memory",
      "kind": "method",
      "name": "memory",
      "of": "zu::session::Session",
      "signature": "fn memory() -> Result<Session>",
      "doc": "A session on a database that never touches the filesystem: the\nblocks a file would hold, and the log beside it, held in memory\nand gone when this session is. It is what the shell opens when\nnobody named a file, and what a caller wanting a graph rather\nthan a directory reaches for."
    },
    {
      "id": "zu::session::Session::on",
      "kind": "method",
      "name": "on",
      "of": "zu::session::Session",
      "signature": "fn on(db: Zu1File) -> Result<Session>",
      "doc": "A session over a file handle the caller opened, which is how\n[`crate::db::Database`] applies a read-only or memory-limited\nopen without this module growing a constructor per option.\n\nThe handle is what the file is registered under if this process\nhas not opened it yet, and dropped if it has: one file is one\nwrite side, and the one already registered is the one every\nother connection is reading through."
    },
    {
      "id": "zu::session::Session::open",
      "kind": "method",
      "name": "open",
      "of": "zu::session::Session",
      "signature": "fn open(path: &Path) -> Result<Session>"
    },
    {
      "id": "zu::session::Session::opening",
      "kind": "method",
      "name": "opening",
      "of": "zu::session::Session",
      "signature": "fn opening(path: &Path, read_only: bool, open: impl FnOnce) -> Result<Session>",
      "doc": "A session on the file `open` opens, opened through the registry\nrather than before it.\n\n[`Self::on`] is for a file the caller already has in hand, and a\nfile already open is a file that was opened at some moment\nnobody here chose: if that moment was inside another handle's\nclose, what it read is the header from before that close folded.\nThis is the same constructor for a caller that can hand over the\nopen itself, which is every caller with a path."
    },
    {
      "id": "zu::session::Session::options",
      "kind": "method",
      "name": "options",
      "of": "zu::session::Session",
      "signature": "fn options(&self) -> &exec::Options",
      "doc": "The execution switches this session runs statements under."
    },
    {
      "id": "zu::session::Session::pinned_epoch",
      "kind": "method",
      "name": "pinned_epoch",
      "of": "zu::session::Session",
      "signature": "fn pinned_epoch(&self) -> Option<u64>",
      "doc": "The oldest epoch any held parameter is pinned to, which is the\none number a caller watching a long-lived session wants."
    },
    {
      "id": "zu::session::Session::pins",
      "kind": "method",
      "name": "pins",
      "of": "zu::session::Session",
      "signature": "fn pins(&self) -> Vec<Pin>",
      "doc": "What the reference parameters this session holds are pinned to,\nin name order, one entry per parameter holding a graph or a\nbinding table (GS01, GS02, GS10 and GS13).\n\nA value parameter is not here because it pins nothing: a number\nis a number at every epoch. The two reference kinds each carry\nthe epoch they were taken at, and that is the pin."
    },
    {
      "id": "zu::session::Session::prepare",
      "kind": "method",
      "name": "prepare",
      "of": "zu::session::Session",
      "signature": "fn prepare(&mut self, source: &str) -> Result<(u64, Vec<String>)>",
      "doc": "Compiles a statement and pins it under an id. The id maps back\nto the source text, so a catalog change between prepare and\nexecute recompiles instead of running a stale plan. Returns the\nid and the parameter names the statement wants, in binder\norder."
    },
    {
      "id": "zu::session::Session::profile",
      "kind": "method",
      "name": "profile",
      "of": "zu::session::Session",
      "signature": "fn profile(&mut self, source: &str, params: &[(&str, Value)]) -> Result<exec::Profile>",
      "doc": "The same run, handing back the counters instead of the\nrendering, for callers that want the numbers. `zu bench\ncardinality` reads q-error off this."
    },
    {
      "id": "zu::session::Session::register_frame",
      "kind": "method",
      "name": "register_frame",
      "of": "zu::session::Session",
      "signature": "fn register_frame(&mut self, frame: Frame) -> Result<()>",
      "doc": "Registers a frame as a table of the working graph, under the\nname it carries.\n\nNothing is copied. The frame says where the caller's columns are\nand holds whatever keeps them alive, and a statement that reads\nit reads those bytes where they lie. Registering a name that is\nalready registered replaces what it stands for and keeps the\ntable id, so a prepared statement over it stays bound to the same\ntable. A name a stored table already holds is refused."
    },
    {
      "id": "zu::session::Session::registered_frames",
      "kind": "method",
      "name": "registered_frames",
      "of": "zu::session::Session",
      "signature": "fn registered_frames(&self) -> Vec<String>",
      "doc": "The registered names, sorted."
    },
    {
      "id": "zu::session::Session::run",
      "kind": "method",
      "name": "run",
      "of": "zu::session::Session",
      "signature": "fn run(&mut self, source: &str, params: &[(&str, Value)]) -> Result<QueryResult>",
      "doc": "Runs one query, compiling it on the first sighting of this text\nand reusing the cached plan afterwards."
    },
    {
      "id": "zu::session::Session::run_streaming",
      "kind": "method",
      "name": "run_streaming",
      "of": "zu::session::Session",
      "signature": "fn run_streaming(&mut self, source: &str, params: &[(&str, Value)], batch_rows: usize, sink: &mut dyn FnMut) -> Result<Streamed>",
      "doc": "Runs one query and hands its rows to `sink` in batches as they\nare made, instead of returning them.\n\nA statement that has to see every row before it can give one,\nwhich is ORDER BY, DISTINCT and the aggregates, runs whole and is\nhanded over in batches afterwards, and so does a statement that\nwrites, because a write runs as the parts it was split into and\nthe rows come out of the last of them. Which happened is in\n[`Streamed::streamed`], and the caller's loop is the same either\nway."
    },
    {
      "id": "zu::session::Session::session_params",
      "kind": "method",
      "name": "session_params",
      "of": "zu::session::Session",
      "signature": "fn session_params(&self) -> Vec<(&str, &str, &Value)>",
      "doc": "The parameters this session is holding, each as its name, the\nword it was set with, and the value it holds, in name order\n(GS01 through GS03).\n\nA statement cannot ask this: `$p` answers what `$p` holds, and\nthere is no expression that answers what a session holds under\nevery name. So a shell that prints them and a test that says\nwhat a reset took away both come here."
    },
    {
      "id": "zu::session::Session::session_schema",
      "kind": "method",
      "name": "session_schema",
      "of": "zu::session::Session",
      "signature": "fn session_schema(&self) -> &str",
      "doc": "The catalog schema this session works in (GS05), which is the\nroot until a `SESSION SET SCHEMA` moves it."
    },
    {
      "id": "zu::session::Session::set_options",
      "kind": "method",
      "name": "set_options",
      "of": "zu::session::Session",
      "signature": "fn set_options(&mut self, options: exec::Options)",
      "doc": "Replaces them. [`crate::db::Config`] is the supported way to do\nthis; the setter is here because the switches belong to the\nsession and a caller holding one directly should not have to\nreach through the environment to change them.\n\nThe interrupt handle survives, whatever the new switches carry.\nIt belongs to whoever opened the session rather than to the\nswitches it rides in, and a caller changing the thread count\nwould otherwise silently take away the only way to stop a\nstatement.\n\nSo does the time zone, for the same reason and a stronger one:\nit is a session characteristic a statement set (GS15), not a\nswitch a caller configured, and a thread count arriving from\n[`crate::db::Config`] has nothing to say about what time it is.\n\nAnd so does the status of the last statement, for that same\nreason. It is what the statement before this one ended with, so\nchanging a switch between two statements must not be able to\nanswer the second one with a blank where the first one's\ncondition was."
    },
    {
      "id": "zu::session::Session::set_stale_bound",
      "kind": "method",
      "name": "set_stale_bound",
      "of": "zu::session::Session",
      "signature": "fn set_stale_bound(&mut self, epochs: u64)",
      "doc": "How many epochs a held binding table may fall behind before the\nsession says so on the statement after it. The default is\n[`DEFAULT_STALE_BOUND`]."
    },
    {
      "id": "zu::session::Session::time_zone",
      "kind": "method",
      "name": "time_zone",
      "of": "zu::session::Session",
      "signature": "fn time_zone(&self) -> i16",
      "doc": "The session time zone as minutes east of UTC (GS07 and GS15),\nnought being the UTC a session opens in."
    },
    {
      "id": "zu::session::Session::unregister_frame",
      "kind": "method",
      "name": "unregister_frame",
      "of": "zu::session::Session",
      "signature": "fn unregister_frame(&mut self, name: &str) -> Result<bool>",
      "doc": "Drops a registered frame, answering whether there was one under\nthat name. The bytes go when the caller's handle on them does,\nwhich is not necessarily now: a statement still reading the frame\nholds it until it ends."
    },
    {
      "id": "zu::session::Session::warm",
      "kind": "method",
      "name": "warm",
      "of": "zu::session::Session",
      "signature": "fn warm(&mut self, source: &str) -> Result<bool>",
      "doc": "Makes sure a statement is compiled and cached, and reports\nwhether it already was. A server preloads its statement set\nwith this at startup; the P0 plan-hit gate times the `true`\npath, which is exactly the work between receiving a known\nquery text and starting execution: the epoch check and the\ncache lookup."
    },
    {
      "id": "zu::session::Session::working_graph",
      "kind": "method",
      "name": "working_graph",
      "of": "zu::session::Session",
      "signature": "fn working_graph(&self) -> u32",
      "doc": "The id of the graph a statement on this session runs against\nwhen it carries no `USE` (GS06)."
    },
    {
      "id": "zu::session::Session::working_graph_ref",
      "kind": "method",
      "name": "working_graph_ref",
      "of": "zu::session::Session",
      "signature": "fn working_graph_ref(&mut self) -> Result<Value>",
      "doc": "A graph reference on the graph this session is working in,\nwhich is what `CURRENT_PROPERTY_GRAPH` will hand back once a\ngraph expression can be written (GE01, G6)."
    },
    {
      "id": "zu::session::Session::write",
      "kind": "method",
      "name": "write",
      "of": "zu::session::Session",
      "signature": "fn write<T>(&mut self, stage: impl FnOnce) -> Result<T>",
      "doc": "Runs one write transaction: `stage` describes the change\nagainst an open transaction, and what it returns comes back\nonce the change is committed and visible.\n\nVisible is the part that costs something. The write commits,\nfolds, and the session reloads, so by the time this returns the\nnext statement reads a catalog and a snapshot that both know\nabout the change. A closure that raises commits nothing and\npublishes no epoch, which is the rollback an implicit\ntransaction owes.\n\nThe frames go to the log here and the wait for the platter does\nnot: it happens in [`Self::durable`], after the write side has\ngone back, so the writer queued behind this one stages into the\nsame sync rather than after it. What that costs is that the\nstate has to be held back too, because a reader shown a commit\na crash could take away is the one thing the deferral must not\nbuy."
    },
    {
      "id": "zu::session::Stale",
      "kind": "enum",
      "name": "Stale",
      "doc": "What has happened to what a held reference parameter names, which is\nwhat decides whether the session says anything about it."
    },
    {
      "id": "zu::session::Stale::Fresh",
      "kind": "variant",
      "name": "Fresh",
      "of": "zu::session::Stale",
      "signature": "Fresh",
      "doc": "The reference still means what it meant."
    },
    {
      "id": "zu::session::Stale::Gone",
      "kind": "variant",
      "name": "Gone",
      "of": "zu::session::Stale",
      "signature": "Gone",
      "doc": "A statement reading it will be refused: the graph is dropped, or\nthe rows name elements of a snapshot that has moved."
    },
    {
      "id": "zu::session::Stale::Old",
      "kind": "variant",
      "name": "Old",
      "of": "zu::session::Stale",
      "signature": "Old",
      "doc": "Behind the bound but still usable: the rows hold no element\nreferences, so what they say is as true now as it was."
    },
    {
      "id": "zu::shared",
      "kind": "module",
      "name": "shared",
      "doc": "The write side of one file, shared by every connection in this\nprocess that has that file open.\n\ndocs/08 §1 is one line: single writer, many snapshot readers. Until\nnow the single was per connection rather than per file. A\n[`Writer`] lived inside the session that opened it, so two\nconnections on one path were two writers, two logs open on one\nsidecar and two handles each flipping the header from its own idea\nof where the roots are, and nothing anywhere said no. It worked\nbecause callers only ever wrote through one of them.\n\nThis is the thing that says no. One [`FileHandle`] per path per\nprocess holds the write side: the file handle that allocates and\nthe writer that logs. A session takes it to write and gives it back\nwhen the statement ends, so writers queue instead of overlapping,\nwhich is the writer lock docs/08 asks for and what a group commit\nhas to have before it can batch anything.\n\nReading stays parallel, which is the point of doing it this way\nround. A session reads through a handle of its own forked from the\nwrite side, so it shares the block cache and the decoded pools with\nevery other connection on the file, and it never takes the lock to\nread. What it takes instead is the header: the write side publishes\nits roots after every commit, and a session picks them up at the\nstart of a statement, which is where a snapshot begins. That is\nalso what makes a commit by one connection visible to the next\nstatement on another, which before this needed a reconnect."
    },
    {
      "id": "zu::shared::FileHandle",
      "kind": "struct",
      "name": "FileHandle",
      "doc": "One process-wide write side per open file.\n\nA session takes the side to write and gives it back afterwards, so\nat most one connection is writing a file at a time and the rest\nqueue in the order they asked. Everything else about a connection,\nthe caches, the plans, the reading handle, stays where it was."
    },
    {
      "id": "zu::shared::FileHandle::attach",
      "kind": "method",
      "name": "attach",
      "of": "zu::shared::FileHandle",
      "signature": "fn attach(path: &Path, read_only: bool, open: impl FnOnce) -> Result<Arc<FileHandle>>",
      "doc": "The handle for the file `open` opens, opening it only if this\nprocess does not already hold one.\n\nThe closure is what keeps the common case honest: a second\nconnection to a file that is already open costs a lock and a\nmap lookup rather than an open, a header read and a free list\nwalk. It is also what makes the open safe: it runs with the\nregistry held, so it cannot read the header of a file another\nhandle is folding on its way out. A caller that opens the file\nitself and hands it over has no such promise, which is why\n[`Self::attach_to`] is for the files that have no other\nconnection to race with."
    },
    {
      "id": "zu::shared::FileHandle::attach_to",
      "kind": "method",
      "name": "attach_to",
      "of": "zu::shared::FileHandle",
      "signature": "fn attach_to(file: Zu1File) -> Result<Arc<FileHandle>>",
      "doc": "The handle for a file the caller has already opened.\n\nThe file is dropped when this process holds a handle on it\nalready, because the one already registered is the one every\nother connection is reading through and a second would be a\nsecond writer of the same log."
    },
    {
      "id": "zu::shared::FileHandle::observe",
      "kind": "method",
      "name": "observe",
      "of": "zu::shared::FileHandle",
      "signature": "fn observe(self: &Arc<Self>) -> (Published, Lease)",
      "doc": "The published state, and a lease on the epoch it is at.\n\nThe two come together under one lock, and [`Self::reclaim`]\ntakes the same one, which is the whole of why a reader never\nreads a block that has been handed back. Either a statement is\ncounted before the reclaim runs, and the reclaim leaves its\nepoch alone, or it arrives after, and what it is handed is the\nepoch the reclaim published rather than the one it superseded."
    },
    {
      "id": "zu::shared::FileHandle::publish",
      "kind": "method",
      "name": "publish",
      "of": "zu::shared::FileHandle",
      "signature": "fn publish(&self, side: &WriteSide)",
      "doc": "Publishes where the write side has got to, for the readers to\npick up at the start of their next statement."
    },
    {
      "id": "zu::shared::FileHandle::publish_staged",
      "kind": "method",
      "name": "publish_staged",
      "of": "zu::shared::FileHandle",
      "signature": "fn publish_staged(&self, next: Published)",
      "doc": "Installs a state whose log is now durable.\n\nA writer behind this one may have staged, synced and published\nwhile this one was waiting, and then this state is stale: what\nit describes the newer one describes too, because they are\ncumulative. So the older one is dropped rather than put in,\nwhich would take the readers backwards."
    },
    {
      "id": "zu::shared::FileHandle::published",
      "kind": "method",
      "name": "published",
      "of": "zu::shared::FileHandle",
      "signature": "fn published(&self) -> Published",
      "doc": "What the write side last published."
    },
    {
      "id": "zu::shared::FileHandle::put",
      "kind": "method",
      "name": "put",
      "of": "zu::shared::FileHandle",
      "signature": "fn put(&self, side: WriteSide)",
      "doc": "Gives the write side back, to the writer at the front of the line\nif anyone is waiting."
    },
    {
      "id": "zu::shared::FileHandle::reader",
      "kind": "method",
      "name": "reader",
      "of": "zu::shared::FileHandle",
      "signature": "fn reader(&self) -> Result<Zu1File>",
      "doc": "A reading handle on this file: its own descriptor, the roots the\nwrite side is at, and the block cache and decoded pools every\nother handle on the file is sharing."
    },
    {
      "id": "zu::shared::FileHandle::reclaim",
      "kind": "method",
      "name": "reclaim",
      "of": "zu::shared::FileHandle",
      "signature": "fn reclaim(&self, side: &mut WriteSide)",
      "doc": "Gives back for allocation the blocks the last checkpoint freed\nthat nothing is reading any more.\n\nCalled by the writer after it has published, so a statement\nstarting from here on reads the epoch that owns those blocks\nrather than the one that let go of them."
    },
    {
      "id": "zu::shared::FileHandle::stage",
      "kind": "method",
      "name": "stage",
      "of": "zu::shared::FileHandle",
      "signature": "fn stage(&self, side: &WriteSide) -> Published",
      "doc": "What [`Self::publish`] would install, without installing it,\nstamped with where it falls in the order writers staged.\n\nThis is the first half of a group commit. The state is taken\nwhile the write side is still held, which is the only moment it\ncan be read consistently, and put in with [`Self::publish_staged`]\nonce the log behind it is on the platter. Between the two the\nwrite side is back with the next writer, which is the whole\npoint: their frames are staged while this one's sync is in the\nair, so one sync covers them both."
    },
    {
      "id": "zu::shared::FileHandle::take",
      "kind": "method",
      "name": "take",
      "of": "zu::shared::FileHandle",
      "signature": "fn take(&self) -> WriteSide",
      "doc": "Takes the write side, waiting for whoever has it. The caller\ngives it back with [`Self::put`], and a caller that does not is\na caller nobody else on this file can write past, so the two go\nin the same scope."
    },
    {
      "id": "zu::shared::Lease",
      "kind": "struct",
      "name": "Lease",
      "doc": "A statement's claim on the epoch it is reading.\n\nA checkpoint lists as free the blocks the epoch it supersedes\nreaches, and would hand them straight back to allocation. That is\nright with one connection, which cannot be reading and writing at\nonce, and wrong with several: a statement on another connection is\nreading those blocks, and the next write puts a column of somebody\nelse's rows in the middle of them. The lease says which epoch a\nstatement is on, so the write side knows what it may not reuse yet.\n\nIt lives for the statement rather than for the connection, because\na connection sitting idle on an old epoch would otherwise hold\nevery block the writer frees after it and grow the file by the\nchurn of everyone else."
    },
    {
      "id": "zu::shared::Published",
      "kind": "struct",
      "name": "Published",
      "doc": "What the write side has published for the readers to pick up.\n\nThe header is where the roots are, and a reader that takes it reads\nthe database the writer has reached, staged folds included: data\nblocks are on the file as they are written, so the header is the\nonly thing a reader is missing. The patches are the commits that\nhave not been folded at all, which live in memory and are read\nthrough rather than read."
    },
    {
      "id": "zu::shared::Published::header",
      "kind": "method",
      "name": "header",
      "of": "zu::shared::Published",
      "signature": "fn header(&self) -> &DatabaseHeader"
    },
    {
      "id": "zu::shared::Published::newer_than",
      "kind": "method",
      "name": "newer_than",
      "of": "zu::shared::Published",
      "signature": "fn newer_than(&self, seen: u64) -> bool",
      "doc": "The state a session should be reading, or `None` if it already\nhas this one."
    },
    {
      "id": "zu::shared::Published::patches",
      "kind": "method",
      "name": "patches",
      "of": "zu::shared::Published",
      "signature": "fn patches(&self) -> &Arc<Patches>"
    },
    {
      "id": "zu::shared::Published::slot",
      "kind": "method",
      "name": "slot",
      "of": "zu::shared::Published",
      "signature": "fn slot(&self) -> usize"
    },
    {
      "id": "zu::shared::Published::version",
      "kind": "method",
      "name": "version",
      "of": "zu::shared::Published",
      "signature": "fn version(&self) -> u64"
    },
    {
      "id": "zu::shared::WriteSide",
      "kind": "struct",
      "name": "WriteSide",
      "doc": "The file handle that allocates and the writer that logs, together,\nbecause neither is right without the other: a fold moves roots on\none and empties the overlay of the other."
    },
    {
      "id": "zu::shared::WriteSide::commits",
      "kind": "method",
      "name": "commits",
      "of": "zu::shared::WriteSide",
      "signature": "fn commits(&self) -> Option<Arc<Commits>>",
      "doc": "What makes this side's commits durable, held past the side\nitself going back so the wait happens off the queue. `None`\nbefore a writer has been opened, which is a side with nothing to\nmake durable."
    },
    {
      "id": "zu::shared::WriteSide::discard_above",
      "kind": "method",
      "name": "discard_above",
      "of": "zu::shared::WriteSide",
      "signature": "fn discard_above(&mut self, floor: u64) -> Result<()>",
      "doc": "Takes the log back to `floor` and drops the writer, which is\nwhat a rollback owes: the frames above the floor are the ones\nbeing taken away, and a fresh writer would replay them back on\ntop of the roots going in."
    },
    {
      "id": "zu::shared::WriteSide::epoch",
      "kind": "method",
      "name": "epoch",
      "of": "zu::shared::WriteSide",
      "signature": "fn epoch(&self) -> u64",
      "doc": "The epoch the writer has committed through, or the file's own\nwhen there is no writer yet."
    },
    {
      "id": "zu::shared::WriteSide::file",
      "kind": "method",
      "name": "file",
      "of": "zu::shared::WriteSide",
      "signature": "fn file(&self) -> &Zu1File",
      "doc": "The file the writer allocates and folds into."
    },
    {
      "id": "zu::shared::WriteSide::file_mut",
      "kind": "method",
      "name": "file_mut",
      "of": "zu::shared::WriteSide",
      "signature": "fn file_mut(&mut self) -> &mut Zu1File"
    },
    {
      "id": "zu::shared::WriteSide::fold_count",
      "kind": "method",
      "name": "fold_count",
      "of": "zu::shared::WriteSide",
      "signature": "fn fold_count(&self) -> u64",
      "doc": "How many folds the writer on this side has run since it was\nopened, or none where no writer has been opened. See\n[`Writer::fold_count`]."
    },
    {
      "id": "zu::shared::WriteSide::fold_patches",
      "kind": "method",
      "name": "fold_patches",
      "of": "zu::shared::WriteSide",
      "signature": "fn fold_patches(&mut self) -> Result<()>",
      "doc": "Puts what the patch is carrying on the file, keeping the writer.\n\nA statement that reads the tables itself rather than through a\nreader sees only what the file holds, and a deferred commit is\nnot on the file. Copying a graph is one, and so is anything else\nthat walks the storage under the query plane. The writer stays\nbecause the statement is still inside a savepoint that will want\nit."
    },
    {
      "id": "zu::shared::WriteSide::fold_writer",
      "kind": "method",
      "name": "fold_writer",
      "of": "zu::shared::WriteSide",
      "signature": "fn fold_writer(&mut self) -> Result<()>",
      "doc": "Folds what the writer is holding and drops it, which is what a\ncaller wanting the raw file has to do first: an appender opens\nthe same sidecar and can truncate it."
    },
    {
      "id": "zu::shared::WriteSide::has_writer",
      "kind": "method",
      "name": "has_writer",
      "of": "zu::shared::WriteSide",
      "signature": "fn has_writer(&self) -> bool",
      "doc": "Whether a writer has been opened on this side yet."
    },
    {
      "id": "zu::shared::WriteSide::open_writer",
      "kind": "method",
      "name": "open_writer",
      "of": "zu::shared::WriteSide",
      "signature": "fn open_writer(&mut self) -> Result<bool>",
      "doc": "Opens the writer if it is not open already, and answers whether\nthis call is what opened it. Opening recovers and folds whatever\nthe log holds, which can move the roots, so the caller that\nopened one publishes afterwards."
    },
    {
      "id": "zu::shared::WriteSide::patches",
      "kind": "method",
      "name": "patches",
      "of": "zu::shared::WriteSide",
      "signature": "fn patches(&self) -> Arc<Patches>",
      "doc": "The cells committed since the last fold, which a reader reads\nthrough."
    },
    {
      "id": "zu::shared::WriteSide::settled",
      "kind": "method",
      "name": "settled",
      "of": "zu::shared::WriteSide",
      "signature": "fn settled(&self) -> bool",
      "doc": "Whether the log owes nothing, so what this side holds is durable\nand may be shown to the readers.\n\nA writer that has staged its frames and not yet waited for the\nplatter is in this side, and publishing it would let a reader\nact on a commit a crash could take back. Whoever owes that sync\npublishes it themselves once it lands, so what this says to skip\nis never lost, only left to the writer it belongs to."
    },
    {
      "id": "zu::shared::WriteSide::write",
      "kind": "method",
      "name": "write",
      "of": "zu::shared::WriteSide",
      "signature": "fn write<T>(&mut self, stage: impl FnOnce) -> Result<Written<T>>",
      "doc": "Runs one transaction against this file. See [`Writer::write`]."
    },
    {
      "id": "zu::snapshot",
      "kind": "module",
      "name": "snapshot",
      "doc": "[`Snapshot`] over a zu1 file: the vectorized read path. Scans skip\nchunks by zone map before touching payload bytes and decode the\npredicate column first, so a filter that misses a chunk costs a\nfence comparison and a filter that hits decodes the other columns\nonly for chunks with survivors. CSR groups pin out of the decoded\npools as shared slices, and gathers batch arbitrary row sets with\none decode per touched chunk.\n\nLike [`Zu1Graph`], the readers and their directories load lazily\nand stay cached, so a snapshot held across queries reads warm.\n\n[`Zu1Graph`]: crate::query::Zu1Graph"
    },
    {
      "id": "zu::snapshot::ColId",
      "kind": "type-alias",
      "name": "ColId",
      "signature": "u32",
      "source": "zu_query::snapshot::ColId",
      "doc": "Column position within a table's props directory."
    },
    {
      "id": "zu::snapshot::ColType",
      "kind": "enum",
      "name": "ColType",
      "source": "zu_query::snapshot::ColType",
      "doc": "The storage type of a property column, the ones the props\ndirectory hands the vector layer today; the full typed catalog is\nM3."
    },
    {
      "id": "zu::snapshot::ColType::Float",
      "kind": "variant",
      "name": "Float",
      "of": "zu::snapshot::ColType",
      "signature": "Float",
      "doc": "IEEE double. It rides the same fixed width lane an integer does\nand is stored as its bit pattern, so the read is the integer\nread and only the type on the vector differs."
    },
    {
      "id": "zu::snapshot::ColType::Int",
      "kind": "variant",
      "name": "Int",
      "of": "zu::snapshot::ColType",
      "signature": "Int"
    },
    {
      "id": "zu::snapshot::ColType::Str",
      "kind": "variant",
      "name": "Str",
      "of": "zu::snapshot::ColType",
      "signature": "Str"
    },
    {
      "id": "zu::snapshot::ColType::Temporal",
      "kind": "variant",
      "name": "Temporal",
      "of": "zu::snapshot::ColType",
      "signature": "Temporal(TemporalLane)",
      "doc": "A date, a time, a datetime or a duration, all of which are one\ncount in one word and differ only in what the count is of. The\ntag says which, so the lane stays the integer lane and only the\nvalue the sink hands back is different."
    },
    {
      "id": "zu::snapshot::CsrPin",
      "kind": "struct",
      "name": "CsrPin",
      "source": "zu_query::snapshot::CsrPin",
      "doc": "A pinned CSR group: shared handles on the decoded offset and\nneighbor arrays. Cloning is two `Arc` bumps, and the slices stay\nvalid however the pools evict behind them."
    },
    {
      "id": "zu::snapshot::CsrPin::degree",
      "kind": "method",
      "name": "degree",
      "of": "zu::snapshot::CsrPin",
      "signature": "fn degree(&self, local: usize) -> u64",
      "doc": "Degree of group-local row `local`, two offset reads."
    },
    {
      "id": "zu::snapshot::CsrPin::list",
      "kind": "method",
      "name": "list",
      "of": "zu::snapshot::CsrPin",
      "signature": "fn list(&self, local: usize) -> &[u64]",
      "doc": "The sorted neighbor list of group-local row `local`."
    },
    {
      "id": "zu::snapshot::CsrPin::neighbors",
      "kind": "field",
      "name": "neighbors",
      "of": "zu::snapshot::CsrPin",
      "signature": "std::sync::Arc<Vec<u64>>",
      "doc": "Neighbor row ids, sorted within each list."
    },
    {
      "id": "zu::snapshot::CsrPin::offsets",
      "kind": "field",
      "name": "offsets",
      "of": "zu::snapshot::CsrPin",
      "signature": "std::sync::Arc<Vec<u64>>",
      "doc": "Slot offsets, `rows + 1` monotone values into `neighbors`."
    },
    {
      "id": "zu::snapshot::Dir",
      "kind": "enum",
      "name": "Dir",
      "source": "zu_query::snapshot::Dir",
      "doc": "Traversal direction over a rel table's CSR."
    },
    {
      "id": "zu::snapshot::Dir::Bwd",
      "kind": "variant",
      "name": "Bwd",
      "of": "zu::snapshot::Dir",
      "signature": "Bwd"
    },
    {
      "id": "zu::snapshot::Dir::Fwd",
      "kind": "variant",
      "name": "Fwd",
      "of": "zu::snapshot::Dir",
      "signature": "Fwd"
    },
    {
      "id": "zu::snapshot::FuncCol",
      "kind": "struct",
      "name": "FuncCol",
      "source": "zu_query::snapshot::FuncCol",
      "doc": "One yielded column of a table function: a value for every node of\nthe domain, in row order, which is how the graph kernels already\nhand their answers back. `null` marks the rows the kernel answered\nnothing for, the unreached nodes `sssp` yields null at, and is\nempty when every row has a value."
    },
    {
      "id": "zu::snapshot::FuncCol::null",
      "kind": "field",
      "name": "null",
      "of": "zu::snapshot::FuncCol",
      "signature": "Vec<bool>"
    },
    {
      "id": "zu::snapshot::FuncCol::nullable",
      "kind": "method",
      "name": "nullable",
      "of": "zu::snapshot::FuncCol",
      "signature": "fn nullable(&self) -> bool",
      "doc": "Whether any row of this column is null, which decides where the\nvalue is allowed to be read: a null answers a projection and\nfails a comparison, but grouping and ordering on one follow\nrules the compiled sinks do not implement."
    },
    {
      "id": "zu::snapshot::FuncCol::values",
      "kind": "field",
      "name": "values",
      "of": "zu::snapshot::FuncCol",
      "signature": "Vec<i64>"
    },
    {
      "id": "zu::snapshot::GroupId",
      "kind": "type-alias",
      "name": "GroupId",
      "signature": "u32",
      "source": "zu_query::snapshot::GroupId",
      "doc": "Node group index within a rel table's CSR."
    },
    {
      "id": "zu::snapshot::RelId",
      "kind": "type-alias",
      "name": "RelId",
      "signature": "u32",
      "source": "zu_query::snapshot::RelId",
      "doc": "Rel table id from the catalog."
    },
    {
      "id": "zu::snapshot::SCAN_ROWS",
      "kind": "constant",
      "name": "SCAN_ROWS",
      "signature": "usize",
      "source": "zu_query::snapshot::SCAN_ROWS",
      "doc": "Rows per scan chunk, the zu1 encoding chunk: small enough that a\nselection fits u16 indices, large enough to amortize one decode."
    },
    {
      "id": "zu::snapshot::ScanChunk",
      "kind": "struct",
      "name": "ScanChunk",
      "source": "zu_query::snapshot::ScanChunk",
      "doc": "One scanned chunk: `columns[i]` holds `rows` values of the `i`th\nrequested column starting at table row `row_base`, and `sel` lists\nthe chunk-local rows that passed the predicate, `None` when every\nrow did."
    },
    {
      "id": "zu::snapshot::ScanChunk::columns",
      "kind": "field",
      "name": "columns",
      "of": "zu::snapshot::ScanChunk",
      "signature": "Vec<zu_vector::ValueVector>"
    },
    {
      "id": "zu::snapshot::ScanChunk::row_base",
      "kind": "field",
      "name": "row_base",
      "of": "zu::snapshot::ScanChunk",
      "signature": "u64"
    },
    {
      "id": "zu::snapshot::ScanChunk::rows",
      "kind": "field",
      "name": "rows",
      "of": "zu::snapshot::ScanChunk",
      "signature": "u32"
    },
    {
      "id": "zu::snapshot::ScanChunk::sel",
      "kind": "field",
      "name": "sel",
      "of": "zu::snapshot::ScanChunk",
      "signature": "Option<zu_vector::SelVector>"
    },
    {
      "id": "zu::snapshot::Snapshot",
      "kind": "trait",
      "name": "Snapshot",
      "source": "zu_query::snapshot::Snapshot",
      "doc": "One consistent view of stored graph data, read in batches. A\nsnapshot pins one commit epoch: two readers at the same epoch see\nthe same bytes, and nothing a snapshot lends out changes under it."
    },
    {
      "id": "zu::snapshot::Snapshot::csr",
      "kind": "method",
      "name": "csr",
      "of": "zu::snapshot::Snapshot",
      "signature": "fn csr(&mut self, rel: RelId, group: GroupId, dir: Dir) -> Result<CsrPin>",
      "doc": "Pins one CSR group of `rel` in `dir`."
    },
    {
      "id": "zu::snapshot::Snapshot::degree_batch",
      "kind": "method",
      "name": "degree_batch",
      "of": "zu::snapshot::Snapshot",
      "signature": "fn degree_batch(&mut self, rel: RelId, nodes: &[u64], dir: Dir) -> Result<u64>",
      "doc": "Sum of degrees over `nodes` in `dir`, offsets only; neighbor\nvalues never decode for a count."
    },
    {
      "id": "zu::snapshot::Snapshot::degrees",
      "kind": "method",
      "name": "degrees",
      "of": "zu::snapshot::Snapshot",
      "signature": "fn degrees(&mut self, rel: RelId, nodes: &[u64], dir: Dir, out: &mut [u64]) -> Result<()>",
      "doc": "Adds each node's degree in `dir` onto `out`, position for\nposition. Adding instead of storing lets an undirected step\naccumulate both sides into one buffer. The default goes through\npinned CSR groups; backends with a cheaper offsets-only path\nshould override it."
    },
    {
      "id": "zu::snapshot::Snapshot::epoch",
      "kind": "method",
      "name": "epoch",
      "of": "zu::snapshot::Snapshot",
      "signature": "fn epoch(&self) -> u64",
      "doc": "The commit epoch this snapshot reads."
    },
    {
      "id": "zu::snapshot::Snapshot::fork",
      "kind": "method",
      "name": "fork",
      "of": "zu::snapshot::Snapshot",
      "signature": "fn fork(&self) -> Option<Box<dyn Snapshot + Send>>",
      "doc": "A second handle on the same epoch for a parallel worker. Forks\nshare warm decoded state where the backend can, so a fork is\nnot a cold reopen: what one worker decodes, the others hit.\n`None` keeps execution single threaded."
    },
    {
      "id": "zu::snapshot::Snapshot::gather",
      "kind": "method",
      "name": "gather",
      "of": "zu::snapshot::Snapshot",
      "signature": "fn gather(&mut self, table: TableId, col: ColId, rows: &[u64], arena: &mut MorselArena) -> Result<ValueVector>",
      "doc": "Gathers a property column for arbitrary `rows` into one vector\nin argument order, decoding each touched chunk once."
    },
    {
      "id": "zu::snapshot::Snapshot::gather_rel",
      "kind": "method",
      "name": "gather_rel",
      "of": "zu::snapshot::Snapshot",
      "signature": "fn gather_rel(&mut self, rel: RelId, col: ColId, ords: &[u64], arena: &mut MorselArena) -> Result<ValueVector>",
      "doc": "Gathers an edge column for arbitrary `ords` into one vector in\nargument order, the edge-side [`Snapshot::gather`]."
    },
    {
      "id": "zu::snapshot::Snapshot::list_into",
      "kind": "method",
      "name": "list_into",
      "of": "zu::snapshot::Snapshot",
      "signature": "fn list_into(&mut self, rel: RelId, node: u64, dir: Dir, out: &mut Vec<u64>) -> Result<()>",
      "doc": "Appends one node's sorted neighbor list in `dir` to `out`,\nreading the range that list occupies rather than the group\naround it. Worth it below [`Snapshot::list_threshold`] lists per\ngroup and a loss above it, since a range read pays its own way\nin per list where a pin pays once for all of them."
    },
    {
      "id": "zu::snapshot::Snapshot::list_ords_into",
      "kind": "method",
      "name": "list_ords_into",
      "of": "zu::snapshot::Snapshot",
      "signature": "fn list_ords_into(&mut self, rel: RelId, node: u64, dir: Dir, out: &mut Vec<u64>) -> Result<()>",
      "doc": "Appends the load-order ordinal of every edge of `node`'s list\nin `dir` to `out`, position for position with\n[`Snapshot::list_into`].\n\nAn edge's ordinal is the row its properties sit in, and it is\nwhat a pair with several edges between it needs: the pair alone\nnames the first of the run for all of them, and counting the\nlist out instead gives every copy its own value."
    },
    {
      "id": "zu::snapshot::Snapshot::list_threshold",
      "kind": "method",
      "name": "list_threshold",
      "of": "zu::snapshot::Snapshot",
      "signature": "fn list_threshold(&mut self, _rel: RelId, _group: GroupId, _dir: Dir) -> Result<Option<usize>>",
      "doc": "How many lists of one group a caller has to want before pinning\nthe group beats reading each list on its own. A pin decodes the\ngroup's whole neighbor array, so it is the right read for a scan\nand the wrong one for a point lookup: a seed with sixteen\nfriends does not need the two million edges its group holds\ndecoded, and on a working set larger than the decoded pool it\ndoes not even get to keep them. Backends that decode nothing\nlazily answer 0, which asks for the pin every time.\n\n`None` is not a very high threshold, it is a refusal: this group\nmust be read a list at a time whatever the caller wants. A\nbackend answers that when a pin would be wrong rather than\nmerely expensive, which is what an unfolded edge over the group\nmakes it, since a pin hands out the arrays the file holds and\nthose are the ones the edge is not in."
    },
    {
      "id": "zu::snapshot::Snapshot::resolve_col",
      "kind": "method",
      "name": "resolve_col",
      "of": "zu::snapshot::Snapshot",
      "signature": "fn resolve_col(&mut self, table: TableId, name: &str) -> Result<Option<(ColId, ColType)>>",
      "doc": "Resolves a property column by name, `None` when the table has\nno column under it."
    },
    {
      "id": "zu::snapshot::Snapshot::resolve_rel_col",
      "kind": "method",
      "name": "resolve_rel_col",
      "of": "zu::snapshot::Snapshot",
      "signature": "fn resolve_rel_col(&mut self, rel: RelId, name: &str) -> Result<Option<(ColId, ColType)>>",
      "doc": "The same for a rel table's edge columns, `None` when the rel\nstores nothing under that name and `None` from a backend with\nno edge columns at all.\n\nAnswering `None` is what keeps the other two edge methods\nhonest: nothing compiles a plan that reads an edge column\nunless this resolved one, so a backend that does not implement\nthem is never asked."
    },
    {
      "id": "zu::snapshot::Snapshot::scan",
      "kind": "method",
      "name": "scan",
      "of": "zu::snapshot::Snapshot",
      "signature": "fn scan(&mut self, table: TableId, chunk: u64, cols: &[ColId], pred: Option<&ZonePred>, arena: &mut MorselArena) -> Result<Option<ScanChunk>>",
      "doc": "Reads chunk `chunk` of `table`, decoding `cols` in order into\nvectors backed by `arena`. `pred` filters on an integer column:\nchunks its zone map excludes come back `None` without touching\npayload bytes, and the predicate column decodes before the rest\nso a chunk with no surviving rows also stops early. `None` past\nthe last chunk."
    },
    {
      "id": "zu::snapshot::Snapshot::seek_key",
      "kind": "method",
      "name": "seek_key",
      "of": "zu::snapshot::Snapshot",
      "signature": "fn seek_key(&mut self, table: TableId, key: u64) -> Result<Option<u64>>",
      "doc": "The dense row a primary key maps to in `table`, `None` when the\nkey is absent or lands past the table. A table loaded without a\nkey index keeps the dense contract, where the key is the row,\nwhich is the same rule property reads follow for `.id`."
    },
    {
      "id": "zu::snapshot::Snapshot::table_function",
      "kind": "method",
      "name": "table_function",
      "of": "zu::snapshot::Snapshot",
      "signature": "fn table_function(&mut self, name: &str, rel: RelId, args: &[i64]) -> Result<Option<FuncCol>>",
      "doc": "Runs a table function kernel over `rel` and hands back the one\ncolumn it yields, dense over the node domain. `None` means this\nbackend has no vectorized answer for the call, either because it\nknows no kernel under that name or because the kernel yields\nsomething the compiled column cannot carry, and the query goes\nback to the row-at-a-time engine, errors included."
    },
    {
      "id": "zu::snapshot::Snapshot::table_rows",
      "kind": "method",
      "name": "table_rows",
      "of": "zu::snapshot::Snapshot",
      "signature": "fn table_rows(&mut self, table: TableId) -> Result<u64>",
      "doc": "Rows in a node table's dense row domain."
    },
    {
      "id": "zu::snapshot::SnapshotCache",
      "kind": "struct",
      "name": "SnapshotCache",
      "doc": "What a snapshot learns while it reads, kept in a value of its own\nso a caller that opens a snapshot per query can carry it over.\n\nA snapshot is a view of one epoch and is built and dropped around a\nsingle execution, but the table readers behind it are not per query\nat all: loading one reads its directory out of the file, and on a\nsmall graph that load costs more than the query it was opened for.\nThe buffers are here for the same reason, so a scan does not start\nby growing a fresh vector every time.\n\nEverything in here is valid for as long as the epoch it was read at\nis, which is the caller's business: hand it back to\n[`Zu1Snapshot::with_cache`] while the epoch holds and drop it when\nthe epoch moves."
    },
    {
      "id": "zu::snapshot::SnapshotCache::set_frames",
      "kind": "method",
      "name": "set_frames",
      "of": "zu::snapshot::SnapshotCache",
      "signature": "fn set_frames(&mut self, frames: Arc<FrameSet>)",
      "doc": "Hands over the frames registered on the session, which the\nsnapshots built from this cache read as tables of the graph."
    },
    {
      "id": "zu::snapshot::SnapshotCache::set_patches",
      "kind": "method",
      "name": "set_patches",
      "of": "zu::snapshot::SnapshotCache",
      "signature": "fn set_patches(&mut self, patches: Arc<Patches>, catalog: &Catalog)",
      "doc": "Hands the cached readers the cells a commit has left in the\noverlay, and keeps them for the readers a later snapshot loads.\n\nThe readers are kept rather than dropped, which is the point: a\ncommit that does not fold does not move the columns they\ndescribe, so the only thing that has changed about them is what\nthey read on top."
    },
    {
      "id": "zu::snapshot::TableId",
      "kind": "type-alias",
      "name": "TableId",
      "signature": "u32",
      "source": "zu_query::snapshot::TableId",
      "doc": "Node table id from the catalog."
    },
    {
      "id": "zu::snapshot::TemporalLane",
      "kind": "enum",
      "name": "TemporalLane",
      "source": "zu_query::snapshot::TemporalLane",
      "doc": "Which temporal type a one word lane holds.\n\nThe zoned types are not here. A zoned value is a count and an offset\nfrom UTC, which is two numbers, and a lane is one word: reading one\nthrough a lane would drop the offset and answer an instant that\nprints somewhere else. Those columns stay off the vector path until\nthere is somewhere to put the second number."
    },
    {
      "id": "zu::snapshot::TemporalLane::Date",
      "kind": "variant",
      "name": "Date",
      "of": "zu::snapshot::TemporalLane",
      "signature": "Date",
      "doc": "Days since 1970-01-01, widened from the 32 bits they are stored\nin, since a lane is a word wide whatever it holds."
    },
    {
      "id": "zu::snapshot::TemporalLane::Duration",
      "kind": "variant",
      "name": "Duration",
      "of": "zu::snapshot::TemporalLane",
      "signature": "Duration(zu_common::DurationKind)",
      "doc": "Months for a year-month duration, nanoseconds for a day-time\none, which is what the kind says."
    },
    {
      "id": "zu::snapshot::TemporalLane::LocalDatetime",
      "kind": "variant",
      "name": "LocalDatetime",
      "of": "zu::snapshot::TemporalLane",
      "signature": "LocalDatetime",
      "doc": "Nanoseconds since 1970-01-01T00:00:00."
    },
    {
      "id": "zu::snapshot::TemporalLane::LocalTime",
      "kind": "variant",
      "name": "LocalTime",
      "of": "zu::snapshot::TemporalLane",
      "signature": "LocalTime",
      "doc": "Nanoseconds since midnight."
    },
    {
      "id": "zu::snapshot::TemporalLane::column_type",
      "kind": "method",
      "name": "column_type",
      "of": "zu::snapshot::TemporalLane",
      "signature": "fn column_type(self) -> ColumnType",
      "doc": "What a result column of this lane is called."
    },
    {
      "id": "zu::snapshot::TemporalLane::logical_type",
      "kind": "method",
      "name": "logical_type",
      "of": "zu::snapshot::TemporalLane",
      "signature": "fn logical_type(self) -> LogicalType",
      "doc": "The temporal type this lane is a lane of."
    },
    {
      "id": "zu::snapshot::TemporalLane::of",
      "kind": "method",
      "name": "of",
      "of": "zu::snapshot::TemporalLane",
      "signature": "fn of(ty: &LogicalType) -> Option<TemporalLane>",
      "doc": "The lane a temporal type takes, `None` for the zoned types,\nwhich take none."
    },
    {
      "id": "zu::snapshot::TemporalLane::phys",
      "kind": "method",
      "name": "phys",
      "of": "zu::snapshot::TemporalLane",
      "signature": "fn phys(self) -> PhysType",
      "doc": "The physical lane the words ride in. A date counts days and the\nother three count one thing each, so the width is a word either\nway and the type is what a kernel reads to know the ordering is\nthe integer ordering."
    },
    {
      "id": "zu::snapshot::TemporalLane::value",
      "kind": "method",
      "name": "value",
      "of": "zu::snapshot::TemporalLane",
      "signature": "fn value(self, word: i64) -> Temporal",
      "doc": "The value one word of this lane holds."
    },
    {
      "id": "zu::snapshot::TemporalLane::word",
      "kind": "method",
      "name": "word",
      "of": "zu::snapshot::TemporalLane",
      "signature": "fn word(self, value: &Temporal) -> Option<i64>",
      "doc": "The word a value of this lane rides as, `None` for a value of\nsome other temporal type: a date is not a datetime and a lane\nthat took one for the other would answer the wrong instant."
    },
    {
      "id": "zu::snapshot::ZonePred",
      "kind": "struct",
      "name": "ZonePred",
      "source": "zu_query::snapshot::ZonePred",
      "doc": "An inclusive value range over an integer column, the pushdown a\nscan checks against zone maps before decoding a chunk. Bounds and\ncompares are in the unsigned domain, matching the stored zones."
    },
    {
      "id": "zu::snapshot::ZonePred::col",
      "kind": "field",
      "name": "col",
      "of": "zu::snapshot::ZonePred",
      "signature": "ColId"
    },
    {
      "id": "zu::snapshot::ZonePred::hi",
      "kind": "field",
      "name": "hi",
      "of": "zu::snapshot::ZonePred",
      "signature": "u64"
    },
    {
      "id": "zu::snapshot::ZonePred::lo",
      "kind": "field",
      "name": "lo",
      "of": "zu::snapshot::ZonePred",
      "signature": "u64"
    },
    {
      "id": "zu::snapshot::ZonePred::skips",
      "kind": "method",
      "name": "skips",
      "of": "zu::snapshot::ZonePred",
      "signature": "fn skips(&self, lo: u64, hi: u64) -> bool",
      "doc": "True when a zone spanning `lo..=hi` cannot hold a match."
    },
    {
      "id": "zu::snapshot::Zu1Snapshot",
      "kind": "struct",
      "name": "Zu1Snapshot",
      "doc": "The vectorized view of one open zu1 file at its current epoch."
    },
    {
      "id": "zu::snapshot::Zu1Snapshot::into_cache",
      "kind": "method",
      "name": "into_cache",
      "of": "zu::snapshot::Zu1Snapshot",
      "signature": "fn into_cache(self) -> SnapshotCache",
      "doc": "Takes back what this snapshot read, to hand to the next one."
    },
    {
      "id": "zu::snapshot::Zu1Snapshot::new",
      "kind": "method",
      "name": "new",
      "of": "zu::snapshot::Zu1Snapshot",
      "signature": "fn new(db: &mut Zu1File, catalog: impl Into<Arc<Catalog>>) -> Self"
    },
    {
      "id": "zu::snapshot::Zu1Snapshot::open",
      "kind": "method",
      "name": "open",
      "of": "zu::snapshot::Zu1Snapshot",
      "signature": "fn open(db: &mut Zu1File) -> Result<Self>",
      "doc": "Loads the catalog from the file itself."
    },
    {
      "id": "zu::snapshot::Zu1Snapshot::with_cache",
      "kind": "method",
      "name": "with_cache",
      "of": "zu::snapshot::Zu1Snapshot",
      "signature": "fn with_cache(db: &mut Zu1File, catalog: impl Into<Arc<Catalog>>, cache: SnapshotCache) -> Self",
      "doc": "A snapshot that starts from what an earlier one already read."
    },
    {
      "id": "zu::sqlite",
      "kind": "module",
      "name": "sqlite",
      "doc": "zuQL against the sqlite engine: the same parse, bind, plan, and\nexecute pipeline as the zu1 facade, reading through [`SqliteStore`].\nAdjacency goes through the lazy CSR cache so traversals run at\nnative speed once a group is built; properties read straight off\nthe B-tree row. The row contract matches zu1's dense domain: node\noffsets are `zrow` values and loaders assign them densely from\nzero, so the two engines answer identical queries with identical\nids and the differential corpus can compare them verbatim."
    },
    {
      "id": "zu::sqlite::SqliteGraph",
      "kind": "struct",
      "name": "SqliteGraph",
      "doc": "The executor's view of one open sqlite store. Adjacency reads\nthrough the store's CSR cache and keeps the last group pinned per\ndirection, so a scan-driven expand pays the cache lock once per\ngroup instead of once per node."
    },
    {
      "id": "zu::sqlite::SqliteGraph::new",
      "kind": "method",
      "name": "new",
      "of": "zu::sqlite::SqliteGraph",
      "signature": "fn new(store: &SqliteStore) -> Result<Self>"
    },
    {
      "id": "zu::sqlite::run",
      "kind": "function",
      "name": "run",
      "signature": "fn run(source: &str, store: &zu_sqlite::SqliteStore, params: &[(&str, zu_query::exec::Value)]) -> zu_common::Result<zu_query::exec::QueryResult>",
      "doc": "Parses, plans, optimizes, and executes one query against an open\nsqlite store, returning the result rows. Execution is sequential:\nthe store holds one connection, so there is no fork for morsel\nworkers to ride."
    },
    {
      "id": "zu::sqlite::schema_of",
      "kind": "function",
      "name": "schema_of",
      "signature": "fn schema_of(store: &zu_sqlite::SqliteStore) -> zu_common::Result<zu_query::binder::Schema>",
      "doc": "Builds the binder schema from the store's `zu_catalog`.\n\nRel tables created before schema version 2 carry no endpoints and\ncannot be bound; recreate them through `create_rel_table`."
    },
    {
      "id": "zu::write",
      "kind": "module",
      "name": "write",
      "doc": "The write side of an open database: the log beside the file, the\noverlay store above it, and the fold that puts a committed change\nwhere the next read will find it.\n\nM3 built all three of those and crash tested them, and [`crate::append`]\nis the first thing on the query side to open one, but an appender is\nthe bulk shape: whole columns at a time, no expression to evaluate,\nnothing read back. A write statement is the other shape, one\ntransaction of a few rows that has to be visible to the clause after\nit, and it needs the same three pieces held differently. This is\nthat holding. A [`Writer`] owns the WAL and the [`Mvcc`] store for\none open file, recovers whatever the log holds above the file's last\ncheckpoint, and turns a staged transaction into a committed epoch.\n\nMost commits fold, and the ones that do not are worth being plain\nabout. A fold rewrites the columns the transaction touched, and the\noverlay exists precisely so a reader could see a committed row\nwithout one, but [`Zu1Graph`] reads the sealed file and nothing\nelse, so a change that is not folded is a change the next `MATCH`\ncannot see, and a write nobody can read is not a write. So a commit\nfolds unless the read side can be handed what it wrote instead,\nwhich is one shape: a value written onto a column of a row that is\nalready there, whether the row is a node or an edge the graph runs\nthrough. Those go into a [`CellPatch`] per table,\nthe readers put them over the values their columns hold, and a fold\nseals them later, at a checkpoint or when the run of them has gone\non long enough. It is worth what it costs to keep narrow, because\nthe fold is not the only thing that goes: it is what moves the\nheader epoch, and moving the epoch is what throws away the session's\nplan cache, its catalog and every decoded chunk it had warm.\n\nNot every fold publishes, and that is the part worth reading twice.\nA fold moves the roots this handle carries; a checkpoint puts them\non disk, and on a platform where the only barrier is a full sync it\ncosts two of them, against one for the log frame the commit already\nsynced. So a commit folds and stops, and the file is checkpointed\nwhen the folds since the last one have taken as much as\n[`checkpoint_due`] allows. Nothing is at risk in between: the\nframe is the durability point, the header on disk still names the\nepoch the last checkpoint folded through, and a crash replays the\nlog from there back to where the folds had got to. What it costs is\nfile growth, because a block freed by an unpublished fold cannot be\nhanded out again, and that is what the threshold bounds.\n\n[`Zu1Graph`]: crate::query::Zu1Graph"
    },
    {
      "id": "zu::write::Patches",
      "kind": "struct",
      "name": "Patches",
      "doc": "Everything the commits since the last fold left for the readers to\nread through, which is what makes a deferred commit visible.\n\nFive shapes, because there are five ways a change can sit above a\ncolumn nobody has rewritten: a new value for a row the column\nalready holds, a whole new row past the end of it, an edge added or\ntaken away, which is a pair the adjacency reader merges into or out\nof two lists and, where it was added, a row of the rel table's\nproperty columns, a row taken away, which is an offset every read of\nthe table filters by, and a label change, which is a word the reader\nanswers with in place of the bitset's."
    },
    {
      "id": "zu::write::Patches::added_rows",
      "kind": "method",
      "name": "added_rows",
      "of": "zu::write::Patches",
      "signature": "fn added_rows(&self, table: u32) -> u64",
      "doc": "How many rows a commit has added to `table` and not folded,\nwhich every bound over the table's rows is short by."
    },
    {
      "id": "zu::write::Patches::cells",
      "kind": "field",
      "name": "cells",
      "of": "zu::write::Patches",
      "signature": "zu_common::IdMap<u32, std::sync::Arc<zu::zu1::props::CellPatch>>",
      "doc": "New values, by table."
    },
    {
      "id": "zu::write::Patches::edges",
      "kind": "field",
      "name": "edges",
      "of": "zu::write::Patches",
      "signature": "zu_common::IdMap<u32, std::sync::Arc<zu::zu1::graph::EdgePatch>>",
      "doc": "Edges added and edges taken away, by rel table."
    },
    {
      "id": "zu::write::Patches::gone",
      "kind": "field",
      "name": "gone",
      "of": "zu::write::Patches",
      "signature": "zu_common::IdMap<u32, std::sync::Arc<[u64]>>",
      "doc": "Rows taken away, by table, ascending. A delete moves nothing:\nthe offsets stay where they are and the fold writes them into\nthe table's tombstone chain, so what the patch carries is what\nthe chain would have held."
    },
    {
      "id": "zu::write::Patches::grown",
      "kind": "method",
      "name": "grown",
      "of": "zu::write::Patches",
      "signature": "fn grown(&self, catalog: &Catalog, rel: u32) -> [u64; 2]",
      "doc": "The same for the two ends of `rel`, the source first, which is\nwhat its adjacency reader answers rows past its CSR with."
    },
    {
      "id": "zu::write::Patches::is_empty",
      "kind": "method",
      "name": "is_empty",
      "of": "zu::write::Patches",
      "signature": "fn is_empty(&self) -> bool"
    },
    {
      "id": "zu::write::Patches::marks",
      "kind": "field",
      "name": "marks",
      "of": "zu::write::Patches",
      "signature": "zu_common::IdMap<u32, std::sync::Arc<zu::zu1::props::LabelPatch>>",
      "doc": "The labels rows are left carrying, by table. A label is not a\ncolumn, so there is no cell to lay a value over: what is kept is\nthe whole word, composed where the commit was taken."
    },
    {
      "id": "zu::write::Patches::new",
      "kind": "method",
      "name": "new",
      "of": "zu::write::Patches",
      "signature": "fn new() -> Self"
    },
    {
      "id": "zu::write::Patches::rows",
      "kind": "field",
      "name": "rows",
      "of": "zu::write::Patches",
      "signature": "zu_common::IdMap<u32, std::sync::Arc<zu::zu1::props::RowPatch>>",
      "doc": "New rows, by table, node tables and rel tables alike. A rel\ntable's are the property rows of the edges in `edges`, under\nthe ordinals those took. A node table's are rows of the table\nitself, so they move the count every reader is bounded by, and\neverything that reads a bound reads it through this."
    },
    {
      "id": "zu::write::Writer",
      "kind": "struct",
      "name": "Writer",
      "doc": "The write side of one open database.\n\nOne of these is the single writer: it holds the only handle on its\noverlay store, and [`Mvcc::begin`] hands out that store's only\nmutable borrow, so a second transaction cannot exist while one is\nopen. Two writers over one file would be two overlay stores and one\nlog between them, and neither would be right, so there is exactly\none place a [`Writer`] lives, which is inside the session that owns\nthe file handle. An appender is the other writer of the same log,\nand it is kept apart the same way: it borrows the file, and a\nsession hands the file out only after dropping its writer."
    },
    {
      "id": "zu::write::Writer::commits",
      "kind": "method",
      "name": "commits",
      "of": "zu::write::Writer",
      "signature": "fn commits(&self) -> Arc<Commits>",
      "doc": "What makes this writer's commits durable, held past the write\nside going back so the wait happens off the lock."
    },
    {
      "id": "zu::write::Writer::discard_above",
      "kind": "method",
      "name": "discard_above",
      "of": "zu::write::Writer",
      "signature": "fn discard_above(&mut self, floor: Epoch) -> Result<()>",
      "doc": "Cuts the log back to where it stood at `floor`, which is what a\nrollback owes a log the folds it is undoing never truncated. A\nwriter is dropped straight after, because the store it holds\ndescribes epochs that have stopped existing."
    },
    {
      "id": "zu::write::Writer::epoch",
      "kind": "method",
      "name": "epoch",
      "of": "zu::write::Writer",
      "signature": "fn epoch(&self) -> Epoch",
      "doc": "The newest committed epoch."
    },
    {
      "id": "zu::write::Writer::fold",
      "kind": "method",
      "name": "fold",
      "of": "zu::write::Writer",
      "signature": "fn fold(&mut self, db: &mut Zu1File) -> Result<()>",
      "doc": "Seals every committed overlay into the file, publishes it and\ntruncates the log. On return the file alone answers what the\noverlays answered, and the header epoch has moved, so anything\ncaching the catalog or a decoded layout has to reload."
    },
    {
      "id": "zu::write::Writer::fold_count",
      "kind": "method",
      "name": "fold_count",
      "of": "zu::write::Writer",
      "signature": "fn fold_count(&self) -> u64",
      "doc": "How many folds this writer has run since it was opened. See\n[`Writer::folds`]."
    },
    {
      "id": "zu::write::Writer::open",
      "kind": "method",
      "name": "open",
      "of": "zu::write::Writer",
      "signature": "fn open(db: &mut Zu1File) -> Result<Writer>",
      "doc": "Opens the write side of `db`, replaying whatever the log holds\nabove the file's last checkpoint and sealing it.\n\nThe sealing is what makes a reopen after a crash the same as\nnever having crashed: a crash between a commit and a checkpoint\nleaves committed transactions in the log and not in the file,\nrecovery brings them back as overlays, and an overlay is what no\nreader reads. A session replays the sidecar when it opens, so on\nthat path this usually finds an empty log and folds nothing;\ndoing it here too is what keeps a [`Writer`] correct for a\ncaller that did not come through a session.\n\nA read-only handle is refused here rather than at the first\ncommit, because opening the write side already creates the log,\nand a log beside a database nobody may write to is a file that\nonly ever confuses the next reader."
    },
    {
      "id": "zu::write::Writer::patches",
      "kind": "method",
      "name": "patches",
      "of": "zu::write::Writer",
      "signature": "fn patches(&self) -> &Arc<Patches>",
      "doc": "The cells the commits since the last fold wrote, which a reader\nputs over the words its columns hold.\n\nThis is the whole of what a deferred commit hands the read side,\nso a reader that has it reads the same database a fold would\nhave left behind. The `Arc` is the version: it is replaced when\na commit adds to the patch and emptied when a fold seals it, and\na reader that holds the current one is up to date."
    },
    {
      "id": "zu::write::Writer::path",
      "kind": "method",
      "name": "path",
      "of": "zu::write::Writer",
      "signature": "fn path(&self) -> &Path",
      "doc": "The log this writer commits to."
    },
    {
      "id": "zu::write::Writer::settled",
      "kind": "method",
      "name": "settled",
      "of": "zu::write::Writer",
      "signature": "fn settled(&self) -> bool",
      "doc": "Whether the log owes nothing. See [`Commits::settled`]."
    },
    {
      "id": "zu::write::Writer::write",
      "kind": "method",
      "name": "write",
      "of": "zu::write::Writer",
      "signature": "fn write<T>(&mut self, db: &mut Zu1File, stage: impl FnOnce) -> Result<Written<T>>",
      "doc": "Runs one transaction: `stage` describes the change against an\nopen [`WriteTxn`], and what it returns comes back alongside the\nepoch the change committed at.\n\nA closure that raises is the rollback path, and it costs\nnothing: staging is local to the transaction and the log has\nnot been touched, so dropping it on the way out of the `?` is\nthe whole of the undo. That is the shape an implicit\ntransaction wants, where a statement that raises anywhere in\nthe middle must leave the database as it found it."
    },
    {
      "id": "zu::write::Written",
      "kind": "struct",
      "name": "Written",
      "doc": "What one [`Writer::write`] did."
    },
    {
      "id": "zu::write::Written::epoch",
      "kind": "field",
      "name": "epoch",
      "of": "zu::write::Written",
      "signature": "zu_common::Epoch",
      "doc": "The epoch the change committed at. A transaction that staged\nnothing publishes nothing, so this is the epoch that was\nalready current when the closure did no work."
    },
    {
      "id": "zu::write::Written::owed",
      "kind": "field",
      "name": "owed",
      "of": "zu::write::Written",
      "signature": "Option<u64>",
      "doc": "How far the log has to be on the platter for this to have\ncommitted, or `None` when nothing is owed.\n\nNothing is owed by a transaction that staged nothing, and\nnothing is owed once a checkpoint has sealed the change into the\nbase file and cut the log, because then what would be waited for\nis not there any more and what it said is durable without it.\n\nThe caller waits for this after it has given the write side\nback, which is what lets the writer behind it stage into the\nsame sync. See [`Commits::sync_through`]."
    },
    {
      "id": "zu::write::Written::value",
      "kind": "field",
      "name": "value",
      "of": "zu::write::Written",
      "signature": "T",
      "doc": "Whatever the staging closure returned, which for an insert is\nusually how many rows it appended."
    },
    {
      "id": "zu::write::checkpoint_slack_bytes",
      "kind": "function",
      "name": "checkpoint_slack_bytes",
      "signature": "fn checkpoint_slack_bytes(file_bytes: u64) -> u64",
      "doc": "How much the file may grow before the next commit stops staging and\ncheckpoints, given what the file already is.\n\nThe rule is [`GROWTH_SHARE`] of the file held between\n[`FLOOR_BYTES`] and [`CEILING_BYTES`]. It is public because it is\nthe bound on how big a file gets between checkpoints, which is a\nthing a bench checks and an operator sizing a disk wants, and\nbecause a copy of the rule written down somewhere else would go\nstale the first time one of the three numbers moved."
    },
    {
      "id": "zu::zu1",
      "kind": "module",
      "name": "zu1",
      "source": "zu_zu1",
      "doc": "The `zu1` native single-file storage engine.\n\nByte-level format is specified in `docs/04-storage-zu1-format.md`.\n`file` holds the headers, block I/O, and the dual-header checkpoint\nflip; `meta` the meta-block chains behind every root pointer. Node\ngroups, segments, and CSR build on top of these within M1."
    },
    {
      "id": "zu::zu1::BLOCK_SIZE",
      "kind": "constant",
      "name": "BLOCK_SIZE",
      "signature": "u32",
      "doc": "Fixed block size in bytes.\n\nIt was 256 KiB, from the research note that took DuckDB's block and\ndid not ask what zu writes into one. What zu writes into one is a\npacking scope: a column directory, one table's adjacency, one key\nindex, each of which has to be freeable on its own, so each starts a\nblock of its own and the tail of that block is waste. A thousand\nnode linkbench graph came to 3.75 MiB of file holding about 400 KiB\nof graph, and a fold, which takes a fresh scope for everything it\nrewrites, spent a megabyte a time on it.\n\nThe waste falls linearly with the block and what a smaller block\ncosts is one more pointer in a segment meta and one more read per\nblock of a big segment. At 32 KiB the same graph loads into 768 KiB\nand a 10k node one into 3.8 MiB rather than 6.5, and the reads came\nout faster rather than slower: a point read pulls a 32 KiB frame\ninto the cache where it used to pull 256."
    },
    {
      "id": "zu::zu1::FORMAT_VERSION",
      "kind": "constant",
      "name": "FORMAT_VERSION",
      "signature": "u16",
      "doc": "Current format version."
    },
    {
      "id": "zu::zu1::Layout",
      "kind": "struct",
      "name": "Layout",
      "doc": "How a store's blocks divide between what the schema costs and what\nthe graph in it costs.\n\nThis exists because a store's size on its own does not divide by a\ngraph. Every engine writes something before it holds anything, and\nzu writes rather a lot of it: the header block, the catalog, the\ntable index and the statistics are four blocks before a single node\nexists. A tool dividing those four blocks by three edges gets a\nnumber in the millions of bits per edge and publishes it as an\nencoding, which happened, repeatedly, to a benchmark harness that\nhad no way to ask this question.\n\nThe split is drawn where it can be drawn honestly. The four schema\nstructures are fixed by the shape of the database and do not know\nhow many rows are under them. Everything else, node groups, column\nsegments, adjacency, key indexes and the per-table directories that\nname them, grows with the graph and is data. The free list is\nneither: it is space this file owns and is not currently using, so\nit is counted apart from both rather than charged to the graph."
    },
    {
      "id": "zu::zu1::Layout::block_size",
      "kind": "field",
      "name": "block_size",
      "of": "zu::zu1::Layout",
      "signature": "u64",
      "doc": "Bytes per block. Every count below is in blocks; this is what\nturns one into bytes."
    },
    {
      "id": "zu::zu1::Layout::blocks",
      "kind": "field",
      "name": "blocks",
      "of": "zu::zu1::Layout",
      "signature": "u64",
      "doc": "Every block the file spans, the header block included."
    },
    {
      "id": "zu::zu1::Layout::bytes",
      "kind": "method",
      "name": "bytes",
      "of": "zu::zu1::Layout",
      "signature": "fn bytes(&self) -> u64",
      "doc": "Total size, as the block count implies it."
    },
    {
      "id": "zu::zu1::Layout::data_blocks",
      "kind": "field",
      "name": "data_blocks",
      "of": "zu::zu1::Layout",
      "signature": "u64",
      "doc": "What is left, which is the graph."
    },
    {
      "id": "zu::zu1::Layout::data_bytes",
      "kind": "method",
      "name": "data_bytes",
      "of": "zu::zu1::Layout",
      "signature": "fn data_bytes(&self) -> u64",
      "doc": "What the graph costs."
    },
    {
      "id": "zu::zu1::Layout::free_blocks",
      "kind": "field",
      "name": "free_blocks",
      "of": "zu::zu1::Layout",
      "signature": "u64",
      "doc": "Blocks the free list names, plus the blocks the free list is\nitself written in."
    },
    {
      "id": "zu::zu1::Layout::free_bytes",
      "kind": "method",
      "name": "free_bytes",
      "of": "zu::zu1::Layout",
      "signature": "fn free_bytes(&self) -> u64",
      "doc": "What the free list is holding on to."
    },
    {
      "id": "zu::zu1::Layout::schema_blocks",
      "kind": "field",
      "name": "schema_blocks",
      "of": "zu::zu1::Layout",
      "signature": "u64",
      "doc": "The header block plus the catalog, table index and statistics\nchains: what this database would weigh holding nothing."
    },
    {
      "id": "zu::zu1::Layout::schema_bytes",
      "kind": "method",
      "name": "schema_bytes",
      "of": "zu::zu1::Layout",
      "signature": "fn schema_bytes(&self) -> u64",
      "doc": "What the schema costs, which is the figure to subtract before\ndividing a store by the graph in it."
    },
    {
      "id": "zu::zu1::MAGIC",
      "kind": "constant",
      "name": "MAGIC",
      "signature": "[u8; 8]",
      "doc": "File magic: UTF-8 図 followed by `ZU1\\0\\n`."
    },
    {
      "id": "zu::zu1::MIN_READER_VERSION",
      "kind": "constant",
      "name": "MIN_READER_VERSION",
      "signature": "u16",
      "doc": "Oldest reader version that can open files we write."
    },
    {
      "id": "zu::zu1::algo",
      "kind": "module",
      "name": "algo",
      "doc": "Whole-graph table function kernels: bfs, pagerank, wcc, sssp, cdlp,\nlcc, triangle_count, louvain (docs/07 §4, TableFunction). Each kernel runs directly\non a rel table's CSR through [`GraphReader`], one sequential sweep per\niteration so the per-direction group cache decodes every group once\nper pass, and returns one value per node in dense row order. The\nquery layer surfaces these through CALL; nothing here parses or\nplans.\n\nThe traversal kernels share one frontier: a visited bitmap, rounds\nthat pin a group's CSR once and expand every frontier node in it\nfrom the pinned arrays, and a direction-optimizing switch that goes\nbottom-up when the frontier is about to touch more edges than are\nleft unexplored.\n\nDeterminism: every kernel sweeps nodes in row order with fixed\niteration or convergence rules, so the same file produces the same\noutput on every run and every machine, which is what lets tests and\nthe Graphalytics harness assert exact values. The frontier is no\nexception: a node's level is the round it was first claimed in, and\nrounds are barriers, so which direction a round ran cannot move it."
    },
    {
      "id": "zu::zu1::algo::CDLP_ROUNDS",
      "kind": "constant",
      "name": "CDLP_ROUNDS",
      "signature": "usize",
      "doc": "The round count Graphalytics fixes for CDLP, so two runs of the\nsame graph are the same partition however far propagation has left\nto go."
    },
    {
      "id": "zu::zu1::algo::PAGERANK_DAMPING",
      "kind": "constant",
      "name": "PAGERANK_DAMPING",
      "signature": "f64",
      "doc": "The damping factor and iteration count Graphalytics fixes for\ncomparable runs."
    },
    {
      "id": "zu::zu1::algo::PAGERANK_ITERATIONS",
      "kind": "constant",
      "name": "PAGERANK_ITERATIONS",
      "signature": "usize"
    },
    {
      "id": "zu::zu1::algo::PAGERANK_MAX_ITERATIONS",
      "kind": "constant",
      "name": "PAGERANK_MAX_ITERATIONS",
      "signature": "usize"
    },
    {
      "id": "zu::zu1::algo::PAGERANK_TOLERANCE",
      "kind": "constant",
      "name": "PAGERANK_TOLERANCE",
      "signature": "f64",
      "doc": "Where the converging form stops. The tolerance is the largest a\nsingle rank moves in a round, the cap is what a run costs on a\ngraph that will not settle. A fixed twenty rounds is the right rule\nonly against a published reference produced the same way; asked for\nthe pagerank of a graph with nothing to match, twenty rounds is\nabout 3.4e-4 off the converged answer, which is outside the 1e-4\nthe LDBC harness compares at. These two are what that harness uses\nfor its own reference."
    },
    {
      "id": "zu::zu1::algo::Walk",
      "kind": "enum",
      "name": "Walk",
      "doc": "Which adjacency a traversal walks. `Out` follows stored edge\ndirection, which is what BFS levels mean on a directed graph.\n`Both` treats the rel table as undirected."
    },
    {
      "id": "zu::zu1::algo::Walk::Both",
      "kind": "variant",
      "name": "Both",
      "of": "zu::zu1::algo::Walk",
      "signature": "Both"
    },
    {
      "id": "zu::zu1::algo::Walk::Out",
      "kind": "variant",
      "name": "Out",
      "of": "zu::zu1::algo::Walk",
      "signature": "Out"
    },
    {
      "id": "zu::zu1::algo::betweenness",
      "kind": "function",
      "name": "betweenness",
      "signature": "fn betweenness(db: &mut zu_zu1::file::Zu1File, reader: &mut zu_zu1::graph::GraphReader, sources: &[u64]) -> zu_common::Result<Vec<f64>>",
      "doc": "Betweenness centrality accumulated from `sources` by Brandes, the\nGAP BC kernel. A node's score is how much of the shortest path\ntraffic out of those sources runs through it: for every node the\nsource reaches, each node in the middle of a shortest path to it\ntakes the fraction of those paths that pass through. The scores are\nthe unnormalized pair dependency sums, which is what the sampled GAP\nrun reports, and a source that names no row contributes nothing\nrather than failing the run.\n\nStored direction, because the reference this is measured against\nrelaxes out edges only. A pair joined twice is two shortest paths\nwhere a single edge is one, which is also what the reference counts:\npath counts are over edges and not over adjacencies, so a parallel\nedge really does double the traffic through the node it arrives at.\n\nNo predecessor lists. What the accumulation wants for a node is the\nnodes one level above it with an edge into it, and that is the\nnode's in edges filtered by level, which the backward CSR already\nholds. Building the lists during the forward pass instead would cost\nan allocation a node and a second copy of the graph in memory, for\nsomething the file can answer.\n\nBoth passes go level by level, and a level is walked in row order so\nthe nodes of one group are one run: a level decodes each group once\nrather than once per node in it. The accumulation is free to do that\nbecause the order inside a level does not matter to it, only that\nthe levels are taken from the far end back."
    },
    {
      "id": "zu::zu1::algo::bfs",
      "kind": "function",
      "name": "bfs",
      "signature": "fn bfs(db: &mut zu_zu1::file::Zu1File, reader: &mut zu_zu1::graph::GraphReader, source: u64) -> zu_common::Result<Vec<u64>>",
      "doc": "Breadth-first levels from `source` following stored edge direction,\nthe Graphalytics and Graph500 BFS kernel. Unreachable nodes get\n`u64::MAX`; the source is level zero."
    },
    {
      "id": "zu::zu1::algo::cdlp",
      "kind": "function",
      "name": "cdlp",
      "signature": "fn cdlp(db: &mut zu_zu1::file::Zu1File, reader: &mut zu_zu1::graph::GraphReader, rounds: usize) -> zu_common::Result<Vec<u64>>",
      "doc": "Community detection by label propagation: `rounds` synchronous\nsweeps, each node taking the label most common among its neighbors\nin both directions, a bidirectional pair voting once per direction,\nthe smallest label winning a tie. A node with no neighbors keeps\nwhat it has.\n\nLabels start at the node's original key rather than at its row,\nbecause the tie-break reads label values and a graph loaded with\n`REORDER` has rows the dataset never named. That makes the answer a\nstatement about ids, which is what the caller asked, and it cannot\nbe repaired after the fact the way a component id can: relabeling a\nfinished run would have to undo ties that were already decided the\nwrong way. A file with no key index is its own key space, so rows\nserve."
    },
    {
      "id": "zu::zu1::algo::lcc",
      "kind": "function",
      "name": "lcc",
      "signature": "fn lcc(db: &mut zu_zu1::file::Zu1File, reader: &mut zu_zu1::graph::GraphReader) -> zu_common::Result<Vec<f64>>",
      "doc": "Local clustering coefficient over the directed graph: for a node\nwhose neighbor set is the union of its out and in neighbors without\nitself, the number of stored edges running from one neighbor to\nanother over `d * (d - 1)`, the count of ordered pairs. Fewer than\ntwo neighbors scores zero, there being no pair to close.\n\nA pair of neighbors closes the triangle once however many edges run\nbetween them, and a self loop on a neighbor closes nothing. Counting\na parallel edge again would put the coefficient of a node above one,\nwhich is not a thing a coefficient does."
    },
    {
      "id": "zu::zu1::algo::louvain",
      "kind": "function",
      "name": "louvain",
      "signature": "fn louvain(db: &mut zu_zu1::file::Zu1File, reader: &mut zu_zu1::graph::GraphReader) -> zu_common::Result<Vec<u64>>",
      "doc": "Louvain community detection over the undirected view: repeated\nlocal-move sweeps in row order until a full sweep moves nothing,\nthen one aggregation level, repeated until aggregation stops\nmerging. Communities are relabeled to the smallest member row so\nthe output is deterministic."
    },
    {
      "id": "zu::zu1::algo::pagerank",
      "kind": "function",
      "name": "pagerank",
      "signature": "fn pagerank(db: &mut zu_zu1::file::Zu1File, reader: &mut zu_zu1::graph::GraphReader, iterations: usize) -> zu_common::Result<Vec<f64>>",
      "doc": "PageRank by power iteration over the forward CSR: fixed iteration\ncount, dangling mass redistributed uniformly, ranks summing to one.\nThis is the form that matches a published Graphalytics reference,\nwhich is itself the same loop run the same number of times."
    },
    {
      "id": "zu::zu1::algo::pagerank_converged",
      "kind": "function",
      "name": "pagerank_converged",
      "signature": "fn pagerank_converged(db: &mut zu_zu1::file::Zu1File, reader: &mut zu_zu1::graph::GraphReader) -> zu_common::Result<Vec<f64>>",
      "doc": "PageRank run until it settles rather than until a counter runs out:\nstops when no rank moves by more than [`PAGERANK_TOLERANCE`] in a\nround, and gives up at [`PAGERANK_MAX_ITERATIONS`]. This is what a\nquery asking for the pagerank of a graph wants, there being no\nfixed-round reference on the other side of it to match."
    },
    {
      "id": "zu::zu1::algo::sssp",
      "kind": "function",
      "name": "sssp",
      "signature": "fn sssp(db: &mut zu_zu1::file::Zu1File, reader: &mut zu_zu1::graph::GraphReader, source: u64) -> zu_common::Result<Vec<u64>>",
      "doc": "Single-source hop distances over the undirected view, SSSP under\nunit weights. Unreachable nodes get `u64::MAX`. The weighted form is\n[`sssp_weighted`], which follows stored direction instead, that\nbeing what the GAP and Graph500 kernels measure."
    },
    {
      "id": "zu::zu1::algo::sssp_weighted",
      "kind": "function",
      "name": "sssp_weighted",
      "signature": "fn sssp_weighted(db: &mut zu_zu1::file::Zu1File, reader: &mut zu_zu1::graph::GraphReader, source: u64, weights: &[u64]) -> zu_common::Result<Vec<u64>>",
      "doc": "Weighted single-source shortest paths from `source` following stored\nedge direction, the GAP SSSP and Graph500 K3 kernel. `weights[i]` is\nthe weight of edge `i` of the load order. Unreachable nodes get\n`u64::MAX`.\n\nThe load order is the order the forward lists lay out, group after\ngroup and list after list, so a walk that counts as it goes reads a\nweight at an index and never searches for one. That is also what\nkeeps a parallel edge honest: two edges over the same pair are two\nslots of the list with two weights, and only the count says which is\nwhich.\n\nDirection, not the undirected view, because the reference this is\nmeasured against relaxes out-edges only. The distances come out the\nsame whichever order the frontier settles in, so there is nothing\nhere for a tie to move."
    },
    {
      "id": "zu::zu1::algo::triangle_count",
      "kind": "function",
      "name": "triangle_count",
      "signature": "fn triangle_count(db: &mut zu_zu1::file::Zu1File, reader: &mut zu_zu1::graph::GraphReader) -> zu_common::Result<Vec<u64>>",
      "doc": "Undirected triangles, one count per node: how many triangles that\nnode is a corner of. The single figure the GAP TC kernel reports is\nthe sum over nodes divided by three, a triangle being counted once\nat each of its corners.\n\nDirection is dropped and a pair joined more than once is still one\nadjacency. A triangle is three nodes that all know each other, and\nhow many stored edges run between two of them does not make more of\nthem, which is the same rule [`lcc`] closes a neighbor pair by.\n\nThe work is the forward algorithm. Every node keeps only the\nneighbors above it, so a triangle is looked at from its smallest\ncorner and found once rather than six times, and the lists being\nintersected are half the length. That adjacency is built once into\none flat `u32` array, four bytes a stored edge, because the shape\nthis kernel wants from the reader is the list of a node somewhere\nelse in the graph once per edge, and the group the reader decodes to\nanswer that is thrown away by the question after it. Two sequential\nsweeps to build it cost two group decodes each; asking as it goes\nwould cost one per edge."
    },
    {
      "id": "zu::zu1::algo::wcc",
      "kind": "function",
      "name": "wcc",
      "signature": "fn wcc(db: &mut zu_zu1::file::Zu1File, reader: &mut zu_zu1::graph::GraphReader) -> zu_common::Result<Vec<u64>>"
    },
    {
      "id": "zu::zu1::cache",
      "kind": "module",
      "name": "cache",
      "doc": "The block cache under every read path (perf/04 section 1).\n\nBefore this module existed every block read heap-allocated a whole\nblock and hit the file, so a warm scan paid malloc plus memcpy plus a\nsyscall per block per touch. Now [`Zu1File`](crate::file::Zu1File)\npins blocks through a shared [`BlockCache`]: a hit is a shard lock,\na map probe, and an `Arc` clone, no allocation and no copy; a miss\nreads the whole block once into a recycled frame. A frame is one\nblock of `Arc<Vec<u8>>`, so a [`PinnedBlock`] guard keeps its frame\nalive even after eviction drops it from the map, which is what makes\nthe guard safe to hold across other cache traffic with no\ninvalidation protocol: committed blocks never change in place, and\nthe one path that rewrites a block, [`Zu1File::write_block`], drops\nthe cached frame so the next read sees the new bytes.\n\nEviction keeps the SIEVE retention rule: every hit sets a visited\nbit, the hand clears visited slots and takes the first unvisited,\nunpinned one. The slots live in a fixed per-shard array scanned in\nrotation rather than SIEVE's insertion-order queue; on block traffic\nthe difference is the scan order of survivors, not what survives.\nWhen every slot in a shard is pinned the read is served from a\ntransient frame that bypasses the pool, so the budget bounds the\ncache itself and pin storms degrade to today's allocate-per-read\nbehavior instead of deadlocking."
    },
    {
      "id": "zu::zu1::cache::BlockCache",
      "kind": "struct",
      "name": "BlockCache",
      "doc": "The shared block cache. Interior locking throughout, so handles\nforked off one [`Zu1File`](crate::file::Zu1File) share it across\nthreads."
    },
    {
      "id": "zu::zu1::cache::BlockCache::new",
      "kind": "method",
      "name": "new",
      "of": "zu::zu1::cache::BlockCache",
      "signature": "fn new(budget: usize) -> Self",
      "doc": "A cache holding at most `budget` bytes of block frames, at least\none frame per shard so tiny budgets still cache something."
    },
    {
      "id": "zu::zu1::cache::BlockCache::stats",
      "kind": "method",
      "name": "stats",
      "of": "zu::zu1::cache::BlockCache",
      "signature": "fn stats(&self) -> CacheStats"
    },
    {
      "id": "zu::zu1::cache::CacheStats",
      "kind": "struct",
      "name": "CacheStats",
      "doc": "Hit, miss, and eviction counts since the cache was built. Tests use\nthese to prove a warm read never reached the file and that a\nbounded-budget run actually evicted."
    },
    {
      "id": "zu::zu1::cache::CacheStats::evictions",
      "kind": "field",
      "name": "evictions",
      "of": "zu::zu1::cache::CacheStats",
      "signature": "u64"
    },
    {
      "id": "zu::zu1::cache::CacheStats::hits",
      "kind": "field",
      "name": "hits",
      "of": "zu::zu1::cache::CacheStats",
      "signature": "u64"
    },
    {
      "id": "zu::zu1::cache::CacheStats::misses",
      "kind": "field",
      "name": "misses",
      "of": "zu::zu1::cache::CacheStats",
      "signature": "u64"
    },
    {
      "id": "zu::zu1::cache::DecodedPool",
      "kind": "struct",
      "name": "DecodedPool",
      "doc": "A budgeted pool of decoded objects keyed by where in the file the\nsegment they decode starts, the decode-on-touch tier of perf/04\nsection 2. The key is a byte offset rather than a block pointer\nbecause a block holds more than one packed segment, and two\nsegments sharing a block are not the same segment.\n\nA dropped table's keys stop being touched and age out;\nthe one real hazard is the free list recycling a pointer into a new\nsegment, which `Zu1File::write_block` covers by calling\n[`Self::remove_block`] on every block it rewrites. Values are `Arc`-handed:\neviction drops the map's reference and any reader still holding the\n`Arc` keeps its snapshot alive, which is safe because decoded\nobjects are immutable under MVCC.\n\nEviction is plain LRU by touch tick, scanned on demand. Pools hold\ntens of entries, whole decoded groups and directories, so the scan\ncosts less than the bookkeeping a linked LRU would add."
    },
    {
      "id": "zu::zu1::cache::DecodedPool::get",
      "kind": "method",
      "name": "get",
      "of": "zu::zu1::cache::DecodedPool",
      "signature": "fn get(&self, key: u64) -> Option<Arc<T>>",
      "doc": "The pooled object for `key`, or `None` on a miss."
    },
    {
      "id": "zu::zu1::cache::DecodedPool::insert",
      "kind": "method",
      "name": "insert",
      "of": "zu::zu1::cache::DecodedPool",
      "signature": "fn insert(&self, key: u64, value: Arc<T>)",
      "doc": "Pools `value` under `key`, evicting least-recently-touched\nentries until the budget holds it. Racing inserts keep the\nfirst entry; both racers already hold a usable `Arc`."
    },
    {
      "id": "zu::zu1::cache::DecodedPool::new",
      "kind": "method",
      "name": "new",
      "of": "zu::zu1::cache::DecodedPool",
      "signature": "fn new(budget: usize) -> Self",
      "doc": "A pool holding at most `budget` bytes of decoded objects. A\nsingle object above the budget is still admitted alone, so a\npool cannot starve its own hot path."
    },
    {
      "id": "zu::zu1::cache::DecodedPool::remove_block",
      "kind": "method",
      "name": "remove_block",
      "of": "zu::zu1::cache::DecodedPool",
      "signature": "fn remove_block(&self, ptr: BlockPtr)",
      "doc": "Drops every entry whose segment starts in the block at `ptr`,\nthe write-invalidation hook: the free list recycles block\npointers, so a rewrite of a segment's first block can give a new\nsegment an old key, and one block can be the first of several\npacked segments. Readers still holding the old `Arc` keep their\nsnapshot, same as eviction."
    },
    {
      "id": "zu::zu1::cache::DecodedPool::stats",
      "kind": "method",
      "name": "stats",
      "of": "zu::zu1::cache::DecodedPool",
      "signature": "fn stats(&self) -> CacheStats"
    },
    {
      "id": "zu::zu1::cache::PinnedBlock",
      "kind": "struct",
      "name": "PinnedBlock",
      "doc": "A pinned block frame. Holding it keeps the frame alive; the\ncache may evict the block meanwhile, which only means the next\nreader refills. Dereferences to the full block's bytes."
    },
    {
      "id": "zu::zu1::cache::PoolBytes",
      "kind": "trait",
      "name": "PoolBytes",
      "doc": "Memory footprint of a pooled decoded object, what a\n[`DecodedPool`]'s budget counts."
    },
    {
      "id": "zu::zu1::cache::PoolBytes::pool_bytes",
      "kind": "method",
      "name": "pool_bytes",
      "of": "zu::zu1::cache::PoolBytes",
      "signature": "fn pool_bytes(&self) -> usize"
    },
    {
      "id": "zu::zu1::cache::SegmentBytes",
      "kind": "enum",
      "name": "SegmentBytes",
      "doc": "Payload bytes handed out of a segment read: a borrowed window into\none pinned block when the span stays inside it, which is the common\ncase since writers prefer contiguous runs, or an assembled copy when\nthe span crosses blocks. Dereferences to the span's bytes either\nway, so readers do not care which they got."
    },
    {
      "id": "zu::zu1::cache::SegmentBytes::Owned",
      "kind": "variant",
      "name": "Owned",
      "of": "zu::zu1::cache::SegmentBytes",
      "signature": "Owned(Vec<u8>)"
    },
    {
      "id": "zu::zu1::cache::SegmentBytes::Pinned",
      "kind": "variant",
      "name": "Pinned",
      "of": "zu::zu1::cache::SegmentBytes",
      "signature": "Pinned { block: PinnedBlock, start: usize, len: usize }"
    },
    {
      "id": "zu::zu1::catalog",
      "kind": "module",
      "name": "catalog",
      "doc": "The catalog and the table index, the two meta chains that make a zu1\nfile self-describing.\n\nThe catalog behind `catalog_root` holds table definitions: node tables\nwith their row domain, rel tables with their FROM and TO node tables\nand edge count. The table index behind `table_index_root` maps a rel\ntable id to the root of its group directory chain, so DDL rewrites\nonly the catalog while a bulk load rewrites only the index entry it\ntouches. Both encodings are version-prefixed and little-endian per\n`docs/04-storage-zu1-format.md` §1.\n\nCatalog layout: `version: u16`, `node_table_count: u32`,\n`rel_table_count: u32`, `label_count: u32`, then per node table\n`id: u32`, `name_len: u16` + UTF-8 bytes, `node_count: u64`,\n`declared_label_count: u16` and that many `label: u16`, then per rel\ntable `id: u32`, name, `from: u32`, `to: u32`, `edge_count: u64`,\nthen the label dictionary as `label_count` names, then\n`graph_type_count: u32` and that many graph types.\n\nA graph type is a name, a closed flag, and its element types: each a\nname, a kind byte, a flag byte, a label set, a key label set behind\nthe byte that says which of the three kinds it is, the two endpoint\nnames for an edge type, and the properties it declares as a name, a\ntype code, and an optionality byte. Properties hang off the element\ntype rather than off the graph, which is what pays for the three\nrelaxed consistency features at once (GG24 to GG26): two element\ntypes may share a key label set and disagree about what a property\nof the same name holds.\n\nThe dictionary is per file and a label is its position in it, which\nis what makes a label set a bitset: a node's labels are a word with\none bit per dictionary entry, and a pattern's labels are a mask over\nthe same word. A node table declares which labels its rows may\ncarry, the first of which is the table's own name and the one every\nrow carries, so a label a table never declares prunes that table at\nplan time rather than being tested a row at a time.\n\nTable index layout: `version: u16`, `entry_count: u32`, then per\nentry `table_id: u32`, `directory_root: u64`."
    },
    {
      "id": "zu::zu1::catalog::Catalog",
      "kind": "struct",
      "name": "Catalog",
      "doc": "The table definitions of one zu1 file. Names share a single\nnamespace across both kinds, so a rel table cannot shadow a node\ntable.\n\nA file always has the root schema and the home graph, whatever else\nit holds: they are what a load with nothing said about where writes\ninto, and dropping the home graph empties it rather than leaving the\nfile with nowhere to put the next table."
    },
    {
      "id": "zu::zu1::catalog::Catalog::add_graph",
      "kind": "method",
      "name": "add_graph",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn add_graph(&mut self, name: &str, schema: &str, graph_type: GraphTypeOf) -> Result<u32>",
      "doc": "Adds a graph (GC04) and answers its id, refusing a name its\nschema already holds and a schema the file does not."
    },
    {
      "id": "zu::zu1::catalog::Catalog::add_graph_type",
      "kind": "method",
      "name": "add_graph_type",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn add_graph_type(&mut self, ty: GraphType) -> Result<()>",
      "doc": "Adds a graph type, refusing a name the file already holds and a\ntype its own rules reject. The labels it names must already be\nin the dictionary, since a type describes the graph it is for\nrather than adding to it."
    },
    {
      "id": "zu::zu1::catalog::Catalog::add_schema",
      "kind": "method",
      "name": "add_schema",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn add_schema(&mut self, path: &str) -> Result<()>",
      "doc": "Adds a schema (GC01), refusing a path that is not absolute and\none the file already holds."
    },
    {
      "id": "zu::zu1::catalog::Catalog::check_graph",
      "kind": "method",
      "name": "check_graph",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn check_graph(&self, schema: &str, graph_type: &GraphTypeOf) -> Result<()>",
      "doc": "Everything `add_graph` refuses a graph for other than a name its\nschema already holds.\n\n`CREATE OR REPLACE GRAPH` frees the blocks the old graph held\nbefore the new one is added, and a refusal after that point would\nleave a file holding neither, so the caller asks first and frees\nnothing when the answer is no."
    },
    {
      "id": "zu::zu1::catalog::Catalog::closed_type_of",
      "kind": "method",
      "name": "closed_type_of",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn closed_type_of(&self, graph: u32) -> Option<&GraphType>",
      "doc": "The closed graph type one graph is of, which is what a write has\nto keep (GG02).\n\n`None` for a graph created with no type and for one whose type\nis open, because both of those describe rather than promise: an\nopen type admits elements it says nothing about, so there is\nnothing a write can break. A named type the file no longer holds\nis `None` too, since a promise nothing states is not one a write\ncan be measured against, and the statement that dropped it is\nwhere that would be caught."
    },
    {
      "id": "zu::zu1::catalog::Catalog::copy_graph_tables",
      "kind": "method",
      "name": "copy_graph_tables",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn copy_graph_tables(&mut self, source: u32, target: u32) -> Result<Vec<(u32, u32, ElementKind)>>",
      "doc": "Copies the table definitions of one graph into another, which is\nthe catalog half of `CREATE GRAPH ... AS COPY OF` (GC04).\n\nEach copy keeps the name it had, which is what makes the copy a\ncopy: a query written against the source runs against the copy\nonce `USE` names it. A name belongs to a graph rather than to\nthe file, so both graphs holding a `person` is two tables and\nnot a clash. What the copies do not keep is their ids, because\nan id is what the table index addresses storage by and the copy\ngets storage of its own.\n\nAnswers the source id, the copy's id and the kind of each table,\nwhich is what [`crate::graph::copy_graph_storage`] needs to copy\nthe blocks those ids stand for."
    },
    {
      "id": "zu::zu1::catalog::Catalog::create_node_in",
      "kind": "method",
      "name": "create_node_in",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn create_node_in(&mut self, graph: u32, name: &str) -> Result<u32>",
      "doc": "Creates an empty node table in one graph and returns its id.\n\nThis is `upsert_node` for a caller that says which graph rather\nthan meaning the home one, and it creates rather than updating:\na name the graph already holds is a table the caller meant to\nfind, not one to make twice. What it is for is the table an\n`INSERT` names by a label nothing declared, which lands in the\ngraph the statement runs against."
    },
    {
      "id": "zu::zu1::catalog::Catalog::declare_label",
      "kind": "method",
      "name": "declare_label",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn declare_label(&mut self, table: u32, label: &str) -> Result<u16>",
      "doc": "Declares that rows of `table` may carry `label`, interning the\nname if the graph has not seen it, and returns its id. Declaring\nis what a plan time prune reads: a table that never declared a\nlabel cannot hold a row with it."
    },
    {
      "id": "zu::zu1::catalog::Catalog::decode",
      "kind": "method",
      "name": "decode",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn decode(bytes: &[u8]) -> Result<Self>"
    },
    {
      "id": "zu::zu1::catalog::Catalog::drop_graph",
      "kind": "method",
      "name": "drop_graph",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn drop_graph(&mut self, id: u32) -> bool",
      "doc": "Drops a graph and every table in it, answering whether there was\none. The storage those tables held is the caller's to free; this\nis the catalog half of `DROP GRAPH`."
    },
    {
      "id": "zu::zu1::catalog::Catalog::drop_graph_type",
      "kind": "method",
      "name": "drop_graph_type",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn drop_graph_type(&mut self, name: &str) -> bool",
      "doc": "Drops a graph type, answering whether there was one."
    },
    {
      "id": "zu::zu1::catalog::Catalog::drop_schema",
      "kind": "method",
      "name": "drop_schema",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn drop_schema(&mut self, path: &str) -> bool",
      "doc": "Drops a schema, answering whether there was one. A schema that\nstill holds graphs is refused by the caller: this is the\nprimitive and the statement is where RESTRICT lives."
    },
    {
      "id": "zu::zu1::catalog::Catalog::encode",
      "kind": "method",
      "name": "encode",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn encode(&self) -> Vec<u8>"
    },
    {
      "id": "zu::zu1::catalog::Catalog::graph",
      "kind": "method",
      "name": "graph",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn graph(&self, schema: &str, name: &str) -> Option<&GraphDef>"
    },
    {
      "id": "zu::zu1::catalog::Catalog::graph_by_id",
      "kind": "method",
      "name": "graph_by_id",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn graph_by_id(&self, id: u32) -> Option<&GraphDef>"
    },
    {
      "id": "zu::zu1::catalog::Catalog::graph_tables",
      "kind": "method",
      "name": "graph_tables",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn graph_tables(&self, graph: u32) -> Vec<(u32, ElementKind)>",
      "doc": "The tables a graph holds, node tables first. Dropping the graph\nhands their blocks back, so the caller reads this before the\ncatalog forgets them."
    },
    {
      "id": "zu::zu1::catalog::Catalog::graph_type",
      "kind": "method",
      "name": "graph_type",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn graph_type(&self, name: &str) -> Option<&GraphType>"
    },
    {
      "id": "zu::zu1::catalog::Catalog::graph_types",
      "kind": "method",
      "name": "graph_types",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn graph_types(&self) -> &[GraphType]",
      "doc": "The graph types this file holds."
    },
    {
      "id": "zu::zu1::catalog::Catalog::graphs",
      "kind": "method",
      "name": "graphs",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn graphs(&self) -> &[GraphDef]",
      "doc": "The graphs this file holds."
    },
    {
      "id": "zu::zu1::catalog::Catalog::grow_node",
      "kind": "method",
      "name": "grow_node",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn grow_node(&mut self, id: u32, node_count: u64) -> Result<()>",
      "doc": "Grows the row domain of the table with this id.\n\nA fold knows which table it appended to, and the name is not\nenough to say which one that is: two graphs in one file each\nhold their own `person`, so growing by name would grow the home\ngraph's table however far from home the rows landed."
    },
    {
      "id": "zu::zu1::catalog::Catalog::has_schema",
      "kind": "method",
      "name": "has_schema",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn has_schema(&self, path: &str) -> bool"
    },
    {
      "id": "zu::zu1::catalog::Catalog::home_graph_id",
      "kind": "method",
      "name": "home_graph_id",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn home_graph_id(&self) -> u32",
      "doc": "The id of the home graph, which is the first graph a file has\nand keeps its id for the file's life. A file whose home graph\nwas dropped and not yet put back reads as the id the first one\nhad, which is the id `ensure_home` gives it back."
    },
    {
      "id": "zu::zu1::catalog::Catalog::infer_graph_type",
      "kind": "method",
      "name": "infer_graph_type",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn infer_graph_type(&self, name: &str, graph: u32) -> Result<GraphType>",
      "doc": "A closed graph type inferred from the tables a graph holds\n(GG04): one node element type per node table keyed on the\ntable's own label, one edge element type per rel table between\nthem. It reads the catalog and never the data, so it costs\nnothing on a large graph."
    },
    {
      "id": "zu::zu1::catalog::Catalog::intern_label",
      "kind": "method",
      "name": "intern_label",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn intern_label(&mut self, name: &str) -> Result<u16>",
      "doc": "Interns a label name and returns its id, which is stable for the\nlife of the file because ids are positions and nothing is ever\nremoved from the dictionary."
    },
    {
      "id": "zu::zu1::catalog::Catalog::label_id",
      "kind": "method",
      "name": "label_id",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn label_id(&self, name: &str) -> Option<u16>"
    },
    {
      "id": "zu::zu1::catalog::Catalog::label_name",
      "kind": "method",
      "name": "label_name",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn label_name(&self, id: u16) -> Option<&str>"
    },
    {
      "id": "zu::zu1::catalog::Catalog::labels",
      "kind": "method",
      "name": "labels",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn labels(&self) -> &[String]",
      "doc": "The label dictionary, a label's id being its position."
    },
    {
      "id": "zu::zu1::catalog::Catalog::load",
      "kind": "method",
      "name": "load",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn load(db: &mut Zu1File) -> Result<Self>",
      "doc": "Loads the committed catalog; a `NULL_BLOCK` root reads as empty."
    },
    {
      "id": "zu::zu1::catalog::Catalog::node_by_id",
      "kind": "method",
      "name": "node_by_id",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn node_by_id(&self, id: u32) -> Option<&NodeTable>"
    },
    {
      "id": "zu::zu1::catalog::Catalog::node_by_name",
      "kind": "method",
      "name": "node_by_name",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn node_by_name(&self, name: &str) -> Option<&NodeTable>",
      "doc": "The node table of that name in the home graph, which is the\ngraph every load writes into and the one a statement means when\nnothing says otherwise. A name is a name in a graph and not in a\nfile, so a name two graphs both use is two tables and this\nanswers the home one."
    },
    {
      "id": "zu::zu1::catalog::Catalog::node_in",
      "kind": "method",
      "name": "node_in",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn node_in(&self, graph: u32, name: &str) -> Option<&NodeTable>",
      "doc": "The node table a graph calls that name."
    },
    {
      "id": "zu::zu1::catalog::Catalog::node_tables",
      "kind": "method",
      "name": "node_tables",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn node_tables(&self) -> &[NodeTable]"
    },
    {
      "id": "zu::zu1::catalog::Catalog::rel_by_id",
      "kind": "method",
      "name": "rel_by_id",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn rel_by_id(&self, id: u32) -> Option<&RelTable>"
    },
    {
      "id": "zu::zu1::catalog::Catalog::rel_by_name",
      "kind": "method",
      "name": "rel_by_name",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn rel_by_name(&self, name: &str) -> Option<&RelTable>"
    },
    {
      "id": "zu::zu1::catalog::Catalog::rel_in",
      "kind": "method",
      "name": "rel_in",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn rel_in(&self, graph: u32, name: &str) -> Option<&RelTable>"
    },
    {
      "id": "zu::zu1::catalog::Catalog::rel_tables",
      "kind": "method",
      "name": "rel_tables",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn rel_tables(&self) -> &[RelTable]"
    },
    {
      "id": "zu::zu1::catalog::Catalog::schemas",
      "kind": "method",
      "name": "schemas",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn schemas(&self) -> &[String]",
      "doc": "The schemas this file holds, the root schema always among them\nunless it was dropped."
    },
    {
      "id": "zu::zu1::catalog::Catalog::set_edge_count",
      "kind": "method",
      "name": "set_edge_count",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn set_edge_count(&mut self, id: u32, edge_count: u64) -> Result<()>",
      "doc": "Records how many edges the table with this id holds, which a\nfold knows because it just rebuilt it. By id for the same reason\nas [`Self::grow_node`]."
    },
    {
      "id": "zu::zu1::catalog::Catalog::store",
      "kind": "method",
      "name": "store",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn store(&self, db: &mut Zu1File) -> Result<()>",
      "doc": "Publishes the catalog: the old chain goes back to the free list,\nthe new one is written into free space, and the header flip\nmakes it the committed one. Nothing else in the file moves,\nwhich is what makes a catalog change cheap however large the\ngraph under it is."
    },
    {
      "id": "zu::zu1::catalog::Catalog::tables_with_label",
      "kind": "method",
      "name": "tables_with_label",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn tables_with_label(&self, label: u16) -> Vec<u32>",
      "doc": "Every node table that declares `label`, which is the scan set a\npattern naming that label alone runs over."
    },
    {
      "id": "zu::zu1::catalog::Catalog::upsert_node",
      "kind": "method",
      "name": "upsert_node",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn upsert_node(&mut self, name: &str, node_count: u64) -> Result<u32>",
      "doc": "Creates or updates a node table and returns its id. The row\ndomain only grows: a load declaring fewer nodes than an earlier\none leaves the count alone, so rel tables already built over the\nlarger domain stay valid."
    },
    {
      "id": "zu::zu1::catalog::Catalog::upsert_rel",
      "kind": "method",
      "name": "upsert_rel",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn upsert_rel(&mut self, name: &str, from: u32, to: u32, edge_count: u64) -> Result<u32>",
      "doc": "Creates or updates a rel table and returns its id. The edges are\ndirected, which is what a rel table has always held; the\nundirected form is `upsert_rel_as`."
    },
    {
      "id": "zu::zu1::catalog::Catalog::upsert_rel_as",
      "kind": "method",
      "name": "upsert_rel_as",
      "of": "zu::zu1::catalog::Catalog",
      "signature": "fn upsert_rel_as(&mut self, name: &str, from: u32, to: u32, edge_count: u64, undirected: bool) -> Result<u32>",
      "doc": "Creates or updates a rel table, saying whether its edges have a\ndirection (GH02).\n\nWhether the edges are directed belongs to the table rather than\nto each edge, because it is a statement about the element type:\na graph type says `(:Peer)-[:FRIEND]-(:Peer)` once and every\nedge of that type is the same way round, or is not one way round\nat all."
    },
    {
      "id": "zu::zu1::catalog::ElementKind",
      "kind": "enum",
      "name": "ElementKind",
      "doc": "Whether an element type describes nodes or edges."
    },
    {
      "id": "zu::zu1::catalog::ElementKind::Edge",
      "kind": "variant",
      "name": "Edge",
      "of": "zu::zu1::catalog::ElementKind",
      "signature": "Edge"
    },
    {
      "id": "zu::zu1::catalog::ElementKind::Node",
      "kind": "variant",
      "name": "Node",
      "of": "zu::zu1::catalog::ElementKind",
      "signature": "Node"
    },
    {
      "id": "zu::zu1::catalog::ElementType",
      "kind": "struct",
      "name": "ElementType",
      "doc": "One element type of a graph type (GG20 to GG26).\n\nTwo element types may share a key label set and differ in their\nproperties (GG24) or, for edges, in their endpoints (GG25). Nothing\nhere forbids that, on purpose: the shape that would have forbidden\nit is a per graph property table, and the whole point of hanging\nproperties off the element type is that the relaxed features cost\nthe strict case nothing."
    },
    {
      "id": "zu::zu1::catalog::ElementType::edge",
      "kind": "method",
      "name": "edge",
      "of": "zu::zu1::catalog::ElementType",
      "signature": "fn edge(name: &str, labels: Vec<u16>, from: &str, to: &str) -> Self",
      "doc": "An edge type between two node types, key label set inferred."
    },
    {
      "id": "zu::zu1::catalog::ElementType::from",
      "kind": "field",
      "name": "from",
      "of": "zu::zu1::catalog::ElementType",
      "signature": "Option<String>",
      "doc": "Edge types only: the element type names of the two endpoints."
    },
    {
      "id": "zu::zu1::catalog::ElementType::key_labels",
      "kind": "field",
      "name": "key_labels",
      "of": "zu::zu1::catalog::ElementType",
      "signature": "KeyLabels"
    },
    {
      "id": "zu::zu1::catalog::ElementType::kind",
      "kind": "field",
      "name": "kind",
      "of": "zu::zu1::catalog::ElementType",
      "signature": "ElementKind"
    },
    {
      "id": "zu::zu1::catalog::ElementType::label_mask",
      "kind": "method",
      "name": "label_mask",
      "of": "zu::zu1::catalog::ElementType",
      "signature": "fn label_mask(&self) -> u64"
    },
    {
      "id": "zu::zu1::catalog::ElementType::labels",
      "kind": "field",
      "name": "labels",
      "of": "zu::zu1::catalog::ElementType",
      "signature": "Vec<u16>",
      "doc": "The labels an element of this type carries, as dictionary ids."
    },
    {
      "id": "zu::zu1::catalog::ElementType::name",
      "kind": "field",
      "name": "name",
      "of": "zu::zu1::catalog::ElementType",
      "signature": "String"
    },
    {
      "id": "zu::zu1::catalog::ElementType::node",
      "kind": "method",
      "name": "node",
      "of": "zu::zu1::catalog::ElementType",
      "signature": "fn node(name: &str, labels: Vec<u16>) -> Self",
      "doc": "A node type carrying `labels`, with the key label set inferred\nfrom them, which is what a definition that names no key means."
    },
    {
      "id": "zu::zu1::catalog::ElementType::open",
      "kind": "field",
      "name": "open",
      "of": "zu::zu1::catalog::ElementType",
      "signature": "bool",
      "doc": "GG01 at the element level: an open type permits properties it\nnever declared."
    },
    {
      "id": "zu::zu1::catalog::ElementType::properties",
      "kind": "field",
      "name": "properties",
      "of": "zu::zu1::catalog::ElementType",
      "signature": "Vec<PropertyType>"
    },
    {
      "id": "zu::zu1::catalog::ElementType::property",
      "kind": "method",
      "name": "property",
      "of": "zu::zu1::catalog::ElementType",
      "signature": "fn property(&self, name: &str) -> Option<&PropertyType>",
      "doc": "The type a property of this name has here, `None` when the type\ndoes not declare one. An open type may still carry it."
    },
    {
      "id": "zu::zu1::catalog::ElementType::selection_mask",
      "kind": "method",
      "name": "selection_mask",
      "of": "zu::zu1::catalog::ElementType",
      "signature": "fn selection_mask(&self) -> u64",
      "doc": "The mask an element's label set must contain for this type to\ndescribe it. A type with no key label set is selected by its\nwhole label set, which is the fallback GG23 asks for."
    },
    {
      "id": "zu::zu1::catalog::ElementType::to",
      "kind": "field",
      "name": "to",
      "of": "zu::zu1::catalog::ElementType",
      "signature": "Option<String>"
    },
    {
      "id": "zu::zu1::catalog::ElementType::undirected",
      "kind": "field",
      "name": "undirected",
      "of": "zu::zu1::catalog::ElementType",
      "signature": "bool",
      "doc": "GH02: whether an edge of this type has a direction. It is a\nproperty of the type rather than of the pattern, which is why\nit lives here and not in the query."
    },
    {
      "id": "zu::zu1::catalog::ElementType::with_key",
      "kind": "method",
      "name": "with_key",
      "of": "zu::zu1::catalog::ElementType",
      "signature": "fn with_key(self, key: Vec<u16>) -> Self",
      "doc": "The same type with the key label set written out (GG21)."
    },
    {
      "id": "zu::zu1::catalog::ElementType::with_property",
      "kind": "method",
      "name": "with_property",
      "of": "zu::zu1::catalog::ElementType",
      "signature": "fn with_property(self, name: &str, ty: LogicalType, optional: bool) -> Self"
    },
    {
      "id": "zu::zu1::catalog::ElementType::without_key",
      "kind": "method",
      "name": "without_key",
      "of": "zu::zu1::catalog::ElementType",
      "signature": "fn without_key(self) -> Self",
      "doc": "The same type with no key label set at all (GG23)."
    },
    {
      "id": "zu::zu1::catalog::GraphDef",
      "kind": "struct",
      "name": "GraphDef",
      "doc": "A graph the catalog holds (GC04): a name in a schema, the type it is\nof, and the tables that are its contents.\n\nThe tables are not listed here. A table names the graph it belongs\nto instead, which makes the contents of a graph a filter over the\ntables and keeps one table from being in two graphs."
    },
    {
      "id": "zu::zu1::catalog::GraphDef::graph_type",
      "kind": "field",
      "name": "graph_type",
      "of": "zu::zu1::catalog::GraphDef",
      "signature": "GraphTypeOf"
    },
    {
      "id": "zu::zu1::catalog::GraphDef::id",
      "kind": "field",
      "name": "id",
      "of": "zu::zu1::catalog::GraphDef",
      "signature": "u32"
    },
    {
      "id": "zu::zu1::catalog::GraphDef::name",
      "kind": "field",
      "name": "name",
      "of": "zu::zu1::catalog::GraphDef",
      "signature": "String"
    },
    {
      "id": "zu::zu1::catalog::GraphDef::schema",
      "kind": "field",
      "name": "schema",
      "of": "zu::zu1::catalog::GraphDef",
      "signature": "String",
      "doc": "The schema this graph lives in, an absolute directory path."
    },
    {
      "id": "zu::zu1::catalog::GraphType",
      "kind": "struct",
      "name": "GraphType",
      "doc": "A graph type: the element types a graph's elements may have, and\nwhether that list is the whole of it (GG01, GG02)."
    },
    {
      "id": "zu::zu1::catalog::GraphType::closed",
      "kind": "field",
      "name": "closed",
      "of": "zu::zu1::catalog::GraphType",
      "signature": "bool",
      "doc": "GG02. A closed type is a guarantee rather than a description:\nevery element matches a declared element type, so the optimizer\nmay resolve a property to a column without asking the row."
    },
    {
      "id": "zu::zu1::catalog::GraphType::element",
      "kind": "method",
      "name": "element",
      "of": "zu::zu1::catalog::GraphType",
      "signature": "fn element(&self, name: &str) -> Option<&ElementType>"
    },
    {
      "id": "zu::zu1::catalog::GraphType::elements",
      "kind": "field",
      "name": "elements",
      "of": "zu::zu1::catalog::GraphType",
      "signature": "Vec<ElementType>"
    },
    {
      "id": "zu::zu1::catalog::GraphType::holder",
      "kind": "method",
      "name": "holder",
      "of": "zu::zu1::catalog::GraphType",
      "signature": "fn holder(&self, kind: ElementKind, labels: u64) -> Option<&ElementType>",
      "doc": "The element type an element carrying `labels` belongs to, if one\nof them describes it. This is what a closed graph type promises\nof every element it holds (GG02).\n\nBelonging is two things at once: the type's key label set picks\nthe element out, and every label the element carries is one the\ntype declares. Selection alone would let an element grow a label\nno type mentions and still count as described, which is the\npromise a closed type is there to make.\n\nMore than one type may describe it, and the first is answered,\nbecause what the caller asks is whether any does."
    },
    {
      "id": "zu::zu1::catalog::GraphType::name",
      "kind": "field",
      "name": "name",
      "of": "zu::zu1::catalog::GraphType",
      "signature": "String"
    },
    {
      "id": "zu::zu1::catalog::GraphType::open",
      "kind": "method",
      "name": "open",
      "of": "zu::zu1::catalog::GraphType",
      "signature": "fn open(name: &str) -> Self",
      "doc": "An open graph type, which is what a graph with no type declared\nfor it has."
    },
    {
      "id": "zu::zu1::catalog::GraphType::types_for",
      "kind": "method",
      "name": "types_for",
      "of": "zu::zu1::catalog::GraphType",
      "signature": "fn types_for(&self, kind: ElementKind, labels: u64) -> Vec<&ElementType>",
      "doc": "Every element type an element carrying `labels` could have.\n\nMore than one is not an error: GG24 and GG25 are exactly the\ncase where two types share a key label set and differ in what\nhangs off it, and the caller picks by what it is doing. An\nempty key label set is contained in every label set and so\nselects every element of its kind, which is what an edge type\ninferred from a rel table has and why the caller narrows those\nby their endpoints."
    },
    {
      "id": "zu::zu1::catalog::GraphType::with",
      "kind": "method",
      "name": "with",
      "of": "zu::zu1::catalog::GraphType",
      "signature": "fn with(self, element: ElementType) -> Self"
    },
    {
      "id": "zu::zu1::catalog::GraphTypeOf",
      "kind": "enum",
      "name": "GraphTypeOf",
      "doc": "The type a graph is of (GG01 to GG04).\n\nISO lets a graph be created with no type at all, with the name of a\ngraph type the catalog holds, or with a type written inline in the\nstatement. A type written inline has no name of its own and is no\ncatalog object, so it lives here rather than in the file's list of\ngraph types: `CREATE GRAPH g { (:Person) }` declares no graph type\ncalled anything."
    },
    {
      "id": "zu::zu1::catalog::GraphTypeOf::Inline",
      "kind": "variant",
      "name": "Inline",
      "of": "zu::zu1::catalog::GraphTypeOf",
      "signature": "Inline(GraphType)",
      "doc": "GG03: written inline, so it is anonymous and belongs to the one\ngraph created with it."
    },
    {
      "id": "zu::zu1::catalog::GraphTypeOf::Named",
      "kind": "variant",
      "name": "Named",
      "of": "zu::zu1::catalog::GraphTypeOf",
      "signature": "Named(String)",
      "doc": "The name of a graph type this file holds."
    },
    {
      "id": "zu::zu1::catalog::GraphTypeOf::Open",
      "kind": "variant",
      "name": "Open",
      "of": "zu::zu1::catalog::GraphTypeOf",
      "signature": "Open",
      "doc": "GG01: any graph. Every element type is allowed, which is what a\nzu1 file has always been."
    },
    {
      "id": "zu::zu1::catalog::HOME_GRAPH",
      "kind": "constant",
      "name": "HOME_GRAPH",
      "signature": "&str",
      "doc": "The graph a zu1 file has always held, and the one a load with no\ngraph named writes into. ISO calls the graph a session works on\nwithout saying so its home graph, which is where the name comes from."
    },
    {
      "id": "zu::zu1::catalog::HOME_GRAPH_ID",
      "kind": "constant",
      "name": "HOME_GRAPH_ID",
      "signature": "u32",
      "doc": "The id of that graph. Ids are handed out in order and never reused,\nso the first graph a file has is always this one."
    },
    {
      "id": "zu::zu1::catalog::KeyLabels",
      "kind": "enum",
      "name": "KeyLabels",
      "doc": "The label subset that picks an element type out of a graph type\n(GG21 to GG23).\n\nISO lets a type declare one, lets it be inferred from the whole\nlabel set when it is not declared, and lets an open graph type hold\na type with none at all. The three cases select differently at\ninsert time, so they are three cases here and not one `Option`."
    },
    {
      "id": "zu::zu1::catalog::KeyLabels::Declared",
      "kind": "variant",
      "name": "Declared",
      "of": "zu::zu1::catalog::KeyLabels",
      "signature": "Declared(Vec<u16>)",
      "doc": "GG21: written out in the type definition."
    },
    {
      "id": "zu::zu1::catalog::KeyLabels::Inferred",
      "kind": "variant",
      "name": "Inferred",
      "of": "zu::zu1::catalog::KeyLabels",
      "signature": "Inferred(Vec<u16>)",
      "doc": "GG22: not written, so it is the whole label set."
    },
    {
      "id": "zu::zu1::catalog::KeyLabels::None",
      "kind": "variant",
      "name": "None",
      "of": "zu::zu1::catalog::KeyLabels",
      "signature": "None",
      "doc": "GG23: the type has none. Open graph types only, and selection\nfalls back to matching the whole label set."
    },
    {
      "id": "zu::zu1::catalog::KeyLabels::ids",
      "kind": "method",
      "name": "ids",
      "of": "zu::zu1::catalog::KeyLabels",
      "signature": "fn ids(&self) -> &[u16]",
      "doc": "The label ids the key names, empty when there is no key."
    },
    {
      "id": "zu::zu1::catalog::MAX_LABELS",
      "kind": "constant",
      "name": "MAX_LABELS",
      "signature": "usize",
      "doc": "The label dictionary is bounded by the width of the bitset a node\ncarries. One word per node is the fast path the whole design is\nsized for (LDBC SNB declares 13 labels, LinkBench and Graph500 one),\nand a wider set waits for the spill representation."
    },
    {
      "id": "zu::zu1::catalog::MAX_PROPERTIES",
      "kind": "constant",
      "name": "MAX_PROPERTIES",
      "signature": "usize",
      "doc": "The most properties one element holds. Generous and finite rather\nthan unlimited, for the reason the label dictionary is bounded: a\ndeclared limit is one the encoding can be built on, and an engine\nthat declares none pays for the possibility on every element of\nevery graph. The standard has a condition for hitting a declared\nlimit, and an engine with no limit can never raise it, so declaring\none is also the honest answer to the question the code asks."
    },
    {
      "id": "zu::zu1::catalog::MAX_TABLE_ID",
      "kind": "constant",
      "name": "MAX_TABLE_ID",
      "signature": "u32",
      "doc": "Table ids live in the 14-bit field of `NodeId`."
    },
    {
      "id": "zu::zu1::catalog::NodeTable",
      "kind": "struct",
      "name": "NodeTable",
      "doc": "A node table: the row domain `0..node_count` that rel tables index\ninto. Property columns land with the column catalog."
    },
    {
      "id": "zu::zu1::catalog::NodeTable::graph",
      "kind": "field",
      "name": "graph",
      "of": "zu::zu1::catalog::NodeTable",
      "signature": "u32",
      "doc": "The graph this table belongs to (GC04). Dropping that graph is\nwhat hands its blocks back."
    },
    {
      "id": "zu::zu1::catalog::NodeTable::id",
      "kind": "field",
      "name": "id",
      "of": "zu::zu1::catalog::NodeTable",
      "signature": "u32"
    },
    {
      "id": "zu::zu1::catalog::NodeTable::label_mask",
      "kind": "method",
      "name": "label_mask",
      "of": "zu::zu1::catalog::NodeTable",
      "signature": "fn label_mask(&self) -> u64",
      "doc": "The declared set as a mask, which is what a plan time prune\ntests a pattern's mask against."
    },
    {
      "id": "zu::zu1::catalog::NodeTable::labels",
      "kind": "field",
      "name": "labels",
      "of": "zu::zu1::catalog::NodeTable",
      "signature": "Vec<u16>",
      "doc": "The labels rows of this table may carry, as dictionary ids.\n`labels[0]` is the table's own name, which every row carries;\nthe rest are optional per row and the bitset column says which\nrows have them."
    },
    {
      "id": "zu::zu1::catalog::NodeTable::name",
      "kind": "field",
      "name": "name",
      "of": "zu::zu1::catalog::NodeTable",
      "signature": "String"
    },
    {
      "id": "zu::zu1::catalog::NodeTable::node_count",
      "kind": "field",
      "name": "node_count",
      "of": "zu::zu1::catalog::NodeTable",
      "signature": "u64"
    },
    {
      "id": "zu::zu1::catalog::NodeTable::primary_label",
      "kind": "method",
      "name": "primary_label",
      "of": "zu::zu1::catalog::NodeTable",
      "signature": "fn primary_label(&self) -> u16",
      "doc": "The label every row of this table carries: the table's name."
    },
    {
      "id": "zu::zu1::catalog::PropertyType",
      "kind": "struct",
      "name": "PropertyType",
      "doc": "One property an element type declares (GG26). The declaration hangs\noff the element type rather than off a name shared by the graph, so\ntwo element types may declare `since` with different types and both\nare right."
    },
    {
      "id": "zu::zu1::catalog::PropertyType::name",
      "kind": "field",
      "name": "name",
      "of": "zu::zu1::catalog::PropertyType",
      "signature": "String"
    },
    {
      "id": "zu::zu1::catalog::PropertyType::optional",
      "kind": "field",
      "name": "optional",
      "of": "zu::zu1::catalog::PropertyType",
      "signature": "bool",
      "doc": "Whether an element of the type may leave the property out."
    },
    {
      "id": "zu::zu1::catalog::PropertyType::ty",
      "kind": "field",
      "name": "ty",
      "of": "zu::zu1::catalog::PropertyType",
      "signature": "zu_common::LogicalType"
    },
    {
      "id": "zu::zu1::catalog::ROOT_SCHEMA",
      "kind": "constant",
      "name": "ROOT_SCHEMA",
      "signature": "&str",
      "doc": "The schema every zu1 file has, and the parent of a graph nobody\nwrote a path for. ISO names catalog objects by an absolute directory\npath, and a file that never created a schema still has this one."
    },
    {
      "id": "zu::zu1::catalog::RelTable",
      "kind": "struct",
      "name": "RelTable",
      "doc": "A rel table: a bulk-loaded CSR pair over `from` and `to` node tables,\nwith its group directory reachable through the table index."
    },
    {
      "id": "zu::zu1::catalog::RelTable::edge_count",
      "kind": "field",
      "name": "edge_count",
      "of": "zu::zu1::catalog::RelTable",
      "signature": "u64"
    },
    {
      "id": "zu::zu1::catalog::RelTable::from",
      "kind": "field",
      "name": "from",
      "of": "zu::zu1::catalog::RelTable",
      "signature": "u32"
    },
    {
      "id": "zu::zu1::catalog::RelTable::graph",
      "kind": "field",
      "name": "graph",
      "of": "zu::zu1::catalog::RelTable",
      "signature": "u32",
      "doc": "The graph this table belongs to, as on a node table."
    },
    {
      "id": "zu::zu1::catalog::RelTable::id",
      "kind": "field",
      "name": "id",
      "of": "zu::zu1::catalog::RelTable",
      "signature": "u32"
    },
    {
      "id": "zu::zu1::catalog::RelTable::name",
      "kind": "field",
      "name": "name",
      "of": "zu::zu1::catalog::RelTable",
      "signature": "String"
    },
    {
      "id": "zu::zu1::catalog::RelTable::to",
      "kind": "field",
      "name": "to",
      "of": "zu::zu1::catalog::RelTable",
      "signature": "u32"
    },
    {
      "id": "zu::zu1::catalog::RelTable::undirected",
      "kind": "field",
      "name": "undirected",
      "of": "zu::zu1::catalog::RelTable",
      "signature": "bool",
      "doc": "Whether the edges here have no direction (GH02). An undirected\nedge is stored once, the way it was written, and read as\nstanding for both ways round: it is the pattern that asks which\nof the two it wants, not the store."
    },
    {
      "id": "zu::zu1::catalog::TableIndex",
      "kind": "struct",
      "name": "TableIndex",
      "doc": "The table index: rel table id to group directory root."
    },
    {
      "id": "zu::zu1::catalog::TableIndex::decode",
      "kind": "method",
      "name": "decode",
      "of": "zu::zu1::catalog::TableIndex",
      "signature": "fn decode(bytes: &[u8]) -> Result<Self>"
    },
    {
      "id": "zu::zu1::catalog::TableIndex::encode",
      "kind": "method",
      "name": "encode",
      "of": "zu::zu1::catalog::TableIndex",
      "signature": "fn encode(&self) -> Vec<u8>"
    },
    {
      "id": "zu::zu1::catalog::TableIndex::entries",
      "kind": "method",
      "name": "entries",
      "of": "zu::zu1::catalog::TableIndex",
      "signature": "fn entries(&self) -> &[(u32, BlockPtr)]"
    },
    {
      "id": "zu::zu1::catalog::TableIndex::get",
      "kind": "method",
      "name": "get",
      "of": "zu::zu1::catalog::TableIndex",
      "signature": "fn get(&self, table_id: u32) -> Option<BlockPtr>"
    },
    {
      "id": "zu::zu1::catalog::TableIndex::load",
      "kind": "method",
      "name": "load",
      "of": "zu::zu1::catalog::TableIndex",
      "signature": "fn load(db: &mut Zu1File) -> Result<Self>",
      "doc": "Loads the committed index; a `NULL_BLOCK` root reads as empty."
    },
    {
      "id": "zu::zu1::catalog::TableIndex::remove",
      "kind": "method",
      "name": "remove",
      "of": "zu::zu1::catalog::TableIndex",
      "signature": "fn remove(&mut self, table_id: u32)"
    },
    {
      "id": "zu::zu1::catalog::TableIndex::set",
      "kind": "method",
      "name": "set",
      "of": "zu::zu1::catalog::TableIndex",
      "signature": "fn set(&mut self, table_id: u32, root: BlockPtr)"
    },
    {
      "id": "zu::zu1::colors",
      "kind": "module",
      "name": "colors",
      "doc": "COLOR summaries: quasi-stable coloring for cardinality estimation\n(docs/07 §6). Nodes split into at most `cap` colors so that nodes\nsharing a color have near-identical per-color degree profiles, then\neach rel table stores one sparse summary per color pair: the edge\ncount and the largest single-node degree across the pair. The\noptimizer walks these matrices to estimate multi-hop cardinalities\nwhere a global mean cannot see that hubs dominate a frontier.\n\nThe coloring is iterated signature refinement. A node's signature is\nits neighbor-count per color in both directions, bucketed by log2 so\nnodes within a factor of two of each other stay together; that\ntolerance is what makes the coloring quasi-stable rather than the\nexponentially large stable refinement. Refinement stops at the\nfixpoint or right before a split would exceed the cap, keeping the\nlast coloring that fit."
    },
    {
      "id": "zu::zu1::colors::ColorSummary",
      "kind": "struct",
      "name": "ColorSummary",
      "doc": "The sparse per-color-pair summary of one rel table's edges.\n`counts[c]` is the number of nodes holding color `c`; each triple is\n`(from_color, to_color, edge_count, max_degree)` where `max_degree`\nis the largest number of edges one source in `from_color` holds into\n`to_color`."
    },
    {
      "id": "zu::zu1::colors::ColorSummary::counts",
      "kind": "field",
      "name": "counts",
      "of": "zu::zu1::colors::ColorSummary",
      "signature": "Vec<u64>"
    },
    {
      "id": "zu::zu1::colors::ColorSummary::edges",
      "kind": "field",
      "name": "edges",
      "of": "zu::zu1::colors::ColorSummary",
      "signature": "u64",
      "doc": "The edges the table held at that epoch. Folds move the catalog's\nedge count and leave the summary where it was, so the two counts\ntogether are how far the table has walked away from its coloring."
    },
    {
      "id": "zu::zu1::colors::ColorSummary::epoch",
      "kind": "field",
      "name": "epoch",
      "of": "zu::zu1::colors::ColorSummary",
      "signature": "u64",
      "doc": "The committed epoch `ANALYZE` read to build this. Nothing after\nit moves the summary, so a reader comparing this against the\nfile's epoch knows how much has happened since."
    },
    {
      "id": "zu::zu1::colors::ColorSummary::triples",
      "kind": "field",
      "name": "triples",
      "of": "zu::zu1::colors::ColorSummary",
      "signature": "Vec<(u32, u32, u64, u64)>"
    },
    {
      "id": "zu::zu1::colors::analyze",
      "kind": "function",
      "name": "analyze",
      "signature": "fn analyze(db: &mut zu_zu1::file::Zu1File) -> zu_common::Result<()>",
      "doc": "ANALYZE: rebuilds every rel table's COLOR summary from its stored\nCSR and publishes them in the stats chain with a checkpoint. The\ndegree histograms written at load stay as they are. Coloring reads\neach forward group once in row order, so the pass is one sequential\nsweep per table plus the in-memory refinement.\n\nEvery summary is stamped with the epoch this read and the edge count\nthe table held at it, which is what lets a later query tell a fresh\nsummary from one the writes have moved on from."
    },
    {
      "id": "zu::zu1::colors::quasi_stable_coloring",
      "kind": "function",
      "name": "quasi_stable_coloring",
      "signature": "fn quasi_stable_coloring(n: u32, edges: &[(u32, u32)], cap: usize) -> Vec<u32>",
      "doc": "Colors `n` nodes over `edges` (sorted by source) into at most `cap`\ncolors. Returns one color per node, colors dense from zero and\nassigned in first-seen node order, so the result is deterministic."
    },
    {
      "id": "zu::zu1::colors::summarize",
      "kind": "function",
      "name": "summarize",
      "signature": "fn summarize(colors: &[u32], edges: &[(u32, u32)]) -> ColorSummary",
      "doc": "Builds the sparse summary of `edges` (sorted by source) under\n`colors`, one run-length pass per source. The staleness stamp is\nleft at zero, since only the caller holding the file knows which\nepoch these edges were read at."
    },
    {
      "id": "zu::zu1::epoch",
      "kind": "module",
      "name": "epoch",
      "doc": "Snapshot epoch accounting: the ledger concurrent readers pin and\nthe checkpoint consults for its fold horizon.\n\nThe single-threaded facade gets snapshot isolation for free from\nthe borrow checker; the shared-reader facade in docs/09 does not,\nand this ledger is its accounting. The writer publishes each commit\nwith [`EpochLedger::advance`], a reader claims a slot and pins the\ncommitted epoch with [`EpochLedger::pin`], and the checkpoint asks\n[`EpochLedger::horizon`] how far it may fold: never past an epoch a\nreader still sees.\n\nThree rules carry the whole safety argument, and the loom suite in\n`tests/loom.rs` exhausts the interleavings behind them. A reader\nmust re-read the committed epoch after storing its pin, because a\nhorizon scan may have missed the store. The pin's store then\nre-read and the horizon's publish then scan are each split by a\nSeqCst fence, the Dekker shape: either the reader's fence comes\nfirst in the fence order and the scan sees the pin, or the\nwriter's comes first and the re-read sees the advanced epoch and\nretries; plain acquire and release orderings allow the store-load\nreordering that breaks both arms, and loom finds it immediately.\nAnd [`EpochLedger::advance`] and [`EpochLedger::horizon`] belong\nto the single writer thread, which the engine's `&mut` commit and\ncheckpoint APIs already serialize. loom rejected the alternative:\na checkpoint thread computing the horizon independently can\nobserve a concurrent commit that a racing reader's re-read does\nnot, scan before that reader's pin lands, and reclaim past the\nepoch the reader snapshots, because no fence pairing orders a\nthird thread's scan against an advance it did not perform."
    },
    {
      "id": "zu::zu1::epoch::EpochLedger",
      "kind": "struct",
      "name": "EpochLedger",
      "doc": "The committed epoch and one pin slot per reader. `EMPTY` marks an\nunpinned slot."
    },
    {
      "id": "zu::zu1::epoch::EpochLedger::advance",
      "kind": "method",
      "name": "advance",
      "of": "zu::zu1::epoch::EpochLedger",
      "signature": "fn advance(&self, epoch: Epoch)",
      "doc": "Publishes `epoch` as committed. The writer calls this after its\nWAL frame syncs and its overlays publish; epochs only advance,\nand only the single writer thread calls this."
    },
    {
      "id": "zu::zu1::epoch::EpochLedger::committed",
      "kind": "method",
      "name": "committed",
      "of": "zu::zu1::epoch::EpochLedger",
      "signature": "fn committed(&self) -> Epoch",
      "doc": "The newest committed epoch."
    },
    {
      "id": "zu::zu1::epoch::EpochLedger::horizon",
      "kind": "method",
      "name": "horizon",
      "of": "zu::zu1::epoch::EpochLedger",
      "signature": "fn horizon(&self) -> Epoch",
      "doc": "The oldest epoch any current reader may still see, the upper\nbound for a fold. Must run on the writer thread: the fence\npairs against each pinning reader's, so a pin the scan misses\nre-read this thread's last advance and retried at or above it,\nwhile a pin the re-read beat is seen by the scan and bounds the\nresult. Computed on any other thread that pairing says nothing\nabout a concurrent advance, and loom finds the\nreclaim-past-a-pin schedule immediately."
    },
    {
      "id": "zu::zu1::epoch::EpochLedger::new",
      "kind": "method",
      "name": "new",
      "of": "zu::zu1::epoch::EpochLedger",
      "signature": "fn new(epoch: Epoch) -> Self",
      "doc": "A ledger with no pinned readers at the given committed epoch."
    },
    {
      "id": "zu::zu1::epoch::EpochLedger::pin",
      "kind": "method",
      "name": "pin",
      "of": "zu::zu1::epoch::EpochLedger",
      "signature": "fn pin(&self, slot: usize) -> Epoch",
      "doc": "Pins the committed epoch into `slot` and returns it. The store,\nfence, then re-read loop closes the race with a concurrent\n[`Self::horizon`]: a scan that missed this pin fenced before\nthis pin's fence, so its advance is visible to the re-read,\nwhich then retries at the newer epoch. The returned epoch is\nnever below a horizon the writer computes."
    },
    {
      "id": "zu::zu1::epoch::EpochLedger::unpin",
      "kind": "method",
      "name": "unpin",
      "of": "zu::zu1::epoch::EpochLedger",
      "signature": "fn unpin(&self, slot: usize)",
      "doc": "Releases `slot`; the reader's snapshot no longer holds the\nhorizon back."
    },
    {
      "id": "zu::zu1::epoch::READER_SLOTS",
      "kind": "constant",
      "name": "READER_SLOTS",
      "signature": "usize",
      "doc": "Fixed reader capacity; the docs/09 facade hands each reader thread\none slot for its lifetime."
    },
    {
      "id": "zu::zu1::file",
      "kind": "module",
      "name": "file",
      "doc": "File headers, block I/O, and the dual-header checkpoint protocol.\n\nThe layout is `docs/04-storage-zu1-format.md` §1: a write-once 4 KiB\nFileHeader, two alternating 4 KiB DatabaseHeaders, padding to 256 KiB,\nthen fixed 256 KiB blocks. Opening reads 12 KiB and picks the valid\nheader with the highest epoch, so a torn header write never damages\ncommitted state; new data goes to free blocks and becomes visible only\nwhen the next header flip publishes it."
    },
    {
      "id": "zu::zu1::file::BlockPtr",
      "kind": "type-alias",
      "name": "BlockPtr",
      "signature": "u64",
      "doc": "Byte index into the file where block `n` starts. Block 0 is the header\nregion, so index 0 doubles as the null pointer in chains and roots."
    },
    {
      "id": "zu::zu1::file::DB_HEADER_SIZE",
      "kind": "constant",
      "name": "DB_HEADER_SIZE",
      "signature": "usize",
      "doc": "On-disk size of one DatabaseHeader slot."
    },
    {
      "id": "zu::zu1::file::DEFAULT_MEMORY_LIMIT",
      "kind": "constant",
      "name": "DEFAULT_MEMORY_LIMIT",
      "signature": "usize",
      "doc": "Memory limit the caches size themselves from when the caller sets\nnone, matching the 128 MiB budget the P9 gate runs under."
    },
    {
      "id": "zu::zu1::file::DatabaseHeader",
      "kind": "struct",
      "name": "DatabaseHeader",
      "doc": "One checkpoint's view of the file: roots of every meta-block chain and\nthe block high-water mark. The valid slot with the highest epoch wins."
    },
    {
      "id": "zu::zu1::file::DatabaseHeader::block_count",
      "kind": "field",
      "name": "block_count",
      "of": "zu::zu1::file::DatabaseHeader",
      "signature": "u64"
    },
    {
      "id": "zu::zu1::file::DatabaseHeader::catalog_root",
      "kind": "field",
      "name": "catalog_root",
      "of": "zu::zu1::file::DatabaseHeader",
      "signature": "BlockPtr"
    },
    {
      "id": "zu::zu1::file::DatabaseHeader::epoch",
      "kind": "field",
      "name": "epoch",
      "of": "zu::zu1::file::DatabaseHeader",
      "signature": "u64"
    },
    {
      "id": "zu::zu1::file::DatabaseHeader::free_list_root",
      "kind": "field",
      "name": "free_list_root",
      "of": "zu::zu1::file::DatabaseHeader",
      "signature": "BlockPtr"
    },
    {
      "id": "zu::zu1::file::DatabaseHeader::stats_root",
      "kind": "field",
      "name": "stats_root",
      "of": "zu::zu1::file::DatabaseHeader",
      "signature": "BlockPtr"
    },
    {
      "id": "zu::zu1::file::DatabaseHeader::table_index_root",
      "kind": "field",
      "name": "table_index_root",
      "of": "zu::zu1::file::DatabaseHeader",
      "signature": "BlockPtr"
    },
    {
      "id": "zu::zu1::file::DatabaseHeader::wal_seq",
      "kind": "field",
      "name": "wal_seq",
      "of": "zu::zu1::file::DatabaseHeader",
      "signature": "u64"
    },
    {
      "id": "zu::zu1::file::DecodedPools",
      "kind": "struct",
      "name": "DecodedPools",
      "doc": "The decoded-object pools of perf/04 section 2, shared by every\nhandle [`Zu1File::reopen`] forks off one open. Keys are the first\nblock pointer of the decoded segment; [`Zu1File::write_block`]\ndrops the key of any block it rewrites, so a recycled pointer can\nnever serve a stale decode."
    },
    {
      "id": "zu::zu1::file::DecodedPools::adjacency",
      "kind": "field",
      "name": "adjacency",
      "of": "zu::zu1::file::DecodedPools",
      "signature": "zu_zu1::cache::DecodedPool<Vec<u64>>",
      "doc": "Decoded neighbor arrays, the bulk of a group."
    },
    {
      "id": "zu::zu1::file::DecodedPools::csr_offsets",
      "kind": "field",
      "name": "csr_offsets",
      "of": "zu::zu1::file::DecodedPools",
      "signature": "zu_zu1::cache::DecodedPool<Vec<u64>>",
      "doc": "Decoded CSR offset arrays, the O(1)-seek side of a group."
    },
    {
      "id": "zu::zu1::file::DecodedPools::fences",
      "kind": "field",
      "name": "fences",
      "of": "zu::zu1::file::DecodedPools",
      "signature": "zu_zu1::cache::DecodedPool<zu_zu1::segment::ChunkDirectory>",
      "doc": "Chunk directories: per-chunk end offsets and fences, the\npk-lookup and probe steering data."
    },
    {
      "id": "zu::zu1::file::FILE_HEADER_SIZE",
      "kind": "constant",
      "name": "FILE_HEADER_SIZE",
      "signature": "usize",
      "doc": "On-disk size of the FileHeader region."
    },
    {
      "id": "zu::zu1::file::FileHeader",
      "kind": "struct",
      "name": "FileHeader",
      "doc": "Write-once identity of a zu1 file."
    },
    {
      "id": "zu::zu1::file::FileHeader::block_size",
      "kind": "field",
      "name": "block_size",
      "of": "zu::zu1::file::FileHeader",
      "signature": "u32"
    },
    {
      "id": "zu::zu1::file::FileHeader::flags",
      "kind": "field",
      "name": "flags",
      "of": "zu::zu1::file::FileHeader",
      "signature": "u64"
    },
    {
      "id": "zu::zu1::file::FileHeader::format_version",
      "kind": "field",
      "name": "format_version",
      "of": "zu::zu1::file::FileHeader",
      "signature": "u16"
    },
    {
      "id": "zu::zu1::file::FileHeader::min_reader_version",
      "kind": "field",
      "name": "min_reader_version",
      "of": "zu::zu1::file::FileHeader",
      "signature": "u16"
    },
    {
      "id": "zu::zu1::file::FileHeader::uuid",
      "kind": "field",
      "name": "uuid",
      "of": "zu::zu1::file::FileHeader",
      "signature": "[u8; 16]"
    },
    {
      "id": "zu::zu1::file::Interrupted",
      "kind": "struct",
      "name": "Interrupted",
      "doc": "What a transaction the process died inside left on a file.\n\nBoth halves are needed to take it back. The file has to be published\npast what the crash left, or the header the transaction wrote wins\nthe next open by being the newer of the two slots, and the log has\nto be cut back to where it stood when the transaction began, or\nreplay puts what the transaction committed back as overlays over the\nheader that went back in."
    },
    {
      "id": "zu::zu1::file::Interrupted::log_floor",
      "kind": "field",
      "name": "log_floor",
      "of": "zu::zu1::file::Interrupted",
      "signature": "zu_common::Epoch",
      "doc": "The newest epoch the log held before the transaction started."
    },
    {
      "id": "zu::zu1::file::Interrupted::published",
      "kind": "field",
      "name": "published",
      "of": "zu::zu1::file::Interrupted",
      "signature": "zu_common::Epoch",
      "doc": "The epoch the crash left published."
    },
    {
      "id": "zu::zu1::file::NULL_BLOCK",
      "kind": "constant",
      "name": "NULL_BLOCK",
      "signature": "BlockPtr",
      "doc": "The null block pointer."
    },
    {
      "id": "zu::zu1::file::PackScope",
      "kind": "struct",
      "name": "PackScope",
      "doc": "A packing scope suspended by [`Zu1File::pack_open`], to be handed\nback to [`Zu1File::pack_close`]. Opaque because the only thing a\ncaller may do with one is put it back."
    },
    {
      "id": "zu::zu1::file::Shared",
      "kind": "struct",
      "name": "Shared",
      "doc": "The caches one file's handles hold in common, for a handle that\nwas opened rather than forked to take up. See [`Zu1File::adopt`]."
    },
    {
      "id": "zu::zu1::file::Zu1File",
      "kind": "struct",
      "name": "Zu1File",
      "doc": "An open zu1 file: block I/O, the free list, and the header flip."
    },
    {
      "id": "zu::zu1::file::Zu1File::active_slot",
      "kind": "method",
      "name": "active_slot",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn active_slot(&self) -> usize",
      "doc": "Which header slot this handle is reading, which a caller passing\nthe header to another handle on the same file passes with it."
    },
    {
      "id": "zu::zu1::file::Zu1File::adopt",
      "kind": "method",
      "name": "adopt",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn adopt(&mut self, shared: &Shared)",
      "doc": "Puts this handle on another handle's caches, which a handle\nopened rather than forked has to do before it reads anything.\n\nA cache is invalidated by the handle that writes the block, and\nit can only invalidate its own. So a second cache on one file is\na cache holding whatever it last saw in a block, and a block the\nwriter has since freed and written over reads back as the thing\nthat used to be in it. What that looks like from a query is a\nprops directory that decodes as somebody else's catalog."
    },
    {
      "id": "zu::zu1::file::Zu1File::allocate_block",
      "kind": "method",
      "name": "allocate_block",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn allocate_block(&mut self) -> BlockPtr",
      "doc": "Returns a block to write into: a committed-free block when one\nexists, otherwise one past the high-water mark. Either way the\nblock becomes durable state only via the next checkpoint.\n\nA transaction narrows what counts as free. A checkpoint inside\none publishes the blocks that transaction has freed, and the\nstate the savepoint keeps still reads them, so those are held in\n[`Self::frozen`] until the transaction ends and this list holds\nonly what was free before it began."
    },
    {
      "id": "zu::zu1::file::Zu1File::begin_savepoint",
      "kind": "method",
      "name": "begin_savepoint",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn begin_savepoint(&mut self, across_statements: bool, log_floor: Epoch) -> Result<()>",
      "doc": "Keeps what the file is now, so that [`Self::rollback_savepoint`]\ncan put it back after any number of statements have committed on\ntop of it.\n\nThis is what makes an explicit transaction more than a word. A\nstatement commits by folding its overlays into new segments and\nflipping the header, and there is no undoing that in memory\nafterwards, so the undo is at the file: keep the roots, refuse to\nreuse anything freed while the transaction runs, and publish the\nkept roots again if the transaction ends in a rollback.\n\nOne savepoint at a time. Nested transactions are not a thing GQL\nhas, and a second one here would quietly become the first.\n\n`across_statements` says whether what is being kept has to\nsurvive the process going away, which is what an explicit\ntransaction needs and a single statement does not: a statement\nthat publishes once publishes in one header flip, and a crash\neither leaves that flip whole or leaves the state before it. See\n[`Self::keep_savepoint`] for what a savepoint that has to\nsurvive costs.\n\nA transaction starts from a folded file, because what is kept\nhere is the file's roots and a rollback publishes them again:\nwhatever the log has not folded into them is not in them, and\nwould go back along with the transaction. Callers fold first,\nwhich is what opening a writer does.\n\n`log_floor` is where the log stands as this is called, which is\nthe newest epoch anything committed before the transaction. A\nrollback cuts the log back to it, so a caller handing over a\nfloor older than what the log holds is asking for those frames\nto be dropped along with the transaction."
    },
    {
      "id": "zu::zu1::file::Zu1File::cache_stats",
      "kind": "method",
      "name": "cache_stats",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn cache_stats(&self) -> CacheStats",
      "doc": "Block cache hit, miss, and eviction counts for this handle's\nshared cache."
    },
    {
      "id": "zu::zu1::file::Zu1File::checkpoint",
      "kind": "method",
      "name": "checkpoint",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn checkpoint(&mut self) -> Result<()>",
      "doc": "Publishes the staged state: persist the free list, fsync data, bump\nthe epoch, write the header into the inactive slot, fsync again. A\ncrash between the two syncs leaves the previous epoch intact."
    },
    {
      "id": "zu::zu1::file::Zu1File::create",
      "kind": "method",
      "name": "create",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn create(path: &Path) -> Result<Self>",
      "doc": "Creates a new database file. Fails if `path` already exists, so an\nexisting database is never silently clobbered."
    },
    {
      "id": "zu::zu1::file::Zu1File::create_in",
      "kind": "method",
      "name": "create_in",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn create_in(vfs: Arc<dyn Vfs>, path: &Path) -> Result<Self>",
      "doc": "[`Self::create`] somewhere other than the filesystem. The vfs is\nkept, so the sidecar log and every reopened handle land in the\nsame place this one did."
    },
    {
      "id": "zu::zu1::file::Zu1File::create_on",
      "kind": "method",
      "name": "create_on",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn create_on(file: Box<dyn VfsFile>, path: &Path) -> Result<Self>",
      "doc": "[`Self::create`] on an explicit file handle; the crash harness\npasses a recording one."
    },
    {
      "id": "zu::zu1::file::Zu1File::db_header",
      "kind": "method",
      "name": "db_header",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn db_header(&self) -> &DatabaseHeader",
      "doc": "The committed database header this handle opened at, including any\nroot updates staged since through [`Self::db_header_mut`]."
    },
    {
      "id": "zu::zu1::file::Zu1File::db_header_mut",
      "kind": "method",
      "name": "db_header_mut",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn db_header_mut(&mut self) -> &mut DatabaseHeader",
      "doc": "Stages root updates for the next checkpoint. Nothing is visible to\nother openers until [`Self::checkpoint`] publishes them."
    },
    {
      "id": "zu::zu1::file::Zu1File::defer_reclaim",
      "kind": "method",
      "name": "defer_reclaim",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn defer_reclaim(&mut self, on: bool)",
      "doc": "Says that a caller above this handle counts the readers, so a\ncheckpoint holds what it frees until that caller says which\nepoch is the oldest anything is reading. Set on the one handle\nof a file that writes; every other handle only reads."
    },
    {
      "id": "zu::zu1::file::Zu1File::file_header",
      "kind": "method",
      "name": "file_header",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn file_header(&self) -> &FileHeader",
      "doc": "Write-once file identity."
    },
    {
      "id": "zu::zu1::file::Zu1File::finish_rollback",
      "kind": "method",
      "name": "finish_rollback",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn finish_rollback(&mut self) -> Result<()>",
      "doc": "Publishes the state a transaction the process died inside was\nholding, and says the transaction is over.\n\nNothing is put back in memory, because opening a file with a\ntransaction open on it reads the kept state to begin with. What\nis left is the file: the kept header goes in with an epoch past\nthe one the crash left, so the next open reads it rather than\nthe middle of the transaction, and only then is the kept state\ncleared. A crash anywhere in here leaves the marker down and the\nnext open does the same thing again."
    },
    {
      "id": "zu::zu1::file::Zu1File::follow",
      "kind": "method",
      "name": "follow",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn follow(&mut self, header: &DatabaseHeader, slot: usize)",
      "doc": "Takes the roots another handle on this file has reached.\n\n[`Self::reopen`] hands a new handle the roots the writer holds,\nand this is the same move made again later: a reader forked\nbefore a commit reads the blocks that commit wrote, because data\nblocks reach the file as they are staged and only the flip waits\nfor a checkpoint, but it has no way to know where they went. The\nheader is that way, so a reader given the writer's header reads\nthe writer's database.\n\nOnly the header moves. The free lists, the savepoint and the\nunpublished count belong to the handle that allocates, and a\nhandle that follows another one does not: it reads. The pin memo\ngoes because it names a block of the state being left behind."
    },
    {
      "id": "zu::zu1::file::Zu1File::free_block",
      "kind": "method",
      "name": "free_block",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn free_block(&mut self, ptr: BlockPtr) -> Result<()>",
      "doc": "Marks `ptr` free. The committed epoch still references it, so it\nbecomes allocatable only after the next checkpoint; until then its\ncontents must survive a crash. Frees staged on a handle that closes\nwithout a checkpoint are dropped, which leaks the blocks until\nVACUUM rewrites the file, never corrupts it."
    },
    {
      "id": "zu::zu1::file::Zu1File::in_savepoint",
      "kind": "method",
      "name": "in_savepoint",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn in_savepoint(&self) -> bool",
      "doc": "Whether a transaction is holding this file to a state it can be\nput back to."
    },
    {
      "id": "zu::zu1::file::Zu1File::interrupted",
      "kind": "method",
      "name": "interrupted",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn interrupted(&self) -> Option<Interrupted>",
      "doc": "What a transaction the process died inside left behind, if this\nhandle opened a file with one open on it.\n\nThe header this handle is reading is already the one that\ntransaction was holding, so a reader needs nothing else. A\nwriter owes the file a publish, which is what\n[`Self::finish_rollback`] is, and owes the log a cut back to the\nfloor, which is the one thing it cannot do itself."
    },
    {
      "id": "zu::zu1::file::Zu1File::is_writable",
      "kind": "method",
      "name": "is_writable",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn is_writable(&self) -> bool",
      "doc": "Whether this handle may write, which is false for one opened by\n[`Self::open_read_only`]."
    },
    {
      "id": "zu::zu1::file::Zu1File::keep_savepoint",
      "kind": "method",
      "name": "keep_savepoint",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn keep_savepoint(&mut self) -> Result<()>",
      "doc": "Puts the state a savepoint is keeping on the file, so that a\nprocess that goes away before the transaction ends leaves\nsomething that says where to go back to.\n\nThis runs before the write it protects reaches the log, which is\nwhere a change becomes something a recovery would bring back,\nrather than before the publish behind it: a commit is durable at\nits log sync and the fold that publishes it comes after, so a\nmarker written at the fold leaves that window uncovered. Every\npath that commits calls this first, and the checkpoint calls it\ntoo, for the publishes that reach the file without a frame.\n\nThe two syncs are what it costs: one here and one at the word\nthat ends the transaction. A statement pays neither until it\ncommits a second time, because one commit and the fold behind it\nare one header flip, which a crash either leaves whole or does\nnot leave at all."
    },
    {
      "id": "zu::zu1::file::Zu1File::open",
      "kind": "method",
      "name": "open",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn open(path: &Path) -> Result<Self>",
      "doc": "Opens an existing database: read 12 KiB, validate the file header,\nand adopt the valid database header with the highest epoch."
    },
    {
      "id": "zu::zu1::file::Zu1File::open_in",
      "kind": "method",
      "name": "open_in",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn open_in(vfs: Arc<dyn Vfs>, path: &Path) -> Result<Self>",
      "doc": "[`Self::open`] somewhere other than the filesystem."
    },
    {
      "id": "zu::zu1::file::Zu1File::open_on",
      "kind": "method",
      "name": "open_on",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn open_on(file: Box<dyn VfsFile>, path: &Path) -> Result<Self>",
      "doc": "[`Self::open`] on an explicit file handle; the crash harness\npasses a recording one."
    },
    {
      "id": "zu::zu1::file::Zu1File::open_read_only",
      "kind": "method",
      "name": "open_read_only",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn open_read_only(path: &Path) -> Result<Self>",
      "doc": "Opens an existing database on a descriptor the operating system\nwill not let this process write through. Every path that would\nhave written refuses first, with the name of the database in the\nerror, so a caller that asked for a read-only handle and then\nwrote learns which promise it broke rather than which syscall\nfailed."
    },
    {
      "id": "zu::zu1::file::Zu1File::open_read_only_in",
      "kind": "method",
      "name": "open_read_only_in",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn open_read_only_in(vfs: Arc<dyn Vfs>, path: &Path) -> Result<Self>",
      "doc": "[`Self::open_read_only`] somewhere other than the filesystem."
    },
    {
      "id": "zu::zu1::file::Zu1File::pack_bytes",
      "kind": "method",
      "name": "pack_bytes",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn pack_bytes(&mut self, payload: &[u8]) -> Result<(Vec<BlockPtr>, u32)>",
      "doc": "Writes `payload` and returns the blocks it landed in and the\nbyte offset it starts at in the first of them.\n\nUnder a tag this is a bump allocator: the payload starts where\nthe last one ended and spills into fresh blocks as it needs\nthem, so a store of thirty small segments costs the bytes they\nweigh rather than thirty blocks. The block under the cursor is\nrewritten whole on every call, which is what lets the caller\nstop at any point without a flush: what is on disk is always\neverything handed out so far."
    },
    {
      "id": "zu::zu1::file::Zu1File::pack_close",
      "kind": "method",
      "name": "pack_close",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn pack_close(&mut self, held: PackScope)",
      "doc": "Ends the scope [`Self::pack_open`] began and resumes the one it\nsuspended."
    },
    {
      "id": "zu::zu1::file::Zu1File::pack_open",
      "kind": "method",
      "name": "pack_open",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn pack_open(&mut self) -> PackScope",
      "doc": "Opens a packing scope: every [`Self::pack_bytes`] until the\nmatching [`Self::pack_close`] shares blocks with the others, and\nwith nothing outside.\n\nA scope is whatever the caller frees as a unit, one property\ndirectory or one rel table's adjacency, because a block holding\ntwo payloads has to be freed when the last of them goes and\nnothing in the file counts what is left in one. Scoping by the\nthing that frees makes the question moot: everything in the\nblock dies on the same call, and that call deduplicates the\npointers it hands back. A scope always starts on a fresh block,\nso two folds of the same table never share one either.\n\nThe returned value is the scope this one suspended and belongs\nback in [`Self::pack_close`]. Scopes nest: an inner one takes\nits own blocks and the outer resumes where it was."
    },
    {
      "id": "zu::zu1::file::Zu1File::pack_reset",
      "kind": "method",
      "name": "pack_reset",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn pack_reset(&mut self)",
      "doc": "Drops any scope still open, which is what a caller that is\nstarting a fold does: a fold that failed partway may have left\none, and a block half filled by work that was thrown away is not\na block the next fold may write into."
    },
    {
      "id": "zu::zu1::file::Zu1File::path",
      "kind": "method",
      "name": "path",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn path(&self) -> &Path",
      "doc": "Where this handle's file is, which is what a caller needs to\nname the sidecar beside it: docs/04 puts the WAL at `<db>.wal`,\nand a caller holding only the handle would otherwise have to\ncarry the path alongside it and hope the two agree."
    },
    {
      "id": "zu::zu1::file::Zu1File::pin_block",
      "kind": "method",
      "name": "pin_block",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn pin_block(&mut self, ptr: BlockPtr) -> Result<PinnedBlock>",
      "doc": "Pins the block at `ptr` in the cache. A warm pin is a map probe\nand an `Arc` clone, no allocation, no copy, no I/O; a miss reads\nthe block once into a recycled frame. This is the read primitive\neverything else builds on."
    },
    {
      "id": "zu::zu1::file::Zu1File::pools",
      "kind": "method",
      "name": "pools",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn pools(&self) -> Arc<DecodedPools>",
      "doc": "The shared decoded-object pools, cheap to clone per lookup."
    },
    {
      "id": "zu::zu1::file::Zu1File::read_block",
      "kind": "method",
      "name": "read_block",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn read_block(&mut self, ptr: BlockPtr) -> Result<Vec<u8>>",
      "doc": "Reads one full block at `ptr` into an owned copy. Cold paths and\ntests use this; hot readers pin instead."
    },
    {
      "id": "zu::zu1::file::Zu1File::read_block_slice",
      "kind": "method",
      "name": "read_block_slice",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn read_block_slice(&mut self, ptr: BlockPtr, offset: usize, len: usize) -> Result<Vec<u8>>",
      "doc": "Reads `len` bytes at `offset` inside the block at `ptr` into an\nowned copy. A cold call now faults the whole block into the\ncache, which is the perf/04 trade: the first point read in a\nblock pays 256 KiB once so every later read in it pays nothing."
    },
    {
      "id": "zu::zu1::file::Zu1File::recycle",
      "kind": "method",
      "name": "recycle",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn recycle(self)",
      "doc": "Retires a fork into its shared pool for the next\n[`Self::reopen`] to reuse. The handle drops its own reference\nto the pool on the way in, so the pool holding it does not keep\nitself alive. Dropping a fork without recycling is fine, the\nnext reopen just pays the OS open again."
    },
    {
      "id": "zu::zu1::file::Zu1File::release_retained",
      "kind": "method",
      "name": "release_retained",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn release_retained(&mut self, floor: u64) -> usize",
      "doc": "Gives back for allocation the blocks a checkpoint listed as free\nthat no reader is on any more, and answers how many.\n\n`floor` is the oldest epoch anything is reading, so an entry the\ncheckpoint tagged with an epoch below it is reachable from\nnothing: every reader has moved past it. A caller with no\nreaders passes `u64::MAX` and gets the whole list back, which is\nthe single-connection case and is why one connection allocates\nexactly as it did before this existed."
    },
    {
      "id": "zu::zu1::file::Zu1File::release_savepoint",
      "kind": "method",
      "name": "release_savepoint",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn release_savepoint(&mut self) -> Result<()>",
      "doc": "Ends the transaction by keeping what it did. Everything it wrote\nis already published, so this only drops what was being held and\nlets allocation reuse free blocks again.\n\nA transaction whose kept state reached the file has one more\nthing to do, which is to say it is over. The state is published\nbefore the saying, so a crash in between takes the transaction\nback, which is the right way round: the word that ends it had\nnot returned yet."
    },
    {
      "id": "zu::zu1::file::Zu1File::reopen",
      "kind": "method",
      "name": "reopen",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn reopen(&self) -> Result<Self>",
      "doc": "A second read handle on the same file carrying this handle's\ncurrent in-memory state. Data blocks are written to the file as\nthey are staged and only the header flip waits for the\ncheckpoint, so adopting this handle's header lets the new\nhandle read exactly what this one reads, staged roots included.\nThe free lists stay empty because a reopened handle exists to\nread; the morsel workers are the caller. The block cache is\nshared, so a fork starts warm and warms its siblings.\n\nHandles retired through [`Self::recycle`] come back first, with\nthis handle's header and a cleared pin memo, so steady-state\nforking costs a mutex pop instead of an OS open."
    },
    {
      "id": "zu::zu1::file::Zu1File::retained_blocks",
      "kind": "method",
      "name": "retained_blocks",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn retained_blocks(&self) -> usize",
      "doc": "How many blocks are waiting on a reader. Nonzero here is a\nconnection reading an epoch the writer has moved past, and the\nfile growing rather than reusing while it does."
    },
    {
      "id": "zu::zu1::file::Zu1File::rollback_savepoint",
      "kind": "method",
      "name": "rollback_savepoint",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn rollback_savepoint(&mut self) -> Result<()>",
      "doc": "Ends the transaction by putting the file back where it started\nand publishing that, so a reader after this reads what a reader\nbefore the transaction read.\n\nThe epoch is the one thing not put back: the kept header is\nrepublished with the next epoch after the newest, because a\nheader written with a lower epoch than the other slot would be\nthe older of the two and the transaction's last state would win\nthe next open. Blocks the transaction wrote past the kept\nhigh-water mark are outside the file the header describes, so the\nnext writer hands them out again; blocks it freed are still\nreferenced by the header going back in, so what it freed is\ndropped rather than published."
    },
    {
      "id": "zu::zu1::file::Zu1File::savepoint_floor",
      "kind": "method",
      "name": "savepoint_floor",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn savepoint_floor(&self) -> Option<Epoch>",
      "doc": "Where the log stood when the open transaction began, which a\nrollback needs because the frames above it are the ones going\naway and a fold that did not publish did not cut them."
    },
    {
      "id": "zu::zu1::file::Zu1File::set_memory_limit",
      "kind": "method",
      "name": "set_memory_limit",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn set_memory_limit(&mut self, memory_limit: usize)",
      "doc": "Rebuilds the caches sized off `memory_limit`: half of it block\nframes per perf/04, 8% CSR offsets, 20% adjacency, 4% fences.\nForks made before the call keep the old caches."
    },
    {
      "id": "zu::zu1::file::Zu1File::shared",
      "kind": "method",
      "name": "shared",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn shared(&self) -> Shared",
      "doc": "What every handle on one file has to be sharing: the block\ncache, the decoded pools above it, and the pool of retired\nhandles."
    },
    {
      "id": "zu::zu1::file::Zu1File::stage_deferred",
      "kind": "method",
      "name": "stage_deferred",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn stage_deferred(&mut self)",
      "doc": "Says a commit went to the log and nothing folded it, which is\nwhere a deferred commit leaves the file.\n\nThe roots do not move and the epoch does not either, because\nwhat the readers are handed is the patch. What does move is the\nlog, and the frames are above the floor a rollback cuts back to\nwith nothing on the file saying they are to be taken back. That\nis the same position [`Self::stage_fold`] leaves an open\nsavepoint in, and it wants the same marker."
    },
    {
      "id": "zu::zu1::file::Zu1File::stage_fold",
      "kind": "method",
      "name": "stage_fold",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn stage_fold(&mut self)",
      "doc": "Says a fold moved the roots this handle reads and stopped short\nof putting them on disk, which is what a writer that folds every\ncommit and checkpoints on a threshold does most of the time.\n\nTwo things follow. The epoch moves, because everything above\nkeys its cached catalogs, readers and plans on it and the roots\nthey describe have moved. And a savepoint open over the fold is\nnow holding against something a crash would find, because the\nframes the fold folded are in the log and nothing has cut them,\nwhich is the same position a publish leaves it in and wants the\nsame marker."
    },
    {
      "id": "zu::zu1::file::Zu1File::sync_data",
      "kind": "method",
      "name": "sync_data",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn sync_data(&mut self) -> Result<()>",
      "doc": "Syncs staged data writes without publishing anything: the header\nslots do not move and a plain reopen still sees the committed\nepoch. The ingest path calls this so its sealed segments are\ndurable before the WAL frame referencing them is."
    },
    {
      "id": "zu::zu1::file::Zu1File::unpublished_blocks",
      "kind": "method",
      "name": "unpublished_blocks",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn unpublished_blocks(&self) -> u64",
      "doc": "Blocks allocated since the last checkpoint. A writer that folds\nwithout publishing watches this, because until it publishes\nnothing it freed can be handed back out and the file grows by\neverything the folds rewrote."
    },
    {
      "id": "zu::zu1::file::Zu1File::vfs",
      "kind": "method",
      "name": "vfs",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn vfs(&self) -> &Arc<dyn Vfs>",
      "doc": "Where this handle's files come from, which is the other half of\nwhat naming the sidecar takes: the name says where beside the\ndatabase it goes, and this says which world that name is in."
    },
    {
      "id": "zu::zu1::file::Zu1File::write_block",
      "kind": "method",
      "name": "write_block",
      "of": "zu::zu1::file::Zu1File",
      "signature": "fn write_block(&mut self, ptr: BlockPtr, data: &[u8]) -> Result<()>",
      "doc": "Writes one full block at `ptr` and drops any cached frame for it,\nso the next read refills from the file."
    },
    {
      "id": "zu::zu1::file::decode_free_list",
      "kind": "function",
      "name": "decode_free_list",
      "signature": "fn decode_free_list(bytes: &[u8], block_count: u64) -> zu_common::Result<Vec<BlockPtr>>",
      "doc": "Decodes a free-list chain payload: concatenated little-endian u64\nblock pointers, each nonnull, in range, and unique. `zu verify` uses\nthis too, so a corrupt list is an open error, not a later overwrite of\nlive data."
    },
    {
      "id": "zu::zu1::fold",
      "kind": "module",
      "name": "fold",
      "doc": "The checkpoint fold: overlays into new sealed segments, published\nby one header flip, per docs/08 section 4.\n\nA fold rebuilds every table the overlay store touched into fresh\nblocks without publishing anything, then updates the catalog root,\nthe table index root, and `wal_seq` in a single [`Zu1File::checkpoint`]\ncall. That one flip is the atomicity point: a crash anywhere before\nit leaves the old roots, the old `wal_seq`, and the intact WAL, so\nrecovery replays the same txns onto the same base and the fold's\nhalf-written blocks are unreferenced garbage. A crash after it finds\n`wal_seq` at the folded epoch, so replay skips everything the fold\nsealed even when the WAL truncation never ran. Double apply is\nimpossible on either side.\n\nDeletes fold as tombstones, not compaction: offsets stay stable and\nrows recycle only under VACUUM per docs/03. The database header has\nno room for another root (its seven words and crc fill the body\nexactly), so each table's tombstone set persists as a meta chain\nunder the reserved table index key `id | TOMBSTONE_KEY`, and\n[`recover`] seeds it back into the overlay store at epoch 0.\n\nColumn ids in overlay data are positions in the table's props\ndirectory, the convention the WAL records established. Fold\ngranularity is the whole table; group-local rebuilds with slack\narrive with the updatable CSR work."
    },
    {
      "id": "zu::zu1::fold::TOMBSTONE_KEY",
      "kind": "constant",
      "name": "TOMBSTONE_KEY",
      "signature": "u32",
      "doc": "Table index entries with this bit carry a tombstone chain for the\nnode table `id & !TOMBSTONE_KEY`. Catalog ids stay under the 14-bit\n`NodeId` field, so the bit can never collide with a real table."
    },
    {
      "id": "zu::zu1::fold::checkpoint_fold",
      "kind": "function",
      "name": "checkpoint_fold",
      "signature": "fn checkpoint_fold(db: &mut zu_zu1::file::Zu1File, mvcc: &mut zu_zu1::txn::Mvcc, wal: &mut zu_zu1::wal::Wal) -> zu_common::Result<()>",
      "doc": "Folds every overlay at the store's current epoch into new sealed\nsegments, publishes them, and truncates the WAL. On return the\noverlay store is empty at the same epoch with persisted tombstones\nreseeded, and the file reads identically to the pre-fold snapshot\nat that epoch. On error nothing published: the header, the WAL, and\nthe store are exactly as they were, and any blocks written are\nunreferenced."
    },
    {
      "id": "zu::zu1::fold::decode_tombstones",
      "kind": "function",
      "name": "decode_tombstones",
      "signature": "fn decode_tombstones(bytes: &[u8]) -> zu_common::Result<Vec<u64>>",
      "doc": "Decodes a tombstone chain, rejecting unsorted or duplicate offsets."
    },
    {
      "id": "zu::zu1::fold::encode_tombstones",
      "kind": "function",
      "name": "encode_tombstones",
      "signature": "fn encode_tombstones(offsets: &[u64]) -> Vec<u8>",
      "doc": "Encodes a sorted tombstone set: `version: u16`, `count: u64`, then\nthe offsets ascending."
    },
    {
      "id": "zu::zu1::fold::recover",
      "kind": "function",
      "name": "recover",
      "signature": "fn recover(db: &mut zu_zu1::file::Zu1File, wal: &mut zu_zu1::wal::Wal) -> zu_common::Result<zu_zu1::txn::Mvcc>",
      "doc": "Rebuilds the overlay store after a crash or reopen: committed txns\nabove the header's `wal_seq` replay from the log, an `IngestRef`\nresolves back into its payload by reading the sealed segments it\nnames, then every persisted tombstone chain seeds back in at epoch\n0.\n\nA file with a transaction still open on it is put back first. The\nlog is cut to what the state going back in had folded, because a\nframe above that is a statement of the transaction being taken back\nand replaying it would put the transaction back on as an overlay,\nand then the kept state is published. Nothing here is a special case\nafterwards: the file reads as one whose last statement was the one\nbefore the transaction started."
    },
    {
      "id": "zu::zu1::fold::staged_fold",
      "kind": "function",
      "name": "staged_fold",
      "signature": "fn staged_fold(db: &mut zu_zu1::file::Zu1File, mvcc: &mut zu_zu1::txn::Mvcc, wal: &mut zu_zu1::wal::Wal) -> zu_common::Result<()>",
      "doc": "Folds the same way, and stops before the header flip.\n\nThis is the shape a write statement wants. The fold is what makes a\ncommitted change something the next `MATCH` reads, so it has to\nhappen per commit; the flip is what makes it something the next\nprocess reads, and the log already says that. Two full syncs of the\nthree a one cell write pays are the flip, and the frame the commit\nsynced covers exactly the window skipping them opens: a crash finds\nthe older header and replays the log back to where the fold had\ngot to.\n\nWhat it costs is file growth. Nothing freed can be handed out again\nuntil a checkpoint publishes, so every fold in between takes fresh\nblocks for what it rewrites, and the caller watches\n[`Zu1File::unpublished_blocks`] and publishes on a threshold."
    },
    {
      "id": "zu::zu1::fullzip",
      "kind": "module",
      "name": "fullzip",
      "doc": "Column segments in the FullZip structural layout of\n`docs/04-storage-zu1-format.md` §3: variable-width values zipped with\ntheir lengths, for STRING past the dict threshold, BLOB, VECTOR, and\nLIST payloads. Zipping means a row range is one contiguous byte range,\nand the per-chunk offset samples make the seek to any row O(1): jump\nto the chunk holding it, then walk at most 1023 inline lengths.\n\nPayload layout, all little-endian: `chunk_count: u32`, then one `u64`\ncumulative compressed end offset per chunk (relative to the body),\nthen one `u64` cumulative zipped size per chunk, then the body of\nconcatenated chunks. Each chunk covers up to 1024 rows and starts with\none encoding id byte: Plain (0) is the zipped form itself, one\n`len: u32` before each row's bytes, and FSST (8) is that form behind\n`zu_encoding::fsst`, self-contained symbol table and all. The writer\nkeeps whichever is smaller per chunk. The zipped sizes are what let a\nreader size and verify every chunk before touching it: the FSST\nceiling is exact, a plain chunk must match to the byte, and a chunk\nclaiming more than [`MAX_CHUNK_RAW`] is rejected before anything is\nallocated.\n\nThe meta's `uncompressed_bytes` holds the total value bytes (lengths\nexcluded), and the full scan cross-checks the decoded total against\nit. The zone map does not apply to byte payloads, so FullZip metas\ncarry `min = max = 0`. As with MiniBlock, the full scan verifies the\npayload crc and every structural claim; the point path skips the crc\nby design and bounds every access instead."
    },
    {
      "id": "zu::zu1::fullzip::MAX_CHUNK_RAW",
      "kind": "constant",
      "name": "MAX_CHUNK_RAW",
      "signature": "usize",
      "doc": "Format rule: a chunk's zipped form holds at most 16 MiB. Chunks cover\nat most 1024 rows, so this admits values averaging 16 KiB per chunk\nwhile bounding what a hostile index can make a reader allocate.\nLarger single values need the continuation design that arrives with\nthe column catalog; the writer refuses them rather than truncate."
    },
    {
      "id": "zu::zu1::fullzip::decode_payload",
      "kind": "function",
      "name": "decode_payload",
      "signature": "fn decode_payload(payload: &[u8], value_count: u64, uncompressed_bytes: u64, bytes_out: &mut Vec<u8>, ends_out: &mut Vec<u64>) -> zu_common::Result<()>",
      "doc": "Decodes a full FullZip payload, appending every value's bytes to\n`bytes_out` and each value's absolute end offset in `bytes_out` to\n`ends_out`, so value `i` of an initially empty pair is\n`bytes_out[ends_out[i - 1]..ends_out[i]]` with `ends_out[-1]` read as\nzero. Verifies every chunk against the index, every row against its\nchunk, and the value byte total against `uncompressed_bytes`. A\nrejected payload leaves both vectors untouched. Public alongside the\nother container decoders so the fuzz targets reach it without a file."
    },
    {
      "id": "zu::zu1::fullzip::encode_payload",
      "kind": "function",
      "name": "encode_payload",
      "signature": "fn encode_payload(values: &[&[u8]], out: &mut Vec<u8>) -> zu_common::Result<u64>",
      "doc": "Encodes `values` into the FullZip payload, appending to `out`, and\nreturns the total value bytes. Public so the fuzz seeds can build a\npayload without a file around it."
    },
    {
      "id": "zu::zu1::fullzip::read_blob_range",
      "kind": "function",
      "name": "read_blob_range",
      "signature": "fn read_blob_range(db: &mut zu_zu1::file::Zu1File, meta: &zu_zu1::segment::SegmentMeta, start: u64, end: u64, bytes_out: &mut Vec<u8>, ends_out: &mut Vec<u64>) -> zu_common::Result<()>",
      "doc": "Point access: appends values `[start, end)` to `bytes_out` and their\nabsolute end offsets to `ends_out`, decoding only the chunks that\ncover the range and reading only the bytes they occupy."
    },
    {
      "id": "zu::zu1::fullzip::read_blob_row",
      "kind": "function",
      "name": "read_blob_row",
      "signature": "fn read_blob_row(db: &mut zu_zu1::file::Zu1File, meta: &zu_zu1::segment::SegmentMeta, row: u64, out: &mut Vec<u8>) -> zu_common::Result<()>",
      "doc": "Point access to one row: appends value `row`'s bytes to `out`."
    },
    {
      "id": "zu::zu1::fullzip::read_blob_segment",
      "kind": "function",
      "name": "read_blob_segment",
      "signature": "fn read_blob_segment(db: &mut zu_zu1::file::Zu1File, meta: &zu_zu1::segment::SegmentMeta, bytes_out: &mut Vec<u8>, ends_out: &mut Vec<u64>) -> zu_common::Result<()>",
      "doc": "Reads a segment back, verifying the payload crc, and appends every\nvalue to `bytes_out` and `ends_out` as [`decode_payload`] does."
    },
    {
      "id": "zu::zu1::fullzip::rewrite_blob_reordered",
      "kind": "function",
      "name": "rewrite_blob_reordered",
      "signature": "fn rewrite_blob_reordered(db: &mut zu_zu1::file::Zu1File, old: &zu_zu1::segment::SegmentMeta, values: &[&[u8]]) -> zu_common::Result<zu_zu1::segment::SegmentMeta>",
      "doc": "The same, for a column that is already on disk in some other order:\nthe values are all of them again, but the table they were encoded\nwith is still good, so it comes off the old segment instead of being\ntrained a second time on the same strings.\n\nThis is the shape an edge write leaves. A rel table holds its edges\nsorted by the row they leave, so an edge added in the middle moves\nevery edge behind it, and the fold hands the whole column back in the\nnew order. Nothing about the strings changed, only where they sit, so\ntraining on them again is work with a known answer: reading the table\nback and encoding with it writes a column of the same size for the\ncost of the encode, and on the LinkBench shape that is 20 ms of an\nadd-link statement gone.\n\nA column that really is different from the one on disk is caught the\nway it always was, per chunk: a chunk that encodes well off the\ntable's own ratio buys a table of its own. If the old segment holds\nnothing to read a table out of, this trains one."
    },
    {
      "id": "zu::zu1::fullzip::rewrite_blob_segment",
      "kind": "function",
      "name": "rewrite_blob_segment",
      "signature": "fn rewrite_blob_segment(db: &mut zu_zu1::file::Zu1File, old: &zu_zu1::segment::SegmentMeta, updates: &std::collections::BTreeMap<u64, Vec<u8>>, appended: &[Vec<u8>]) -> zu_common::Result<zu_zu1::segment::SegmentMeta>",
      "doc": "Writes the segment a fold leaves behind when it changed `updates`\nand appended `appended` to `old`: the chunks holding a changed row\nare read and encoded again, and every other chunk keeps the bytes it\nalready encodes to, index entries and all.\n\nThis is [`crate::segment::rewrite_segment`] for the byte side, and it\nsaves more than that one does, because a blob chunk costs a symbol\ntable to encode and a walk of its inline lengths to read. A one cell\nwrite into a 100k row string column used to decode the column into a\nvector per row and encode all 98 chunks of it back; now it touches\nthe one chunk the row is in. The table is trained on the rows that\nare about to be written rather than on the column, which is exactly\nwhat the rewrite exists to avoid reading, and a chunk carries its own\ntable when it needs one, so a chunk encoded now still decodes beside\nchunks encoded against an older table."
    },
    {
      "id": "zu::zu1::fullzip::write_blob_segment",
      "kind": "function",
      "name": "write_blob_segment",
      "signature": "fn write_blob_segment(db: &mut zu_zu1::file::Zu1File, values: &[&[u8]]) -> zu_common::Result<zu_zu1::segment::SegmentMeta>",
      "doc": "Encodes `values` chunk by chunk and writes the FullZip payload out\nthrough the file's placer."
    },
    {
      "id": "zu::zu1::graph",
      "kind": "module",
      "name": "graph",
      "doc": "Bulk-loaded graph storage: node groups of 131,072 rows, per-group CSR\nin both directions, and the group directory meta chain.\n\nThis is the read-optimized COPY path of `docs/04-storage-zu1-format.md`\n§2 and §4. Each group stores two segments per direction: slot offsets\n(131,073 monotone values, so delta wins the cascade) and neighbor ids\nas dense row ids, sorted per list, which is what hits the bits per\nedge target. Fwd is keyed by source, Bwd by destination, so both\nout-neighbors and in-neighbors answer without scanning. Slack gaps and\nthe spill chain arrive with the updatable CSR; bulk-built groups are\ndense.\n\nDirectory layout (version-prefixed, hand-rolled):\n`version: u16`, `from_count: u64`, `to_count: u64`,\n`edge_count: u64`, `props: BlockPtr`, `group_count: u32`,\n`has_keys: u8`, then when `has_keys` is 1 the key and row\n`SegmentMeta` of the primary-key index, then per group\n`row_count: u32`, `edge_base: u64`, and the fwd offsets, fwd\nneighbors, bwd offsets, and bwd neighbors `SegmentMeta`.\n\nA rel table runs between two node tables, which need not be the same\none and need not be the same size: `from_count` is the row domain\nthe forward direction is keyed by and `to_count` the one the\nbackward direction is. The two share a single group array, as long\nas the longer of them needs, because every reader reaches a group by\nrow and a direction at once and the shorter end simply has nothing\npast its own domain. A group beyond an end's rows holds that end's\nempty CSR, the same one a table of no rows loads, which costs a\nsegment of one offset and no neighbors.\n\nEach rel table's directory is its own meta chain, reached through the\ncatalog and the table index of `crate::catalog`, so one file holds any\nnumber of named graphs and a bulk load replaces only the table it\nnames.\n\nEdges carry properties the way nodes do, through a props directory of\n`crate::props`, hung off `props` here rather than off the table index,\nwhose entry for a rel id is this directory. The row domain of those\ncolumns is the edge ordinal: the position of an edge in the sorted\nload order, which is also its position in the forward neighbor arrays\nread group after group, so `edge_base` plus the slot a destination\nsits in names the row without anything being stored per edge to say\nso. Reading a property backward costs the search that finds the slot\n(see [`GraphReader::edge_ordinal`]), and nothing in either direction\ncosts a permutation on disk."
    },
    {
      "id": "zu::zu1::graph::CsrArrays",
      "kind": "type-alias",
      "name": "CsrArrays",
      "signature": "(std::sync::Arc<Vec<u64>>, std::sync::Arc<Vec<u64>>)",
      "doc": "A pooled pin of one direction of a group's CSR: the decoded offset\nand neighbor arrays as shared handles."
    },
    {
      "id": "zu::zu1::graph::Densified",
      "kind": "type-alias",
      "name": "Densified",
      "signature": "(Vec<(u32, u32)>, Vec<u64>)",
      "doc": "A densified edge list: the edges over dense rows, plus the original\nkey of every row in the shape [`bulk_load_keyed`] takes."
    },
    {
      "id": "zu::zu1::graph::Direction",
      "kind": "enum",
      "name": "Direction",
      "doc": "Traversal direction: Fwd follows edges source to destination, Bwd the\nreverse."
    },
    {
      "id": "zu::zu1::graph::Direction::Bwd",
      "kind": "variant",
      "name": "Bwd",
      "of": "zu::zu1::graph::Direction",
      "signature": "Bwd"
    },
    {
      "id": "zu::zu1::graph::Direction::Fwd",
      "kind": "variant",
      "name": "Fwd",
      "of": "zu::zu1::graph::Direction",
      "signature": "Fwd"
    },
    {
      "id": "zu::zu1::graph::DirectionMeta",
      "kind": "struct",
      "name": "DirectionMeta",
      "doc": "One direction of a group's CSR: `row_count + 1` offsets into the\nneighbor segment."
    },
    {
      "id": "zu::zu1::graph::DirectionMeta::neighbors",
      "kind": "field",
      "name": "neighbors",
      "of": "zu::zu1::graph::DirectionMeta",
      "signature": "zu_zu1::segment::SegmentMeta"
    },
    {
      "id": "zu::zu1::graph::DirectionMeta::offsets",
      "kind": "field",
      "name": "offsets",
      "of": "zu::zu1::graph::DirectionMeta",
      "signature": "zu_zu1::segment::SegmentMeta"
    },
    {
      "id": "zu::zu1::graph::Directory",
      "kind": "struct",
      "name": "Directory",
      "doc": "The per-table group directory, stored as one meta chain."
    },
    {
      "id": "zu::zu1::graph::Directory::decode",
      "kind": "method",
      "name": "decode",
      "of": "zu::zu1::graph::Directory",
      "signature": "fn decode(bytes: &[u8]) -> Result<Self>",
      "doc": "Decodes a directory chain payload. Public alongside the other\ncontainer decoders so tooling and the fuzz targets reach it\nwithout a file around it."
    },
    {
      "id": "zu::zu1::graph::Directory::edge_count",
      "kind": "field",
      "name": "edge_count",
      "of": "zu::zu1::graph::Directory",
      "signature": "u64"
    },
    {
      "id": "zu::zu1::graph::Directory::from_count",
      "kind": "field",
      "name": "from_count",
      "of": "zu::zu1::graph::Directory",
      "signature": "u64",
      "doc": "Rows of the node table the edges leave, the domain a source id\nand the forward CSR are numbered in."
    },
    {
      "id": "zu::zu1::graph::Directory::groups",
      "kind": "field",
      "name": "groups",
      "of": "zu::zu1::graph::Directory",
      "signature": "Vec<GroupMeta>"
    },
    {
      "id": "zu::zu1::graph::Directory::keys",
      "kind": "field",
      "name": "keys",
      "of": "zu::zu1::graph::Directory",
      "signature": "Option<zu_zu1::keys::KeyIndex>",
      "doc": "Primary-key index over original node ids, present when the load\nrelabeled rows."
    },
    {
      "id": "zu::zu1::graph::Directory::one_domain",
      "kind": "method",
      "name": "one_domain",
      "of": "zu::zu1::graph::Directory",
      "signature": "fn one_domain(&self) -> Result<u64>",
      "doc": "The one row domain of a rel table that runs inside a single node\ntable, for the algorithms whose answer is a value per node and\nwhich therefore only mean anything on such a table. A table\nbetween two ends of different sizes has no such domain and is\nrefused here; the binder refuses the cross table case ahead of\nthis, and this is what keeps a caller reaching past it honest."
    },
    {
      "id": "zu::zu1::graph::Directory::props",
      "kind": "field",
      "name": "props",
      "of": "zu::zu1::graph::Directory",
      "signature": "zu_zu1::file::BlockPtr",
      "doc": "Root of the edge property chain, [`NULL_BLOCK`] when the table\nstores none. Its row domain is the edge ordinal."
    },
    {
      "id": "zu::zu1::graph::Directory::rows",
      "kind": "method",
      "name": "rows",
      "of": "zu::zu1::graph::Directory",
      "signature": "fn rows(&self, dir: Direction) -> u64",
      "doc": "The row domain `dir` is keyed by: sources for Fwd, destinations\nfor Bwd. This is the range a node id must be in to have a list\nin that direction at all."
    },
    {
      "id": "zu::zu1::graph::Directory::to_count",
      "kind": "field",
      "name": "to_count",
      "of": "zu::zu1::graph::Directory",
      "signature": "u64",
      "doc": "Rows of the node table the edges arrive at, the domain a\ndestination id and the backward CSR are numbered in. Equal to\n`from_count` when both ends are the same table, which is what a\nload naming one node table produces."
    },
    {
      "id": "zu::zu1::graph::EdgePatch",
      "kind": "struct",
      "name": "EdgePatch",
      "doc": "Read access to a bulk-loaded graph, caching the most recently decoded\ngroup per direction so sequential scans decode each group once. The\ntwo directions cache independently because a plan often walks both\non the same rel row by row, an expand backward feeding a count\nforward, and a shared slot would decode a full group per row.\nThe edges committed since the last fold, held the way a reader\nwants them rather than the way the log wrote them.\n\nAn edge added to a rel table does not have to wait for a fold to be\nreadable: the file holds a sorted list per node per direction, and\na handful of extra edges merge into one of those as it is read. So\na commit that only adds edges leaves them here, the reader merges,\nand the CSR is rebuilt once for the run of them rather than once\neach.\n\nAn edge taken away is the same trade the other way round. It cannot\nbe tombstoned the way a row is, because a row is named by an offset\nthe reader already has in hand and an edge is named by the pair it\nruns between; so what is kept is the pair, and the merge drops it\nout of both lists on the way past. A `DETACH DELETE` is where they\ncome from, and it is the reason the row it took away can go without\nthe CSR being rebuilt around it.\n\nAn edge's ordinal is its row in the rel table's property columns.\nThe base holds `base` of them, in the order the forward CSR lays\nthem out, and an edge in here takes the next one after those, so\nthe two spaces do not overlap and an unfolded edge names its own\nvalues. Both directions carry the ordinal, which is what makes the\nsame edge read forward and read backward carry the same row.\n\nIt is held as a sealed run with the recent edges beside it, the way\n[`CellPatch`](crate::props::CellPatch) holds the cells and for the\nsame reason. A commit that added to the lists used to copy the whole\nof them, because a reader was holding the patch as it stood before,\nand a run of a few thousand commits of that is quadratic in how long\nthe run gets. So the run sits behind an `Arc` a copy shares, the\nedges since the last seal sit in a side of their own, and what a\ncommit copies is bounded by [`EDGE_SEAL`] however many edges the run\nhas reached."
    },
    {
      "id": "zu::zu1::graph::EdgePatch::add",
      "kind": "method",
      "name": "add",
      "of": "zu::zu1::graph::EdgePatch",
      "signature": "fn add(&mut self, src: u64, dst: u64) -> u64",
      "doc": "Takes one edge and answers with the ordinal it was given.\n\nA pair the dead set holds stays in it. The two sets say different\nthings about different copies: the dead set covers whatever the\nfile holds under that pair, and the lists here hold the one copy\nthis patch has added. A pair that was taken away and put back is\nexactly that shape, and it has to be, because the copy going in\ncarries the values this statement wrote and the file's copies\ncarry the ones the last fold sealed. Reviving the file's copies\ninstead would put the old values back and leave two edges where\nthe statement asked for one.\n\nWhat this does rest on is that the patch holds at most one copy\nof a pair, which is what [`Self::holds`] is asked before a\ndeferred add is taken."
    },
    {
      "id": "zu::zu1::graph::EdgePatch::adds",
      "kind": "method",
      "name": "adds",
      "of": "zu::zu1::graph::EdgePatch",
      "signature": "fn adds(&self) -> u64",
      "doc": "How many edges this holds, which is what a writer bounds when it\ndecides whether to keep deferring the fold."
    },
    {
      "id": "zu::zu1::graph::EdgePatch::adds_on",
      "kind": "method",
      "name": "adds_on",
      "of": "zu::zu1::graph::EdgePatch",
      "signature": "fn adds_on(&self, node: u64, dir: Direction) -> impl Iterator",
      "doc": "The neighbors `node` has in `dir` that this patch put there,\nwhich are exactly the ones no read of the file turns up. A\nwriter deciding whether a row can go asks this beside the file's\nown list, because between the two is the whole list a fold would\nhave found on the row."
    },
    {
      "id": "zu::zu1::graph::EdgePatch::base",
      "kind": "method",
      "name": "base",
      "of": "zu::zu1::graph::EdgePatch",
      "signature": "fn base(&self) -> u64",
      "doc": "The first ordinal this patch handed out, so a caller holding one\ncan tell an edge in here from an edge in the file."
    },
    {
      "id": "zu::zu1::graph::EdgePatch::drops",
      "kind": "method",
      "name": "drops",
      "of": "zu::zu1::graph::EdgePatch",
      "signature": "fn drops(&self, node: u64, other: u64, dir: Direction) -> bool",
      "doc": "Whether this has taken away the edges between `node` and `other`,\n`node` being the end the list is keyed by."
    },
    {
      "id": "zu::zu1::graph::EdgePatch::holds",
      "kind": "method",
      "name": "holds",
      "of": "zu::zu1::graph::EdgePatch",
      "signature": "fn holds(&self, src: u64, dst: u64) -> bool",
      "doc": "Whether this holds an edge from `src` to `dst`, which is what a\nwriter asks before adding one: two edges over one pair would\ntake ordinals that are not consecutive, and every read of an\nedge property counts on a pair's copies being so."
    },
    {
      "id": "zu::zu1::graph::EdgePatch::is_empty",
      "kind": "method",
      "name": "is_empty",
      "of": "zu::zu1::graph::EdgePatch",
      "signature": "fn is_empty(&self) -> bool",
      "doc": "Whether it has neither added nor taken anything away."
    },
    {
      "id": "zu::zu1::graph::EdgePatch::new",
      "kind": "method",
      "name": "new",
      "of": "zu::zu1::graph::EdgePatch",
      "signature": "fn new(base: u64) -> Self",
      "doc": "An empty patch over a base holding `base` edges."
    },
    {
      "id": "zu::zu1::graph::EdgePatch::ordinal",
      "kind": "method",
      "name": "ordinal",
      "of": "zu::zu1::graph::EdgePatch",
      "signature": "fn ordinal(&self, src: u64, dst: u64) -> Option<u64>",
      "doc": "The ordinal the copy of `src -> dst` this patch added took, which\nis also the row of the rel table its property values sit in, so\na writer with a value to put on that edge knows where it goes."
    },
    {
      "id": "zu::zu1::graph::EdgePatch::remove",
      "kind": "method",
      "name": "remove",
      "of": "zu::zu1::graph::EdgePatch",
      "signature": "fn remove(&mut self, src: u64, dst: u64)",
      "doc": "Takes one edge away, by the pair it runs between, which is every\ncopy of it the file holds.\n\nA pair this patch added itself comes out of the lists first. The\ndead set is left alone either way, because what it says is what\nthe file holds under the pair, and taking the patch's own copy\naway does not change that: a pair the file holds and the patch\nhas added is already marked, and one the file does not hold has\nnothing to mark. The ordinal the copy took stays spent, and so\ndoes the row of edge properties that went with it. Both are only\never reached through the lists this drops it from, and the next\nedge takes the next ordinal either way, so what is left behind is\na row nothing reads rather than a hole in a run something counts\non.\n\nA pair added before the last seal is in the run, and taking it\nout of there copies the run. That is the same write the recent\nside answers for free, and it is rare for the same reason: an\nedge and the delete that takes it back arrive together or not at\nall."
    },
    {
      "id": "zu::zu1::graph::EdgePatch::removed",
      "kind": "method",
      "name": "removed",
      "of": "zu::zu1::graph::EdgePatch",
      "signature": "fn removed(&self) -> u64",
      "doc": "How many pairs it has taken away, the other half of the same\nbound."
    },
    {
      "id": "zu::zu1::graph::Ends",
      "kind": "struct",
      "name": "Ends",
      "doc": "The two ends of a rel table: the node table each is, and how many\nrows that table holds. [`Ends::within`] is the common case where\nboth ends are one table, which is what an edge list over a single\nnode table loads as."
    },
    {
      "id": "zu::zu1::graph::Ends::between",
      "kind": "method",
      "name": "between",
      "of": "zu::zu1::graph::Ends",
      "signature": "fn between(from: (&str, u64), to: (&str, u64)) -> Self",
      "doc": "Ends in two node tables, which may still be the same one."
    },
    {
      "id": "zu::zu1::graph::Ends::from",
      "kind": "field",
      "name": "from",
      "of": "zu::zu1::graph::Ends",
      "signature": "(&str, u64)"
    },
    {
      "id": "zu::zu1::graph::Ends::to",
      "kind": "field",
      "name": "to",
      "of": "zu::zu1::graph::Ends",
      "signature": "(&str, u64)"
    },
    {
      "id": "zu::zu1::graph::Ends::within",
      "kind": "method",
      "name": "within",
      "of": "zu::zu1::graph::Ends",
      "signature": "fn within(table: &str, count: u64) -> Self",
      "doc": "Both ends in one node table of `count` rows."
    },
    {
      "id": "zu::zu1::graph::GraphReader",
      "kind": "struct",
      "name": "GraphReader"
    },
    {
      "id": "zu::zu1::graph::GraphReader::csr_group",
      "kind": "method",
      "name": "csr_group",
      "of": "zu::zu1::graph::GraphReader",
      "signature": "fn csr_group(&self, db: &mut Zu1File, group: usize, dir: Direction) -> Result<CsrArrays>",
      "doc": "Pool-backed pins of one group's CSR in `dir`: the offset and\nneighbor arrays as shared handles. Warm calls are two pool map\nprobes and two `Arc` clones, no decode and no copy, which is\nwhat the Snapshot csr surface lends out as borrowed slices."
    },
    {
      "id": "zu::zu1::graph::GraphReader::degree_batch",
      "kind": "method",
      "name": "degree_batch",
      "of": "zu::zu1::graph::GraphReader",
      "signature": "fn degree_batch(&mut self, db: &mut Zu1File, nodes: &[u64], dir: Direction) -> Result<u64>",
      "doc": "Sum of degrees over `nodes` in `dir`. This is the counting\nexpand's bulk read: it touches the 8% offsets pool and never the\n20% adjacency pool, so a count over a hub's neighborhood costs\noffset diffs, not decoded neighbor megabytes."
    },
    {
      "id": "zu::zu1::graph::GraphReader::degree_of",
      "kind": "method",
      "name": "degree_of",
      "of": "zu::zu1::graph::GraphReader",
      "signature": "fn degree_of(&self, db: &mut Zu1File, node: u64, dir: Direction) -> Result<u64>",
      "doc": "Degree of `node` in `dir` from the pooled offset array alone;\nthe neighbor values never decode for a count."
    },
    {
      "id": "zu::zu1::graph::GraphReader::degrees_into",
      "kind": "method",
      "name": "degrees_into",
      "of": "zu::zu1::graph::GraphReader",
      "signature": "fn degrees_into(&mut self, db: &mut Zu1File, nodes: &[u64], dir: Direction, out: &mut [u64]) -> Result<()>",
      "doc": "Adds each node's degree in `dir` onto `out`, position for\nposition, from the pooled offset arrays alone. Same read shape\nas `degree_batch`, kept per row so a caller can multiply\ndegrees across rels instead of summing one."
    },
    {
      "id": "zu::zu1::graph::GraphReader::directory",
      "kind": "method",
      "name": "directory",
      "of": "zu::zu1::graph::GraphReader",
      "signature": "fn directory(&self) -> &Directory"
    },
    {
      "id": "zu::zu1::graph::GraphReader::edge_ordinal",
      "kind": "method",
      "name": "edge_ordinal",
      "of": "zu::zu1::graph::GraphReader",
      "signature": "fn edge_ordinal(&mut self, db: &mut Zu1File, src: u64, dst: u64) -> Result<Option<u64>>",
      "doc": "The row of the edge property columns that `src -> dst` holds,\nand `None` when the edge is not there.\n\nThe ordinal is the edge's place in the load order, which the\nforward CSR lays out group after group and list after list, so\nit is the group's base plus the slot the destination sits in.\nAn edge reached forward could have its ordinal counted out as\nthe expand walks the list, and the vectorized read does exactly\nthat; this is the answer for an edge that arrived any other way,\na backward expand above all, where the slot in the backward\narray says nothing about the forward one.\n\nThis takes the cached-group path [`Self::neighbors_dir`] takes,\nnot the point path [`Self::has_edge`] takes, because an edge\nproperty is read once per edge a pattern matched and those edges\narrive together: the point path decodes an offset chunk and a\nneighbor chunk per call, which measured at about 5us per\nproperty read, against a binary search over a slice the group\ncache already holds. A caller reading one ordinal on its own\npays a group decode for it, which is the same bet the neighbor\nlist makes.\n\nA pair that runs twice names two edges with two ordinals, and a\nlookup given nothing but the pair cannot pick between them: this\nanswers with the first, the earliest of the run in load order.\nA caller that wants every copy takes [`Self::edge_run`], which\nis this plus the length of the run."
    },
    {
      "id": "zu::zu1::graph::GraphReader::edge_run",
      "kind": "method",
      "name": "edge_run",
      "of": "zu::zu1::graph::GraphReader",
      "signature": "fn edge_run(&mut self, db: &mut Zu1File, src: u64, dst: u64) -> Result<Option<(u64, u64)>>",
      "doc": "The whole run of `src -> dst`: the ordinal of its first edge and\nhow many edges the pair holds, so the copies are `base`,\n`base + 1`, up to `base + count - 1`.\n\nA pattern that binds both endpoints matches once per edge, not\nonce per pair, and this is what lets it. The forward list keeps\na pair's copies next to each other, so the run is one\n`partition_point` for its start and a scan for its end, and the\nscan is over equal values a decoded group already holds."
    },
    {
      "id": "zu::zu1::graph::GraphReader::has_edge",
      "kind": "method",
      "name": "has_edge",
      "of": "zu::zu1::graph::GraphReader",
      "signature": "fn has_edge(&self, db: &mut Zu1File, src: u64, dst: u64) -> Result<bool>",
      "doc": "Edge probe on the forward direction: does `src` point at `dst`?"
    },
    {
      "id": "zu::zu1::graph::GraphReader::has_edge_dir",
      "kind": "method",
      "name": "has_edge_dir",
      "of": "zu::zu1::graph::GraphReader",
      "signature": "fn has_edge_dir(&self, db: &mut Zu1File, node: u64, other: u64, dir: Direction) -> Result<bool>",
      "doc": "Edge probe: does `node` list `other` in `dir`? Two offset values\nlocate the list, then the fence array names the one chunk that\ncould hold `other`, so a probe decodes at most one neighbor chunk\nhowever large the degree. This is the primitive behind\n`MATCH (a)-[]->(b)` on bound endpoints."
    },
    {
      "id": "zu::zu1::graph::GraphReader::in_neighbors_into",
      "kind": "method",
      "name": "in_neighbors_into",
      "of": "zu::zu1::graph::GraphReader",
      "signature": "fn in_neighbors_into(&self, db: &mut Zu1File, node: u64, out: &mut Vec<u64>) -> Result<()>",
      "doc": "Point access to the in-neighbor list."
    },
    {
      "id": "zu::zu1::graph::GraphReader::list_chunks",
      "kind": "method",
      "name": "list_chunks",
      "of": "zu::zu1::graph::GraphReader",
      "signature": "fn list_chunks(&self, group: usize, dir: Direction) -> usize"
    },
    {
      "id": "zu::zu1::graph::GraphReader::load",
      "kind": "method",
      "name": "load",
      "of": "zu::zu1::graph::GraphReader",
      "signature": "fn load(db: &mut Zu1File) -> Result<Self>",
      "doc": "Opens the only rel table in the file, the common single-graph\ncase. A file holding several rel tables needs [`Self::load_table`]\nwith a name."
    },
    {
      "id": "zu::zu1::graph::GraphReader::load_table",
      "kind": "method",
      "name": "load_table",
      "of": "zu::zu1::graph::GraphReader",
      "signature": "fn load_table(db: &mut Zu1File, name: &str) -> Result<Self>",
      "doc": "Opens the rel table called `name` through the catalog and the\ntable index."
    },
    {
      "id": "zu::zu1::graph::GraphReader::lookup_key",
      "kind": "method",
      "name": "lookup_key",
      "of": "zu::zu1::graph::GraphReader",
      "signature": "fn lookup_key(&mut self, db: &mut Zu1File, key: u64) -> Result<Option<u64>>",
      "doc": "Resolves an original id through the primary-key index, or errors\nwhen the file was loaded without one. The key segment's chunk\ndirectory loads on the first call and is reused after, so a\nlookup costs two chunk decodes."
    },
    {
      "id": "zu::zu1::graph::GraphReader::neighbor_ordinals_into",
      "kind": "method",
      "name": "neighbor_ordinals_into",
      "of": "zu::zu1::graph::GraphReader",
      "signature": "fn neighbor_ordinals_into(&mut self, db: &mut Zu1File, node: u64, dir: Direction, out: &mut Vec<u64>) -> Result<()>",
      "doc": "Replaces `out` with the load-order ordinal of every edge in\n`node`'s list in `dir`, in list order, so `out[i]` is the\nproperty row of the `i`th neighbor [`Self::neighbors_dir`]\nreports for the same arguments.\n\nThis is what a pattern that reads an edge property wants and\n[`Self::edge_ordinal`] cannot give it. A pair that runs twice is\ntwo edges with two rows, and a lookup holding nothing but the\npair answers with the first of the run for both, so a walk over\na graph where pairs repeat reads one copy's value for every\ncopy. Counting the list out instead is exact, and on the\ngenerated finance graphs a pair repeating is the common case\nrather than the corner one: an account there sends three hundred\ntransfers to a hundred and fifty counterparties.\n\nForward the answer is a range, the group's base plus the list's\nplace in it, which is [`Self::out_neighbors_from`]. Backward the\nslot in the reverse array says nothing about the forward one, so\neach run of one source is looked up once and its copies counted\noff from there. The two directions agree on which copy is which,\nwhich is what makes an edge read forward and the same edge read\nbackward carry the same value: a run of k copies fills k slots\nin each direction and the `i`th backward slot takes the `i`th\nforward ordinal."
    },
    {
      "id": "zu::zu1::graph::GraphReader::neighbors",
      "kind": "method",
      "name": "neighbors",
      "of": "zu::zu1::graph::GraphReader",
      "signature": "fn neighbors(&mut self, db: &mut Zu1File, node: u64) -> Result<&[u64]>",
      "doc": "Returns the sorted out-neighbor list of `node`."
    },
    {
      "id": "zu::zu1::graph::GraphReader::neighbors_dir",
      "kind": "method",
      "name": "neighbors_dir",
      "of": "zu::zu1::graph::GraphReader",
      "signature": "fn neighbors_dir(&mut self, db: &mut Zu1File, node: u64, dir: Direction) -> Result<&[u64]>",
      "doc": "Returns `node`'s sorted list in `dir`, decoding the node's group\non a cache miss.\n\nA node with unfolded edges is answered out of the merge buffer\ninstead of the decoded group, because the list it wants is not\none the file holds anywhere. Every other node, which on a patch\nof a few hundred edges is very nearly all of them, takes the\nslice the group cache already has."
    },
    {
      "id": "zu::zu1::graph::GraphReader::neighbors_dir_into",
      "kind": "method",
      "name": "neighbors_dir_into",
      "of": "zu::zu1::graph::GraphReader",
      "signature": "fn neighbors_dir_into(&self, db: &mut Zu1File, node: u64, dir: Direction, out: &mut Vec<u64>) -> Result<()>",
      "doc": "Point access: appends `node`'s sorted list in `dir` to `out`\nwithout decoding the group. Two offset values locate the list,\nthen only the chunks covering it are read, so a 1-hop read\ntouches at most `2 + ceil(degree / 1024) + 1` chunk decodes and\nbytes on that order rather than the group's megabytes."
    },
    {
      "id": "zu::zu1::graph::GraphReader::neighbors_into",
      "kind": "method",
      "name": "neighbors_into",
      "of": "zu::zu1::graph::GraphReader",
      "signature": "fn neighbors_into(&self, db: &mut Zu1File, node: u64, out: &mut Vec<u64>) -> Result<()>",
      "doc": "Point access to the out-neighbor list."
    },
    {
      "id": "zu::zu1::graph::GraphReader::out_neighbors_from",
      "kind": "method",
      "name": "out_neighbors_from",
      "of": "zu::zu1::graph::GraphReader",
      "signature": "fn out_neighbors_from(&mut self, db: &mut Zu1File, node: u64) -> Result<(&[u64], u64)>",
      "doc": "`node`'s forward list together with the ordinal of the first\nedge in it, so the `i`th neighbor is edge `base + i` of the load\norder and an edge property is a slice index rather than a\nsearch.\n\n[`Self::edge_ordinal`] answers the same question for one pair\nand costs a binary search to do it, which is the right trade for\nan edge that arrived from somewhere else. A kernel walking the\nforward lists already knows where it is, and on a graph where a\npair can repeat the count is the only thing that tells the\ncopies apart."
    },
    {
      "id": "zu::zu1::graph::GraphReader::pinnable",
      "kind": "method",
      "name": "pinnable",
      "of": "zu::zu1::graph::GraphReader",
      "signature": "fn pinnable(&self, group: usize, dir: Direction) -> bool",
      "doc": "Chunks the neighbor array of `group` in `dir` is stored in,\ndirectory only, no decode. This is what says whether pinning a\ngroup is worth it: the pin decodes every one of these chunks,\nand reading one node's list as a range decodes about one of\nthem, so a caller wanting fewer lists than there are chunks is\nbetter off reading each one. It is the same rule\n[`Self::degrees_into`] uses on the offset array.\nWhether pinning `group` in `dir` is still the cheap read. A\ngroup the patch reaches into is built on every pin, so while a\npatch is up the caller is better off taking one list at a time\nhowever many it wants.\n\nA group holding a row appended since the CSR was built is not\npinnable either, and for a harder reason: the offset array of\nthe group stops at the rows the file held, so there is nothing\nto hand a caller for the rows past that and no place to put\nthem. Those rows are read one list at a time like any other."
    },
    {
      "id": "zu::zu1::graph::GraphReader::set_edges",
      "kind": "method",
      "name": "set_edges",
      "of": "zu::zu1::graph::GraphReader",
      "signature": "fn set_edges(&mut self, edges: Option<Arc<EdgePatch>>)",
      "doc": "Hands this reader the edges committed since the last fold, or\ntakes them back away with `None`. Every read below merges them\ninto what the file holds, so a caller that sets this reads a\ngraph the CSR does not hold yet."
    },
    {
      "id": "zu::zu1::graph::GraphReader::set_grown",
      "kind": "method",
      "name": "set_grown",
      "of": "zu::zu1::graph::GraphReader",
      "signature": "fn set_grown(&mut self, from: u64, to: u64)",
      "doc": "Hands this reader the rows the node tables at its two ends have\ntaken on since the last fold, source end first, or takes them\nback away with a pair of zeroes."
    },
    {
      "id": "zu::zu1::graph::GroupMeta",
      "kind": "struct",
      "name": "GroupMeta",
      "doc": "One node group's CSR pair."
    },
    {
      "id": "zu::zu1::graph::GroupMeta::bwd",
      "kind": "field",
      "name": "bwd",
      "of": "zu::zu1::graph::GroupMeta",
      "signature": "DirectionMeta"
    },
    {
      "id": "zu::zu1::graph::GroupMeta::dir",
      "kind": "method",
      "name": "dir",
      "of": "zu::zu1::graph::GroupMeta",
      "signature": "fn dir(&self, dir: Direction) -> &DirectionMeta"
    },
    {
      "id": "zu::zu1::graph::GroupMeta::edge_base",
      "kind": "field",
      "name": "edge_base",
      "of": "zu::zu1::graph::GroupMeta",
      "signature": "u64",
      "doc": "How many edges the groups before this one hold, which is the\nordinal of this group's first forward edge. Stored rather than\nsummed on load because summing means reading the last offset of\nevery group's offsets segment, a chunk read per group, before a\nreader answers anything."
    },
    {
      "id": "zu::zu1::graph::GroupMeta::fwd",
      "kind": "field",
      "name": "fwd",
      "of": "zu::zu1::graph::GroupMeta",
      "signature": "DirectionMeta"
    },
    {
      "id": "zu::zu1::graph::GroupMeta::row_count",
      "kind": "field",
      "name": "row_count",
      "of": "zu::zu1::graph::GroupMeta",
      "signature": "u32",
      "doc": "Rows of the FROM table this group covers, which is what the\nforward direction is keyed by. The backward direction is keyed\nby the TO table and carries its own row count in the length of\nits offsets array, so a group past one end's domain holds an\nempty CSR for that end and a full one for the other."
    },
    {
      "id": "zu::zu1::graph::bulk_load",
      "kind": "function",
      "name": "bulk_load",
      "signature": "fn bulk_load(db: &mut zu_zu1::file::Zu1File, node_count: u64, edges: &[(u32, u32)]) -> zu_common::Result<Directory>",
      "doc": "Bulk-loads both CSR directions from an edge list into `db` as the\ndefault tables `node` and `edge`, publishing them with a checkpoint."
    },
    {
      "id": "zu::zu1::graph::bulk_load_as",
      "kind": "function",
      "name": "bulk_load_as",
      "signature": "fn bulk_load_as(db: &mut zu_zu1::file::Zu1File, node_table: &str, rel_table: &str, node_count: u64, edges: &[(u32, u32)]) -> zu_common::Result<Directory>",
      "doc": "[`bulk_load_keyed`] without a key index."
    },
    {
      "id": "zu::zu1::graph::bulk_load_between",
      "kind": "function",
      "name": "bulk_load_between",
      "signature": "fn bulk_load_between(db: &mut zu_zu1::file::Zu1File, ends: Ends, rel_table: &str, edges: &[(u32, u32)], undirected: bool) -> zu_common::Result<Directory>",
      "doc": "[`bulk_load_as`] for a rel table whose ends are different node\ntables, so a source id is a row of `ends.from` and a destination id\na row of `ends.to`.\n\nThis is what a labelled graph needs: `(:Person)-[:LIVES_IN]->(:City)`\nis two node tables of unrelated sizes with one rel table between\nthem, and reading it backward from a city has to land in the person\ntable rather than in a row of its own."
    },
    {
      "id": "zu::zu1::graph::bulk_load_between_keyed",
      "kind": "function",
      "name": "bulk_load_between_keyed",
      "signature": "fn bulk_load_between_keyed(db: &mut zu_zu1::file::Zu1File, ends: Ends, rel_table: &str, edges: &[(u32, u32)], key_by_row: Option<&[u64]>, undirected: bool) -> zu_common::Result<Directory>",
      "doc": "[`bulk_load_between`] carrying the primary-key index of its FROM\ntable, which is what a dataset of many node files needs.\n\nThe index is sized to `ends.from`, so `key_by_row` is the original id\nof every row of that table and nothing else. Only one rel table has\nto carry it: a lookup finds the index through whichever table leaves\nthe node table, so the second one would be the same map written\ntwice."
    },
    {
      "id": "zu::zu1::graph::bulk_load_keyed",
      "kind": "function",
      "name": "bulk_load_keyed",
      "signature": "fn bulk_load_keyed(db: &mut zu_zu1::file::Zu1File, node_table: &str, rel_table: &str, node_count: u64, edges: &[(u32, u32)], key_by_row: Option<&[u64]>) -> zu_common::Result<Directory>",
      "doc": "Bulk-loads both CSR directions from an edge list into `db` as the rel\ntable `rel_table` over the node table `node_table`, then publishes\nthe catalog, table index, and directory with a checkpoint. `edges`\nmust be sorted by `(src, dst)` and node ids must be dense row ids\nbelow `node_count`. The reverse direction is built from an internally\nsorted `(dst, src)` copy, so peak memory holds the edge list twice.\nA rel table with the same name is replaced and its blocks recycle one\ncheckpoint later; other tables in the file are untouched. The node\ntable's row domain only grows across loads. Returns the directory.\n`key_by_row`, when given, is the original id of every row (the\npre-`REORDER` labels) and builds the primary-key index alongside the\nCSRs; it must hold exactly `node_count` unique keys."
    },
    {
      "id": "zu::zu1::graph::bulk_load_undirected_as",
      "kind": "function",
      "name": "bulk_load_undirected_as",
      "signature": "fn bulk_load_undirected_as(db: &mut zu_zu1::file::Zu1File, node_table: &str, rel_table: &str, node_count: u64, edges: &[(u32, u32)]) -> zu_common::Result<Directory>",
      "doc": "[`bulk_load_as`] for edges with no direction (GH02).\n\nThe edge list is stored the way it is written and nothing is\nmirrored: the reverse CSR every load builds is what answers the\nother way round, and the rel table says the two ways are one edge.\nA pattern that asks for a direction is what tells them apart, so an\nundirected table costs a directed one nothing on disk."
    },
    {
      "id": "zu::zu1::graph::copy_graph_storage",
      "kind": "function",
      "name": "copy_graph_storage",
      "signature": "fn copy_graph_storage(db: &mut zu_zu1::file::Zu1File, tables: &[(u32, u32, zu_zu1::catalog::ElementKind)]) -> zu_common::Result<()>",
      "doc": "Copies everything the tables of one graph hold into blocks of their\nown, which is the storage half of `CREATE GRAPH ... AS COPY OF`\n(GC04).\n\n`tables` pairs each source table id with the id the catalog gave its\ncopy, which the caller has already put in the new graph. The copy is\nblock for block: every segment block of the source is read whole and\nwritten into a freshly allocated one, and the directories are\nre-encoded only because a directory names the blocks its segments\nlive in and those are now different blocks. The bytes of a column,\na CSR array and a key index are the source's byte for byte, so the\ncopy costs a read and a write per block and no decode, and it does\nnot matter to it what the columns hold.\n\nNothing is shared with the source. A copy that pointed at the same\nsegments would be a second name for one graph, and the first write\nto either would show up in both; `COPY OF` is a graph that starts\nout equal and goes its own way.\n\nNothing is published here either, as in [`free_graph_storage`]: the\ntable index and the statistics are staged and the checkpoint that\nstores the catalog makes all three visible at once."
    },
    {
      "id": "zu::zu1::graph::create_empty_rel",
      "kind": "function",
      "name": "create_empty_rel",
      "signature": "fn create_empty_rel(db: &mut zu_zu1::file::Zu1File, catalog: &mut zu_zu1::catalog::Catalog, name: &str, from: u32, to: u32, undirected: bool) -> zu_common::Result<u32>",
      "doc": "Creates a rel table that holds no edges yet, between two node\ntables the caller has already got the ids of, and returns its id.\n\nThis is what an `INSERT` needs when it writes an edge of a type no\ntable is named by. A bulk load makes a rel table out of an edge list\nand would make the node tables with it, which is the wrong shape\nhere twice over: the ends are already there, and there are no edges\nyet because the statement that wants the table is the one about to\nwrite the first one.\n\nThe directory is the one a table of no edges has: both row domains\nas the catalog says they are, and a group per group of the longer\nend holding an empty CSR each way. The catalog is the caller's to\nstore, which is what keeps the table and the rows of the statement\nthat wanted it in one checkpoint."
    },
    {
      "id": "zu::zu1::graph::densify_keyed",
      "kind": "function",
      "name": "densify_keyed",
      "signature": "fn densify_keyed(keys: &[u64], edges: &[(u64, u64)]) -> zu_common::Result<Densified>",
      "doc": "Maps keyed edges onto dense rows: each key's rank in the sorted\ndeduplicated key list becomes its row id, and both endpoints of\nevery edge resolve through that ranking. Returns the mapped edges in\ninput order plus the key of every row, which is exactly the\n`key_by_row` contract of [`bulk_load_keyed`]. An edge endpoint\nabsent from the key list is an error naming the key, because a\nsilently invented node would corrupt the row domain."
    },
    {
      "id": "zu::zu1::graph::free_graph_storage",
      "kind": "function",
      "name": "free_graph_storage",
      "signature": "fn free_graph_storage(db: &mut zu_zu1::file::Zu1File, catalog: &zu_zu1::catalog::Catalog, graph: u32) -> zu_common::Result<()>",
      "doc": "Frees everything the tables of one graph hold, which is the storage\nhalf of `DROP GRAPH` (GC04).\n\nA dropped graph hands its blocks back rather than merely losing its\nname: the free list grows by what its tables held and the next load\nwrites into those blocks instead of past the end of the file. That\nis the whole point of the statement on a file of any size, and it is\nwhy this walks the tables while the catalog still says which ones\nwere its.\n\nNothing is published here. The table index and the statistics are\nstaged, the caller takes the tables out of the catalog it holds, and\nthe checkpoint that stores that catalog makes all three visible at\nonce, so a crash between them leaves the graph exactly as it was."
    },
    {
      "id": "zu::zu1::graph::read_edge_csv",
      "kind": "function",
      "name": "read_edge_csv",
      "signature": "fn read_edge_csv(path: &std::path::Path) -> zu_common::Result<Vec<(u32, u32)>>",
      "doc": "Reads a comma separated `src,dst` edge list. The first line may be a\nheader and is skipped when its first two fields do not parse as\nintegers; a row that fails to parse anywhere else is an error naming\nthe line, same contract as the SNAP reader. Fields are trimmed, so\n`1, 2` and CRLF endings both work, and columns past the second are\nignored the way the SNAP reader ignores trailing fields."
    },
    {
      "id": "zu::zu1::graph::read_edge_csv_with_props",
      "kind": "function",
      "name": "read_edge_csv_with_props",
      "signature": "fn read_edge_csv_with_props(path: &std::path::Path, props: bool) -> zu_common::Result<zu_zu1::props::EdgesWithProps>",
      "doc": "[`read_edge_csv`] with the columns that are neither endpoint read as\nedge property columns, which is what the header is for.\n\nThe header is the schema. Every cell is `name:TYPE`, the form LDBC\nwrites and every bulk loader in this space parses: `:START_ID` and\n`:END_ID` are the two endpoints, `:TYPE`, `:LABEL` and `:ID` carry\nload-time structure and are read from no further, and a named cell\nof `INT64`, `FLOAT64`, `BOOL` or `STRING` is a property column of\nthat name. A cell with no type token is a string column, except that\n`src` and `dst` name the endpoints, which is how a hand written file\nwith no LDBC in it says the same thing. A file whose header names\nneither endpoint uses its first two columns, the way the parquet\nreader falls back to the first two fields.\n\nA column is dense: every row owes it a value, and an empty field is\nan error rather than a null, because an edge column is addressed by\nthe ordinal and a row that skipped one would shift every value after\nit. A row with a different field count than the header is an error\nfor the same reason, which is also what a comma inside an unquoted\nvalue reads as, since fields are split on commas and nothing else.\n\nWith `props` false, or on a file with no header, this is the reader\nabove and no column is built."
    },
    {
      "id": "zu::zu1::graph::read_edge_list",
      "kind": "function",
      "name": "read_edge_list",
      "signature": "fn read_edge_list(path: &std::path::Path) -> zu_common::Result<Vec<(u32, u32)>>",
      "doc": "Reads a whitespace separated `src dst` edge list, the SNAP layout,\nskipping empty lines and `#` comments."
    },
    {
      "id": "zu::zu1::graph::read_key_edge_list",
      "kind": "function",
      "name": "read_key_edge_list",
      "signature": "fn read_key_edge_list(path: &std::path::Path) -> zu_common::Result<Vec<(u64, u64)>>",
      "doc": "Reads a whitespace separated `src dst` edge list of u64 keys: the\nSNAP layout widened to sources whose ids do not fit u32."
    },
    {
      "id": "zu::zu1::graph::read_key_list",
      "kind": "function",
      "name": "read_key_list",
      "signature": "fn read_key_list(path: &std::path::Path) -> zu_common::Result<Vec<u64>>",
      "doc": "Reads a node key list, one u64 per line, skipping empty lines and\n`#` comments. Keys are original source ids too wide for dense rows;\nLDBC SNB ids are the motivating corpus."
    },
    {
      "id": "zu::zu1::graph::read_node_csv",
      "kind": "function",
      "name": "read_node_csv",
      "signature": "fn read_node_csv(path: &std::path::Path) -> zu_common::Result<zu_zu1::props::NodesWithProps>",
      "doc": "Reads a node file: one row a line, the row's own id in the column the\nheader marked `:ID`, and every named column beside it a property of\nthat row.\n\nThe first line is the header, always, which is the one place this\ndiffers from [`read_edge_csv_with_props`]. An edge file can be two\nintegers a line and still be a graph; a node file with no header\nnames no property and no id, so there is nothing to read it as.\n\nThe row an id gets is the line it was written on, counting from zero\npast the header. A rel file names its endpoints by id and the loader\nmaps them through that, so the order of a node file is the order of\nthe table it loads as, and nothing else decides it."
    },
    {
      "id": "zu::zu1::graph::read_rel_csv_keyed",
      "kind": "function",
      "name": "read_rel_csv_keyed",
      "signature": "fn read_rel_csv_keyed(path: &std::path::Path) -> zu_common::Result<zu_zu1::props::KeyedEdgesWithProps>",
      "doc": "Reads a rel file whose endpoints are the ids a node file gave its\nrows rather than row offsets, together with the edge property columns\nthe header names.\n\nThe ids stay as they were written. Which row of which node table each\none is takes both node files to answer, and this reads one file, so\nthe translation belongs to the loader that holds all of them."
    },
    {
      "id": "zu::zu1::ingest",
      "kind": "module",
      "name": "ingest",
      "doc": "The bulk-load WAL bypass of docs/08 sections 3 and 6.\n\nAn ingest writes its payload once, as sealed segments in the data\nfile, and the WAL carries only an `IngestRef` frame naming them, so\nthe log stays the same handful of bytes whether the load is ten\nrows or ten million. The commit protocol orders three durability\npoints: the segments and their manifest chain are written and the\ndata file synced first, then the WAL frame referencing them is\nappended and synced, which is the commit point, and only then does\nthe overlay publish. A crash before the WAL sync leaves the\nsegments as unreferenced garbage in a file whose committed header\nnever knew them; a crash after it leaves a log whose replay reads\nthe segments back into the overlay.\n\nTwo rules make the reference safe to trust across a crash. First,\ningest allocation never touches the committed free list: after a\ncrash that list is what the reopened file hands out, so blocks\nwhose only reference is a WAL frame must sit past the committed\nhigh-water mark, where nothing allocates until a header flip\npublishes the new watermark. The manifest records that watermark\nand recovery raises the reopened handle's in-memory count to it\nbefore reading. Second, the fold that seals the ingested data into\nthe base frees the manifest and segment blocks in the same flip, so\nthey return to circulation only once no replay can need them."
    },
    {
      "id": "zu::zu1::ingest::ingest_edges",
      "kind": "function",
      "name": "ingest_edges",
      "signature": "fn ingest_edges(db: &mut zu_zu1::file::Zu1File, wal: &mut zu_zu1::wal::Wal, mvcc: &mut zu_zu1::txn::Mvcc, rel: u32, src: &[u64], dst: &[u64]) -> zu_common::Result<zu_common::Epoch>",
      "doc": "Bulk-appends edges to a rel table: both endpoint lists sealed as\nsegments in the data file, one `IngestRef` in the log. The edges\njoin the overlay like any committed `insert_rel` batch and the next\nfold merges them into the CSR."
    },
    {
      "id": "zu::zu1::ingest::ingest_nodes",
      "kind": "function",
      "name": "ingest_nodes",
      "signature": "fn ingest_nodes(db: &mut zu_zu1::file::Zu1File, wal: &mut zu_zu1::wal::Wal, mvcc: &mut zu_zu1::txn::Mvcc, table: u32, cols: &[(u32, zu_zu1::props::PropValues)]) -> zu_common::Result<zu_common::Epoch>",
      "doc": "Bulk-appends rows to a node table: every stored column of the table\nexactly once, each carrying one value per new row, sealed straight\ninto the data file with only an `IngestRef` in the log. Visibility\nmatches any other commit: the rows exist at the returned epoch and\nnot before, and the next fold seals them into the base."
    },
    {
      "id": "zu::zu1::keys",
      "kind": "module",
      "name": "keys",
      "doc": "Primary-key index: original node ids surviving `REORDER`, the sealed\nlevel of `docs/04-storage-zu1-format.md` §7.\n\nA relabeled graph stores rows under new ids, so without this index a\ncaller must carry the mapping out of band. The index stores the pairs\n`(key, row)` sorted by key as two ordinary MiniBlock segments: the\nkeys, which delta-pack tightly because they are sorted, and the rows\nin key order. A lookup fence-searches the key segment for the one\nchunk that could hold the key, decodes it, and on a hit reads the\nsingle matching row value, so it costs two chunk decodes however many\nkeys exist. Misses outside the key zone map cost nothing at all.\n\nThe mutable hash overlay of §7 arrives with the updatable CSR; sealed\nbulk loads only need the static level."
    },
    {
      "id": "zu::zu1::keys::KeyIndex",
      "kind": "struct",
      "name": "KeyIndex",
      "doc": "Locations of the two index segments, stored in the group directory."
    },
    {
      "id": "zu::zu1::keys::KeyIndex::keys",
      "kind": "field",
      "name": "keys",
      "of": "zu::zu1::keys::KeyIndex",
      "signature": "zu_zu1::segment::SegmentMeta"
    },
    {
      "id": "zu::zu1::keys::KeyIndex::rows",
      "kind": "field",
      "name": "rows",
      "of": "zu::zu1::keys::KeyIndex",
      "signature": "zu_zu1::segment::SegmentMeta"
    },
    {
      "id": "zu::zu1::keys::KeyReader",
      "kind": "struct",
      "name": "KeyReader",
      "doc": "Read access to a sealed index: holds both segments' chunk\ndirectories so every lookup goes straight to its one chunk, and a\ndecoded-chunk cache per segment so warm lookups that repeat a chunk\nskip the read and the decode. That cache is the B2 budget: a warm\npoint lookup is a fence partition point and a binary search."
    },
    {
      "id": "zu::zu1::keys::KeyReader::load",
      "kind": "method",
      "name": "load",
      "of": "zu::zu1::keys::KeyReader",
      "signature": "fn load(db: &mut Zu1File, index: KeyIndex) -> Result<Self>"
    },
    {
      "id": "zu::zu1::keys::KeyReader::lookup",
      "kind": "method",
      "name": "lookup",
      "of": "zu::zu1::keys::KeyReader",
      "signature": "fn lookup(&mut self, db: &mut Zu1File, key: u64) -> Result<Option<u64>>",
      "doc": "Returns the row stored under `key`, or `None`."
    },
    {
      "id": "zu::zu1::keys::key_by_row",
      "kind": "function",
      "name": "key_by_row",
      "signature": "fn key_by_row(db: &mut zu_zu1::file::Zu1File, index: &KeyIndex, node_count: u64) -> zu_common::Result<Vec<Option<u64>>>",
      "doc": "Reads the index back in the shape [`write_key_index`] took it,\n`key_by_row[row]` being the key of that row over the whole row\ndomain, and `None` where the index holds no key for the row.\n\nA kernel whose answer is stated in original ids rather than in rows\nneeds every key, and asking for them one at a time costs a fence\nsearch each to learn what two sequential segment scans already say.\n\nThe gaps are the rows a DELETE took away, which the index stops\ncovering the moment the fold rebuilds it. They are `None` rather\nthan any particular number because every number is a key some row\ncould legitimately hold."
    },
    {
      "id": "zu::zu1::keys::verify_key_index",
      "kind": "function",
      "name": "verify_key_index",
      "signature": "fn verify_key_index(db: &mut zu_zu1::file::Zu1File, index: &KeyIndex, node_count: u64, dead: &[u64]) -> zu_common::Result<()>",
      "doc": "Full check for `zu verify`: both segments decode clean (crc and zone\nincluded via the scan path), the keys ascend strictly, and the rows\nare exactly the live ones, which are the row domain less the sorted\noffsets `dead` names."
    },
    {
      "id": "zu::zu1::keys::write_key_index",
      "kind": "function",
      "name": "write_key_index",
      "signature": "fn write_key_index(db: &mut zu_zu1::file::Zu1File, key_by_row: &[u64]) -> zu_common::Result<KeyIndex>",
      "doc": "Builds the sealed index from `key_by_row`, where `key_by_row[row]` is\nthe key of row `row`. Keys must be unique."
    },
    {
      "id": "zu::zu1::keys::write_key_index_live",
      "kind": "function",
      "name": "write_key_index_live",
      "signature": "fn write_key_index_live(db: &mut zu_zu1::file::Zu1File, key_by_row: &[u64], dead: &[u64]) -> zu_common::Result<KeyIndex>",
      "doc": "The same over the rows `dead` does not name, which are sorted row\noffsets a DELETE took away.\n\nA row nothing holds any more has no original id, and keeping its key\nwould be worse than pointless: the key is the one thing a later\nINSERT can reasonably ask for back, and an index that still lists it\nrefuses the reuse as a duplicate of a row that is not there. So the\nindex covers the live rows, and a lookup of a key a DELETE took away\nmisses the way a lookup of a key nobody ever held does."
    },
    {
      "id": "zu::zu1::layout",
      "kind": "function",
      "name": "layout",
      "signature": "fn layout(path: &std::path::Path) -> zu_common::Result<Layout>",
      "doc": "Reads a file's [`Layout`].\n\nIt follows four chains and decodes the free list, which is a handful\nof block reads and no scan of the graph, so it is cheap enough to\nrun after every load. It validates every crc it touches on the way,\nbecause [`meta::chain_blocks`] does, but it is not [`verify`]: a\nfile whose segments are corrupt still reports a layout."
    },
    {
      "id": "zu::zu1::meta",
      "kind": "module",
      "name": "meta",
      "doc": "Meta-block chains: linked lists of blocks carrying a byte payload,\nused for the catalog, group directories, free list, and stats.\n\nPer-block layout: `next: u64 LE` (NULL_BLOCK terminates), `len: u32 LE`\nbytes of payload in this block, `crc32c: u32 LE` of that payload, then\nthe payload itself. The chain is written back to front so every `next`\npointer is known before its block is written."
    },
    {
      "id": "zu::zu1::meta::CHAIN_CAPACITY",
      "kind": "constant",
      "name": "CHAIN_CAPACITY",
      "signature": "usize",
      "doc": "Payload bytes one chain block can carry."
    },
    {
      "id": "zu::zu1::meta::chain_blocks",
      "kind": "function",
      "name": "chain_blocks",
      "signature": "fn chain_blocks(db: &mut zu_zu1::file::Zu1File, head: zu_zu1::file::BlockPtr) -> zu_common::Result<Vec<zu_zu1::file::BlockPtr>>",
      "doc": "Follows a chain from `head` and returns the blocks it occupies, with\nthe same validation as [`read_chain`]. The free path uses this to\nrecycle a chain's own storage."
    },
    {
      "id": "zu::zu1::meta::read_chain",
      "kind": "function",
      "name": "read_chain",
      "signature": "fn read_chain(db: &mut zu_zu1::file::Zu1File, head: zu_zu1::file::BlockPtr) -> zu_common::Result<Vec<u8>>",
      "doc": "Follows a chain from `head` and returns the concatenated payload.\n`NULL_BLOCK` reads as empty. Every block's crc is verified, and a chain\nlonger than the block count is rejected as a cycle."
    },
    {
      "id": "zu::zu1::meta::write_chain",
      "kind": "function",
      "name": "write_chain",
      "signature": "fn write_chain(db: &mut zu_zu1::file::Zu1File, payload: &[u8]) -> zu_common::Result<zu_zu1::file::BlockPtr>",
      "doc": "Writes `payload` as a chain of freshly allocated blocks and returns the\nhead pointer, or `NULL_BLOCK` for an empty payload."
    },
    {
      "id": "zu::zu1::props",
      "kind": "module",
      "name": "props",
      "doc": "Node property columns. A node table's entry in the table index\npoints at a props directory chain: variable width columns stored as\nFullZip blob segments, fixed width columns as cascade-encoded u64\nsegments, every column row-aligned with the table's row domain.\n\nA column carries a [`LogicalType`] from the lattice of gql/plan/02,\nwhich is what tells a reader whether the word it pulls out of a\nfixed width lane is a count, a truth value, a float, a day or a\nnanosecond. The lane itself does not care: the cascade encodes 64\nbit words, so a boolean, a float and a date all ride the encoding\nthe integer columns already had, and only the type at the top says\nwhat a word means. This is the whole reason properties can widen\npast strings and integers without a second storage path.\n\nDirectory layout: `version: u16`, `node_count: u64`,\n`column_count: u32`, then per column `name_len: u16` + UTF-8 bytes,\n`type: u8` (a code from [`TYPE_CODES`], or the list code and then\nthe element's code), the column's `SegmentMeta`, and from version 4\na `nullable: u8` flag followed, when it is set, by the segment meta\nof the column's validity words.\n\nA column is dense whether or not it holds a null: every row of the\ntable's domain has a value in the column's segment. What a null adds\nis a second segment saying which of those values a reader may look\nat, one bit per row packed into 64 bit words that ride the same\ninteger cascade the fixed width columns do. A column with no null in\nit has no validity segment, so nothing written before a property\ncould be null costs anything to read now."
    },
    {
      "id": "zu::zu1::props::CellPatch",
      "kind": "struct",
      "name": "CellPatch",
      "doc": "The cell writes a commit made that no fold has sealed into the\ncolumns of one table yet.\n\nA commit used to fold because a reader read the sealed file and\nnothing else, so a change that was not folded was a change the next\n`MATCH` could not see. Rewriting a column to change one cell of it\nis most of what a point write cost. A reader holding one of these\nreads the column as it stands and puts the newer values over the\nrows the patch names, which is what lets the fold wait for the\ncheckpoint.\n\nA value goes over the value the row already holds, so what is\nrefused is the write that has nowhere to go over: a value taken\naway, which lives in the validity mask. It folds the way it always\ndid. A label has a bitset of its own and [`LabelPatch`] carries it."
    },
    {
      "id": "zu::zu1::props::CellPatch::bytes",
      "kind": "method",
      "name": "bytes",
      "of": "zu::zu1::props::CellPatch",
      "signature": "fn bytes(&self) -> usize",
      "doc": "How many bytes the strings in it hold between them, the other\nthing that writer bounds: a word is a word, but a string is\nwhatever the statement wrote, and a run of long ones would be\ncarried until the next fold."
    },
    {
      "id": "zu::zu1::props::CellPatch::cells",
      "kind": "method",
      "name": "cells",
      "of": "zu::zu1::props::CellPatch",
      "signature": "fn cells(&self) -> usize",
      "doc": "How many cells this holds, which is what a writer bounds when it\ndecides whether to keep deferring the fold."
    },
    {
      "id": "zu::zu1::props::CellPatch::is_empty",
      "kind": "method",
      "name": "is_empty",
      "of": "zu::zu1::props::CellPatch",
      "signature": "fn is_empty(&self) -> bool"
    },
    {
      "id": "zu::zu1::props::CellPatch::set",
      "kind": "method",
      "name": "set",
      "of": "zu::zu1::props::CellPatch",
      "signature": "fn set(&mut self, col: usize, row: u64, word: u64)",
      "doc": "Writes `word` onto row `row` of lane column `col`.\n\nA row written twice before a fold is in here once, holding what\nthe later of the two commits left, which is why this answers\nwith nothing: there is no history to keep, only the value a read\ngets now."
    },
    {
      "id": "zu::zu1::props::CellPatch::set_bytes",
      "kind": "method",
      "name": "set_bytes",
      "of": "zu::zu1::props::CellPatch",
      "signature": "fn set_bytes(&mut self, col: usize, row: u64, value: Vec<u8>)",
      "doc": "The same for a blob column, where what the row held also has to\ncome off the byte count the writer bounds."
    },
    {
      "id": "zu::zu1::props::EdgesWithProps",
      "kind": "type-alias",
      "name": "EdgesWithProps",
      "signature": "(Vec<(u32, u32)>, Vec<OwnedColumn>)",
      "doc": "An edge list and one owned column per edge property, which is the\npair every reader outside the file produces: the edges in the order\nthe source had them and one value per edge in the same order."
    },
    {
      "id": "zu::zu1::props::KeyedEdgesWithProps",
      "kind": "type-alias",
      "name": "KeyedEdgesWithProps",
      "signature": "(Vec<(u64, u64)>, Vec<OwnedColumn>)",
      "doc": "The same pair for a rel file that names its endpoints by the ids a\nnode file gave its rows rather than by row offsets, which is what a\ndataset of many node tables is written with. Which row of which table\neach id is takes every node file to answer, so the translation is the\nloader's and not the reader's."
    },
    {
      "id": "zu::zu1::props::LabelPatch",
      "kind": "struct",
      "name": "LabelPatch",
      "doc": "The labels a commit put on rows that no fold has written into the\nbitset yet.\n\nA label change is written as a pair of masks, the bits to put on a\nrow and the bits to take off it, because two statements can name two\nlabels of one row and both have to land. What a reader wants is the\nword, so the composing is done once, on the way in: the writer reads\nthe word the row carried, puts the masks over it, and what is kept\nhere is the answer. That also puts the rules a change has to keep,\nwhich is the fold's business, where the row it lands on is known."
    },
    {
      "id": "zu::zu1::props::LabelPatch::get",
      "kind": "method",
      "name": "get",
      "of": "zu::zu1::props::LabelPatch",
      "signature": "fn get(&self, row: u64) -> Option<u64>",
      "doc": "The word `row` carries, `None` when no commit has named it and\nthe bitset underneath is the whole answer."
    },
    {
      "id": "zu::zu1::props::LabelPatch::is_empty",
      "kind": "method",
      "name": "is_empty",
      "of": "zu::zu1::props::LabelPatch",
      "signature": "fn is_empty(&self) -> bool"
    },
    {
      "id": "zu::zu1::props::LabelPatch::len",
      "kind": "method",
      "name": "len",
      "of": "zu::zu1::props::LabelPatch",
      "signature": "fn len(&self) -> usize",
      "doc": "How many rows this names, which is what a writer bounds when it\ndecides whether to keep deferring the fold."
    },
    {
      "id": "zu::zu1::props::LabelPatch::set",
      "kind": "method",
      "name": "set",
      "of": "zu::zu1::props::LabelPatch",
      "signature": "fn set(&mut self, row: u64, word: u64)",
      "doc": "Puts `word` on `row`, which is what the row carries from here\nuntil a fold seals it."
    },
    {
      "id": "zu::zu1::props::ListElement",
      "kind": "enum",
      "name": "ListElement",
      "doc": "One element of a stored list, in the shape the row format keeps it.\n\nA lane type is a word, the same 64 bit word the scalar lane holds\nfor that type, so a list of dates and a date column agree about what\na day is. Everything else is a run of bytes with its length in front."
    },
    {
      "id": "zu::zu1::props::ListElement::Blob",
      "kind": "variant",
      "name": "Blob",
      "of": "zu::zu1::props::ListElement",
      "signature": "Blob(&[u8])"
    },
    {
      "id": "zu::zu1::props::ListElement::Word",
      "kind": "variant",
      "name": "Word",
      "of": "zu::zu1::props::ListElement",
      "signature": "Word(u64)"
    },
    {
      "id": "zu::zu1::props::ListRows",
      "kind": "enum",
      "name": "ListRows",
      "doc": "How a row of a list column says how many elements it holds.\n\nThe two are not tellable apart from the bytes, which is why this is\ncarried rather than worked out: a bounded `LIST<INT32>(768)` row of\n767 counted elements is the same length as one of 768 uncounted\nones. The column entry says, and a reader takes the answer."
    },
    {
      "id": "zu::zu1::props::ListRows::Counted",
      "kind": "variant",
      "name": "Counted",
      "of": "zu::zu1::props::ListRows",
      "signature": "Counted(usize)",
      "doc": "Every row says its own count, in this many bytes."
    },
    {
      "id": "zu::zu1::props::ListRows::Fixed",
      "kind": "variant",
      "name": "Fixed",
      "of": "zu::zu1::props::ListRows",
      "signature": "Fixed(usize)",
      "doc": "The directory says the count and no row carries one, which is a\ncolumn every row of which was written at its declared bound."
    },
    {
      "id": "zu::zu1::props::NodesWithProps",
      "kind": "type-alias",
      "name": "NodesWithProps",
      "signature": "(Vec<u64>, Vec<OwnedColumn>)",
      "doc": "A node file read whole: the id of every row in row order, and one\nowned column per property the header named."
    },
    {
      "id": "zu::zu1::props::OwnedColumn",
      "kind": "struct",
      "name": "OwnedColumn",
      "doc": "One column held whole, in the shape a reader outside the file\nproduces it: owned values in the row order the source had them.\n\n`PropValues` borrows, which is right at the store call and wrong for\na column that has to survive being read, reordered and only then\nstored. That is the parquet loader's shape exactly, so the owned\nform carries the four types an external edge list brings and\n`store_rel_props_owned` does the borrowing at the one point it is\nneeded."
    },
    {
      "id": "zu::zu1::props::OwnedColumn::name",
      "kind": "field",
      "name": "name",
      "of": "zu::zu1::props::OwnedColumn",
      "signature": "String"
    },
    {
      "id": "zu::zu1::props::OwnedColumn::values",
      "kind": "field",
      "name": "values",
      "of": "zu::zu1::props::OwnedColumn",
      "signature": "OwnedValues"
    },
    {
      "id": "zu::zu1::props::OwnedValues",
      "kind": "enum",
      "name": "OwnedValues",
      "doc": "The values of an [`OwnedColumn`]."
    },
    {
      "id": "zu::zu1::props::OwnedValues::Bool",
      "kind": "variant",
      "name": "Bool",
      "of": "zu::zu1::props::OwnedValues",
      "signature": "Bool(Vec<bool>)"
    },
    {
      "id": "zu::zu1::props::OwnedValues::Float",
      "kind": "variant",
      "name": "Float",
      "of": "zu::zu1::props::OwnedValues",
      "signature": "Float(Vec<f64>)"
    },
    {
      "id": "zu::zu1::props::OwnedValues::Int",
      "kind": "variant",
      "name": "Int",
      "of": "zu::zu1::props::OwnedValues",
      "signature": "Int(Vec<u64>)"
    },
    {
      "id": "zu::zu1::props::OwnedValues::Str",
      "kind": "variant",
      "name": "Str",
      "of": "zu::zu1::props::OwnedValues",
      "signature": "Str(Vec<Vec<u8>>)"
    },
    {
      "id": "zu::zu1::props::OwnedValues::is_empty",
      "kind": "method",
      "name": "is_empty",
      "of": "zu::zu1::props::OwnedValues",
      "signature": "fn is_empty(&self) -> bool",
      "doc": "Whether the column holds no rows."
    },
    {
      "id": "zu::zu1::props::OwnedValues::len",
      "kind": "method",
      "name": "len",
      "of": "zu::zu1::props::OwnedValues",
      "signature": "fn len(&self) -> usize",
      "doc": "Rows held."
    },
    {
      "id": "zu::zu1::props::OwnedValues::permuted",
      "kind": "method",
      "name": "permuted",
      "of": "zu::zu1::props::OwnedValues",
      "signature": "fn permuted(&self, order: &[u32]) -> Self",
      "doc": "The same values in the order `order` names, where `order[i]` is\nthe position the value now at row `i` came from.\n\nThis is how a property column follows its edges. A bulk load\nsorts the edge list, and sorting is what numbers the ordinals a\ncolumn is addressed by, so a column read alongside an unsorted\nedge list belongs to the wrong rows until it is moved the same\nway the edges were."
    },
    {
      "id": "zu::zu1::props::PropColumn",
      "kind": "struct",
      "name": "PropColumn"
    },
    {
      "id": "zu::zu1::props::PropColumn::count_width",
      "kind": "field",
      "name": "count_width",
      "of": "zu::zu1::props::PropColumn",
      "signature": "u8",
      "doc": "How many bytes each row of this list column spends saying its own\nelement count, where it says one at all. Four is what every\nversion before 12 wrote and what an unbounded list still writes.\n\nIt is carried rather than worked out from the declared bound\nbecause a column is read at the width its rows were written at,\nand those are two different questions once the rule has changed\nonce. A version 11 column of `LIST<INT8>(4)` rows holds four byte\ncounts, and a fold that leaves those rows alone has to leave the\nfour with them, whatever a column written today would have used."
    },
    {
      "id": "zu::zu1::props::PropColumn::fixed_len",
      "kind": "field",
      "name": "fixed_len",
      "of": "zu::zu1::props::PropColumn",
      "signature": "Option<u32>",
      "doc": "The element count every row of this list column holds, where the\nrows do not carry one themselves. `None` is every other column,\nand a list column whose rows say their own count.\n\nThis is the encoding half of schema/06 §2 written down: the type\nabove is the declaration, `LIST<FLOAT32>(768)`, and this says\nthat this column was written whole at exactly the bound, so the\nblock may drop what the declaration already gives. It cannot be\nworked out from the bytes, which is why it is a field: a bounded\n`LIST<INT32>(768)` column of 767 counted elements a row is the\nsame 3072 bytes a row as one of 768 uncounted ones."
    },
    {
      "id": "zu::zu1::props::PropColumn::is_lane",
      "kind": "method",
      "name": "is_lane",
      "of": "zu::zu1::props::PropColumn",
      "signature": "fn is_lane(&self) -> bool",
      "doc": "Whether this column stores in the fixed width lane, which is one\n64 bit word per row through the integer cascade, as against the\nblob segments a string or a byte string uses.\n\nEvery fixed stride type of eight bytes or less rides the lane:\nthe cascade encodes words and does not care what the bits mean,\nand the column's type is what says whether a word is a count, a\ntruth value, a float, a day or a nanosecond."
    },
    {
      "id": "zu::zu1::props::PropColumn::list_rows",
      "kind": "method",
      "name": "list_rows",
      "of": "zu::zu1::props::PropColumn",
      "signature": "fn list_rows(&self) -> ListRows",
      "doc": "How to read a row of this list column, which is what\n[`list_elements`] wants beside the bytes: the count the directory\nholds, or the width the row says its own in."
    },
    {
      "id": "zu::zu1::props::PropColumn::meta",
      "kind": "field",
      "name": "meta",
      "of": "zu::zu1::props::PropColumn",
      "signature": "zu_zu1::segment::SegmentMeta"
    },
    {
      "id": "zu::zu1::props::PropColumn::name",
      "kind": "field",
      "name": "name",
      "of": "zu::zu1::props::PropColumn",
      "signature": "String"
    },
    {
      "id": "zu::zu1::props::PropColumn::ty",
      "kind": "field",
      "name": "ty",
      "of": "zu::zu1::props::PropColumn",
      "signature": "zu_common::LogicalType"
    },
    {
      "id": "zu::zu1::props::PropColumn::validity",
      "kind": "field",
      "name": "validity",
      "of": "zu::zu1::props::PropColumn",
      "signature": "Option<zu_zu1::segment::SegmentMeta>",
      "doc": "The column's validity words, one bit per row and the low bit of\na word first, set where the row holds a value. `None` is a\ncolumn every row of which holds one, which is what every column\nwas before a property could be null."
    },
    {
      "id": "zu::zu1::props::PropColumn::zones",
      "kind": "field",
      "name": "zones",
      "of": "zu::zu1::props::PropColumn",
      "signature": "Option<zu_zu1::segment::SegmentMeta>",
      "doc": "The zone plane of a zoned column: one word a row, the offset\nfrom UTC in minutes that the row's value was written with.\n`None` is every other column, which has no second plane.\n\nIt is a segment beside `meta` rather than a second column\nbecause it is not a property: no query names it, nothing\ndeclares it, and it means nothing without the instant it sits\nbeside. Physically it is a lane segment like any other, which is\nwhy a column of one zone costs almost nothing for it."
    },
    {
      "id": "zu::zu1::props::PropInput",
      "kind": "struct",
      "name": "PropInput",
      "doc": "One column at store time: what it is called, the value of every row\nof it, and which of those rows hold one."
    },
    {
      "id": "zu::zu1::props::PropInput::declared",
      "kind": "field",
      "name": "declared",
      "of": "zu::zu1::props::PropInput",
      "signature": "Option<&zu_common::LogicalType>",
      "doc": "The type the catalog says this column has, where that is\nnarrower than the type the values arrive in. `None` is a column\nwhose declaration is exactly what its values imply.\n\nThis is the split of schema/06 §2 at the point it starts: the\ndeclaration comes from the catalog, the values come from the\nbatch, and the encoding is chosen here from both. A caller with\nno catalog to consult passes `None` and gets what it always got.\nWhat the declaration may say is checked against every value\nbefore anything is written, because a column that is declared\n`BINARY(16)` and holds a row of 15 octets is a column whose own\ntype is a lie, and the first reader to trust it reads the next\nrow's bytes."
    },
    {
      "id": "zu::zu1::props::PropInput::dense",
      "kind": "method",
      "name": "dense",
      "of": "zu::zu1::props::PropInput",
      "signature": "fn dense(name: &str, values: PropValues) -> Self",
      "doc": "A column with a value in every row."
    },
    {
      "id": "zu::zu1::props::PropInput::name",
      "kind": "field",
      "name": "name",
      "of": "zu::zu1::props::PropInput",
      "signature": "&str"
    },
    {
      "id": "zu::zu1::props::PropInput::typed",
      "kind": "method",
      "name": "typed",
      "of": "zu::zu1::props::PropInput",
      "signature": "fn typed(name: &str, values: PropValues, declared: &LogicalType) -> Self",
      "doc": "A column with a value in every row, of the type the catalog says\nit has rather than the type its values imply."
    },
    {
      "id": "zu::zu1::props::PropInput::validity",
      "kind": "field",
      "name": "validity",
      "of": "zu::zu1::props::PropInput",
      "signature": "Option<&[u64]>",
      "doc": "One bit per row, the low bit of a word first, set where the row\nholds a value. `None` is a column with a value in every row.\n\nA null row still needs something in `values`, because the column\nis dense either way; what goes there is never read, so a zero or\nan empty string is the usual choice. The mask is what a reader\nconsults, and it is the only thing that says the row is null."
    },
    {
      "id": "zu::zu1::props::PropInput::values",
      "kind": "field",
      "name": "values",
      "of": "zu::zu1::props::PropInput",
      "signature": "PropValues"
    },
    {
      "id": "zu::zu1::props::PropValues",
      "kind": "enum",
      "name": "PropValues",
      "doc": "Row-ordered values for one column at store time.\n\nThe fixed width arms all end up as 64 bit words in the same lane;\nthey are kept apart here so a caller states what it is storing once,\nat the point where it still knows, rather than handing over a bag of\nwords and a type that has to agree with them."
    },
    {
      "id": "zu::zu1::props::PropValues::Bool",
      "kind": "variant",
      "name": "Bool",
      "of": "zu::zu1::props::PropValues",
      "signature": "Bool(&[bool])"
    },
    {
      "id": "zu::zu1::props::PropValues::Bytes",
      "kind": "variant",
      "name": "Bytes",
      "of": "zu::zu1::props::PropValues",
      "signature": "Bytes(&[&[u8]])"
    },
    {
      "id": "zu::zu1::props::PropValues::Date",
      "kind": "variant",
      "name": "Date",
      "of": "zu::zu1::props::PropValues",
      "signature": "Date(&[i32])",
      "doc": "Days since the epoch."
    },
    {
      "id": "zu::zu1::props::PropValues::Duration",
      "kind": "variant",
      "name": "Duration",
      "of": "zu::zu1::props::PropValues",
      "signature": "Duration(zu_common::DurationKind, &[i64])",
      "doc": "Months for a year-month duration, nanoseconds for a day-time one."
    },
    {
      "id": "zu::zu1::props::PropValues::Float",
      "kind": "variant",
      "name": "Float",
      "of": "zu::zu1::props::PropValues",
      "signature": "Float(&[f64])"
    },
    {
      "id": "zu::zu1::props::PropValues::Int",
      "kind": "variant",
      "name": "Int",
      "of": "zu::zu1::props::PropValues",
      "signature": "Int(&[u64])"
    },
    {
      "id": "zu::zu1::props::PropValues::List",
      "kind": "variant",
      "name": "List",
      "of": "zu::zu1::props::PropValues",
      "signature": "List { elem: &zu_common::LogicalType, rows: &[&[ListElement]] }",
      "doc": "One list per row, every list holding elements of `elem`.\n\nThe elements arrive already reduced to what the row format\nkeeps, a word or a run of bytes, so this one arm carries every\nelement type rather than growing a variant per type the way the\nscalar arms do. Which of the two an element must be is settled\nby `elem`, and an element that is the other one is refused at\nstore time."
    },
    {
      "id": "zu::zu1::props::PropValues::LocalDatetime",
      "kind": "variant",
      "name": "LocalDatetime",
      "of": "zu::zu1::props::PropValues",
      "signature": "LocalDatetime(&[i64])",
      "doc": "Nanoseconds since the epoch."
    },
    {
      "id": "zu::zu1::props::PropValues::LocalTime",
      "kind": "variant",
      "name": "LocalTime",
      "of": "zu::zu1::props::PropValues",
      "signature": "LocalTime(&[i64])",
      "doc": "Nanoseconds since midnight."
    },
    {
      "id": "zu::zu1::props::PropValues::Str",
      "kind": "variant",
      "name": "Str",
      "of": "zu::zu1::props::PropValues",
      "signature": "Str(&[&[u8]])"
    },
    {
      "id": "zu::zu1::props::PropValues::ZonedDatetime",
      "kind": "variant",
      "name": "ZonedDatetime",
      "of": "zu::zu1::props::PropValues",
      "signature": "ZonedDatetime { nanos: &[i64], zones: &[i16] }",
      "doc": "Nanoseconds since the epoch in UTC, and the offset from UTC in\nminutes the value was written with, one of each per row.\n\nThe instant is UTC and the offset is beside it rather than\nfolded into it, which is what lets two rows written in two zones\nfor one instant be one number in the lane and still each print\nwhere they were written."
    },
    {
      "id": "zu::zu1::props::PropValues::ZonedTime",
      "kind": "variant",
      "name": "ZonedTime",
      "of": "zu::zu1::props::PropValues",
      "signature": "ZonedTime { nanos: &[i64], zones: &[i16] }",
      "doc": "Nanoseconds since midnight in the offset's own day, and the\noffset from UTC in minutes, one of each per row."
    },
    {
      "id": "zu::zu1::props::PropValues::is_empty",
      "kind": "method",
      "name": "is_empty",
      "of": "zu::zu1::props::PropValues",
      "signature": "fn is_empty(&self) -> bool"
    },
    {
      "id": "zu::zu1::props::PropValues::len",
      "kind": "method",
      "name": "len",
      "of": "zu::zu1::props::PropValues",
      "signature": "fn len(&self) -> usize",
      "doc": "Rows this column carries."
    },
    {
      "id": "zu::zu1::props::PropValues::none_of",
      "kind": "method",
      "name": "none_of",
      "of": "zu::zu1::props::PropValues",
      "signature": "fn none_of(ty: &LogicalType) -> Option<PropValues>",
      "doc": "No values at all, in the arm a column declared `ty` holds them\nin, and `None` for a type no column holds.\n\nThis is [`PropValues::ty`] read the other way, and it is not its\ninverse. The other direction widens: a run of bytes says only\nthat it is a run of bytes, so `BINARY(16)` and `BYTES` both\nanswer the blob arm here and only the second of them comes back\nout of `ty`. What a caller wants from this is the arm, since the\narm is which side of the store the column is on, and the\ndeclaration it passes beside it is the rest of the answer.\n\nA table being made has no rows in it yet, which is why what\ncomes back holds none."
    },
    {
      "id": "zu::zu1::props::PropValues::ty",
      "kind": "method",
      "name": "ty",
      "of": "zu::zu1::props::PropValues",
      "signature": "fn ty(&self) -> LogicalType",
      "doc": "The type a column holding these values is declared as."
    },
    {
      "id": "zu::zu1::props::PropsDirectory",
      "kind": "struct",
      "name": "PropsDirectory",
      "doc": "The property columns of one node table."
    },
    {
      "id": "zu::zu1::props::PropsDirectory::columns",
      "kind": "field",
      "name": "columns",
      "of": "zu::zu1::props::PropsDirectory",
      "signature": "Vec<PropColumn>"
    },
    {
      "id": "zu::zu1::props::PropsDirectory::decode",
      "kind": "method",
      "name": "decode",
      "of": "zu::zu1::props::PropsDirectory",
      "signature": "fn decode(bytes: &[u8]) -> Result<Self>"
    },
    {
      "id": "zu::zu1::props::PropsDirectory::labels",
      "kind": "field",
      "name": "labels",
      "of": "zu::zu1::props::PropsDirectory",
      "signature": "Option<zu_zu1::segment::SegmentMeta>",
      "doc": "The label bitset, one word per row, bit `i` set where the row\ncarries dictionary label `i`. `None` is a table whose rows carry\nits name and nothing else, which is every table until something\ndeclares a second label, and reading one costs nothing because\nthere is nothing to read.\n\nIt rides here rather than in `columns` because it is not a\nproperty: no query names it, no schema declares it, and its\nmeaning is fixed by the catalog's dictionary rather than by a\ncolumn type. Physically it is a column like any other, one lane\nsegment over the table's row domain."
    },
    {
      "id": "zu::zu1::props::PropsDirectory::node_count",
      "kind": "field",
      "name": "node_count",
      "of": "zu::zu1::props::PropsDirectory",
      "signature": "u64"
    },
    {
      "id": "zu::zu1::props::PropsReader",
      "kind": "struct",
      "name": "PropsReader",
      "doc": "Point reads over one table's property columns, keeping decoded\nchunks between reads the way the key reader does: integer columns\nthrough the segment chunk cache, string columns as the last decoded\nFullZip chunk per column, so a scan or a hot row set stays on slice\ncopies instead of a chunk decode per read."
    },
    {
      "id": "zu::zu1::props::PropsReader::chunk_bounds",
      "kind": "method",
      "name": "chunk_bounds",
      "of": "zu::zu1::props::PropsReader",
      "signature": "fn chunk_bounds(&mut self, db: &mut Zu1File, col: usize, chunk: usize) -> Result<Option<(u64, u64)>>",
      "doc": "The value bounds of `chunk` in `col`, `None` when the column\nwas not written sorted and only the segment-level zone applies."
    },
    {
      "id": "zu::zu1::props::PropsReader::col",
      "kind": "method",
      "name": "col",
      "of": "zu::zu1::props::PropsReader",
      "signature": "fn col(&self, name: &str) -> Option<usize>"
    },
    {
      "id": "zu::zu1::props::PropsReader::columns",
      "kind": "method",
      "name": "columns",
      "of": "zu::zu1::props::PropsReader",
      "signature": "fn columns(&self) -> &[PropColumn]"
    },
    {
      "id": "zu::zu1::props::PropsReader::gather_int",
      "kind": "method",
      "name": "gather_int",
      "of": "zu::zu1::props::PropsReader",
      "signature": "fn gather_int(&mut self, db: &mut Zu1File, col: usize, rows: &[u64], out: &mut Vec<u64>) -> Result<()>",
      "doc": "Gathers `col` for arbitrary `rows`, writing `out[i]` for\n`rows[i]`, the batched read of perf/04 section 5: rows sort by\nposition, runs sharing a chunk decode it once, and values\nscatter back to the caller's order.\n\nThe decode goes through the same chunk cache the point read\nkeeps, which is what makes a small gather cheap. An expand hands\nthis a handful of rows per call and calls it once per source\nrow, so a per-call decode meant decoding a whole chunk to read\none value out of it, and a pipeline reading a property off an\nexpanded level ran slower than the row at a time engine it\nreplaces. Warm, a gather is a binary search and a slice copy.\nThe cache holds every chunk it touches, the same bound the point\nread carries, and eviction stays the buffer manager's job\n(docs/09, M3)."
    },
    {
      "id": "zu::zu1::props::PropsReader::gather_str",
      "kind": "method",
      "name": "gather_str",
      "of": "zu::zu1::props::PropsReader",
      "signature": "fn gather_str(&mut self, db: &mut Zu1File, col: usize, rows: &[u64], out_bytes: &mut Vec<u8>, out_ends: &mut Vec<u64>) -> Result<()>",
      "doc": "Gathers a string column for arbitrary `rows`: the values land\nconcatenated in `out_bytes` in row-argument order, value `i`\nending at `out_ends[i]`. Chunk runs decode once, like\n[`Self::gather_int`]; the scatter goes through a span table so\nthe output order is the caller's even though decoding walks the\nrows sorted."
    },
    {
      "id": "zu::zu1::props::PropsReader::has_labels",
      "kind": "method",
      "name": "has_labels",
      "of": "zu::zu1::props::PropsReader",
      "signature": "fn has_labels(&self) -> bool",
      "doc": "Whether the table has a label bitset at all, which is what says\nwhether a scan has a word to read per row. A commit that has not\nfolded counts: its words are labels the rows carry, and the\nfirst of them on a table that stored none is the reason the\nbitset is about to exist."
    },
    {
      "id": "zu::zu1::props::PropsReader::is_nullable",
      "kind": "method",
      "name": "is_nullable",
      "of": "zu::zu1::props::PropsReader",
      "signature": "fn is_nullable(&self, col: usize) -> bool",
      "doc": "Whether `col` has a null in it anywhere, which is what says\nwhether a reader has to ask [`Self::is_valid`] at all."
    },
    {
      "id": "zu::zu1::props::PropsReader::is_valid",
      "kind": "method",
      "name": "is_valid",
      "of": "zu::zu1::props::PropsReader",
      "signature": "fn is_valid(&mut self, db: &mut Zu1File, col: usize, row: u64) -> Result<bool>",
      "doc": "Whether row `row` of `col` holds a value.\n\nA column with no validity segment holds one in every row, which\nis the answer without a read, so a graph that stores no null\npays nothing for the question."
    },
    {
      "id": "zu::zu1::props::PropsReader::label_word",
      "kind": "method",
      "name": "label_word",
      "of": "zu::zu1::props::PropsReader",
      "signature": "fn label_word(&mut self, db: &mut Zu1File, row: u64) -> Result<Option<u64>>",
      "doc": "The label bitset of row `row`, `None` when the table stores\nnone, which is a table whose rows carry its name and nothing\nelse. The bits are dictionary positions, so a caller tests a\npattern's mask against the word with one AND."
    },
    {
      "id": "zu::zu1::props::PropsReader::list_rows",
      "kind": "method",
      "name": "list_rows",
      "of": "zu::zu1::props::PropsReader",
      "signature": "fn list_rows(&self, col: usize) -> ListRows",
      "doc": "How to read a row of list column `col`, which is what\n[`list_elements`] wants beside the bytes."
    },
    {
      "id": "zu::zu1::props::PropsReader::meta",
      "kind": "method",
      "name": "meta",
      "of": "zu::zu1::props::PropsReader",
      "signature": "fn meta(&self, col: usize) -> &SegmentMeta",
      "doc": "The segment meta of `col`: value count and the segment-level\nzone bounds, what a scan needs to size itself and to skip the\nwhole column without reading a block."
    },
    {
      "id": "zu::zu1::props::PropsReader::new",
      "kind": "method",
      "name": "new",
      "of": "zu::zu1::props::PropsReader",
      "signature": "fn new(directory: PropsDirectory) -> Self"
    },
    {
      "id": "zu::zu1::props::PropsReader::read_int",
      "kind": "method",
      "name": "read_int",
      "of": "zu::zu1::props::PropsReader",
      "signature": "fn read_int(&mut self, db: &mut Zu1File, col: usize, row: u64) -> Result<u64>"
    },
    {
      "id": "zu::zu1::props::PropsReader::read_int_column",
      "kind": "method",
      "name": "read_int_column",
      "of": "zu::zu1::props::PropsReader",
      "signature": "fn read_int_column(&mut self, db: &mut Zu1File, col: usize, out: &mut Vec<u64>) -> Result<()>",
      "doc": "Decodes the whole of an integer column into `out`, chunk after\nchunk through one reusable buffer.\n\nThis is for a caller that wants every value at once and in\norder, which a whole-graph kernel does: an edge weight is read\nin whatever order the frontier settles, so a lazy chunk read\nwould revisit chunks and a gather would want an index vector as\nbig as the answer. Nothing is cached on the reader, so the eight\nbytes an edge stay the caller's to drop."
    },
    {
      "id": "zu::zu1::props::PropsReader::read_str",
      "kind": "method",
      "name": "read_str",
      "of": "zu::zu1::props::PropsReader",
      "signature": "fn read_str(&mut self, db: &mut Zu1File, col: usize, row: u64, out: &mut Vec<u8>) -> Result<()>"
    },
    {
      "id": "zu::zu1::props::PropsReader::read_zoned",
      "kind": "method",
      "name": "read_zoned",
      "of": "zu::zu1::props::PropsReader",
      "signature": "fn read_zoned(&mut self, db: &mut Zu1File, col: usize, row: u64) -> Result<(i64, i16)>",
      "doc": "The whole of row `row` of a zoned column: the instant in the\nlane, and the offset from UTC in minutes the value was written\nwith, from the plane beside it.\n\nEvery other read of a zoned column answers the instant, which is\nwhat a comparison, a sort and a range all want and is what the\nlane holds. This is the read that materialises a value, and it\nis the only one that touches the second plane, so a scan that\nfilters on a timestamp and returns no timestamp never pays for\nthe zones at all."
    },
    {
      "id": "zu::zu1::props::PropsReader::rows",
      "kind": "method",
      "name": "rows",
      "of": "zu::zu1::props::PropsReader",
      "signature": "fn rows(&self) -> u64",
      "doc": "Rows in the table's domain; every column is row-aligned to it.\n\nThe rows a commit appended are in here as well, because they are\nrows of the table and a scan that stopped short of them would\nanswer a query without them. What tells the two apart is\n[`Self::stored`], which is where the columns end."
    },
    {
      "id": "zu::zu1::props::PropsReader::scan_int_chunk",
      "kind": "method",
      "name": "scan_int_chunk",
      "of": "zu::zu1::props::PropsReader",
      "signature": "fn scan_int_chunk(&mut self, db: &mut Zu1File, col: usize, chunk: usize, out: &mut Vec<u64>) -> Result<()>",
      "doc": "Decodes `chunk` of an integer column into `out`, the scan unit\nread: one chunk into the caller's reusable buffer, nothing held."
    },
    {
      "id": "zu::zu1::props::PropsReader::scan_str_range",
      "kind": "method",
      "name": "scan_str_range",
      "of": "zu::zu1::props::PropsReader",
      "signature": "fn scan_str_range(&mut self, db: &mut Zu1File, col: usize, start: u64, end: u64, bytes: &mut Vec<u8>, ends: &mut Vec<u64>) -> Result<()>",
      "doc": "Reads the string values of rows `start..end` of `col`, the scan\nunit for string columns: values concatenated in `bytes`, row\n`i - start` ending at `ends[i - start]`."
    },
    {
      "id": "zu::zu1::props::PropsReader::set_added",
      "kind": "method",
      "name": "set_added",
      "of": "zu::zu1::props::PropsReader",
      "signature": "fn set_added(&mut self, added: Option<Arc<RowPatch>>)",
      "doc": "The same for the committed rows past the end of them."
    },
    {
      "id": "zu::zu1::props::PropsReader::set_marks",
      "kind": "method",
      "name": "set_marks",
      "of": "zu::zu1::props::PropsReader",
      "signature": "fn set_marks(&mut self, marks: Option<Arc<LabelPatch>>)",
      "doc": "And for the labels the bitset does not carry yet."
    },
    {
      "id": "zu::zu1::props::PropsReader::set_patch",
      "kind": "method",
      "name": "set_patch",
      "of": "zu::zu1::props::PropsReader",
      "signature": "fn set_patch(&mut self, patch: Option<Arc<CellPatch>>)",
      "doc": "Hands this reader the committed cells its columns do not hold\nyet, or takes them away when a fold has sealed them."
    },
    {
      "id": "zu::zu1::props::PropsReader::zone",
      "kind": "method",
      "name": "zone",
      "of": "zu::zu1::props::PropsReader",
      "signature": "fn zone(&self, col: usize) -> Option<(u64, u64)>",
      "doc": "The value bounds of the whole of `col`, which is what says\nwhether a scan has to look at the column at all. `None` for an\nempty column, which bounds nothing.\n\nThis is [`Self::meta`] with the unsealed words folded in, and\nthe reason to ask it rather than read the bounds off the meta is\nthat a write the columns do not hold yet can sit outside them."
    },
    {
      "id": "zu::zu1::props::RowPatch",
      "kind": "struct",
      "name": "RowPatch"
    },
    {
      "id": "zu::zu1::props::RowPatch::base",
      "kind": "method",
      "name": "base",
      "of": "zu::zu1::props::RowPatch",
      "signature": "fn base(&self) -> u64",
      "doc": "The first row this holds, so a caller with a row number can tell\nan unfolded row from a stored one."
    },
    {
      "id": "zu::zu1::props::RowPatch::bytes",
      "kind": "method",
      "name": "bytes",
      "of": "zu::zu1::props::RowPatch",
      "signature": "fn bytes(&self) -> usize",
      "doc": "How many bytes of string the rows hold between them, which a\nwriter bounds along with the count of them."
    },
    {
      "id": "zu::zu1::props::RowPatch::is_empty",
      "kind": "method",
      "name": "is_empty",
      "of": "zu::zu1::props::RowPatch",
      "signature": "fn is_empty(&self) -> bool"
    },
    {
      "id": "zu::zu1::props::RowPatch::len",
      "kind": "method",
      "name": "len",
      "of": "zu::zu1::props::RowPatch",
      "signature": "fn len(&self) -> usize",
      "doc": "How many rows this holds, which is what a writer bounds when it\ndecides whether to keep deferring the fold."
    },
    {
      "id": "zu::zu1::props::RowPatch::new",
      "kind": "method",
      "name": "new",
      "of": "zu::zu1::props::RowPatch",
      "signature": "fn new(base: u64, key: Option<usize>) -> Self",
      "doc": "An empty patch over columns holding `base` rows, where `key` is\nthe column a key lookup will ask about if the table has one."
    },
    {
      "id": "zu::zu1::props::RowPatch::push",
      "kind": "method",
      "name": "push",
      "of": "zu::zu1::props::RowPatch",
      "signature": "fn push(&mut self, cells: Vec<Cell>) -> u64",
      "doc": "Takes one row, a cell per column, and answers with the row\nnumber it was given."
    },
    {
      "id": "zu::zu1::props::RowPatch::row_with",
      "kind": "method",
      "name": "row_with",
      "of": "zu::zu1::props::RowPatch",
      "signature": "fn row_with(&self, col: usize, word: u64) -> Option<u64>",
      "doc": "The last row whose `col` holds `word`, `None` when none of them\ndoes.\n\nThis is the key index for the rows nothing has folded yet. A\nkey index is built by a fold and a deferred commit runs none,\nso the rows in here are in no index, and what the fold would\nhave put in one is the value each of them holds in the table's\n`id` column.\n\nThe last rather than the first, because a run of deferred\ncommits can hold one key twice: a row takes a key, goes away,\nand a row after it takes the key the first one left free. Only\none of them is a row that is still there, and it is the last,\nbecause a key is only free to be taken again once the row\nholding it has gone and a row that has gone does not come back.\n\nThe sealed chunks answer out of the index each of them built as\nit was sealed, and the chunk being filled is walked, so what a\nlookup costs is a probe per sealed chunk and a walk of at most\n[`PATCH_CHUNK`] rows. Asked about any other column it walks the\nwhole run, which is what it used to do for every column and is\nwhat the fold's own index is for."
    },
    {
      "id": "zu::zu1::props::RowPatch::set",
      "kind": "method",
      "name": "set",
      "of": "zu::zu1::props::RowPatch",
      "signature": "fn set(&mut self, col: usize, row: u64, value: Cell) -> bool",
      "doc": "Writes `value` over what row `row` holds in `col`, and answers\nwhether it could.\n\nA row this patch appended is one no column holds yet, so a\nstatement writing onto it has nowhere else for the value to go:\nthe lane patch lays words over stored rows and there is no\nstored row under this one. The value goes over the one the\nappend carried and a read gets what the last write left, which\nis what the fold seals as well, because it asks the overlay for\neach appended row rather than taking the append at its word.\n\nThe key column is refused. Each sealed chunk carries an index of\nit, built as the chunk was sealed and shared with every reader\nholding an older copy of this patch, so moving a key here would\nmean rebuilding an index those readers are still reading out of.\nA key write onto an unfolded row folds instead."
    },
    {
      "id": "zu::zu1::props::RowPatch::settable",
      "kind": "method",
      "name": "settable",
      "of": "zu::zu1::props::RowPatch",
      "signature": "fn settable(&self, col: usize, row: u64) -> bool",
      "doc": "Whether [`Self::set`] would take a write of `col` on row `row`,\nwhich a writer asks before it takes the commit that would make\none. It has to be asked rather than tried, because a commit that\ncannot be patched has to fold whole and nothing of it may reach\nthe patch first."
    },
    {
      "id": "zu::zu1::props::list_elements",
      "kind": "function",
      "name": "list_elements",
      "signature": "fn list_elements(elem: &zu_common::LogicalType, bytes: &[u8], rows: ListRows) -> zu_common::Result<Vec<ListElement>>",
      "doc": "Reads back a row written by `encode_list_row`.\n\n`rows` is [`PropColumn::list_rows`], which says whether the count is\nin the directory or at the head of the row and how wide the head is.\nNeither can be settled from the bytes the way the element width can:\na bounded `LIST<INT32>(768)` row of 767 counted elements is the same\nlength as one of 768 uncounted ones, and the head that says 767 is\none, two or four bytes according to what the column was written at.\nSo the directory says, and this takes the answer rather than guessing\nat it.\n\nThe elements borrow the buffer they came out of, so a read of a list\ncolumn is the blob read and a walk over it, with no allocation per\nelement."
    },
    {
      "id": "zu::zu1::props::load_props",
      "kind": "function",
      "name": "load_props",
      "signature": "fn load_props(db: &mut zu_zu1::file::Zu1File, table_id: u32) -> zu_common::Result<Option<PropsDirectory>>",
      "doc": "Loads the props directory of a node table, `None` when the table\nhas no properties stored."
    },
    {
      "id": "zu::zu1::props::load_props_at",
      "kind": "function",
      "name": "load_props_at",
      "signature": "fn load_props_at(db: &mut zu_zu1::file::Zu1File, root: zu_zu1::file::BlockPtr) -> zu_common::Result<PropsDirectory>",
      "doc": "Loads the props directory a chain root names."
    },
    {
      "id": "zu::zu1::props::load_rel_props",
      "kind": "function",
      "name": "load_rel_props",
      "signature": "fn load_rel_props(db: &mut zu_zu1::file::Zu1File, rel_id: u32) -> zu_common::Result<Option<PropsDirectory>>",
      "doc": "Loads the edge property directory of a rel table, `None` when the\ntable stores no edge properties."
    },
    {
      "id": "zu::zu1::props::storable",
      "kind": "function",
      "name": "storable",
      "signature": "fn storable(ty: &zu_common::LogicalType) -> bool",
      "doc": "Whether a column may be declared this type at all.\n\nA graph type may name a type no column holds, because naming one is\na promise about values and holding one is a layout. So the caller\nthat turns a declaration into a table asks this first, and refuses\nthe statement naming the property, rather than letting the store\nrefuse a column halfway through making the table."
    },
    {
      "id": "zu::zu1::props::store_labels",
      "kind": "function",
      "name": "store_labels",
      "signature": "fn store_labels<S>(db: &mut zu_zu1::file::Zu1File, node_table: &str, rows: &[Vec<S>]) -> zu_common::Result<()>",
      "doc": "Stores the label bitset of a node table, replacing any earlier one,\nand declares in the catalog every label a row of it carries.\n\nWord `i` is row `i` of the table and bit `l` of it says the row\ncarries dictionary label `l`. The table's own label is set on every\nrow whether the caller sets it or not: a row of a table is what that\ntable is called, and a bitset that said otherwise would answer a\npattern differently from the catalog.\n\nDeclaring is the point of the catalog half. A table that never\ndeclares a label cannot hold a row with it, so a pattern naming that\nlabel prunes the table at plan time rather than reading a word per\nrow to find out."
    },
    {
      "id": "zu::zu1::props::store_props",
      "kind": "function",
      "name": "store_props",
      "signature": "fn store_props(db: &mut zu_zu1::file::Zu1File, node_table: &str, columns: &[(&str, PropValues)]) -> zu_common::Result<PropsDirectory>",
      "doc": "Stores the property columns of `node_table`, replacing any earlier\nset whole; every column must hold exactly one value per row of the\ntable's domain. The table must already exist, so a load always\nprecedes its properties."
    },
    {
      "id": "zu::zu1::props::store_props_for",
      "kind": "function",
      "name": "store_props_for",
      "signature": "fn store_props_for(db: &mut zu_zu1::file::Zu1File, table_id: u32, node_count: u64, columns: &[PropInput]) -> zu_common::Result<PropsDirectory>",
      "doc": "The same store over a table named by its id, which is what a caller\nwho has already resolved the name means, and the only way to reach a\ntable outside the home graph: a name is a name in a graph, and the\nstore above resolves one in the graph a load writes into.\n\n`node_count` is the row domain the columns have to cover, which is\nthe table's own count and is nought for a table nothing has written\nto yet."
    },
    {
      "id": "zu::zu1::props::store_props_nullable",
      "kind": "function",
      "name": "store_props_nullable",
      "signature": "fn store_props_nullable(db: &mut zu_zu1::file::Zu1File, node_table: &str, columns: &[PropInput]) -> zu_common::Result<PropsDirectory>",
      "doc": "The same store, for columns some rows of which hold no value."
    },
    {
      "id": "zu::zu1::props::store_props_owned",
      "kind": "function",
      "name": "store_props_owned",
      "signature": "fn store_props_owned(db: &mut zu_zu1::file::Zu1File, node_table: &str, columns: &[OwnedColumn]) -> zu_common::Result<PropsDirectory>",
      "doc": "The same store, for columns a caller holds whole rather than as\nborrowed slices, which is what a node file read off disk produces.\n\nThis is [`store_rel_props_owned`] over a node table, and it exists\nfor the same reason: `PropValues::Str` wants a slice of slices and an\nowned column has a vector of vectors, so the pointers have to live\nsomewhere for the length of the call."
    },
    {
      "id": "zu::zu1::props::store_rel_props",
      "kind": "function",
      "name": "store_rel_props",
      "signature": "fn store_rel_props(db: &mut zu_zu1::file::Zu1File, rel_table: &str, columns: &[(&str, PropValues)]) -> zu_common::Result<PropsDirectory>",
      "doc": "Stores the property columns of the rel table `rel_table`, replacing\nany earlier set whole. Every column holds one value per edge, in the\norder the edges were loaded in, which is sorted by source and then by\ndestination: value `i` belongs to edge `i` of that order, and that is\nthe only thing tying a column to an edge, so a caller that hands over\na column in another order has silently mislabeled its graph.\n\nA pair may run twice. Two edges over the same endpoints are two\nslots of the forward list and two values of the column, and a walk\nthat counts as it goes has each one's own\n([`crate::graph::GraphReader::out_neighbors_from`]). What such a\ntable cannot do is answer a lookup given nothing but the pair with\nboth values, and `edge_ordinal` says which of the two it picks."
    },
    {
      "id": "zu::zu1::props::store_rel_props_for",
      "kind": "function",
      "name": "store_rel_props_for",
      "signature": "fn store_rel_props_for(db: &mut zu_zu1::file::Zu1File, rel_id: u32, edge_count: u64, columns: &[PropInput]) -> zu_common::Result<PropsDirectory>",
      "doc": "The same store over a rel table named by its id, which is what a\ncaller who has already resolved the name means, and the only way to\nreach a table outside the home graph. This is [`store_props_for`] on\nthe edge side, and the difference is where the columns hang: a node\ntable's props are the table index entry, a rel table's are a field\nof the directory that entry holds."
    },
    {
      "id": "zu::zu1::props::store_rel_props_nullable",
      "kind": "function",
      "name": "store_rel_props_nullable",
      "signature": "fn store_rel_props_nullable(db: &mut zu_zu1::file::Zu1File, rel_table: &str, columns: &[PropInput]) -> zu_common::Result<PropsDirectory>",
      "doc": "The same store, for columns some edges of which hold no value."
    },
    {
      "id": "zu::zu1::props::store_rel_props_owned",
      "kind": "function",
      "name": "store_rel_props_owned",
      "signature": "fn store_rel_props_owned(db: &mut zu_zu1::file::Zu1File, rel_table: &str, columns: &[OwnedColumn]) -> zu_common::Result<PropsDirectory>",
      "doc": "The same store, for columns a caller holds whole rather than as\nborrowed slices, which is what a parquet edge list produces.\n\nThe string arm is the only reason this exists: `PropValues::Str`\nwants a slice of slices and an owned column has a vector of\nvectors, so the pointers have to live somewhere for the length of\nthe call."
    },
    {
      "id": "zu::zu1::props::stored_label_word",
      "kind": "function",
      "name": "stored_label_word",
      "signature": "fn stored_label_word(db: &mut zu_zu1::file::Zu1File, directory: &PropsDirectory, row: u64) -> zu_common::Result<Option<u64>>",
      "doc": "The label word row `row` carries in a stored bitset, `None` when the\ntable stores none or the row is past the end of it, which is a row\ncarrying its table's own label and nothing else.\n\nThis is [`PropsReader::label_word`] for a caller holding a directory\nrather than a reader, which is the write side: it asks once per\nstatement, to put a change's masks over the word the row had, so\nthere is nothing for a cache to save."
    },
    {
      "id": "zu::zu1::props::zoned",
      "kind": "function",
      "name": "zoned",
      "signature": "fn zoned(ty: &zu_common::LogicalType) -> bool",
      "doc": "Whether this is one of the two zoned temporal types, which are the\ncolumn shape that is two planes rather than one.\n\nA zoned value is an instant and the offset from UTC it was written\nwith, which is two numbers, and everything else here is one. The two\nare stored as two segments over the same rows: the instants in the\nscalar lane, where they get the integer cascade and where a\ncomparison, a sort and a range estimate all read them, and the\noffsets in a plane of their own that only materialising a value\ntouches. A column of one zone, which is what a column of timestamps\nalmost always is, pays a handful of bytes for the whole of the\nsecond plane."
    },
    {
      "id": "zu::zu1::reorder",
      "kind": "module",
      "name": "reorder",
      "doc": "NodeId relabeling before bulk load, `docs/04-storage-zu1-format.md` §5.\n\n`COPY ... WITH (REORDER = degree | bfs | none)` relabels nodes so that\nneighbors land on nearby ids, which is what lets delta plus bitpack\nreach the bits/edge budget: gap sizes track id locality, and one wide\ngap per 1024-value chunk sets the width for the whole chunk. BFS from\nmax-degree roots is the default in the spec; degree ordering is the\ncheaper fallback. The mapping is a bijection over `0..node_count`, so\nthe relabeled graph is isomorphic to the input."
    },
    {
      "id": "zu::zu1::reorder::Reorder",
      "kind": "enum",
      "name": "Reorder",
      "doc": "Node order at COPY, `REORDER = degree | bfs | none`."
    },
    {
      "id": "zu::zu1::reorder::Reorder::Bfs",
      "kind": "variant",
      "name": "Bfs",
      "of": "zu::zu1::reorder::Reorder",
      "signature": "Bfs"
    },
    {
      "id": "zu::zu1::reorder::Reorder::Degree",
      "kind": "variant",
      "name": "Degree",
      "of": "zu::zu1::reorder::Reorder",
      "signature": "Degree"
    },
    {
      "id": "zu::zu1::reorder::Reorder::None",
      "kind": "variant",
      "name": "None",
      "of": "zu::zu1::reorder::Reorder",
      "signature": "None"
    },
    {
      "id": "zu::zu1::reorder::Reorder::parse",
      "kind": "method",
      "name": "parse",
      "of": "zu::zu1::reorder::Reorder",
      "signature": "fn parse(s: &str) -> Option<Self>",
      "doc": "Parses the spec spelling, lowercase."
    },
    {
      "id": "zu::zu1::reorder::bfs_order",
      "kind": "function",
      "name": "bfs_order",
      "signature": "fn bfs_order(node_count: u64, edges: &[(u32, u32)]) -> Vec<u32>",
      "doc": "Old id to new id in BFS dequeue order, restarting from the unvisited\nnode of highest degree until every component (and every isolated node)\nis numbered. Neighbor lists are walked in sorted id order, so the\nmapping is deterministic."
    },
    {
      "id": "zu::zu1::reorder::degree_order",
      "kind": "function",
      "name": "degree_order",
      "signature": "fn degree_order(node_count: u64, edges: &[(u32, u32)]) -> Vec<u32>",
      "doc": "Old id to new id, densest nodes first. Ties break toward the lower old\nid so the mapping is deterministic."
    },
    {
      "id": "zu::zu1::reorder::load_order",
      "kind": "function",
      "name": "load_order",
      "signature": "fn load_order(edges: &mut [(u32, u32)]) -> Vec<u32>",
      "doc": "Sorts an edge list into load order and reports where every edge\ncame from: `order[i]` is the input position of the edge that ends up\nat ordinal `i`.\n\nLoad order is source and then destination, which is what numbers the\nedge ordinals a property column is addressed by, so an edge list and\nits columns arriving in some other order have to move together. This\nis the only thing that ties them, and relabeling the nodes changes\nthe answer for nearly every edge, which is why reorder owes its\nproperty columns this permutation. Ties keep input order, so the\npermutation is deterministic and duplicate pairs stay adjacent for\nwhoever has to refuse them."
    },
    {
      "id": "zu::zu1::reorder::relabel",
      "kind": "function",
      "name": "relabel",
      "signature": "fn relabel(edges: &mut [(u32, u32)], map: &[u32])",
      "doc": "Applies an old-to-new map in place. The caller re-sorts afterward."
    },
    {
      "id": "zu::zu1::rows",
      "kind": "module",
      "name": "rows",
      "doc": "Columns of byte rows: the Stride structural layout, and which layout\none of these columns takes.\n\nA byte row column is FullZip (`crate::fullzip`) unless every row of\nit is the same length, in which case it is Stride. Stride is the\nlayout with nothing in it: `stride: u32`, then `value_count * stride`\nbytes of rows end to end. There is no chunk index, because row `i`\nbegins at `4 + i * stride` and nothing has to be walked to find it,\nand there is no compression pass, because the columns this layout is\nfor hold numbers rather than text. A 768 dimension `FLOAT32`\nembedding is the case it exists for: FullZip trains a symbol table on\na sample of the column and then encodes every chunk twice to find out\nthat float bytes do not compress, which is a scan of the column for\nan answer known before it starts.\n\nWhich layout a column took is in its meta, so no caller has to\nremember: [`write_rows`] chooses and [`read_rows`] and\n[`read_rows_range`] ask. A column that stops being uniform, because a\nrow was written into it that is a different length, becomes FullZip\nat the next write with nothing to migrate.\n\nThe crc covers the whole payload and is verified by [`read_rows`],\nthe full scan. The point path skips it the way the other two layouts\ndo, and bounds every access against the meta instead: the one\nstructural claim a Stride payload makes is that `4 + count * stride`\nis its length, and that is checked before any row is named."
    },
    {
      "id": "zu::zu1::rows::read_rows",
      "kind": "function",
      "name": "read_rows",
      "signature": "fn read_rows(db: &mut zu_zu1::file::Zu1File, meta: &zu_zu1::segment::SegmentMeta, bytes_out: &mut Vec<u8>, ends_out: &mut Vec<u64>) -> zu_common::Result<()>",
      "doc": "Reads a byte row column back whole, verifying the payload crc, and\nappends every row to `bytes_out` with its end offset in `ends_out`."
    },
    {
      "id": "zu::zu1::rows::read_rows_range",
      "kind": "function",
      "name": "read_rows_range",
      "signature": "fn read_rows_range(db: &mut zu_zu1::file::Zu1File, meta: &zu_zu1::segment::SegmentMeta, start: u64, end: u64, bytes_out: &mut Vec<u8>, ends_out: &mut Vec<u64>) -> zu_common::Result<()>",
      "doc": "Point access: appends rows `[start, end)` to `bytes_out` and their\nend offsets to `ends_out`, reading only the bytes they occupy."
    },
    {
      "id": "zu::zu1::rows::rewrite_rows",
      "kind": "function",
      "name": "rewrite_rows",
      "signature": "fn rewrite_rows(db: &mut zu_zu1::file::Zu1File, old: &zu_zu1::segment::SegmentMeta, updates: &std::collections::BTreeMap<u64, Vec<u8>>, appended: &[Vec<u8>]) -> zu_common::Result<zu_zu1::segment::SegmentMeta>",
      "doc": "[`crate::fullzip::rewrite_blob_segment`] for a column that may be in\neither layout: `updates` written over the rows they name and\n`appended` added on the end.\n\nA FullZip column keeps the per chunk rewrite it has, which is what\nthat function is for. A Stride one has no chunks to spare and no\nencoding to skip, so it is read out and written again, which moves\nthe same bytes the FullZip path moves and does none of the work\naround them. A column that stops being uniform, which is what a\nnull does, comes back as FullZip and nothing has to migrate it.\n\nA FullZip column that becomes uniform stays FullZip. Deciding the\nother way would mean a column changing layout under a single cell\nwrite, and the layout is worth having where the column is written\nwhole, which is where the choice is made."
    },
    {
      "id": "zu::zu1::rows::rewrite_rows_reordered",
      "kind": "function",
      "name": "rewrite_rows_reordered",
      "signature": "fn rewrite_rows_reordered(db: &mut zu_zu1::file::Zu1File, old: &zu_zu1::segment::SegmentMeta, values: &[&[u8]]) -> zu_common::Result<zu_zu1::segment::SegmentMeta>",
      "doc": "[`crate::fullzip::rewrite_blob_reordered`] for a column that may be\nin either layout: the same values again in a new order.\n\nThe FullZip path exists to keep the symbol table the column was\nencoded with rather than train a second one on the same bytes. A\nStride column has no table, so a reorder is the write and nothing\nelse."
    },
    {
      "id": "zu::zu1::rows::spans",
      "kind": "function",
      "name": "spans",
      "signature": "fn spans(bytes: &[u8], ends: &[u64]) -> Vec<&[u8]>",
      "doc": "The rows a `bytes`, `ends` pair holds, as the slices every writer\nhere takes."
    },
    {
      "id": "zu::zu1::rows::uniform_len",
      "kind": "function",
      "name": "uniform_len",
      "signature": "fn uniform_len(values: &[&[u8]]) -> Option<usize>",
      "doc": "The length every row of `values` has, or `None` for a column no\nstride describes.\n\nA column of no rows has no stride to speak of and a column of empty\nrows would give a stride of zero, which is a divisor. Both go to\nFullZip, where they cost nothing anyway."
    },
    {
      "id": "zu::zu1::rows::write_rows",
      "kind": "function",
      "name": "write_rows",
      "signature": "fn write_rows(db: &mut zu_zu1::file::Zu1File, values: &[&[u8]]) -> zu_common::Result<zu_zu1::segment::SegmentMeta>",
      "doc": "Writes a column of byte rows in whichever layout fits it."
    },
    {
      "id": "zu::zu1::segment",
      "kind": "module",
      "name": "segment",
      "doc": "Column segments in the MiniBlock structural layout of\n`docs/04-storage-zu1-format.md` §3: values packed in chunks of 1024,\neach chunk an independently decodable `encode_auto` cascade, behind a\nchunk index of u32 end offsets. A point read decodes only the chunks\ncovering the wanted rows and touches bytes on the order of the chunk,\nnot the segment.\n\nPayload layout, all little-endian: `chunk_count: u32`, then one `u32`\ncumulative end offset per chunk (relative to the body), then one `u64`\nfence per chunk holding the chunk's last value, then the body of\nconcatenated chunk cascades. The encoding id travels inside each\nchunk, so the reader needs no side channel beyond the `SegmentMeta`.\nThe fences are what make [`probe`] cheap: inside any sorted row range\nthe fence of a fully covered chunk is an in-order sample, so a binary\nsearch over fences names the one chunk that could hold a value and the\nprobe decodes that chunk alone. They cost 8 bytes per 1024 values,\nabout 0.06 bits per value.\nMetas serialize into meta-block chains with a fixed layout:\n`value_count: u64`, `payload_len: u64`, `uncompressed_bytes: u64`,\n`min: u64`, `max: u64`, `crc32c: u32`, `block_count: u32`,\n`structural: u8`, then one `u64` per block pointer. The structural\nbyte names the payload layout, MiniBlock here or FullZip in\n`crate::fullzip`, and an unknown id is an error naming it (docs/04\n§10). The min and max are the segment's zone map\n(`docs/04` §6): [`probe`] answers absent for any value outside them\nwithout touching the payload, and the full scan cross-checks them\nagainst the decoded values.\n\nThe segment crc covers the whole payload and is verified by the full\nscan path and `zu verify`. The point path skips it by design (checking\nit would mean reading everything); it bounds every access by the meta\nand rejects non-monotone indexes, truncated chunks, and count\nmismatches instead."
    },
    {
      "id": "zu::zu1::segment::CHUNK_ROWS",
      "kind": "constant",
      "name": "CHUNK_ROWS",
      "signature": "usize",
      "doc": "Rows per MiniBlock chunk, the unit of point access."
    },
    {
      "id": "zu::zu1::segment::ChunkCache",
      "kind": "struct",
      "name": "ChunkCache",
      "doc": "Decoded chunks held by a reader between lookups, one slot per chunk\nfilled on first touch and kept. Warm means touched before: a lookup\nthat lands on a held chunk costs a binary search, no block read and\nno decode, which is the B2 budget. Memory is bounded by the decoded\nsegment, the same order as the group cache the graph reader keeps;\neviction is the buffer manager's job (docs/09, M3)."
    },
    {
      "id": "zu::zu1::segment::ChunkDirectory",
      "kind": "struct",
      "name": "ChunkDirectory",
      "doc": "The chunk index and fence array of one segment, held in memory so\nrepeated sorted lookups skip the two span reads a cold [`probe`]\npays. For a 4.8 M value segment this is about 55 KiB."
    },
    {
      "id": "zu::zu1::segment::SegmentMeta",
      "kind": "struct",
      "name": "SegmentMeta",
      "doc": "Location and integrity data for one stored segment."
    },
    {
      "id": "zu::zu1::segment::SegmentMeta::at",
      "kind": "method",
      "name": "at",
      "of": "zu::zu1::segment::SegmentMeta",
      "signature": "fn at(&self) -> u64",
      "doc": "Where the payload starts in the file, which is what names a\nsegment when a block pointer no longer does: the pools above the\nblock cache key on this."
    },
    {
      "id": "zu::zu1::segment::SegmentMeta::blocks",
      "kind": "field",
      "name": "blocks",
      "of": "zu::zu1::segment::SegmentMeta",
      "signature": "Vec<zu_zu1::file::BlockPtr>"
    },
    {
      "id": "zu::zu1::segment::SegmentMeta::crc",
      "kind": "field",
      "name": "crc",
      "of": "zu::zu1::segment::SegmentMeta",
      "signature": "u32"
    },
    {
      "id": "zu::zu1::segment::SegmentMeta::decode",
      "kind": "method",
      "name": "decode",
      "of": "zu::zu1::segment::SegmentMeta",
      "signature": "fn decode(bytes: &[u8], pos: usize) -> Result<(Self, usize)>",
      "doc": "Reads a meta from `bytes` starting at `pos`, returning the meta and\nthe position after it."
    },
    {
      "id": "zu::zu1::segment::SegmentMeta::decode_unpacked",
      "kind": "method",
      "name": "decode_unpacked",
      "of": "zu::zu1::segment::SegmentMeta",
      "signature": "fn decode_unpacked(bytes: &[u8], pos: usize) -> Result<(Self, usize)>",
      "doc": "The same, for a directory written before payloads could be\npacked: its metas carry no start word and every one of them\nbegins at the head of its first block."
    },
    {
      "id": "zu::zu1::segment::SegmentMeta::encode",
      "kind": "method",
      "name": "encode",
      "of": "zu::zu1::segment::SegmentMeta",
      "signature": "fn encode(&self, out: &mut Vec<u8>)",
      "doc": "Appends the meta to `out`."
    },
    {
      "id": "zu::zu1::segment::SegmentMeta::encoded_len",
      "kind": "method",
      "name": "encoded_len",
      "of": "zu::zu1::segment::SegmentMeta",
      "signature": "fn encoded_len(&self) -> usize",
      "doc": "Serialized size in bytes."
    },
    {
      "id": "zu::zu1::segment::SegmentMeta::max",
      "kind": "field",
      "name": "max",
      "of": "zu::zu1::segment::SegmentMeta",
      "signature": "u64",
      "doc": "Zone map: the largest value in the segment, 0 when empty."
    },
    {
      "id": "zu::zu1::segment::SegmentMeta::min",
      "kind": "field",
      "name": "min",
      "of": "zu::zu1::segment::SegmentMeta",
      "signature": "u64",
      "doc": "Zone map: the smallest value in the segment, 0 when empty."
    },
    {
      "id": "zu::zu1::segment::SegmentMeta::payload_len",
      "kind": "field",
      "name": "payload_len",
      "of": "zu::zu1::segment::SegmentMeta",
      "signature": "u64"
    },
    {
      "id": "zu::zu1::segment::SegmentMeta::sorted",
      "kind": "field",
      "name": "sorted",
      "of": "zu::zu1::segment::SegmentMeta",
      "signature": "bool",
      "doc": "The writer saw the values in ascending order, which upgrades the\nfence array to a per-chunk zone map: chunk `i` holds values in\n`fences[i-1]..=fences[i]` (`min..=fences[0]` for the first), so\na range scan can skip chunks without reading them. Unsorted\nsegments keep only the segment-level min and max."
    },
    {
      "id": "zu::zu1::segment::SegmentMeta::start",
      "kind": "field",
      "name": "start",
      "of": "zu::zu1::segment::SegmentMeta",
      "signature": "u32",
      "doc": "Byte offset the payload starts at inside `blocks[0]`. A block\nholds as many small payloads as fit (`Zu1File::pack_bytes`), so\nwhere a segment starts is part of naming it."
    },
    {
      "id": "zu::zu1::segment::SegmentMeta::structural",
      "kind": "field",
      "name": "structural",
      "of": "zu::zu1::segment::SegmentMeta",
      "signature": "Structural"
    },
    {
      "id": "zu::zu1::segment::SegmentMeta::uncompressed_bytes",
      "kind": "field",
      "name": "uncompressed_bytes",
      "of": "zu::zu1::segment::SegmentMeta",
      "signature": "u64"
    },
    {
      "id": "zu::zu1::segment::SegmentMeta::value_count",
      "kind": "field",
      "name": "value_count",
      "of": "zu::zu1::segment::SegmentMeta",
      "signature": "u64"
    },
    {
      "id": "zu::zu1::segment::Structural",
      "kind": "enum",
      "name": "Structural",
      "doc": "Structural layout of a segment payload (docs/04 §3): MiniBlock packs\nfixed-width values in 1024-row cascade chunks, FullZip zips\nvariable-width values with their lengths (`crate::fullzip`), and\nStride lays equal-length byte rows end to end (`crate::rows`). The\nids are format-stable; readers reject any other value by name.\n\nThe ids skip 2 and 3 because the structural byte carries the sorted\nflag in bit 1, so an id with that bit set would be two things."
    },
    {
      "id": "zu::zu1::segment::Structural::FullZip",
      "kind": "variant",
      "name": "FullZip",
      "of": "zu::zu1::segment::Structural",
      "signature": "FullZip"
    },
    {
      "id": "zu::zu1::segment::Structural::MiniBlock",
      "kind": "variant",
      "name": "MiniBlock",
      "of": "zu::zu1::segment::Structural",
      "signature": "MiniBlock"
    },
    {
      "id": "zu::zu1::segment::Structural::Stride",
      "kind": "variant",
      "name": "Stride",
      "of": "zu::zu1::segment::Structural",
      "signature": "Stride"
    },
    {
      "id": "zu::zu1::segment::cached_chunk",
      "kind": "function",
      "name": "cached_chunk",
      "signature": "fn cached_chunk(db: &mut zu_zu1::file::Zu1File, meta: &SegmentMeta, dir: &ChunkDirectory, cache: &mut ChunkCache, target: usize) -> zu_common::Result<&[u64]>",
      "doc": "Decodes chunk `target` of `meta`'s segment through `cache`, reusing\nthe held values when the chunk was decoded before."
    },
    {
      "id": "zu::zu1::segment::chunk_zone",
      "kind": "function",
      "name": "chunk_zone",
      "signature": "fn chunk_zone(meta: &SegmentMeta, dir: &ChunkDirectory, i: usize) -> Option<(u64, u64)>",
      "doc": "The inclusive value bounds of chunk `i`, known without decoding when\nthe writer recorded the segment as sorted: the previous fence floors\nthe chunk and its own fence caps it. `None` when the segment is\nunsorted and the fences are just last values, not bounds."
    },
    {
      "id": "zu::zu1::segment::decode_chunk",
      "kind": "function",
      "name": "decode_chunk",
      "signature": "fn decode_chunk(db: &mut zu_zu1::file::Zu1File, meta: &SegmentMeta, dir: &ChunkDirectory, target: usize, out: &mut Vec<u64>) -> zu_common::Result<()>",
      "doc": "Decodes chunk `target` of `meta`'s segment into `out`, which is\ncleared first. The scan path drives this with one reusable scratch\nvector, since a scan touches each chunk once and a per-chunk cache\nwould only hold memory it never reads again."
    },
    {
      "id": "zu::zu1::segment::find_in_sorted",
      "kind": "function",
      "name": "find_in_sorted",
      "signature": "fn find_in_sorted(db: &mut zu_zu1::file::Zu1File, meta: &SegmentMeta, dir: &ChunkDirectory, value: u64) -> zu_common::Result<Option<u64>>",
      "doc": "Position of `value` in a segment whose rows are sorted ascending, or\n`None` when absent. The fences name the single chunk that could hold\nthe value, so a hit costs one chunk decode; the zone map answers\nmisses outside `[min, max]` from the meta alone."
    },
    {
      "id": "zu::zu1::segment::find_in_sorted_cached",
      "kind": "function",
      "name": "find_in_sorted_cached",
      "signature": "fn find_in_sorted_cached(db: &mut zu_zu1::file::Zu1File, meta: &SegmentMeta, dir: &ChunkDirectory, cache: &mut ChunkCache, value: u64) -> zu_common::Result<Option<u64>>",
      "doc": "[`find_in_sorted`] through a decoded-chunk cache: a warm lookup that\nlands on the cached chunk costs a fence partition point and a binary\nsearch, no read and no decode."
    },
    {
      "id": "zu::zu1::segment::load_chunk_directory",
      "kind": "function",
      "name": "load_chunk_directory",
      "signature": "fn load_chunk_directory(db: &mut zu_zu1::file::Zu1File, meta: &SegmentMeta) -> zu_common::Result<ChunkDirectory>",
      "doc": "Loads the chunk index and fences of `meta`'s segment."
    },
    {
      "id": "zu::zu1::segment::load_chunk_directory_pooled",
      "kind": "function",
      "name": "load_chunk_directory_pooled",
      "signature": "fn load_chunk_directory_pooled(db: &mut zu_zu1::file::Zu1File, pool: &zu_zu1::cache::DecodedPool<ChunkDirectory>, meta: &SegmentMeta) -> zu_common::Result<std::sync::Arc<ChunkDirectory>>",
      "doc": "Loads `meta`'s chunk directory through `pool`, keyed like\n[`read_segment_pooled`], so forked readers decode each directory\nonce between them instead of once per handle."
    },
    {
      "id": "zu::zu1::segment::locate",
      "kind": "function",
      "name": "locate",
      "signature": "fn locate(db: &mut zu_zu1::file::Zu1File, meta: &SegmentMeta, start: u64, end: u64, value: u64) -> zu_common::Result<Option<u64>>",
      "doc": "Where `value` sits in `values[start..end)`, as a position in the\nwhole segment, and `None` when the range does not hold it. Same\nsearch and same cost as [`probe`], which is written in terms of it.\n\nThe position is what an edge property column is indexed by: a\nneighbor list is one node's slice of the segment, so the slot a\ndestination lands in is the edge's place in the load order, and the\ncaller adds the group's base to get the ordinal."
    },
    {
      "id": "zu::zu1::segment::probe",
      "kind": "function",
      "name": "probe",
      "signature": "fn probe(db: &mut zu_zu1::file::Zu1File, meta: &SegmentMeta, start: u64, end: u64, value: u64) -> zu_common::Result<bool>",
      "doc": "Membership probe: is `value` among `values[start..end)`? The rows in\nthat range must be sorted ascending, which CSR neighbor lists are.\nThe fences of chunks fully covered by the range are in-order samples\nof it, so a binary search over them names the single chunk that could\nhold `value`, and only that chunk is read and decoded: a probe\ntouches bytes on the order of one chunk regardless of the degree.\nA value outside the segment's zone map answers absent from the meta\nalone, without reading the payload at all."
    },
    {
      "id": "zu::zu1::segment::read_one_cached",
      "kind": "function",
      "name": "read_one_cached",
      "signature": "fn read_one_cached(db: &mut zu_zu1::file::Zu1File, meta: &SegmentMeta, dir: &ChunkDirectory, cache: &mut ChunkCache, pos: u64) -> zu_common::Result<u64>",
      "doc": "Reads the single value at `pos` through a decoded-chunk cache, the\nwarm point companion of [`read_range`]."
    },
    {
      "id": "zu::zu1::segment::read_range",
      "kind": "function",
      "name": "read_range",
      "signature": "fn read_range(db: &mut zu_zu1::file::Zu1File, meta: &SegmentMeta, start: u64, end: u64, out: &mut Vec<u64>) -> zu_common::Result<()>",
      "doc": "Point access: appends `values[start..end]` to `out`, decoding only the\nchunks that cover the range and reading only the bytes they occupy."
    },
    {
      "id": "zu::zu1::segment::read_segment",
      "kind": "function",
      "name": "read_segment",
      "signature": "fn read_segment(db: &mut zu_zu1::file::Zu1File, meta: &SegmentMeta, out: &mut Vec<u64>) -> zu_common::Result<()>",
      "doc": "Reads a segment back, verifying the payload crc, and appends the\ndecoded values to `out`."
    },
    {
      "id": "zu::zu1::segment::read_segment_pooled",
      "kind": "function",
      "name": "read_segment_pooled",
      "signature": "fn read_segment_pooled(db: &mut zu_zu1::file::Zu1File, pool: &zu_zu1::cache::DecodedPool<Vec<u64>>, meta: &SegmentMeta) -> zu_common::Result<std::sync::Arc<Vec<u64>>>",
      "doc": "Reads a whole segment through `pool`, decoding it at most once per\npooled lifetime. The key is where the payload starts in the file,\nwhich every segment has since even an empty one carries its chunk\ncount, and which is unique per committed segment version."
    },
    {
      "id": "zu::zu1::segment::rewrite_segment",
      "kind": "function",
      "name": "rewrite_segment",
      "signature": "fn rewrite_segment(db: &mut zu_zu1::file::Zu1File, old: &SegmentMeta, values: &[u64], dirty: &std::collections::BTreeSet<usize>) -> zu_common::Result<SegmentMeta>",
      "doc": "Writes the segment `values` describes, encoding only the chunks\n`dirty` names and copying the encoded bytes of the rest out of\n`old`.\n\nA fold rewrites a column because a cell of it changed, and picking\nthe cascade is most of what that costs: the selector sizes every\nscheme it knows against a chunk before it takes one, which is the\nright trade for a chunk that is new and pure waste for a chunk that\ncame back out of the file and is going into it unchanged. Those\nbytes are already an encoding of exactly these values, so they are\ncopied and the selector is asked only about what moved. A one cell\nwrite touches one chunk, whatever the table is holding.\n\n`values` is the whole new column rather than the chunks that moved,\nbecause the fences, the zone map and the sorted flag are the\nsegment's rather than a chunk's, and reading them off the values is\none pass over memory beside the encode this is not doing."
    },
    {
      "id": "zu::zu1::segment::write_segment",
      "kind": "function",
      "name": "write_segment",
      "signature": "fn write_segment(db: &mut zu_zu1::file::Zu1File, values: &[u64]) -> zu_common::Result<SegmentMeta>",
      "doc": "Encodes `values` chunk by chunk with the cascade selector and writes\nthe MiniBlock payload out through the file's placer."
    },
    {
      "id": "zu::zu1::stats",
      "kind": "module",
      "name": "stats",
      "doc": "Statistics blocks: per-direction degree histograms in log2 buckets\nper rel table, the docs/07 §6 inputs the optimizer's cardinality\nestimates and pessimistic caps read. The payload lives in a meta\nchain at the header's `stats_root`, rewritten whole whenever a rel\ntable loads. Bucket `i` counts source nodes whose degree falls in\n`[2^i, 2^(i+1))`; zero-degree nodes are absent and recoverable from\nthe table's node count. An unknown version decodes as no stats, the\nskippable contract of docs/04 for the stats section."
    },
    {
      "id": "zu::zu1::stats::COLOR_CAP",
      "kind": "constant",
      "name": "COLOR_CAP",
      "signature": "usize",
      "doc": "The most colors one summary may carry (docs/07 §6)."
    },
    {
      "id": "zu::zu1::stats::ColStats",
      "kind": "struct",
      "name": "ColStats",
      "doc": "One property column's statistics: how many rows it has, how many\ndistinct values, the most frequent of those values, and equi-depth\nbucket boundaries over the rest (perf/12 section 1).\n\nValues are the order-preserving byte keys of [`zu_common::int_key`]\nfor an integer column and the raw bytes for a string one, so a\nboundary, a top value, and the literal a query compares against all\nmeet as bytes and the comparison means what the query means."
    },
    {
      "id": "zu::zu1::stats::ColStats::bounds",
      "kind": "field",
      "name": "bounds",
      "of": "zu::zu1::stats::ColStats",
      "signature": "Vec<Vec<u8>>",
      "doc": "Equi-depth boundaries, ascending, [`HIST_BUCKETS`] + 1 of them\nwhen the column has enough distinct values to fill them.\nBoundaries are truncated to [`VALUE_CAP`], so a boundary is a\nprefix of a real value and lands a hair below it, which moves a\nbucket edge and not a row count."
    },
    {
      "id": "zu::zu1::stats::ColStats::ndv",
      "kind": "field",
      "name": "ndv",
      "of": "zu::zu1::stats::ColStats",
      "signature": "u64",
      "doc": "Distinct values, exact when the whole column was read and a\nsketch estimate when it was sampled. Never zero on a non-empty\ncolumn, so `1/ndv` is always a selectivity."
    },
    {
      "id": "zu::zu1::stats::ColStats::rows",
      "kind": "field",
      "name": "rows",
      "of": "zu::zu1::stats::ColStats",
      "signature": "u64"
    },
    {
      "id": "zu::zu1::stats::ColStats::top",
      "kind": "field",
      "name": "top",
      "of": "zu::zu1::stats::ColStats",
      "signature": "Vec<(Vec<u8>, u64)>",
      "doc": "The most frequent values with their row counts, most frequent\nfirst, at most [`TOP_VALUES`] of them. Values longer than\n[`VALUE_CAP`] are left out rather than truncated, because a\ntruncated key would match a literal it is not equal to."
    },
    {
      "id": "zu::zu1::stats::DegreeNorms",
      "kind": "struct",
      "name": "DegreeNorms",
      "doc": "The lp norms of one direction's degree sequence (perf/12 section 1),\nthe inputs to the LpBound-style ceilings the DP clamps with. Every\none of them is over nodes that hold at least one edge; a node with\nno edges contributes nothing to any of them."
    },
    {
      "id": "zu::zu1::stats::DegreeNorms::l1",
      "kind": "field",
      "name": "l1",
      "of": "zu::zu1::stats::DegreeNorms",
      "signature": "f64",
      "doc": "Sum of degrees, which is the edge count."
    },
    {
      "id": "zu::zu1::stats::DegreeNorms::l2",
      "kind": "field",
      "name": "l2",
      "of": "zu::zu1::stats::DegreeNorms",
      "signature": "f64",
      "doc": "Square root of the sum of squared degrees."
    },
    {
      "id": "zu::zu1::stats::DegreeNorms::l3",
      "kind": "field",
      "name": "l3",
      "of": "zu::zu1::stats::DegreeNorms",
      "signature": "f64",
      "doc": "Cube root of the sum of cubed degrees."
    },
    {
      "id": "zu::zu1::stats::DegreeNorms::linf",
      "kind": "field",
      "name": "linf",
      "of": "zu::zu1::stats::DegreeNorms",
      "signature": "f64",
      "doc": "The largest degree of any one node."
    },
    {
      "id": "zu::zu1::stats::DegreeStats",
      "kind": "struct",
      "name": "DegreeStats",
      "doc": "Both degree sequences of one rel table plus the one number that\nmixes them. `cross` is the sum over nodes of out-degree times\nin-degree, which is the exact count of two hop paths through the\ntable and the mixed norm perf/12 section 2.3 asks for."
    },
    {
      "id": "zu::zu1::stats::DegreeStats::cross",
      "kind": "field",
      "name": "cross",
      "of": "zu::zu1::stats::DegreeStats",
      "signature": "f64"
    },
    {
      "id": "zu::zu1::stats::DegreeStats::inn",
      "kind": "field",
      "name": "inn",
      "of": "zu::zu1::stats::DegreeStats",
      "signature": "DegreeNorms"
    },
    {
      "id": "zu::zu1::stats::DegreeStats::out",
      "kind": "field",
      "name": "out",
      "of": "zu::zu1::stats::DegreeStats",
      "signature": "DegreeNorms"
    },
    {
      "id": "zu::zu1::stats::HIST_BUCKETS",
      "kind": "constant",
      "name": "HIST_BUCKETS",
      "signature": "usize",
      "doc": "Equi-depth buckets one column keeps (perf/12 section 1)."
    },
    {
      "id": "zu::zu1::stats::RelStats",
      "kind": "struct",
      "name": "RelStats",
      "doc": "The degree histograms of one rel table, forward then backward, the\ndegree-sequence norms of the same two directions, and the COLOR\nsummary when `ANALYZE` has built one. Bulk load drops the summary\nbecause a reloaded table invalidates its coloring."
    },
    {
      "id": "zu::zu1::stats::RelStats::colors",
      "kind": "field",
      "name": "colors",
      "of": "zu::zu1::stats::RelStats",
      "signature": "Option<zu_zu1::colors::ColorSummary>"
    },
    {
      "id": "zu::zu1::stats::RelStats::in_hist",
      "kind": "field",
      "name": "in_hist",
      "of": "zu::zu1::stats::RelStats",
      "signature": "Vec<u64>"
    },
    {
      "id": "zu::zu1::stats::RelStats::norms",
      "kind": "field",
      "name": "norms",
      "of": "zu::zu1::stats::RelStats",
      "signature": "DegreeStats",
      "doc": "All zero for a table loaded before the norms existed, which\nreads as absent."
    },
    {
      "id": "zu::zu1::stats::RelStats::out_hist",
      "kind": "field",
      "name": "out_hist",
      "of": "zu::zu1::stats::RelStats",
      "signature": "Vec<u64>"
    },
    {
      "id": "zu::zu1::stats::Stats",
      "kind": "struct",
      "name": "Stats",
      "doc": "Every rel table's statistics, keyed by catalog table id, and every\nnode table's property column statistics, keyed by table id then\ncolumn name."
    },
    {
      "id": "zu::zu1::stats::Stats::cols",
      "kind": "field",
      "name": "cols",
      "of": "zu::zu1::stats::Stats",
      "signature": "std::collections::BTreeMap<u32, std::collections::BTreeMap<String, ColStats>>"
    },
    {
      "id": "zu::zu1::stats::Stats::decode",
      "kind": "method",
      "name": "decode",
      "of": "zu::zu1::stats::Stats",
      "signature": "fn decode(payload: &[u8]) -> Result<Self>"
    },
    {
      "id": "zu::zu1::stats::Stats::encode",
      "kind": "method",
      "name": "encode",
      "of": "zu::zu1::stats::Stats",
      "signature": "fn encode(&self) -> Vec<u8>"
    },
    {
      "id": "zu::zu1::stats::Stats::load",
      "kind": "method",
      "name": "load",
      "of": "zu::zu1::stats::Stats",
      "signature": "fn load(db: &mut Zu1File) -> Result<Self>",
      "doc": "Reads the stats chain; an absent chain or an unknown version is\nan empty set, never an error."
    },
    {
      "id": "zu::zu1::stats::Stats::rels",
      "kind": "field",
      "name": "rels",
      "of": "zu::zu1::stats::Stats",
      "signature": "std::collections::BTreeMap<u32, RelStats>"
    },
    {
      "id": "zu::zu1::stats::Stats::store",
      "kind": "method",
      "name": "store",
      "of": "zu::zu1::stats::Stats",
      "signature": "fn store(&self, db: &mut Zu1File) -> Result<()>",
      "doc": "Rewrites the whole chain and points the header at it. The old\nchain's blocks are freed by the caller's checkpoint path via\n[`crate::graph::free_chain`]; this only writes."
    },
    {
      "id": "zu::zu1::stats::TOP_VALUES",
      "kind": "constant",
      "name": "TOP_VALUES",
      "signature": "usize",
      "doc": "The most frequent values one column keeps (perf/12 section 1)."
    },
    {
      "id": "zu::zu1::stats::VALUE_CAP",
      "kind": "constant",
      "name": "VALUE_CAP",
      "signature": "usize",
      "doc": "Longest value a column statistic stores."
    },
    {
      "id": "zu::zu1::stats::column_stats",
      "kind": "function",
      "name": "column_stats",
      "signature": "fn column_stats(keys: &[&[u8]]) -> ColStats",
      "doc": "The statistics of one column, over the order-preserving byte keys\nof its values in row order."
    },
    {
      "id": "zu::zu1::stats::degree_cross",
      "kind": "function",
      "name": "degree_cross",
      "signature": "fn degree_cross(out: &[(u32, u32)], inn: &[(u32, u32)]) -> f64",
      "doc": "Sum over nodes of out-degree times in-degree, which is the number\nof two hop paths the table holds. Both lists are sorted on their\nfirst component, so the runs line up with a merge and a node\nmissing from either side contributes nothing."
    },
    {
      "id": "zu::zu1::stats::degree_histogram",
      "kind": "function",
      "name": "degree_histogram",
      "signature": "fn degree_histogram(sorted: &[(u32, u32)]) -> Vec<u64>",
      "doc": "The log2 degree histogram of one direction, from an edge list\nsorted by source: one run per source node, bucket `floor(log2 deg)`."
    },
    {
      "id": "zu::zu1::stats::degree_norms",
      "kind": "function",
      "name": "degree_norms",
      "signature": "fn degree_norms(sorted: &[(u32, u32)]) -> DegreeNorms",
      "doc": "The lp norms of one direction's degree sequence, from the same\nsource-sorted edge list the histogram reads. The power sums run in\nu128 because a hub-heavy graph can put the cube past what a u64\nholds, and only the roots come back as f64."
    },
    {
      "id": "zu::zu1::txn",
      "kind": "module",
      "name": "txn",
      "doc": "The single-writer commit path and its in-memory MVCC overlays.\n\nCommitted state per docs/08 section 2 is the base epoch data in the\nfile plus per-table overlays: appended rows, tombstones, property\nupdate chains newest-first, and overlay edges, every entry stamped\nwith its commit epoch. A reader pinned at epoch E sees base data\nplus overlay entries with commit epoch at or below E, so snapshots\nstay consistent while the writer commits past them. Version data\nnever reaches the columnar file; a checkpoint folds overlays into\nnew sealed segments and recovery rebuilds them from the WAL alone.\n\nSingle writer is enforced by the borrow checker: [`Mvcc::begin`]\nhands out a [`WriteTxn`] holding the store's only mutable borrow,\nso a second writer cannot exist while one is open. Commit appends\nthe txn's records to the WAL, syncs through the `TxnCommit` frame,\nand only then publishes the staged changes to the overlays, so a\ncrash on either side of the sync leaves the pre-txn or post-txn\nstate and nothing else."
    },
    {
      "id": "zu::zu1::txn::Cell",
      "kind": "enum",
      "name": "Cell",
      "doc": "One overlay cell value, the unit of an update chain entry and of an\nappended row's column."
    },
    {
      "id": "zu::zu1::txn::Cell::Int",
      "kind": "variant",
      "name": "Int",
      "of": "zu::zu1::txn::Cell",
      "signature": "Int(u64)"
    },
    {
      "id": "zu::zu1::txn::Cell::Null",
      "kind": "variant",
      "name": "Null",
      "of": "zu::zu1::txn::Cell",
      "signature": "Null",
      "doc": "The absence a `REMOVE` leaves behind. A column holds it the way\nstorage does, as a row whose validity bit is clear, so what the\nfold writes for one of these is a bit rather than a value."
    },
    {
      "id": "zu::zu1::txn::Cell::Str",
      "kind": "variant",
      "name": "Str",
      "of": "zu::zu1::txn::Cell",
      "signature": "Str(Vec<u8>)"
    },
    {
      "id": "zu::zu1::txn::CellWrite",
      "kind": "type-alias",
      "name": "CellWrite",
      "signature": "(u32, u64, u32, Cell)",
      "doc": "One write onto an existing row: table, row offset, column position\nin the props directory, and the value."
    },
    {
      "id": "zu::zu1::txn::Deferred",
      "kind": "enum",
      "name": "Deferred",
      "doc": "One change a reader can be shown before a fold has sealed it."
    },
    {
      "id": "zu::zu1::txn::Deferred::Cell",
      "kind": "variant",
      "name": "Cell",
      "of": "zu::zu1::txn::Deferred",
      "signature": "Cell(CellWrite)",
      "doc": "A value written onto a column of a row that is already there."
    },
    {
      "id": "zu::zu1::txn::Deferred::DeadRel",
      "kind": "variant",
      "name": "DeadRel",
      "of": "zu::zu1::txn::Deferred",
      "signature": "DeadRel(u32, u64, u64)",
      "doc": "An edge taken away: the rel table and the pair it ran between.\nA pair is the whole name of an edge, so this takes every copy of\nit, which is what the fold does with the same pair."
    },
    {
      "id": "zu::zu1::txn::Deferred::Edge",
      "kind": "variant",
      "name": "Edge",
      "of": "zu::zu1::txn::Deferred",
      "signature": "Edge(u32, u64, u64, Vec<(u32, Cell)>)",
      "doc": "An edge added to a rel table: the table, the rows it runs\nbetween, and the cell it holds in each column the table stores,\nby position in the props directory."
    },
    {
      "id": "zu::zu1::txn::Deferred::Gone",
      "kind": "variant",
      "name": "Gone",
      "of": "zu::zu1::txn::Deferred",
      "signature": "Gone(u32, u64)",
      "doc": "A row taken away: the table and the offset it kept. Nothing of\nthe row moves, here or in a fold, so what a reader is handed is\nthe offset and what makes the row gone is the reader."
    },
    {
      "id": "zu::zu1::txn::Deferred::Labels",
      "kind": "variant",
      "name": "Labels",
      "of": "zu::zu1::txn::Deferred",
      "signature": "Labels(u32, u64, u64, u64)",
      "doc": "Labels put on a row and taken off it: the table, the offset, and\nthe two masks the change was written as, which are disjoint."
    },
    {
      "id": "zu::zu1::txn::Deferred::Marks",
      "kind": "variant",
      "name": "Marks",
      "of": "zu::zu1::txn::Deferred",
      "signature": "Marks(u32, u64, u64)",
      "doc": "The labels a row is left carrying, whole: the table, the offset\nand the word. A change is a pair of masks and a reader wants the\nword, so the writer reads what the row carried, puts the masks\nover it, and hands on the answer. Only the writer makes one of\nthese, out of the [`Deferred::Labels`] it was given."
    },
    {
      "id": "zu::zu1::txn::Deferred::RelCell",
      "kind": "variant",
      "name": "RelCell",
      "of": "zu::zu1::txn::Deferred",
      "signature": "RelCell(RelCellWrite)",
      "doc": "A value written onto a column of an edge the graph already runs\nthrough."
    },
    {
      "id": "zu::zu1::txn::Deferred::Rows",
      "kind": "variant",
      "name": "Rows",
      "of": "zu::zu1::txn::Deferred",
      "signature": "Rows(u32, Vec<Vec<(u32, Cell)>>)",
      "doc": "Rows added to a node table: the table, and the cell each new row\nholds in each column, by position in the props directory. They\ngo on the end of the table in the order they are in here, which\nis the order a fold would append them in."
    },
    {
      "id": "zu::zu1::txn::IngestPayload",
      "kind": "enum",
      "name": "IngestPayload",
      "doc": "The payload of one resolved or freshly written ingest: the sealed\ndata as the overlay will serve it until a fold seals it into the\nbase. The blocks in the file hold the durable copy; this is the\nin-memory image recovery reads back from them."
    },
    {
      "id": "zu::zu1::txn::IngestPayload::Edges",
      "kind": "variant",
      "name": "Edges",
      "of": "zu::zu1::txn::IngestPayload",
      "signature": "Edges { src: Vec<u64>, dst: Vec<u64> }"
    },
    {
      "id": "zu::zu1::txn::IngestPayload::Nodes",
      "kind": "variant",
      "name": "Nodes",
      "of": "zu::zu1::txn::IngestPayload",
      "signature": "Nodes { cols: Vec<zu_zu1::wal::WalColumn>, rows: u64 }"
    },
    {
      "id": "zu::zu1::txn::Mvcc",
      "kind": "struct",
      "name": "Mvcc",
      "doc": "The committed overlay store and the epoch counter, owned by\nwhichever handle owns the write side of the database."
    },
    {
      "id": "zu::zu1::txn::Mvcc::appended_rows",
      "kind": "method",
      "name": "appended_rows",
      "of": "zu::zu1::txn::Mvcc",
      "signature": "fn appended_rows(&self, table: u32, epoch: Epoch) -> u64",
      "doc": "Rows appended to `table` and visible at `epoch`."
    },
    {
      "id": "zu::zu1::txn::Mvcc::appends_carry_columns",
      "kind": "method",
      "name": "appends_carry_columns",
      "of": "zu::zu1::txn::Mvcc",
      "signature": "fn appends_carry_columns(&self, table: u32, epoch: Epoch) -> bool",
      "doc": "Whether any append batch of `table` visible at `epoch` carries\ncolumn values, which a fold must merge into stored props."
    },
    {
      "id": "zu::zu1::txn::Mvcc::begin",
      "kind": "method",
      "name": "begin",
      "of": "zu::zu1::txn::Mvcc",
      "signature": "fn begin(&mut self) -> WriteTxn",
      "doc": "Opens the write transaction. The mutable borrow is the writer\nlock: a second call cannot compile while one txn is open."
    },
    {
      "id": "zu::zu1::txn::Mvcc::cell",
      "kind": "method",
      "name": "cell",
      "of": "zu::zu1::txn::Mvcc",
      "signature": "fn cell(&self, table: u32, base_count: u64, offset: u64, col: u32, epoch: Epoch) -> Option<Cell>",
      "doc": "The newest overlay value of one cell at `epoch`: an update chain\nentry first, then an appended row's stored column. `base_count`\nis the row count of the sealed file, where appended offsets\nstart. Returns `None` when the base file holds the answer."
    },
    {
      "id": "zu::zu1::txn::Mvcc::col_updates",
      "kind": "method",
      "name": "col_updates",
      "of": "zu::zu1::txn::Mvcc",
      "signature": "fn col_updates(&self, table: u32, col: u32, base_count: u64, epoch: Epoch) -> Vec<(u64, Cell)>",
      "doc": "The rows of one column the overlay holds a value for at\n`epoch`, ascending, as the pairs a fold applies to the column it\nis rewriting.\n\nThis is [`Mvcc::cell`] turned around. A fold asking `cell` about\nevery row of a column pays a hash probe per row to be told that\nalmost none of them were written, so folding a one cell write\ninto a hundred thousand row table cost a hundred thousand\nprobes to find the one. The overlay already knows which rows it\nholds, and those are the rows a write changed.\n\nAppended rows are not in here even when a later statement wrote\nover one. They arrive with the batch that appended them, which\na fold reads whole because every one of its rows is new, so\n`cell` stays the way to read them and stays the one place the\nchain wins over the batch."
    },
    {
      "id": "zu::zu1::txn::Mvcc::dead_edges",
      "kind": "method",
      "name": "dead_edges",
      "of": "zu::zu1::txn::Mvcc",
      "signature": "fn dead_edges(&self, rel: u32, epoch: Epoch) -> Vec<(u64, u64)>",
      "doc": "The pairs `rel` has lost by `epoch`, ascending, which is what a\nfold drops out of the CSR it rebuilds. The base copies of a pair\ngo on the first delete of it, so that is the stamp this reads."
    },
    {
      "id": "zu::zu1::txn::Mvcc::edge_cells",
      "kind": "method",
      "name": "edge_cells",
      "of": "zu::zu1::txn::Mvcc",
      "signature": "fn edge_cells(&self, rel: u32, epoch: Epoch) -> impl Iterator",
      "doc": "The same edges with what they carry, which is what a fold of a\nrel table that stores edge properties has to write."
    },
    {
      "id": "zu::zu1::txn::Mvcc::edge_gone",
      "kind": "method",
      "name": "edge_gone",
      "of": "zu::zu1::txn::Mvcc",
      "signature": "fn edge_gone(&self, rel: u32, src: u64, dst: u64, since: Epoch, epoch: Epoch) -> bool",
      "doc": "Whether an edge over two rows that has stood since `since` is\ngone for a reader at `epoch`.\n\nThe pair is the name of the edge, so what this asks is whether\nanything took the pair away in the window the edge has been\nthere for. An edge the base holds has stood since the file was\nsealed, which is epoch zero as far as the overlay is concerned,\nand an overlay edge has stood since the commit that added it: a\ndelete before that one is about the copies that were there then\nand says nothing about this one."
    },
    {
      "id": "zu::zu1::txn::Mvcc::edge_updates",
      "kind": "method",
      "name": "edge_updates",
      "of": "zu::zu1::txn::Mvcc",
      "signature": "fn edge_updates(&self, rel: u32, epoch: Epoch) -> BTreeMap<(u64, u64, u32), Cell>",
      "doc": "What `rel` has had written onto its edges by `epoch`, keyed by\nthe rows an edge runs between and the column, one cell each.\n\nA cell's chain holds every value written to it in order, and the\none a reader at `epoch` sees is the last one written at or before\nit, which is what makes two assignments to one property in one\nstatement end with the second."
    },
    {
      "id": "zu::zu1::txn::Mvcc::edges",
      "kind": "method",
      "name": "edges",
      "of": "zu::zu1::txn::Mvcc",
      "signature": "fn edges(&self, rel: u32, epoch: Epoch) -> impl Iterator",
      "doc": "Overlay edges of `rel` visible at `epoch`, for checkpoint folds."
    },
    {
      "id": "zu::zu1::txn::Mvcc::epoch",
      "kind": "method",
      "name": "epoch",
      "of": "zu::zu1::txn::Mvcc",
      "signature": "fn epoch(&self) -> Epoch",
      "doc": "The newest committed epoch; a new reader pins this."
    },
    {
      "id": "zu::zu1::txn::Mvcc::has_edge_updates",
      "kind": "method",
      "name": "has_edge_updates",
      "of": "zu::zu1::txn::Mvcc",
      "signature": "fn has_edge_updates(&self, rel: u32, epoch: Epoch) -> bool",
      "doc": "Whether `rel` holds any edge update visible at `epoch`, which is\nwhat says a fold has work to do on a rel table nothing else\ntouched."
    },
    {
      "id": "zu::zu1::txn::Mvcc::has_label_changes",
      "kind": "method",
      "name": "has_label_changes",
      "of": "zu::zu1::txn::Mvcc",
      "signature": "fn has_label_changes(&self, table: u32, epoch: Epoch) -> bool",
      "doc": "Whether `table` holds any label change visible at `epoch`, which\nis what says a fold has work to do on a table whose rows and\nproperties nothing touched."
    },
    {
      "id": "zu::zu1::txn::Mvcc::has_updates",
      "kind": "method",
      "name": "has_updates",
      "of": "zu::zu1::txn::Mvcc",
      "signature": "fn has_updates(&self, table: u32, epoch: Epoch) -> bool",
      "doc": "Whether `table` holds any update chain entry visible at `epoch`."
    },
    {
      "id": "zu::zu1::txn::Mvcc::ingest_roots",
      "kind": "method",
      "name": "ingest_roots",
      "of": "zu::zu1::txn::Mvcc",
      "signature": "fn ingest_roots(&self, epoch: Epoch) -> Vec<BlockPtr>",
      "doc": "Manifest roots of ingests committed at or below `epoch`, for\nthe fold to free once their data is sealed into the base."
    },
    {
      "id": "zu::zu1::txn::Mvcc::is_deleted",
      "kind": "method",
      "name": "is_deleted",
      "of": "zu::zu1::txn::Mvcc",
      "signature": "fn is_deleted(&self, table: u32, offset: u64, epoch: Epoch) -> bool",
      "doc": "Whether the row at `offset` is deleted for a reader at `epoch`."
    },
    {
      "id": "zu::zu1::txn::Mvcc::label_changes",
      "kind": "method",
      "name": "label_changes",
      "of": "zu::zu1::txn::Mvcc",
      "signature": "fn label_changes(&self, table: u32, epoch: Epoch) -> BTreeMap<u64, (u64, u64)>",
      "doc": "What `table` has had put on its rows' labels and taken off them\nby `epoch`, keyed by offset: one pair of masks per row, the bits\nto set and the bits to clear.\n\nA row's chain is composed rather than read at its end, because\ntwo statements can change two labels of one row and both have to\nland. Composing keeps the two masks disjoint, so the fold can\nwrite `(word | add) & !remove` and get the same answer whichever\norder it applies them in."
    },
    {
      "id": "zu::zu1::txn::Mvcc::neighbors",
      "kind": "method",
      "name": "neighbors",
      "of": "zu::zu1::txn::Mvcc",
      "signature": "fn neighbors(&self, rel: u32, node: u64, reversed: bool, epoch: Epoch, out: &mut Vec<u64>)",
      "doc": "Appends the overlay neighbors of `node` visible at `epoch` to\n`out`: destinations of overlay edges leaving it, or sources of\nedges entering it when `reversed`. Tombstoned endpoints are the\nfacade's concern; the overlay stores edges as committed."
    },
    {
      "id": "zu::zu1::txn::Mvcc::new",
      "kind": "method",
      "name": "new",
      "of": "zu::zu1::txn::Mvcc",
      "signature": "fn new(epoch: Epoch) -> Self",
      "doc": "A store with no overlays at the given committed epoch, the\nstate right after a checkpoint or a fresh create."
    },
    {
      "id": "zu::zu1::txn::Mvcc::recover",
      "kind": "method",
      "name": "recover",
      "of": "zu::zu1::txn::Mvcc",
      "signature": "fn recover(wal: &Wal, floor: Epoch) -> Result<Self>",
      "doc": "Rebuilds the overlay store from the log: every committed txn\nabove `floor` replays into overlays exactly as its commit\npublished it, and the epoch counter resumes past the last one.\nAn `IngestRef` in the log is an error here; recovery with a\ndatabase file at hand goes through [`Self::recover_with`], whose\nresolver reads the referenced sealed segments back."
    },
    {
      "id": "zu::zu1::txn::Mvcc::recover_with",
      "kind": "method",
      "name": "recover_with",
      "of": "zu::zu1::txn::Mvcc",
      "signature": "fn recover_with(wal: &Wal, floor: Epoch, resolve: impl FnMut) -> Result<Self>",
      "doc": "[`Self::recover`] with a resolver that turns each committed\n`IngestRef` back into its payload by reading the sealed\nsegments its manifest names, returning the payload and the\nmanifest root for the next fold to free."
    },
    {
      "id": "zu::zu1::txn::Mvcc::rels_touched",
      "kind": "method",
      "name": "rels_touched",
      "of": "zu::zu1::txn::Mvcc",
      "signature": "fn rels_touched(&self) -> Vec<u32>",
      "doc": "Rel tables holding overlay edges, sorted like the node tables."
    },
    {
      "id": "zu::zu1::txn::Mvcc::seed_tombstones",
      "kind": "method",
      "name": "seed_tombstones",
      "of": "zu::zu1::txn::Mvcc",
      "signature": "fn seed_tombstones(&mut self, table: u32, offsets: &[u64])",
      "doc": "Seeds tombstones a checkpoint persisted into the base file. They\nenter at epoch 0 so every reader sees them, matching their state\nas folded rather than freshly deleted."
    },
    {
      "id": "zu::zu1::txn::Mvcc::soft",
      "kind": "method",
      "name": "soft",
      "of": "zu::zu1::txn::Mvcc",
      "signature": "fn soft(&self) -> bool",
      "doc": "Whether everything this store holds is a shape a reader can be\nshown without a fold sealing it first."
    },
    {
      "id": "zu::zu1::txn::Mvcc::tables_touched",
      "kind": "method",
      "name": "tables_touched",
      "of": "zu::zu1::txn::Mvcc",
      "signature": "fn tables_touched(&self) -> Vec<u32>",
      "doc": "Node tables holding any overlay state, sorted so a fold walks\nthem deterministically."
    },
    {
      "id": "zu::zu1::txn::Mvcc::take_deferred",
      "kind": "method",
      "name": "take_deferred",
      "of": "zu::zu1::txn::Mvcc",
      "signature": "fn take_deferred(&mut self) -> Vec<Deferred>",
      "doc": "The changes the last commit published, in statement order.\nEmpty when that commit held anything of another shape, and\nempty again once it has been read."
    },
    {
      "id": "zu::zu1::txn::Mvcc::tombstones",
      "kind": "method",
      "name": "tombstones",
      "of": "zu::zu1::txn::Mvcc",
      "signature": "fn tombstones(&self, table: u32, epoch: Epoch) -> Vec<u64>",
      "doc": "Tombstoned offsets of `table` visible at `epoch`, ascending."
    },
    {
      "id": "zu::zu1::txn::Mvcc::touched_cols",
      "kind": "method",
      "name": "touched_cols",
      "of": "zu::zu1::txn::Mvcc",
      "signature": "fn touched_cols(&self, table: u32, epoch: Epoch) -> BTreeSet<u32>",
      "doc": "Which columns of `table` hold an update chain entry visible at\n`epoch`, the columns a fold has to rewrite. The ones outside it\ncome through untouched, and answering the whole set in one pass\nis what keeps that check off the column count."
    },
    {
      "id": "zu::zu1::txn::RelCellWrite",
      "kind": "type-alias",
      "name": "RelCellWrite",
      "signature": "(u32, u64, u64, u32, Cell)",
      "doc": "One write onto an edge: rel table, the rows the edge runs between,\ncolumn position in the props directory, and the value.\n\nThe pair is the only name a statement has for an edge. Which row of\nthe property columns it holds is the ordinal it was loaded at, and\nworking that out takes the adjacency reader, which lives on the\nwrite side rather than in here."
    },
    {
      "id": "zu::zu1::txn::WriteTxn",
      "kind": "struct",
      "name": "WriteTxn",
      "doc": "An open write transaction: stages mutations locally and publishes\nthem on [`WriteTxn::commit`]. Dropping it without committing\nabandons everything staged, on disk and in memory alike."
    },
    {
      "id": "zu::zu1::txn::WriteTxn::commit",
      "kind": "method",
      "name": "commit",
      "of": "zu::zu1::txn::WriteTxn",
      "signature": "fn commit(self, wal: &mut Wal) -> Result<Epoch>",
      "doc": "Commits: appends every staged record and the `TxnCommit` frame,\nsyncs, then publishes the overlays. The fdatasync inside\n[`Wal::commit`] is the commit point; failure before it leaves\nthe store untouched and the tail torn, which replay drops."
    },
    {
      "id": "zu::zu1::txn::WriteTxn::delete",
      "kind": "method",
      "name": "delete",
      "of": "zu::zu1::txn::WriteTxn",
      "signature": "fn delete(&mut self, table: u32, offset: u64)",
      "doc": "Stages one row deletion."
    },
    {
      "id": "zu::zu1::txn::WriteTxn::delete_rel",
      "kind": "method",
      "name": "delete_rel",
      "of": "zu::zu1::txn::WriteTxn",
      "signature": "fn delete_rel(&mut self, rel: u32, src: u64, dst: u64)",
      "doc": "Stages the removal of the edges between two rows. An edge is\nnamed by the rows it runs between, so a table holding a pair\ntwice loses both, which is what a `DETACH DELETE` of either end\nwants anyway."
    },
    {
      "id": "zu::zu1::txn::WriteTxn::insert_nodes",
      "kind": "method",
      "name": "insert_nodes",
      "of": "zu::zu1::txn::WriteTxn",
      "signature": "fn insert_nodes(&mut self, table: u32, cols: Vec<(u32, Vec<Cell>)>) -> Result<u64>",
      "doc": "Stages a columnar batch of new rows for `table`. Every column\nmust carry the same row count."
    },
    {
      "id": "zu::zu1::txn::WriteTxn::insert_rel",
      "kind": "method",
      "name": "insert_rel",
      "of": "zu::zu1::txn::WriteTxn",
      "signature": "fn insert_rel(&mut self, rel: u32, src: u64, dst: u64)",
      "doc": "Stages one new edge carrying nothing, which is every edge in a\ntable that stores no properties on its edges."
    },
    {
      "id": "zu::zu1::txn::WriteTxn::insert_rel_carrying",
      "kind": "method",
      "name": "insert_rel_carrying",
      "of": "zu::zu1::txn::WriteTxn",
      "signature": "fn insert_rel_carrying(&mut self, rel: u32, src: u64, dst: u64, cols: Vec<(u32, Cell)>)",
      "doc": "Stages one new edge and the cells it holds: a column's position\nin the rel table's props directory and the value this edge takes\nthere, one entry per column the table stores."
    },
    {
      "id": "zu::zu1::txn::WriteTxn::is_empty",
      "kind": "method",
      "name": "is_empty",
      "of": "zu::zu1::txn::WriteTxn",
      "signature": "fn is_empty(&self) -> bool",
      "doc": "Whether nothing has been staged."
    },
    {
      "id": "zu::zu1::txn::WriteTxn::stage_commit",
      "kind": "method",
      "name": "stage_commit",
      "of": "zu::zu1::txn::WriteTxn",
      "signature": "fn stage_commit(self, wal: &mut Wal) -> Result<(Epoch, Option<u64>)>",
      "doc": "[`Self::commit`] without the wait for the platter: the frames go\nto the log and what comes back is the epoch and the byte the log\nhas to be durable through for the transaction to have committed.\n\nThe overlays are applied here, before that wait, because they\nare what the writer itself reads and it has to see its own\nwrite. What must not happen before the wait is publishing them\nto the other connections, and that is the caller's to hold back.\n\nA transaction that staged nothing owes nothing and is already\ncommitted, trivially."
    },
    {
      "id": "zu::zu1::txn::WriteTxn::update",
      "kind": "method",
      "name": "update",
      "of": "zu::zu1::txn::WriteTxn",
      "signature": "fn update(&mut self, table: u32, offset: u64, col: u32, value: Cell)",
      "doc": "Stages one cell update."
    },
    {
      "id": "zu::zu1::txn::WriteTxn::update_labels",
      "kind": "method",
      "name": "update_labels",
      "of": "zu::zu1::txn::WriteTxn",
      "signature": "fn update_labels(&mut self, table: u32, row: u64, add: u64, remove: u64) -> Result<()>",
      "doc": "Stages a change to one row's labels: the bits to set and the\nbits to clear, against the graph's label dictionary. The two\nmasks have to be disjoint, because a label cannot be both put on\na row and taken off it by one change."
    },
    {
      "id": "zu::zu1::txn::WriteTxn::update_rel",
      "kind": "method",
      "name": "update_rel",
      "of": "zu::zu1::txn::WriteTxn",
      "signature": "fn update_rel(&mut self, rel: u32, src: u64, dst: u64, col: u32, value: Cell)",
      "doc": "Stages one cell update on an edge that is already there. The\nedge is named by the rows it runs between, the way a removed one\nis, and `col` is the column's position in the rel table's props\ndirectory."
    },
    {
      "id": "zu::zu1::verify",
      "kind": "function",
      "name": "verify",
      "signature": "fn verify(path: &std::path::Path) -> zu_common::Result<u64>",
      "doc": "Walks the whole file checking every crc: file header, database\nheaders, the catalog, the table index, and each rel table's group\ndirectory with every column segment it lists, the primary-key index\nincluded when the table carries one. Cross-checks the three\nagainst each other (every rel table has a directory, every index\nentry belongs to a catalog table, node entries decode as props\ndirectories with row-aligned columns, counts agree), then decodes\nthe free list and\nrejects a file whose free list claims a block a live chain or segment\nuses, since allocating such a block would overwrite live data.\nReturns the number of payload bytes verified."
    },
    {
      "id": "zu::zu1::vfs",
      "kind": "module",
      "name": "vfs",
      "doc": "The file shim under the storage layer.\n\n[`Zu1File`](crate::file::Zu1File) and [`Wal`](crate::wal::Wal) do\ntheir I/O through [`VfsFile`], whose methods are exactly the\nsyscalls the engine issues: positioned reads and writes, length\nchanges, and the two sync flavors. Production code runs on\n[`RealFile`], a thin pass-through. The crash harness runs on\n[`RecordingFile`], which logs every mutating call before forwarding\nit, so a test can cut the log at any syscall boundary, rebuild the\nfile image a crash at that point could leave, and check recovery\nagainst it. Reads are not logged; a crash cannot lose a read.\n\n[`Vfs`] is the other half: where [`VfsFile`] is one open file, a\n[`Vfs`] is where files come from. A database is not one file but\ntwo, the base and the sidecar log, and the second is opened by name\nfrom the first's path long after the first was opened. So a database\nthat lives somewhere other than the filesystem cannot be a handle\npassed in at the open; it has to be a thing that answers a path.\n[`RealVfs`] is the filesystem and is what every open uses unless it\nwas told otherwise. [`MemVfs`] is a map from path to bytes, which is\nwhat a database with no file on disk is made of."
    },
    {
      "id": "zu::zu1::vfs::IoEvent",
      "kind": "enum",
      "name": "IoEvent",
      "doc": "One mutating syscall, in issue order across both files."
    },
    {
      "id": "zu::zu1::vfs::IoEvent::SetLen",
      "kind": "variant",
      "name": "SetLen",
      "of": "zu::zu1::vfs::IoEvent",
      "signature": "SetLen { file: IoFile, len: u64 }"
    },
    {
      "id": "zu::zu1::vfs::IoEvent::Sync",
      "kind": "variant",
      "name": "Sync",
      "of": "zu::zu1::vfs::IoEvent",
      "signature": "Sync { file: IoFile }"
    },
    {
      "id": "zu::zu1::vfs::IoEvent::Write",
      "kind": "variant",
      "name": "Write",
      "of": "zu::zu1::vfs::IoEvent",
      "signature": "Write { file: IoFile, offset: u64, bytes: Vec<u8> }"
    },
    {
      "id": "zu::zu1::vfs::IoFile",
      "kind": "enum",
      "name": "IoFile",
      "doc": "Which file an [`IoEvent`] belongs to. Sync barriers are per file:\nsyncing the database never makes an unsynced WAL write durable."
    },
    {
      "id": "zu::zu1::vfs::IoFile::Db",
      "kind": "variant",
      "name": "Db",
      "of": "zu::zu1::vfs::IoFile",
      "signature": "Db"
    },
    {
      "id": "zu::zu1::vfs::IoFile::Wal",
      "kind": "variant",
      "name": "Wal",
      "of": "zu::zu1::vfs::IoFile",
      "signature": "Wal"
    },
    {
      "id": "zu::zu1::vfs::IoLog",
      "kind": "type-alias",
      "name": "IoLog",
      "signature": "std::sync::Arc<std::sync::Mutex<Vec<IoEvent>>>",
      "doc": "The shared log a workload's recording files append to."
    },
    {
      "id": "zu::zu1::vfs::MemFile",
      "kind": "struct",
      "name": "MemFile",
      "doc": "One file of a [`MemVfs`], which is a vector of bytes and a promise\nabout whether this handle may write to it.\n\nThe handles on one file share the bytes, because two handles on one\nfile share the file. That is what makes a fork for reading see what\nthe write side put there, which on a real filesystem the kernel does\nand here has to be said."
    },
    {
      "id": "zu::zu1::vfs::MemVfs",
      "kind": "struct",
      "name": "MemVfs",
      "doc": "A filesystem made of memory: a map from path to bytes.\n\nOne of these is one database, because a database is a base file and\na sidecar log and the two have to find each other by name. Nothing\nhere is written anywhere, so nothing here survives the process, and\nthe last handle going away is what frees it.\n\nThe paths are still paths. A memory database is opened under a name\nof its own so that the write side, which is registered per path per\nprocess, keeps working exactly as it does for a file: two memory\ndatabases are two names and never meet, and two connections to one\nare one name and share a writer."
    },
    {
      "id": "zu::zu1::vfs::MemVfs::bytes",
      "kind": "method",
      "name": "bytes",
      "of": "zu::zu1::vfs::MemVfs",
      "signature": "fn bytes(&self) -> u64",
      "doc": "How many bytes this filesystem is holding, across every file in\nit. What a caller who asked for a database in memory would want\nto know about the memory."
    },
    {
      "id": "zu::zu1::vfs::MemVfs::new",
      "kind": "method",
      "name": "new",
      "of": "zu::zu1::vfs::MemVfs",
      "signature": "fn new() -> MemVfs"
    },
    {
      "id": "zu::zu1::vfs::RealFile",
      "kind": "struct",
      "name": "RealFile",
      "doc": "The production implementation: every call is the syscall it names."
    },
    {
      "id": "zu::zu1::vfs::RealFile::create_new",
      "kind": "method",
      "name": "create_new",
      "of": "zu::zu1::vfs::RealFile",
      "signature": "fn create_new(path: &Path) -> Result<Self>",
      "doc": "Creates the file, failing when it already exists."
    },
    {
      "id": "zu::zu1::vfs::RealFile::open_or_create",
      "kind": "method",
      "name": "open_or_create",
      "of": "zu::zu1::vfs::RealFile",
      "signature": "fn open_or_create(path: &Path) -> Result<Self>",
      "doc": "Opens for reading and writing, creating when missing, the WAL\ncontract."
    },
    {
      "id": "zu::zu1::vfs::RealFile::open_r",
      "kind": "method",
      "name": "open_r",
      "of": "zu::zu1::vfs::RealFile",
      "signature": "fn open_r(path: &Path) -> Result<Self>",
      "doc": "Opens an existing file for reading only, so the operating\nsystem refuses a write this process should not have attempted\nand a database on a read-only mount opens at all."
    },
    {
      "id": "zu::zu1::vfs::RealFile::open_rw",
      "kind": "method",
      "name": "open_rw",
      "of": "zu::zu1::vfs::RealFile",
      "signature": "fn open_rw(path: &Path) -> Result<Self>",
      "doc": "Opens an existing file for reading and writing."
    },
    {
      "id": "zu::zu1::vfs::RealVfs",
      "kind": "struct",
      "name": "RealVfs",
      "doc": "The filesystem, which is where a database lives unless it was told\notherwise."
    },
    {
      "id": "zu::zu1::vfs::RealVfs::shared",
      "kind": "method",
      "name": "shared",
      "of": "zu::zu1::vfs::RealVfs",
      "signature": "fn shared() -> Arc<dyn Vfs>",
      "doc": "The one every open uses when nobody passed one in."
    },
    {
      "id": "zu::zu1::vfs::RecordingFile",
      "kind": "struct",
      "name": "RecordingFile",
      "doc": "A pass-through that logs every mutating syscall before issuing it."
    },
    {
      "id": "zu::zu1::vfs::RecordingFile::new",
      "kind": "method",
      "name": "new",
      "of": "zu::zu1::vfs::RecordingFile",
      "signature": "fn new(inner: RealFile, id: IoFile, log: IoLog) -> Self"
    },
    {
      "id": "zu::zu1::vfs::Vfs",
      "kind": "trait",
      "name": "Vfs",
      "doc": "Where files come from.\n\nThe four opens are the four the engine issues, named for what they\npromise rather than for the flags they would pass: a create that\nrefuses to clobber, a read-write open of something that is already\nthere, a read-only one, and the log's open-or-create. `exists` and\n`remove` are the two questions asked about a file without opening\nit, both of them about the sidecar log.\n\nImplementations are shared by every handle on a database, so this is\n`Send + Sync` and takes `&self`."
    },
    {
      "id": "zu::zu1::vfs::Vfs::create_new",
      "kind": "method",
      "name": "create_new",
      "of": "zu::zu1::vfs::Vfs",
      "signature": "fn create_new(&self, path: &Path) -> Result<Box<dyn VfsFile>>"
    },
    {
      "id": "zu::zu1::vfs::Vfs::durable",
      "kind": "method",
      "name": "durable",
      "of": "zu::zu1::vfs::Vfs",
      "signature": "fn durable(&self) -> bool",
      "doc": "Whether what this hands back survives the process. A database on\none that says no has nothing to recover after a crash, and the\nwork done to make it recoverable is work nobody will collect."
    },
    {
      "id": "zu::zu1::vfs::Vfs::exists",
      "kind": "method",
      "name": "exists",
      "of": "zu::zu1::vfs::Vfs",
      "signature": "fn exists(&self, path: &Path) -> bool"
    },
    {
      "id": "zu::zu1::vfs::Vfs::open_or_create",
      "kind": "method",
      "name": "open_or_create",
      "of": "zu::zu1::vfs::Vfs",
      "signature": "fn open_or_create(&self, path: &Path) -> Result<Box<dyn VfsFile>>"
    },
    {
      "id": "zu::zu1::vfs::Vfs::open_r",
      "kind": "method",
      "name": "open_r",
      "of": "zu::zu1::vfs::Vfs",
      "signature": "fn open_r(&self, path: &Path) -> Result<Box<dyn VfsFile>>"
    },
    {
      "id": "zu::zu1::vfs::Vfs::open_rw",
      "kind": "method",
      "name": "open_rw",
      "of": "zu::zu1::vfs::Vfs",
      "signature": "fn open_rw(&self, path: &Path) -> Result<Box<dyn VfsFile>>"
    },
    {
      "id": "zu::zu1::vfs::Vfs::remove",
      "kind": "method",
      "name": "remove",
      "of": "zu::zu1::vfs::Vfs",
      "signature": "fn remove(&self, path: &Path) -> Result<()>"
    },
    {
      "id": "zu::zu1::vfs::VfsFile",
      "kind": "trait",
      "name": "VfsFile",
      "doc": "The syscall surface of one open file. `len` is fstat, hence the\n`Result`; emptiness is the caller's question to ask of the value."
    },
    {
      "id": "zu::zu1::vfs::VfsFile::dup",
      "kind": "method",
      "name": "dup",
      "of": "zu::zu1::vfs::VfsFile",
      "signature": "fn dup(&self) -> Result<Box<dyn VfsFile>>",
      "doc": "A second handle on the same file.\n\nGroup commit is what wants this: the sync that makes a batch of\ncommits durable runs while the writer that staged them has\nalready let go of the log, so it cannot be the same handle. A\nsync is a promise about the file rather than about the\ndescriptor, so syncing this one makes durable what was written\nthrough the other."
    },
    {
      "id": "zu::zu1::vfs::VfsFile::len",
      "kind": "method",
      "name": "len",
      "of": "zu::zu1::vfs::VfsFile",
      "signature": "fn len(&self) -> Result<u64>"
    },
    {
      "id": "zu::zu1::vfs::VfsFile::read_exact_at",
      "kind": "method",
      "name": "read_exact_at",
      "of": "zu::zu1::vfs::VfsFile",
      "signature": "fn read_exact_at(&mut self, buf: &mut [u8], offset: u64) -> Result<()>"
    },
    {
      "id": "zu::zu1::vfs::VfsFile::set_len",
      "kind": "method",
      "name": "set_len",
      "of": "zu::zu1::vfs::VfsFile",
      "signature": "fn set_len(&mut self, len: u64) -> Result<()>"
    },
    {
      "id": "zu::zu1::vfs::VfsFile::sync_all",
      "kind": "method",
      "name": "sync_all",
      "of": "zu::zu1::vfs::VfsFile",
      "signature": "fn sync_all(&mut self) -> Result<()>"
    },
    {
      "id": "zu::zu1::vfs::VfsFile::sync_data",
      "kind": "method",
      "name": "sync_data",
      "of": "zu::zu1::vfs::VfsFile",
      "signature": "fn sync_data(&mut self) -> Result<()>"
    },
    {
      "id": "zu::zu1::vfs::VfsFile::write_all_at",
      "kind": "method",
      "name": "write_all_at",
      "of": "zu::zu1::vfs::VfsFile",
      "signature": "fn write_all_at(&mut self, buf: &[u8], offset: u64) -> Result<()>"
    },
    {
      "id": "zu::zu1::wal",
      "kind": "module",
      "name": "wal",
      "doc": "Redo-only write-ahead log, the sidecar file next to a zu1 database.\n\nThe record format is the engine-shared one from docs/08 section 3:\n`len: u32 LE | crc32c: u32 LE | body`, where the body is\n`epoch: u64 LE | kind: u8 | payload` and both length and crc cover\nthe body. The s3 engine batches the same records into objects when\nM5 lands; the sqlite engine delegates to SQLite's own WAL and never\ntouches this file.\n\nReplay stops at the first frame that does not verify: a short\nprefix, a length past the end of the file, or a crc mismatch all\nmean the same thing, a torn tail from a crash mid-append, and\neverything before the tear is intact because the single writer\nappends frames in order. A frame whose crc verifies but whose\npayload does not parse is different: that is corruption or version\nskew, not a tear, and it surfaces as an error instead of a silent\nstop. Delivery is transactional: records buffer per txn and reach\nthe sink only once their `TxnCommit` frame has been read, so a tear\nafter the last commit loses nothing that was promised durable."
    },
    {
      "id": "zu::zu1::wal::Commits",
      "kind": "struct",
      "name": "Commits",
      "doc": "The open sidecar log: an append position, a staging buffer and the\nframe codec around them. Commit durability is one `fdatasync` per\ncommit; the 1 ms group-commit window from docs/08 arrives with the\nwriter queue.\n\nA transaction's frames are built in memory and go to the file in one\nwrite at commit, because a small write is nearly all syscall: a one\ncell update is a begin frame, an update frame and a commit frame,\nthree pwrites of about thirty bytes each, and on this laptop those\nthree cost more processor time than everything the statement did\nbefore them. Nothing about durability changes, since the commit\nframe is still what reaches the disk last and the sync still follows\nit.\n\nThe room those frames go into is asked for ahead of them. A write\nthat makes a file longer is a write plus an allocation plus a size\nthe sync has to carry, and a commit that does that every time pays\nfor it every time: measured on this laptop, a hundred byte frame\nand its sync cost 34 us of processor time and 3.75 ms of wall\nappending, against 25 us and 3.17 ms into space the file already\nhad. So the log takes its space in chunks and writes zeros over it,\nand a commit writes inside what it already has.\n\nZeros are not padding, they are the end of the log. A frame is a\nlength and a crc, and a length of zero is shorter than the smallest\nrecord, so the scan that replay and open both use stops at the\nfirst byte of unused reservation. That is the same stop a torn tail\ngets and it is why nothing else is needed to say where the log\nends. It is also why every path that takes frames away, the cut\nafter a checkpoint and the rollback of a transaction the process\ndied inside, writes zeros over what it took rather than leaving\nbytes that once parsed.\nThe sync a commit waits on, shared by every writer on one log.\n\nA commit is durable once the log is on the platter through the byte\nits commit frame ends at. The log is append only, so that is a\nprefix property: one sync makes durable everything staged before it,\nwhoever staged it. Which is what lets commits share one.\n\nA writer that finds nobody syncing becomes the leader, and syncs the\nlog as far as it has been staged rather than only as far as its own\nframes go, so the writers that staged behind it while it waited for\nthe write side come along for free. A writer that finds a sync\nalready running waits for it, and either it covered them or they\nlead the next one. So a burst of n commits costs one sync rather\nthan n, and the leader pauses before issuing it so that the writers\nwho are still staging when it takes the lead are in that one too.\n\nThe sync runs on a second handle on the log, because by then the\nwriter that staged the frames has given the write side back and the\nlog belongs to somebody else."
    },
    {
      "id": "zu::zu1::wal::Commits::settled",
      "kind": "method",
      "name": "settled",
      "of": "zu::zu1::wal::Commits",
      "signature": "fn settled(&self) -> bool",
      "doc": "Whether the log owes nothing: every byte staged is on the\nplatter, so what the write side holds can be shown to a reader."
    },
    {
      "id": "zu::zu1::wal::Commits::sync_through",
      "kind": "method",
      "name": "sync_through",
      "of": "zu::zu1::wal::Commits",
      "signature": "fn sync_through(&self, need: u64) -> Result<()>",
      "doc": "Returns once the log is durable through `need` bytes, syncing it\nhere if nobody else is already doing it.\n\nThis is the commit point. It runs with the write side let go of,\nso the writers behind this one are staging their own frames\nwhile this sync is in the air, and one sync covers all of them."
    },
    {
      "id": "zu::zu1::wal::Wal",
      "kind": "struct",
      "name": "Wal"
    },
    {
      "id": "zu::zu1::wal::Wal::append",
      "kind": "method",
      "name": "append",
      "of": "zu::zu1::wal::Wal",
      "signature": "fn append(&mut self, epoch: Epoch, rec: &WalRecord) -> Result<()>",
      "doc": "Stages one frame. Nothing reaches the file until the buffer\nfills or the commit pushes it, and durability comes from\n[`Wal::commit`] either way: a crash before it leaves the staged\nframes unwritten, or leaves a spilled prefix with no commit\nframe after it, and replay drops the whole uncommitted txn in\nboth cases."
    },
    {
      "id": "zu::zu1::wal::Wal::commit",
      "kind": "method",
      "name": "commit",
      "of": "zu::zu1::wal::Wal",
      "signature": "fn commit(&mut self, epoch: Epoch) -> Result<()>",
      "doc": "Stages the txn's `TxnCommit` frame, writes everything the txn\nstaged in one go and syncs the file. The commit record hitting\ndisk is the commit point: replay delivers a txn exactly when\nthat frame verifies, and it is last in the buffer, so a write\nthe disk tears anywhere leaves a txn replay refuses.\n\nThis is the whole commit, sync included, for the caller with\nnobody to share the sync with. A caller that wants to let the\nwrite side go before it waits for the platter wants\n[`Wal::stage_commit`] and then [`Commits::sync_through`], which\nis the same two steps with the lock let go of in between."
    },
    {
      "id": "zu::zu1::wal::Wal::commits",
      "kind": "method",
      "name": "commits",
      "of": "zu::zu1::wal::Wal",
      "signature": "fn commits(&self) -> &Arc<Commits>",
      "doc": "What makes this log's commits durable, to be held past the write\nside going back."
    },
    {
      "id": "zu::zu1::wal::Wal::is_empty",
      "kind": "method",
      "name": "is_empty",
      "of": "zu::zu1::wal::Wal",
      "signature": "fn is_empty(&self) -> bool"
    },
    {
      "id": "zu::zu1::wal::Wal::len",
      "kind": "method",
      "name": "len",
      "of": "zu::zu1::wal::Wal",
      "signature": "fn len(&self) -> u64",
      "doc": "Bytes of intact frames, the input to the checkpoint trigger.\nStaged frames count: they are what the next commit is about to\nmake durable, and a trigger that ignored them would read a log\nmid-transaction as shorter than it is about to be."
    },
    {
      "id": "zu::zu1::wal::Wal::open",
      "kind": "method",
      "name": "open",
      "of": "zu::zu1::wal::Wal",
      "signature": "fn open(path: &Path) -> Result<Self>",
      "doc": "Opens the log at `path`, creating it when missing, and truncates\nthe torn tail so the next append starts at the last intact\nframe. Crashing mid-append leaves a frame that fails its length\nor crc check; everything before it is kept."
    },
    {
      "id": "zu::zu1::wal::Wal::open_in",
      "kind": "method",
      "name": "open_in",
      "of": "zu::zu1::wal::Wal",
      "signature": "fn open_in(vfs: &Arc<dyn Vfs>, path: &Path) -> Result<Self>",
      "doc": "[`Self::open`] somewhere other than the filesystem, for a log\nthat belongs to a database that is not on disk either."
    },
    {
      "id": "zu::zu1::wal::Wal::open_on",
      "kind": "method",
      "name": "open_on",
      "of": "zu::zu1::wal::Wal",
      "signature": "fn open_on(file: Box<dyn VfsFile>) -> Result<Self>",
      "doc": "[`Self::open`] on an explicit file handle; the crash harness\npasses a recording one."
    },
    {
      "id": "zu::zu1::wal::Wal::replay",
      "kind": "method",
      "name": "replay",
      "of": "zu::zu1::wal::Wal",
      "signature": "fn replay(&self, floor: Epoch, sink: impl FnMut) -> Result<()>",
      "doc": "Replays committed transactions in log order through `sink`,\nwhich sees every frame of a txn including its `TxnBegin` and\n`TxnCommit`. Records with epoch at or below `floor` are already\nfolded into the base file and skip. A `CheckpointNote` raises\nthe floor the same way and is not delivered; the notes are\ngathered in a first pass so a note folds the txns before it in\nthe log, which is where the txns it folded sit. Reads go\nthrough a second descriptor so an open writer keeps its append\nposition."
    },
    {
      "id": "zu::zu1::wal::Wal::rollback_above",
      "kind": "method",
      "name": "rollback_above",
      "of": "zu::zu1::wal::Wal",
      "signature": "fn rollback_above(&mut self, floor: Epoch) -> Result<()>",
      "doc": "Drops the frames of a transaction the process died inside, which\nare the ones committed after it began: the marker it left says\nwhere the log stood then, so everything above that floor is the\ntransaction's own and goes with it.\n\nEpochs only go up along the log, so this is a prefix cut rather\nthan a rewrite. Frames at or below the floor were committed\nbefore the transaction and stay, whether the base file has\nfolded them already or replay is about to bring them back."
    },
    {
      "id": "zu::zu1::wal::Wal::stage_commit",
      "kind": "method",
      "name": "stage_commit",
      "of": "zu::zu1::wal::Wal",
      "signature": "fn stage_commit(&mut self, epoch: Epoch) -> Result<u64>",
      "doc": "Writes the txn's frames and its `TxnCommit` out to the log\nwithout waiting for the platter, and returns the byte the log\nhas to be durable through for this txn to have committed.\n\nNothing is committed when this returns. What it buys is that the\nwrite side can go back to the next writer now, so their frames\nare staged while this one's sync is in the air and one sync\ncovers them both."
    },
    {
      "id": "zu::zu1::wal::Wal::truncate",
      "kind": "method",
      "name": "truncate",
      "of": "zu::zu1::wal::Wal",
      "signature": "fn truncate(&mut self) -> Result<()>",
      "doc": "Empties the log after a checkpoint has folded and published\neverything it held.\n\nThe cut does not sync, and it is the one place in the write path\nthat does not have to. The header the checkpoint published names\nthe epoch it folded through, and replay skips every frame at or\nbelow it, so a crash that finds the old bytes still on disk\nreplays nothing: what those frames say is already in the base\nfile. It costs one full sync per statement to make the file\nshorter sooner, which is a quarter of what a one cell write pays\nfor durability it already has."
    },
    {
      "id": "zu::zu1::wal::WalColumn",
      "kind": "struct",
      "name": "WalColumn",
      "doc": "One logged column of a node insert: the column's position in the\ntable's props directory and its row-ordered values."
    },
    {
      "id": "zu::zu1::wal::WalColumn::col",
      "kind": "field",
      "name": "col",
      "of": "zu::zu1::wal::WalColumn",
      "signature": "u32"
    },
    {
      "id": "zu::zu1::wal::WalColumn::values",
      "kind": "field",
      "name": "values",
      "of": "zu::zu1::wal::WalColumn",
      "signature": "WalValues"
    },
    {
      "id": "zu::zu1::wal::WalRecord",
      "kind": "enum",
      "name": "WalRecord",
      "doc": "One logical redo record. The epoch travels in the frame header, not\nhere: every record of a txn carries that txn's commit epoch."
    },
    {
      "id": "zu::zu1::wal::WalRecord::CheckpointNote",
      "kind": "variant",
      "name": "CheckpointNote",
      "of": "zu::zu1::wal::WalRecord",
      "signature": "CheckpointNote"
    },
    {
      "id": "zu::zu1::wal::WalRecord::DdlCatalog",
      "kind": "variant",
      "name": "DdlCatalog",
      "of": "zu::zu1::wal::WalRecord",
      "signature": "DdlCatalog { delta: Vec<u8> }"
    },
    {
      "id": "zu::zu1::wal::WalRecord::Delete",
      "kind": "variant",
      "name": "Delete",
      "of": "zu::zu1::wal::WalRecord",
      "signature": "Delete { table: u32, ids: Vec<u64> }"
    },
    {
      "id": "zu::zu1::wal::WalRecord::IngestRef",
      "kind": "variant",
      "name": "IngestRef",
      "of": "zu::zu1::wal::WalRecord",
      "signature": "IngestRef { table: u32, ptrs: Vec<u64> }"
    },
    {
      "id": "zu::zu1::wal::WalRecord::LabelUpdate",
      "kind": "variant",
      "name": "LabelUpdate",
      "of": "zu::zu1::wal::WalRecord",
      "signature": "LabelUpdate { table: u32, offsets: Vec<u64>, add: u64, remove: u64 }",
      "doc": "Which labels one txn put on rows and which it took off them. The\ntwo masks are a bit per label of the graph's dictionary, and the\nrows named share them, so one record is one shape of change over\nhowever many rows were changed that way. A label is not a column,\nso there is no column here and no value: the row's label word is\none word, and what a change to it says is which bits go on and\nwhich come off."
    },
    {
      "id": "zu::zu1::wal::WalRecord::NodeInsert",
      "kind": "variant",
      "name": "NodeInsert",
      "of": "zu::zu1::wal::WalRecord",
      "signature": "NodeInsert { table: u32, cols: Vec<WalColumn> }"
    },
    {
      "id": "zu::zu1::wal::WalRecord::RelDelete",
      "kind": "variant",
      "name": "RelDelete",
      "of": "zu::zu1::wal::WalRecord",
      "signature": "RelDelete { rel: u32, src: Vec<u64>, dst: Vec<u64> }",
      "doc": "The edges one txn took away, named by the rows they run between\nbecause that is the only name an edge has: the file holds them\nin a CSR that a fold rebuilds, so there is no offset to log."
    },
    {
      "id": "zu::zu1::wal::WalRecord::RelInsert",
      "kind": "variant",
      "name": "RelInsert",
      "of": "zu::zu1::wal::WalRecord",
      "signature": "RelInsert { rel: u32, src: Vec<u64>, dst: Vec<u64>, cols: Vec<WalColumn> }"
    },
    {
      "id": "zu::zu1::wal::WalRecord::RelUpdate",
      "kind": "variant",
      "name": "RelUpdate",
      "of": "zu::zu1::wal::WalRecord",
      "signature": "RelUpdate { rel: u32, col: u32, src: Vec<u64>, dst: Vec<u64>, values: WalValues }",
      "doc": "What one txn changed on edges that were already there, named the\nway a removed edge is: by the rows the edge runs between. One\nrecord carries one column, so the values run beside the pairs the\nway a node update's run beside its offsets."
    },
    {
      "id": "zu::zu1::wal::WalRecord::TxnBegin",
      "kind": "variant",
      "name": "TxnBegin",
      "of": "zu::zu1::wal::WalRecord",
      "signature": "TxnBegin"
    },
    {
      "id": "zu::zu1::wal::WalRecord::TxnCommit",
      "kind": "variant",
      "name": "TxnCommit",
      "of": "zu::zu1::wal::WalRecord",
      "signature": "TxnCommit"
    },
    {
      "id": "zu::zu1::wal::WalRecord::Update",
      "kind": "variant",
      "name": "Update",
      "of": "zu::zu1::wal::WalRecord",
      "signature": "Update { table: u32, group: u64, col: u32, offsets: Vec<u64>, values: WalValues }"
    },
    {
      "id": "zu::zu1::wal::WalValues",
      "kind": "enum",
      "name": "WalValues",
      "doc": "Row-ordered values for one logged column, mirroring the two\nproperty types the props store holds."
    },
    {
      "id": "zu::zu1::wal::WalValues::Int",
      "kind": "variant",
      "name": "Int",
      "of": "zu::zu1::wal::WalValues",
      "signature": "Int(Vec<u64>)"
    },
    {
      "id": "zu::zu1::wal::WalValues::Null",
      "kind": "variant",
      "name": "Null",
      "of": "zu::zu1::wal::WalValues",
      "signature": "Null(u32)",
      "doc": "This many absences in a row, which is what a `REMOVE` writes.\nAn absence carries nothing per row, so the count is the whole\nrecord: what a reader needs to know is which rows the record\nnames, and the offsets beside it already say."
    },
    {
      "id": "zu::zu1::wal::WalValues::Str",
      "kind": "variant",
      "name": "Str",
      "of": "zu::zu1::wal::WalValues",
      "signature": "Str(Vec<Vec<u8>>)"
    },
    {
      "id": "zu::zu1::wal::WalValues::is_empty",
      "kind": "method",
      "name": "is_empty",
      "of": "zu::zu1::wal::WalValues",
      "signature": "fn is_empty(&self) -> bool"
    },
    {
      "id": "zu::zu1::wal::WalValues::len",
      "kind": "method",
      "name": "len",
      "of": "zu::zu1::wal::WalValues",
      "signature": "fn len(&self) -> usize"
    },
    {
      "id": "zu::zu1::wal::commit_counters",
      "kind": "function",
      "name": "commit_counters",
      "signature": "fn commit_counters() -> (u64, u64)",
      "doc": "The two counters above, as they stand."
    }
  ]
}
