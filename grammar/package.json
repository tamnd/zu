{
  "name": "zu-grammar",
  "version": "0.0.0",
  "private": true,
  "description": "The tree-sitter and Shiki grammars for zuQL, and the checks that hold them to the engine",
  "license": "Apache-2.0",
  "scripts": {
    "generate": "cd tree-sitter-gql && tree-sitter generate",
    "test": "npm run generate && npm run grammar && npm run corpus && npm run shiki",
    "grammar": "cd tree-sitter-gql && tree-sitter test",
    "corpus": "node corpus.mjs",
    "shiki": "node shiki/check.mjs"
  },
  "devDependencies": {
    "shiki": "3.14.0",
    "tree-sitter-cli": "0.25.10"
  }
}
